import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { ToolDefinition, ToolResponse } from '../types.js';
/**
 * Searches Shopify documentation with the given query
 * Automatically retries with alternate backend if first attempt fails
 * @param prompt The search query for Shopify documentation
 * @param parameters Optional search parameters
 * @returns The formatted response or error message
 */
export declare function searchShopifyDocs(prompt: string, parameters?: Record<string, string>): Promise<string>;
export type SearchShopifyDocsParams = {
    prompt: string;
    max_num_results?: number;
};
export declare function searchShopifyDocsToolDef(): ToolDefinition<SearchShopifyDocsParams, ToolResponse>;
export default function searchShopifyDocsTool(server: McpServer): Promise<void>;
//# sourceMappingURL=index.d.ts.map