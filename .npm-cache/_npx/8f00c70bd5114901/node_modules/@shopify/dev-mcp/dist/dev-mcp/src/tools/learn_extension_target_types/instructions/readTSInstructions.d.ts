export interface TypeExtractionResult {
    types: string[];
    imports: Set<string>;
    processedFiles: Set<string>;
}
export interface ExtensionTarget {
    name: string;
    description: string;
}
export interface TargetDescriptionsResult {
    description: string;
    targets: ExtensionTarget[];
}
interface TSReadConfig {
    packageName: string;
    surface: string;
    extensionTarget?: string;
}
/**
 * Builds formatted context instructions from extracted types
 */
export declare function buildContextInstructions(extensionTarget: string, result: TypeExtractionResult): string;
/**
 * Main function to extract TypeScript instructions for a given configuration
 */
export declare function extractTSInstructions(config: TSReadConfig): string;
/**
 * Reads extension target descriptions for a given API name
 * @param apiName - The API name (e.g., 'polaris-admin-extensions', 'polaris-customer-account-extensions')
 * @returns An object containing the formatted description string and the array of targets
 * @throws Error if API is not recognized, doesn't have extension targets, or targets cannot be read
 */
export declare function readTargetDescriptions(apiName: string, packageBasePath: string): TargetDescriptionsResult;
export {};
//# sourceMappingURL=readTSInstructions.d.ts.map