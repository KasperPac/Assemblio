import { ValidationToolResult } from '../../../shopify-dev-tools/src/types';
type ThemeCodeblock = {
    fileName: string;
    fileType: "assets" | "blocks" | "config" | "layout" | "locales" | "sections" | "snippets" | "templates";
    content: string;
};
/**
 * Validates codeblocks in a theme
 * @param codeblocks - An array of objects containing the filename, filetype, and content of the codeblock
 * @returns ValidationToolResult with details for each codeblock
 */
export default function validateThemeCodeblocks(codeblocks: ThemeCodeblock[]): Promise<ValidationToolResult>;
export {};
//# sourceMappingURL=themeCodeBlock.d.ts.map