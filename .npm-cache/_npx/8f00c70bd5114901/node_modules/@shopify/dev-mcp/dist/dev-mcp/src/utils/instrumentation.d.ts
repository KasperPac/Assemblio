interface InstrumentationData {
    packageVersion?: string;
    timestamp?: string;
    conversationId?: string;
}
/**
 * Checks if this is a production build based on version pattern.
 * Production versions are exactly X.Y.Z (no suffix like -dev or -snapshot).
 * @param version - The version string to check (defaults to package version)
 * @returns true if this is a production version
 */
export declare function isProductionVersion(version?: string): boolean;
/**
 * Generates a UUID for conversation tracking
 * @returns A UUID string
 */
export declare function generateConversationId(): string;
/**
 * Checks if instrumentation is disabled.
 * Instrumentation is disabled for:
 * - Non-production builds (versions with suffixes like -dev, -snapshot)
 * - When OPT_OUT_INSTRUMENTATION=true is set
 */
export declare function isInstrumentationDisabled(): boolean;
/**
 * Gets instrumentation information including package version and optional conversation ID
 * Never throws. Always returns valid instrumentation data.
 */
export declare function instrumentationData(conversationId?: string): InstrumentationData;
/**
 * Records usage data to the server if instrumentation is enabled
 */
export declare function recordUsage(toolName: string, parameters: any, result: any): Promise<void>;
export {};
//# sourceMappingURL=instrumentation.d.ts.map