/**
 * Get the path to a specific eval file
 */
export declare function getEvalFilePath(relativePath: string): string;
/**
 * Read an eval file's content
 */
export declare function readEvalFile(relativePath: string): string;
/**
 * Get all top-level YAML configs dynamically
 * This function discovers all YAML files in the root of the promptfoo directory
 * and returns them as a camelCase keyed object
 */
export declare function getTopLevelConfigs(): Record<string, string>;
/**
 * Dynamically generated eval configs from all top-level YAML files
 * Excludes subdirectories (like configs with e2e tests)
 *
 * Examples:
 * - admin.yaml → { admin: 'admin.yaml' }
 * - admin-extensions.yaml → { adminExtensions: 'admin-extensions.yaml' }
 * - liquid-full.yaml → { liquidFull: 'liquid-full.yaml' }
 */
export declare const evalConfigs: Record<string, string>;
//# sourceMappingURL=promptfoo.d.ts.map