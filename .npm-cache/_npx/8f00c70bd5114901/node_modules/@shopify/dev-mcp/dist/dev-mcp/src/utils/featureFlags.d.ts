import { FeatureFlags } from '../flags.js';
import { APICategory, APIMapping } from '../../../shopify-dev-tools/src/types/api-types';
/**
 * Get all enabled APIs based on feature flags
 * @param flags - Feature flags to check (defaults to production flags)
 * @returns Array of enabled API configurations
 */
export declare function getEnabledAPIs(flags?: FeatureFlags): APIMapping[];
/**
 * Get enabled APIs filtered by category
 * @param category - API category to filter by
 * @param flags - Feature flags to check (defaults to production flags)
 * @returns Array of enabled API configurations for the specified category
 */
export declare function getEnabledAPIsByCategory(category: APICategory, flags?: FeatureFlags): APIMapping[];
/**
 * Check if a specific feature flag is enabled
 * @param flagName - Name of the feature flag to check
 * @returns Whether the feature flag is enabled
 */
export declare function isFeatureEnabled(flagName: keyof FeatureFlags): boolean | import('../flags.js').LiquidValidationMode;
//# sourceMappingURL=featureFlags.d.ts.map