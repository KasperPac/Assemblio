import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { ToolDefinition } from './types.js';
/**
 * Registers a tool definition with an MCP server
 * Automatically adds conversationId to the schema for MCP context (except for learn_shopify_api)
 * Wraps the handler with error handling and usage recording
 * @param server The MCP server instance
 * @param tool The tool definition to register
 */
export declare function registerTool<TArgs, TResult>(server: McpServer, tool: ToolDefinition<TArgs, TResult>): void;
//# sourceMappingURL=utils.d.ts.map