var da = Object.defineProperty;
var pa = (t, e, n) => e in t ? da(t, e, { enumerable: !0, configurable: !0, writable: !0, value: n }) : t[e] = n;
var zt = (t, e, n) => pa(t, typeof e != "symbol" ? e + "" : e, n);
import { readFileSync as Xn, existsSync as Vs } from "node:fs";
import $ from "node:path";
import { fileURLToPath as Nn } from "node:url";
import Mn from "node:fs/promises";
import qs from "node:zlib";
import * as k from "path";
import { join as sn, normalize as rn } from "path";
import L from "typescript";
import { fileURLToPath as ha } from "url";
import * as fe from "fs/promises";
import { access as fa } from "fs/promises";
import { createRequire as Ci } from "module";
import * as Te from "fs";
import { readFileSync as ki } from "fs";
import { randomUUID as Ri } from "crypto";
import { themeCheckRun as ma } from "@shopify/theme-check-node";
import { path as ga, toSourceCode as ya, check as _a, recommended as va, SourceCodeType as Ta, extractDocDefinition as Ia, toSchema as Gs, FileType as ba } from "@shopify/theme-check-common";
import { ThemeLiquidDocsManager as Sa } from "@shopify/theme-check-docs-updater";
var j;
(function(t) {
  t.assertEqual = (i) => {
  };
  function e(i) {
  }
  t.assertIs = e;
  function n(i) {
    throw new Error();
  }
  t.assertNever = n, t.arrayToEnum = (i) => {
    const r = {};
    for (const a of i)
      r[a] = a;
    return r;
  }, t.getValidEnumValues = (i) => {
    const r = t.objectKeys(i).filter((o) => typeof i[i[o]] != "number"), a = {};
    for (const o of r)
      a[o] = i[o];
    return t.objectValues(a);
  }, t.objectValues = (i) => t.objectKeys(i).map(function(r) {
    return i[r];
  }), t.objectKeys = typeof Object.keys == "function" ? (i) => Object.keys(i) : (i) => {
    const r = [];
    for (const a in i)
      Object.prototype.hasOwnProperty.call(i, a) && r.push(a);
    return r;
  }, t.find = (i, r) => {
    for (const a of i)
      if (r(a))
        return a;
  }, t.isInteger = typeof Number.isInteger == "function" ? (i) => Number.isInteger(i) : (i) => typeof i == "number" && Number.isFinite(i) && Math.floor(i) === i;
  function s(i, r = " | ") {
    return i.map((a) => typeof a == "string" ? `'${a}'` : a).join(r);
  }
  t.joinValues = s, t.jsonStringifyReplacer = (i, r) => typeof r == "bigint" ? r.toString() : r;
})(j || (j = {}));
var Hs;
(function(t) {
  t.mergeShapes = (e, n) => ({
    ...e,
    ...n
    // second overwrites first
  });
})(Hs || (Hs = {}));
const I = j.arrayToEnum([
  "string",
  "nan",
  "number",
  "integer",
  "float",
  "boolean",
  "date",
  "bigint",
  "symbol",
  "function",
  "undefined",
  "null",
  "array",
  "object",
  "unknown",
  "promise",
  "void",
  "never",
  "map",
  "set"
]), ze = (t) => {
  switch (typeof t) {
    case "undefined":
      return I.undefined;
    case "string":
      return I.string;
    case "number":
      return Number.isNaN(t) ? I.nan : I.number;
    case "boolean":
      return I.boolean;
    case "function":
      return I.function;
    case "bigint":
      return I.bigint;
    case "symbol":
      return I.symbol;
    case "object":
      return Array.isArray(t) ? I.array : t === null ? I.null : t.then && typeof t.then == "function" && t.catch && typeof t.catch == "function" ? I.promise : typeof Map < "u" && t instanceof Map ? I.map : typeof Set < "u" && t instanceof Set ? I.set : typeof Date < "u" && t instanceof Date ? I.date : I.object;
    default:
      return I.unknown;
  }
}, g = j.arrayToEnum([
  "invalid_type",
  "invalid_literal",
  "custom",
  "invalid_union",
  "invalid_union_discriminator",
  "invalid_enum_value",
  "unrecognized_keys",
  "invalid_arguments",
  "invalid_return_type",
  "invalid_date",
  "invalid_string",
  "too_small",
  "too_big",
  "invalid_intersection_types",
  "not_multiple_of",
  "not_finite"
]);
class Be extends Error {
  get errors() {
    return this.issues;
  }
  constructor(e) {
    super(), this.issues = [], this.addIssue = (s) => {
      this.issues = [...this.issues, s];
    }, this.addIssues = (s = []) => {
      this.issues = [...this.issues, ...s];
    };
    const n = new.target.prototype;
    Object.setPrototypeOf ? Object.setPrototypeOf(this, n) : this.__proto__ = n, this.name = "ZodError", this.issues = e;
  }
  format(e) {
    const n = e || function(r) {
      return r.message;
    }, s = { _errors: [] }, i = (r) => {
      for (const a of r.issues)
        if (a.code === "invalid_union")
          a.unionErrors.map(i);
        else if (a.code === "invalid_return_type")
          i(a.returnTypeError);
        else if (a.code === "invalid_arguments")
          i(a.argumentsError);
        else if (a.path.length === 0)
          s._errors.push(n(a));
        else {
          let o = s, c = 0;
          for (; c < a.path.length; ) {
            const l = a.path[c];
            c === a.path.length - 1 ? (o[l] = o[l] || { _errors: [] }, o[l]._errors.push(n(a))) : o[l] = o[l] || { _errors: [] }, o = o[l], c++;
          }
        }
    };
    return i(this), s;
  }
  static assert(e) {
    if (!(e instanceof Be))
      throw new Error(`Not a ZodError: ${e}`);
  }
  toString() {
    return this.message;
  }
  get message() {
    return JSON.stringify(this.issues, j.jsonStringifyReplacer, 2);
  }
  get isEmpty() {
    return this.issues.length === 0;
  }
  flatten(e = (n) => n.message) {
    const n = {}, s = [];
    for (const i of this.issues)
      if (i.path.length > 0) {
        const r = i.path[0];
        n[r] = n[r] || [], n[r].push(e(i));
      } else
        s.push(e(i));
    return { formErrors: s, fieldErrors: n };
  }
  get formErrors() {
    return this.flatten();
  }
}
Be.create = (t) => new Be(t);
const Kn = (t, e) => {
  let n;
  switch (t.code) {
    case g.invalid_type:
      t.received === I.undefined ? n = "Required" : n = `Expected ${t.expected}, received ${t.received}`;
      break;
    case g.invalid_literal:
      n = `Invalid literal value, expected ${JSON.stringify(t.expected, j.jsonStringifyReplacer)}`;
      break;
    case g.unrecognized_keys:
      n = `Unrecognized key(s) in object: ${j.joinValues(t.keys, ", ")}`;
      break;
    case g.invalid_union:
      n = "Invalid input";
      break;
    case g.invalid_union_discriminator:
      n = `Invalid discriminator value. Expected ${j.joinValues(t.options)}`;
      break;
    case g.invalid_enum_value:
      n = `Invalid enum value. Expected ${j.joinValues(t.options)}, received '${t.received}'`;
      break;
    case g.invalid_arguments:
      n = "Invalid function arguments";
      break;
    case g.invalid_return_type:
      n = "Invalid function return type";
      break;
    case g.invalid_date:
      n = "Invalid date";
      break;
    case g.invalid_string:
      typeof t.validation == "object" ? "includes" in t.validation ? (n = `Invalid input: must include "${t.validation.includes}"`, typeof t.validation.position == "number" && (n = `${n} at one or more positions greater than or equal to ${t.validation.position}`)) : "startsWith" in t.validation ? n = `Invalid input: must start with "${t.validation.startsWith}"` : "endsWith" in t.validation ? n = `Invalid input: must end with "${t.validation.endsWith}"` : j.assertNever(t.validation) : t.validation !== "regex" ? n = `Invalid ${t.validation}` : n = "Invalid";
      break;
    case g.too_small:
      t.type === "array" ? n = `Array must contain ${t.exact ? "exactly" : t.inclusive ? "at least" : "more than"} ${t.minimum} element(s)` : t.type === "string" ? n = `String must contain ${t.exact ? "exactly" : t.inclusive ? "at least" : "over"} ${t.minimum} character(s)` : t.type === "number" ? n = `Number must be ${t.exact ? "exactly equal to " : t.inclusive ? "greater than or equal to " : "greater than "}${t.minimum}` : t.type === "bigint" ? n = `Number must be ${t.exact ? "exactly equal to " : t.inclusive ? "greater than or equal to " : "greater than "}${t.minimum}` : t.type === "date" ? n = `Date must be ${t.exact ? "exactly equal to " : t.inclusive ? "greater than or equal to " : "greater than "}${new Date(Number(t.minimum))}` : n = "Invalid input";
      break;
    case g.too_big:
      t.type === "array" ? n = `Array must contain ${t.exact ? "exactly" : t.inclusive ? "at most" : "less than"} ${t.maximum} element(s)` : t.type === "string" ? n = `String must contain ${t.exact ? "exactly" : t.inclusive ? "at most" : "under"} ${t.maximum} character(s)` : t.type === "number" ? n = `Number must be ${t.exact ? "exactly" : t.inclusive ? "less than or equal to" : "less than"} ${t.maximum}` : t.type === "bigint" ? n = `BigInt must be ${t.exact ? "exactly" : t.inclusive ? "less than or equal to" : "less than"} ${t.maximum}` : t.type === "date" ? n = `Date must be ${t.exact ? "exactly" : t.inclusive ? "smaller than or equal to" : "smaller than"} ${new Date(Number(t.maximum))}` : n = "Invalid input";
      break;
    case g.custom:
      n = "Invalid input";
      break;
    case g.invalid_intersection_types:
      n = "Intersection results could not be merged";
      break;
    case g.not_multiple_of:
      n = `Number must be a multiple of ${t.multipleOf}`;
      break;
    case g.not_finite:
      n = "Number must be finite";
      break;
    default:
      n = e.defaultError, j.assertNever(t);
  }
  return { message: n };
};
let Na = Kn;
function Ea() {
  return Na;
}
const Oa = (t) => {
  const { data: e, path: n, errorMaps: s, issueData: i } = t, r = [...n, ...i.path || []], a = {
    ...i,
    path: r
  };
  if (i.message !== void 0)
    return {
      ...i,
      path: r,
      message: i.message
    };
  let o = "";
  const c = s.filter((l) => !!l).slice().reverse();
  for (const l of c)
    o = l(a, { data: e, defaultError: o }).message;
  return {
    ...i,
    path: r,
    message: o
  };
};
function T(t, e) {
  const n = Ea(), s = Oa({
    issueData: e,
    data: t.data,
    path: t.path,
    errorMaps: [
      t.common.contextualErrorMap,
      // contextual error map is first priority
      t.schemaErrorMap,
      // then schema-bound map if available
      n,
      // then global override map
      n === Kn ? void 0 : Kn
      // then global default map
    ].filter((i) => !!i)
  });
  t.common.issues.push(s);
}
class he {
  constructor() {
    this.value = "valid";
  }
  dirty() {
    this.value === "valid" && (this.value = "dirty");
  }
  abort() {
    this.value !== "aborted" && (this.value = "aborted");
  }
  static mergeArray(e, n) {
    const s = [];
    for (const i of n) {
      if (i.status === "aborted")
        return x;
      i.status === "dirty" && e.dirty(), s.push(i.value);
    }
    return { status: e.value, value: s };
  }
  static async mergeObjectAsync(e, n) {
    const s = [];
    for (const i of n) {
      const r = await i.key, a = await i.value;
      s.push({
        key: r,
        value: a
      });
    }
    return he.mergeObjectSync(e, s);
  }
  static mergeObjectSync(e, n) {
    const s = {};
    for (const i of n) {
      const { key: r, value: a } = i;
      if (r.status === "aborted" || a.status === "aborted")
        return x;
      r.status === "dirty" && e.dirty(), a.status === "dirty" && e.dirty(), r.value !== "__proto__" && (typeof a.value < "u" || i.alwaysSet) && (s[r.value] = a.value);
    }
    return { status: e.value, value: s };
  }
}
const x = Object.freeze({
  status: "aborted"
}), Pt = (t) => ({ status: "dirty", value: t }), be = (t) => ({ status: "valid", value: t }), Bs = (t) => t.status === "aborted", Qs = (t) => t.status === "dirty", bt = (t) => t.status === "valid", an = (t) => typeof Promise < "u" && t instanceof Promise;
var E;
(function(t) {
  t.errToObj = (e) => typeof e == "string" ? { message: e } : e || {}, t.toString = (e) => typeof e == "string" ? e : e == null ? void 0 : e.message;
})(E || (E = {}));
class Fe {
  constructor(e, n, s, i) {
    this._cachedPath = [], this.parent = e, this.data = n, this._path = s, this._key = i;
  }
  get path() {
    return this._cachedPath.length || (Array.isArray(this._key) ? this._cachedPath.push(...this._path, ...this._key) : this._cachedPath.push(...this._path, this._key)), this._cachedPath;
  }
}
const Ys = (t, e) => {
  if (bt(e))
    return { success: !0, data: e.value };
  if (!t.common.issues.length)
    throw new Error("Validation failed but no issues detected.");
  return {
    success: !1,
    get error() {
      if (this._error)
        return this._error;
      const n = new Be(t.common.issues);
      return this._error = n, this._error;
    }
  };
};
function R(t) {
  if (!t)
    return {};
  const { errorMap: e, invalid_type_error: n, required_error: s, description: i } = t;
  if (e && (n || s))
    throw new Error(`Can't use "invalid_type_error" or "required_error" in conjunction with custom error map.`);
  return e ? { errorMap: e, description: i } : { errorMap: (a, o) => {
    const { message: c } = t;
    return a.code === "invalid_enum_value" ? { message: c ?? o.defaultError } : typeof o.data > "u" ? { message: c ?? s ?? o.defaultError } : a.code !== "invalid_type" ? { message: o.defaultError } : { message: c ?? n ?? o.defaultError };
  }, description: i };
}
class U {
  get description() {
    return this._def.description;
  }
  _getType(e) {
    return ze(e.data);
  }
  _getOrReturnCtx(e, n) {
    return n || {
      common: e.parent.common,
      data: e.data,
      parsedType: ze(e.data),
      schemaErrorMap: this._def.errorMap,
      path: e.path,
      parent: e.parent
    };
  }
  _processInputParams(e) {
    return {
      status: new he(),
      ctx: {
        common: e.parent.common,
        data: e.data,
        parsedType: ze(e.data),
        schemaErrorMap: this._def.errorMap,
        path: e.path,
        parent: e.parent
      }
    };
  }
  _parseSync(e) {
    const n = this._parse(e);
    if (an(n))
      throw new Error("Synchronous parse encountered promise.");
    return n;
  }
  _parseAsync(e) {
    const n = this._parse(e);
    return Promise.resolve(n);
  }
  parse(e, n) {
    const s = this.safeParse(e, n);
    if (s.success)
      return s.data;
    throw s.error;
  }
  safeParse(e, n) {
    const s = {
      common: {
        issues: [],
        async: (n == null ? void 0 : n.async) ?? !1,
        contextualErrorMap: n == null ? void 0 : n.errorMap
      },
      path: (n == null ? void 0 : n.path) || [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data: e,
      parsedType: ze(e)
    }, i = this._parseSync({ data: e, path: s.path, parent: s });
    return Ys(s, i);
  }
  "~validate"(e) {
    var s, i;
    const n = {
      common: {
        issues: [],
        async: !!this["~standard"].async
      },
      path: [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data: e,
      parsedType: ze(e)
    };
    if (!this["~standard"].async)
      try {
        const r = this._parseSync({ data: e, path: [], parent: n });
        return bt(r) ? {
          value: r.value
        } : {
          issues: n.common.issues
        };
      } catch (r) {
        (i = (s = r == null ? void 0 : r.message) == null ? void 0 : s.toLowerCase()) != null && i.includes("encountered") && (this["~standard"].async = !0), n.common = {
          issues: [],
          async: !0
        };
      }
    return this._parseAsync({ data: e, path: [], parent: n }).then((r) => bt(r) ? {
      value: r.value
    } : {
      issues: n.common.issues
    });
  }
  async parseAsync(e, n) {
    const s = await this.safeParseAsync(e, n);
    if (s.success)
      return s.data;
    throw s.error;
  }
  async safeParseAsync(e, n) {
    const s = {
      common: {
        issues: [],
        contextualErrorMap: n == null ? void 0 : n.errorMap,
        async: !0
      },
      path: (n == null ? void 0 : n.path) || [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data: e,
      parsedType: ze(e)
    }, i = this._parse({ data: e, path: s.path, parent: s }), r = await (an(i) ? i : Promise.resolve(i));
    return Ys(s, r);
  }
  refine(e, n) {
    const s = (i) => typeof n == "string" || typeof n > "u" ? { message: n } : typeof n == "function" ? n(i) : n;
    return this._refinement((i, r) => {
      const a = e(i), o = () => r.addIssue({
        code: g.custom,
        ...s(i)
      });
      return typeof Promise < "u" && a instanceof Promise ? a.then((c) => c ? !0 : (o(), !1)) : a ? !0 : (o(), !1);
    });
  }
  refinement(e, n) {
    return this._refinement((s, i) => e(s) ? !0 : (i.addIssue(typeof n == "function" ? n(s, i) : n), !1));
  }
  _refinement(e) {
    return new lt({
      schema: this,
      typeName: C.ZodEffects,
      effect: { type: "refinement", refinement: e }
    });
  }
  superRefine(e) {
    return this._refinement(e);
  }
  constructor(e) {
    this.spa = this.safeParseAsync, this._def = e, this.parse = this.parse.bind(this), this.safeParse = this.safeParse.bind(this), this.parseAsync = this.parseAsync.bind(this), this.safeParseAsync = this.safeParseAsync.bind(this), this.spa = this.spa.bind(this), this.refine = this.refine.bind(this), this.refinement = this.refinement.bind(this), this.superRefine = this.superRefine.bind(this), this.optional = this.optional.bind(this), this.nullable = this.nullable.bind(this), this.nullish = this.nullish.bind(this), this.array = this.array.bind(this), this.promise = this.promise.bind(this), this.or = this.or.bind(this), this.and = this.and.bind(this), this.transform = this.transform.bind(this), this.brand = this.brand.bind(this), this.default = this.default.bind(this), this.catch = this.catch.bind(this), this.describe = this.describe.bind(this), this.pipe = this.pipe.bind(this), this.readonly = this.readonly.bind(this), this.isNullable = this.isNullable.bind(this), this.isOptional = this.isOptional.bind(this), this["~standard"] = {
      version: 1,
      vendor: "zod",
      validate: (n) => this["~validate"](n)
    };
  }
  optional() {
    return He.create(this, this._def);
  }
  nullable() {
    return ut.create(this, this._def);
  }
  nullish() {
    return this.nullable().optional();
  }
  array() {
    return Pe.create(this);
  }
  promise() {
    return pn.create(this, this._def);
  }
  or(e) {
    return cn.create([this, e], this._def);
  }
  and(e) {
    return ln.create(this, e, this._def);
  }
  transform(e) {
    return new lt({
      ...R(this._def),
      schema: this,
      typeName: C.ZodEffects,
      effect: { type: "transform", transform: e }
    });
  }
  default(e) {
    const n = typeof e == "function" ? e : () => e;
    return new hn({
      ...R(this._def),
      innerType: this,
      defaultValue: n,
      typeName: C.ZodDefault
    });
  }
  brand() {
    return new Fi({
      typeName: C.ZodBranded,
      type: this,
      ...R(this._def)
    });
  }
  catch(e) {
    const n = typeof e == "function" ? e : () => e;
    return new fn({
      ...R(this._def),
      innerType: this,
      catchValue: n,
      typeName: C.ZodCatch
    });
  }
  describe(e) {
    const n = this.constructor;
    return new n({
      ...this._def,
      description: e
    });
  }
  pipe(e) {
    return Is.create(this, e);
  }
  readonly() {
    return mn.create(this);
  }
  isOptional() {
    return this.safeParse(void 0).success;
  }
  isNullable() {
    return this.safeParse(null).success;
  }
}
const wa = /^c[^\s-]{8,}$/i, Aa = /^[0-9a-z]+$/, xa = /^[0-9A-HJKMNP-TV-Z]{26}$/i, Ca = /^[0-9a-fA-F]{8}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{12}$/i, ka = /^[a-z0-9_-]{21}$/i, Ra = /^[A-Za-z0-9-_]+\.[A-Za-z0-9-_]+\.[A-Za-z0-9-_]*$/, La = /^[-+]?P(?!$)(?:(?:[-+]?\d+Y)|(?:[-+]?\d+[.,]\d+Y$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:(?:[-+]?\d+W)|(?:[-+]?\d+[.,]\d+W$))?(?:(?:[-+]?\d+D)|(?:[-+]?\d+[.,]\d+D$))?(?:T(?=[\d+-])(?:(?:[-+]?\d+H)|(?:[-+]?\d+[.,]\d+H$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:[-+]?\d+(?:[.,]\d+)?S)?)??$/, Da = /^(?!\.)(?!.*\.\.)([A-Z0-9_'+\-\.]*)[A-Z0-9_+-]@([A-Z0-9][A-Z0-9\-]*\.)+[A-Z]{2,}$/i, Pa = "^(\\p{Extended_Pictographic}|\\p{Emoji_Component})+$";
let $n;
const Fa = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])$/, Ua = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\/(3[0-2]|[12]?[0-9])$/, Ma = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$/, $a = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))\/(12[0-8]|1[01][0-9]|[1-9]?[0-9])$/, ja = /^([0-9a-zA-Z+/]{4})*(([0-9a-zA-Z+/]{2}==)|([0-9a-zA-Z+/]{3}=))?$/, Va = /^([0-9a-zA-Z-_]{4})*(([0-9a-zA-Z-_]{2}(==)?)|([0-9a-zA-Z-_]{3}(=)?))?$/, Li = "((\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-((0[13578]|1[02])-(0[1-9]|[12]\\d|3[01])|(0[469]|11)-(0[1-9]|[12]\\d|30)|(02)-(0[1-9]|1\\d|2[0-8])))", qa = new RegExp(`^${Li}$`);
function Di(t) {
  let e = "[0-5]\\d";
  t.precision ? e = `${e}\\.\\d{${t.precision}}` : t.precision == null && (e = `${e}(\\.\\d+)?`);
  const n = t.precision ? "+" : "?";
  return `([01]\\d|2[0-3]):[0-5]\\d(:${e})${n}`;
}
function Ga(t) {
  return new RegExp(`^${Di(t)}$`);
}
function Ha(t) {
  let e = `${Li}T${Di(t)}`;
  const n = [];
  return n.push(t.local ? "Z?" : "Z"), t.offset && n.push("([+-]\\d{2}:?\\d{2})"), e = `${e}(${n.join("|")})`, new RegExp(`^${e}$`);
}
function Ba(t, e) {
  return !!((e === "v4" || !e) && Fa.test(t) || (e === "v6" || !e) && Ma.test(t));
}
function Qa(t, e) {
  if (!Ra.test(t))
    return !1;
  try {
    const [n] = t.split(".");
    if (!n)
      return !1;
    const s = n.replace(/-/g, "+").replace(/_/g, "/").padEnd(n.length + (4 - n.length % 4) % 4, "="), i = JSON.parse(atob(s));
    return !(typeof i != "object" || i === null || "typ" in i && (i == null ? void 0 : i.typ) !== "JWT" || !i.alg || e && i.alg !== e);
  } catch {
    return !1;
  }
}
function Ya(t, e) {
  return !!((e === "v4" || !e) && Ua.test(t) || (e === "v6" || !e) && $a.test(t));
}
class Ge extends U {
  _parse(e) {
    if (this._def.coerce && (e.data = String(e.data)), this._getType(e) !== I.string) {
      const r = this._getOrReturnCtx(e);
      return T(r, {
        code: g.invalid_type,
        expected: I.string,
        received: r.parsedType
      }), x;
    }
    const s = new he();
    let i;
    for (const r of this._def.checks)
      if (r.kind === "min")
        e.data.length < r.value && (i = this._getOrReturnCtx(e, i), T(i, {
          code: g.too_small,
          minimum: r.value,
          type: "string",
          inclusive: !0,
          exact: !1,
          message: r.message
        }), s.dirty());
      else if (r.kind === "max")
        e.data.length > r.value && (i = this._getOrReturnCtx(e, i), T(i, {
          code: g.too_big,
          maximum: r.value,
          type: "string",
          inclusive: !0,
          exact: !1,
          message: r.message
        }), s.dirty());
      else if (r.kind === "length") {
        const a = e.data.length > r.value, o = e.data.length < r.value;
        (a || o) && (i = this._getOrReturnCtx(e, i), a ? T(i, {
          code: g.too_big,
          maximum: r.value,
          type: "string",
          inclusive: !0,
          exact: !0,
          message: r.message
        }) : o && T(i, {
          code: g.too_small,
          minimum: r.value,
          type: "string",
          inclusive: !0,
          exact: !0,
          message: r.message
        }), s.dirty());
      } else if (r.kind === "email")
        Da.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
          validation: "email",
          code: g.invalid_string,
          message: r.message
        }), s.dirty());
      else if (r.kind === "emoji")
        $n || ($n = new RegExp(Pa, "u")), $n.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
          validation: "emoji",
          code: g.invalid_string,
          message: r.message
        }), s.dirty());
      else if (r.kind === "uuid")
        Ca.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
          validation: "uuid",
          code: g.invalid_string,
          message: r.message
        }), s.dirty());
      else if (r.kind === "nanoid")
        ka.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
          validation: "nanoid",
          code: g.invalid_string,
          message: r.message
        }), s.dirty());
      else if (r.kind === "cuid")
        wa.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
          validation: "cuid",
          code: g.invalid_string,
          message: r.message
        }), s.dirty());
      else if (r.kind === "cuid2")
        Aa.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
          validation: "cuid2",
          code: g.invalid_string,
          message: r.message
        }), s.dirty());
      else if (r.kind === "ulid")
        xa.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
          validation: "ulid",
          code: g.invalid_string,
          message: r.message
        }), s.dirty());
      else if (r.kind === "url")
        try {
          new URL(e.data);
        } catch {
          i = this._getOrReturnCtx(e, i), T(i, {
            validation: "url",
            code: g.invalid_string,
            message: r.message
          }), s.dirty();
        }
      else r.kind === "regex" ? (r.regex.lastIndex = 0, r.regex.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
        validation: "regex",
        code: g.invalid_string,
        message: r.message
      }), s.dirty())) : r.kind === "trim" ? e.data = e.data.trim() : r.kind === "includes" ? e.data.includes(r.value, r.position) || (i = this._getOrReturnCtx(e, i), T(i, {
        code: g.invalid_string,
        validation: { includes: r.value, position: r.position },
        message: r.message
      }), s.dirty()) : r.kind === "toLowerCase" ? e.data = e.data.toLowerCase() : r.kind === "toUpperCase" ? e.data = e.data.toUpperCase() : r.kind === "startsWith" ? e.data.startsWith(r.value) || (i = this._getOrReturnCtx(e, i), T(i, {
        code: g.invalid_string,
        validation: { startsWith: r.value },
        message: r.message
      }), s.dirty()) : r.kind === "endsWith" ? e.data.endsWith(r.value) || (i = this._getOrReturnCtx(e, i), T(i, {
        code: g.invalid_string,
        validation: { endsWith: r.value },
        message: r.message
      }), s.dirty()) : r.kind === "datetime" ? Ha(r).test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
        code: g.invalid_string,
        validation: "datetime",
        message: r.message
      }), s.dirty()) : r.kind === "date" ? qa.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
        code: g.invalid_string,
        validation: "date",
        message: r.message
      }), s.dirty()) : r.kind === "time" ? Ga(r).test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
        code: g.invalid_string,
        validation: "time",
        message: r.message
      }), s.dirty()) : r.kind === "duration" ? La.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
        validation: "duration",
        code: g.invalid_string,
        message: r.message
      }), s.dirty()) : r.kind === "ip" ? Ba(e.data, r.version) || (i = this._getOrReturnCtx(e, i), T(i, {
        validation: "ip",
        code: g.invalid_string,
        message: r.message
      }), s.dirty()) : r.kind === "jwt" ? Qa(e.data, r.alg) || (i = this._getOrReturnCtx(e, i), T(i, {
        validation: "jwt",
        code: g.invalid_string,
        message: r.message
      }), s.dirty()) : r.kind === "cidr" ? Ya(e.data, r.version) || (i = this._getOrReturnCtx(e, i), T(i, {
        validation: "cidr",
        code: g.invalid_string,
        message: r.message
      }), s.dirty()) : r.kind === "base64" ? ja.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
        validation: "base64",
        code: g.invalid_string,
        message: r.message
      }), s.dirty()) : r.kind === "base64url" ? Va.test(e.data) || (i = this._getOrReturnCtx(e, i), T(i, {
        validation: "base64url",
        code: g.invalid_string,
        message: r.message
      }), s.dirty()) : j.assertNever(r);
    return { status: s.value, value: e.data };
  }
  _regex(e, n, s) {
    return this.refinement((i) => e.test(i), {
      validation: n,
      code: g.invalid_string,
      ...E.errToObj(s)
    });
  }
  _addCheck(e) {
    return new Ge({
      ...this._def,
      checks: [...this._def.checks, e]
    });
  }
  email(e) {
    return this._addCheck({ kind: "email", ...E.errToObj(e) });
  }
  url(e) {
    return this._addCheck({ kind: "url", ...E.errToObj(e) });
  }
  emoji(e) {
    return this._addCheck({ kind: "emoji", ...E.errToObj(e) });
  }
  uuid(e) {
    return this._addCheck({ kind: "uuid", ...E.errToObj(e) });
  }
  nanoid(e) {
    return this._addCheck({ kind: "nanoid", ...E.errToObj(e) });
  }
  cuid(e) {
    return this._addCheck({ kind: "cuid", ...E.errToObj(e) });
  }
  cuid2(e) {
    return this._addCheck({ kind: "cuid2", ...E.errToObj(e) });
  }
  ulid(e) {
    return this._addCheck({ kind: "ulid", ...E.errToObj(e) });
  }
  base64(e) {
    return this._addCheck({ kind: "base64", ...E.errToObj(e) });
  }
  base64url(e) {
    return this._addCheck({
      kind: "base64url",
      ...E.errToObj(e)
    });
  }
  jwt(e) {
    return this._addCheck({ kind: "jwt", ...E.errToObj(e) });
  }
  ip(e) {
    return this._addCheck({ kind: "ip", ...E.errToObj(e) });
  }
  cidr(e) {
    return this._addCheck({ kind: "cidr", ...E.errToObj(e) });
  }
  datetime(e) {
    return typeof e == "string" ? this._addCheck({
      kind: "datetime",
      precision: null,
      offset: !1,
      local: !1,
      message: e
    }) : this._addCheck({
      kind: "datetime",
      precision: typeof (e == null ? void 0 : e.precision) > "u" ? null : e == null ? void 0 : e.precision,
      offset: (e == null ? void 0 : e.offset) ?? !1,
      local: (e == null ? void 0 : e.local) ?? !1,
      ...E.errToObj(e == null ? void 0 : e.message)
    });
  }
  date(e) {
    return this._addCheck({ kind: "date", message: e });
  }
  time(e) {
    return typeof e == "string" ? this._addCheck({
      kind: "time",
      precision: null,
      message: e
    }) : this._addCheck({
      kind: "time",
      precision: typeof (e == null ? void 0 : e.precision) > "u" ? null : e == null ? void 0 : e.precision,
      ...E.errToObj(e == null ? void 0 : e.message)
    });
  }
  duration(e) {
    return this._addCheck({ kind: "duration", ...E.errToObj(e) });
  }
  regex(e, n) {
    return this._addCheck({
      kind: "regex",
      regex: e,
      ...E.errToObj(n)
    });
  }
  includes(e, n) {
    return this._addCheck({
      kind: "includes",
      value: e,
      position: n == null ? void 0 : n.position,
      ...E.errToObj(n == null ? void 0 : n.message)
    });
  }
  startsWith(e, n) {
    return this._addCheck({
      kind: "startsWith",
      value: e,
      ...E.errToObj(n)
    });
  }
  endsWith(e, n) {
    return this._addCheck({
      kind: "endsWith",
      value: e,
      ...E.errToObj(n)
    });
  }
  min(e, n) {
    return this._addCheck({
      kind: "min",
      value: e,
      ...E.errToObj(n)
    });
  }
  max(e, n) {
    return this._addCheck({
      kind: "max",
      value: e,
      ...E.errToObj(n)
    });
  }
  length(e, n) {
    return this._addCheck({
      kind: "length",
      value: e,
      ...E.errToObj(n)
    });
  }
  /**
   * Equivalent to `.min(1)`
   */
  nonempty(e) {
    return this.min(1, E.errToObj(e));
  }
  trim() {
    return new Ge({
      ...this._def,
      checks: [...this._def.checks, { kind: "trim" }]
    });
  }
  toLowerCase() {
    return new Ge({
      ...this._def,
      checks: [...this._def.checks, { kind: "toLowerCase" }]
    });
  }
  toUpperCase() {
    return new Ge({
      ...this._def,
      checks: [...this._def.checks, { kind: "toUpperCase" }]
    });
  }
  get isDatetime() {
    return !!this._def.checks.find((e) => e.kind === "datetime");
  }
  get isDate() {
    return !!this._def.checks.find((e) => e.kind === "date");
  }
  get isTime() {
    return !!this._def.checks.find((e) => e.kind === "time");
  }
  get isDuration() {
    return !!this._def.checks.find((e) => e.kind === "duration");
  }
  get isEmail() {
    return !!this._def.checks.find((e) => e.kind === "email");
  }
  get isURL() {
    return !!this._def.checks.find((e) => e.kind === "url");
  }
  get isEmoji() {
    return !!this._def.checks.find((e) => e.kind === "emoji");
  }
  get isUUID() {
    return !!this._def.checks.find((e) => e.kind === "uuid");
  }
  get isNANOID() {
    return !!this._def.checks.find((e) => e.kind === "nanoid");
  }
  get isCUID() {
    return !!this._def.checks.find((e) => e.kind === "cuid");
  }
  get isCUID2() {
    return !!this._def.checks.find((e) => e.kind === "cuid2");
  }
  get isULID() {
    return !!this._def.checks.find((e) => e.kind === "ulid");
  }
  get isIP() {
    return !!this._def.checks.find((e) => e.kind === "ip");
  }
  get isCIDR() {
    return !!this._def.checks.find((e) => e.kind === "cidr");
  }
  get isBase64() {
    return !!this._def.checks.find((e) => e.kind === "base64");
  }
  get isBase64url() {
    return !!this._def.checks.find((e) => e.kind === "base64url");
  }
  get minLength() {
    let e = null;
    for (const n of this._def.checks)
      n.kind === "min" && (e === null || n.value > e) && (e = n.value);
    return e;
  }
  get maxLength() {
    let e = null;
    for (const n of this._def.checks)
      n.kind === "max" && (e === null || n.value < e) && (e = n.value);
    return e;
  }
}
Ge.create = (t) => new Ge({
  checks: [],
  typeName: C.ZodString,
  coerce: (t == null ? void 0 : t.coerce) ?? !1,
  ...R(t)
});
function Wa(t, e) {
  const n = (t.toString().split(".")[1] || "").length, s = (e.toString().split(".")[1] || "").length, i = n > s ? n : s, r = Number.parseInt(t.toFixed(i).replace(".", "")), a = Number.parseInt(e.toFixed(i).replace(".", ""));
  return r % a / 10 ** i;
}
class St extends U {
  constructor() {
    super(...arguments), this.min = this.gte, this.max = this.lte, this.step = this.multipleOf;
  }
  _parse(e) {
    if (this._def.coerce && (e.data = Number(e.data)), this._getType(e) !== I.number) {
      const r = this._getOrReturnCtx(e);
      return T(r, {
        code: g.invalid_type,
        expected: I.number,
        received: r.parsedType
      }), x;
    }
    let s;
    const i = new he();
    for (const r of this._def.checks)
      r.kind === "int" ? j.isInteger(e.data) || (s = this._getOrReturnCtx(e, s), T(s, {
        code: g.invalid_type,
        expected: "integer",
        received: "float",
        message: r.message
      }), i.dirty()) : r.kind === "min" ? (r.inclusive ? e.data < r.value : e.data <= r.value) && (s = this._getOrReturnCtx(e, s), T(s, {
        code: g.too_small,
        minimum: r.value,
        type: "number",
        inclusive: r.inclusive,
        exact: !1,
        message: r.message
      }), i.dirty()) : r.kind === "max" ? (r.inclusive ? e.data > r.value : e.data >= r.value) && (s = this._getOrReturnCtx(e, s), T(s, {
        code: g.too_big,
        maximum: r.value,
        type: "number",
        inclusive: r.inclusive,
        exact: !1,
        message: r.message
      }), i.dirty()) : r.kind === "multipleOf" ? Wa(e.data, r.value) !== 0 && (s = this._getOrReturnCtx(e, s), T(s, {
        code: g.not_multiple_of,
        multipleOf: r.value,
        message: r.message
      }), i.dirty()) : r.kind === "finite" ? Number.isFinite(e.data) || (s = this._getOrReturnCtx(e, s), T(s, {
        code: g.not_finite,
        message: r.message
      }), i.dirty()) : j.assertNever(r);
    return { status: i.value, value: e.data };
  }
  gte(e, n) {
    return this.setLimit("min", e, !0, E.toString(n));
  }
  gt(e, n) {
    return this.setLimit("min", e, !1, E.toString(n));
  }
  lte(e, n) {
    return this.setLimit("max", e, !0, E.toString(n));
  }
  lt(e, n) {
    return this.setLimit("max", e, !1, E.toString(n));
  }
  setLimit(e, n, s, i) {
    return new St({
      ...this._def,
      checks: [
        ...this._def.checks,
        {
          kind: e,
          value: n,
          inclusive: s,
          message: E.toString(i)
        }
      ]
    });
  }
  _addCheck(e) {
    return new St({
      ...this._def,
      checks: [...this._def.checks, e]
    });
  }
  int(e) {
    return this._addCheck({
      kind: "int",
      message: E.toString(e)
    });
  }
  positive(e) {
    return this._addCheck({
      kind: "min",
      value: 0,
      inclusive: !1,
      message: E.toString(e)
    });
  }
  negative(e) {
    return this._addCheck({
      kind: "max",
      value: 0,
      inclusive: !1,
      message: E.toString(e)
    });
  }
  nonpositive(e) {
    return this._addCheck({
      kind: "max",
      value: 0,
      inclusive: !0,
      message: E.toString(e)
    });
  }
  nonnegative(e) {
    return this._addCheck({
      kind: "min",
      value: 0,
      inclusive: !0,
      message: E.toString(e)
    });
  }
  multipleOf(e, n) {
    return this._addCheck({
      kind: "multipleOf",
      value: e,
      message: E.toString(n)
    });
  }
  finite(e) {
    return this._addCheck({
      kind: "finite",
      message: E.toString(e)
    });
  }
  safe(e) {
    return this._addCheck({
      kind: "min",
      inclusive: !0,
      value: Number.MIN_SAFE_INTEGER,
      message: E.toString(e)
    })._addCheck({
      kind: "max",
      inclusive: !0,
      value: Number.MAX_SAFE_INTEGER,
      message: E.toString(e)
    });
  }
  get minValue() {
    let e = null;
    for (const n of this._def.checks)
      n.kind === "min" && (e === null || n.value > e) && (e = n.value);
    return e;
  }
  get maxValue() {
    let e = null;
    for (const n of this._def.checks)
      n.kind === "max" && (e === null || n.value < e) && (e = n.value);
    return e;
  }
  get isInt() {
    return !!this._def.checks.find((e) => e.kind === "int" || e.kind === "multipleOf" && j.isInteger(e.value));
  }
  get isFinite() {
    let e = null, n = null;
    for (const s of this._def.checks) {
      if (s.kind === "finite" || s.kind === "int" || s.kind === "multipleOf")
        return !0;
      s.kind === "min" ? (n === null || s.value > n) && (n = s.value) : s.kind === "max" && (e === null || s.value < e) && (e = s.value);
    }
    return Number.isFinite(n) && Number.isFinite(e);
  }
}
St.create = (t) => new St({
  checks: [],
  typeName: C.ZodNumber,
  coerce: (t == null ? void 0 : t.coerce) || !1,
  ...R(t)
});
class jt extends U {
  constructor() {
    super(...arguments), this.min = this.gte, this.max = this.lte;
  }
  _parse(e) {
    if (this._def.coerce)
      try {
        e.data = BigInt(e.data);
      } catch {
        return this._getInvalidInput(e);
      }
    if (this._getType(e) !== I.bigint)
      return this._getInvalidInput(e);
    let s;
    const i = new he();
    for (const r of this._def.checks)
      r.kind === "min" ? (r.inclusive ? e.data < r.value : e.data <= r.value) && (s = this._getOrReturnCtx(e, s), T(s, {
        code: g.too_small,
        type: "bigint",
        minimum: r.value,
        inclusive: r.inclusive,
        message: r.message
      }), i.dirty()) : r.kind === "max" ? (r.inclusive ? e.data > r.value : e.data >= r.value) && (s = this._getOrReturnCtx(e, s), T(s, {
        code: g.too_big,
        type: "bigint",
        maximum: r.value,
        inclusive: r.inclusive,
        message: r.message
      }), i.dirty()) : r.kind === "multipleOf" ? e.data % r.value !== BigInt(0) && (s = this._getOrReturnCtx(e, s), T(s, {
        code: g.not_multiple_of,
        multipleOf: r.value,
        message: r.message
      }), i.dirty()) : j.assertNever(r);
    return { status: i.value, value: e.data };
  }
  _getInvalidInput(e) {
    const n = this._getOrReturnCtx(e);
    return T(n, {
      code: g.invalid_type,
      expected: I.bigint,
      received: n.parsedType
    }), x;
  }
  gte(e, n) {
    return this.setLimit("min", e, !0, E.toString(n));
  }
  gt(e, n) {
    return this.setLimit("min", e, !1, E.toString(n));
  }
  lte(e, n) {
    return this.setLimit("max", e, !0, E.toString(n));
  }
  lt(e, n) {
    return this.setLimit("max", e, !1, E.toString(n));
  }
  setLimit(e, n, s, i) {
    return new jt({
      ...this._def,
      checks: [
        ...this._def.checks,
        {
          kind: e,
          value: n,
          inclusive: s,
          message: E.toString(i)
        }
      ]
    });
  }
  _addCheck(e) {
    return new jt({
      ...this._def,
      checks: [...this._def.checks, e]
    });
  }
  positive(e) {
    return this._addCheck({
      kind: "min",
      value: BigInt(0),
      inclusive: !1,
      message: E.toString(e)
    });
  }
  negative(e) {
    return this._addCheck({
      kind: "max",
      value: BigInt(0),
      inclusive: !1,
      message: E.toString(e)
    });
  }
  nonpositive(e) {
    return this._addCheck({
      kind: "max",
      value: BigInt(0),
      inclusive: !0,
      message: E.toString(e)
    });
  }
  nonnegative(e) {
    return this._addCheck({
      kind: "min",
      value: BigInt(0),
      inclusive: !0,
      message: E.toString(e)
    });
  }
  multipleOf(e, n) {
    return this._addCheck({
      kind: "multipleOf",
      value: e,
      message: E.toString(n)
    });
  }
  get minValue() {
    let e = null;
    for (const n of this._def.checks)
      n.kind === "min" && (e === null || n.value > e) && (e = n.value);
    return e;
  }
  get maxValue() {
    let e = null;
    for (const n of this._def.checks)
      n.kind === "max" && (e === null || n.value < e) && (e = n.value);
    return e;
  }
}
jt.create = (t) => new jt({
  checks: [],
  typeName: C.ZodBigInt,
  coerce: (t == null ? void 0 : t.coerce) ?? !1,
  ...R(t)
});
class es extends U {
  _parse(e) {
    if (this._def.coerce && (e.data = !!e.data), this._getType(e) !== I.boolean) {
      const s = this._getOrReturnCtx(e);
      return T(s, {
        code: g.invalid_type,
        expected: I.boolean,
        received: s.parsedType
      }), x;
    }
    return be(e.data);
  }
}
es.create = (t) => new es({
  typeName: C.ZodBoolean,
  coerce: (t == null ? void 0 : t.coerce) || !1,
  ...R(t)
});
class on extends U {
  _parse(e) {
    if (this._def.coerce && (e.data = new Date(e.data)), this._getType(e) !== I.date) {
      const r = this._getOrReturnCtx(e);
      return T(r, {
        code: g.invalid_type,
        expected: I.date,
        received: r.parsedType
      }), x;
    }
    if (Number.isNaN(e.data.getTime())) {
      const r = this._getOrReturnCtx(e);
      return T(r, {
        code: g.invalid_date
      }), x;
    }
    const s = new he();
    let i;
    for (const r of this._def.checks)
      r.kind === "min" ? e.data.getTime() < r.value && (i = this._getOrReturnCtx(e, i), T(i, {
        code: g.too_small,
        message: r.message,
        inclusive: !0,
        exact: !1,
        minimum: r.value,
        type: "date"
      }), s.dirty()) : r.kind === "max" ? e.data.getTime() > r.value && (i = this._getOrReturnCtx(e, i), T(i, {
        code: g.too_big,
        message: r.message,
        inclusive: !0,
        exact: !1,
        maximum: r.value,
        type: "date"
      }), s.dirty()) : j.assertNever(r);
    return {
      status: s.value,
      value: new Date(e.data.getTime())
    };
  }
  _addCheck(e) {
    return new on({
      ...this._def,
      checks: [...this._def.checks, e]
    });
  }
  min(e, n) {
    return this._addCheck({
      kind: "min",
      value: e.getTime(),
      message: E.toString(n)
    });
  }
  max(e, n) {
    return this._addCheck({
      kind: "max",
      value: e.getTime(),
      message: E.toString(n)
    });
  }
  get minDate() {
    let e = null;
    for (const n of this._def.checks)
      n.kind === "min" && (e === null || n.value > e) && (e = n.value);
    return e != null ? new Date(e) : null;
  }
  get maxDate() {
    let e = null;
    for (const n of this._def.checks)
      n.kind === "max" && (e === null || n.value < e) && (e = n.value);
    return e != null ? new Date(e) : null;
  }
}
on.create = (t) => new on({
  checks: [],
  coerce: (t == null ? void 0 : t.coerce) || !1,
  typeName: C.ZodDate,
  ...R(t)
});
class Ws extends U {
  _parse(e) {
    if (this._getType(e) !== I.symbol) {
      const s = this._getOrReturnCtx(e);
      return T(s, {
        code: g.invalid_type,
        expected: I.symbol,
        received: s.parsedType
      }), x;
    }
    return be(e.data);
  }
}
Ws.create = (t) => new Ws({
  typeName: C.ZodSymbol,
  ...R(t)
});
class ts extends U {
  _parse(e) {
    if (this._getType(e) !== I.undefined) {
      const s = this._getOrReturnCtx(e);
      return T(s, {
        code: g.invalid_type,
        expected: I.undefined,
        received: s.parsedType
      }), x;
    }
    return be(e.data);
  }
}
ts.create = (t) => new ts({
  typeName: C.ZodUndefined,
  ...R(t)
});
class ns extends U {
  _parse(e) {
    if (this._getType(e) !== I.null) {
      const s = this._getOrReturnCtx(e);
      return T(s, {
        code: g.invalid_type,
        expected: I.null,
        received: s.parsedType
      }), x;
    }
    return be(e.data);
  }
}
ns.create = (t) => new ns({
  typeName: C.ZodNull,
  ...R(t)
});
class zs extends U {
  constructor() {
    super(...arguments), this._any = !0;
  }
  _parse(e) {
    return be(e.data);
  }
}
zs.create = (t) => new zs({
  typeName: C.ZodAny,
  ...R(t)
});
class ss extends U {
  constructor() {
    super(...arguments), this._unknown = !0;
  }
  _parse(e) {
    return be(e.data);
  }
}
ss.create = (t) => new ss({
  typeName: C.ZodUnknown,
  ...R(t)
});
class Ze extends U {
  _parse(e) {
    const n = this._getOrReturnCtx(e);
    return T(n, {
      code: g.invalid_type,
      expected: I.never,
      received: n.parsedType
    }), x;
  }
}
Ze.create = (t) => new Ze({
  typeName: C.ZodNever,
  ...R(t)
});
class Js extends U {
  _parse(e) {
    if (this._getType(e) !== I.undefined) {
      const s = this._getOrReturnCtx(e);
      return T(s, {
        code: g.invalid_type,
        expected: I.void,
        received: s.parsedType
      }), x;
    }
    return be(e.data);
  }
}
Js.create = (t) => new Js({
  typeName: C.ZodVoid,
  ...R(t)
});
class Pe extends U {
  _parse(e) {
    const { ctx: n, status: s } = this._processInputParams(e), i = this._def;
    if (n.parsedType !== I.array)
      return T(n, {
        code: g.invalid_type,
        expected: I.array,
        received: n.parsedType
      }), x;
    if (i.exactLength !== null) {
      const a = n.data.length > i.exactLength.value, o = n.data.length < i.exactLength.value;
      (a || o) && (T(n, {
        code: a ? g.too_big : g.too_small,
        minimum: o ? i.exactLength.value : void 0,
        maximum: a ? i.exactLength.value : void 0,
        type: "array",
        inclusive: !0,
        exact: !0,
        message: i.exactLength.message
      }), s.dirty());
    }
    if (i.minLength !== null && n.data.length < i.minLength.value && (T(n, {
      code: g.too_small,
      minimum: i.minLength.value,
      type: "array",
      inclusive: !0,
      exact: !1,
      message: i.minLength.message
    }), s.dirty()), i.maxLength !== null && n.data.length > i.maxLength.value && (T(n, {
      code: g.too_big,
      maximum: i.maxLength.value,
      type: "array",
      inclusive: !0,
      exact: !1,
      message: i.maxLength.message
    }), s.dirty()), n.common.async)
      return Promise.all([...n.data].map((a, o) => i.type._parseAsync(new Fe(n, a, n.path, o)))).then((a) => he.mergeArray(s, a));
    const r = [...n.data].map((a, o) => i.type._parseSync(new Fe(n, a, n.path, o)));
    return he.mergeArray(s, r);
  }
  get element() {
    return this._def.type;
  }
  min(e, n) {
    return new Pe({
      ...this._def,
      minLength: { value: e, message: E.toString(n) }
    });
  }
  max(e, n) {
    return new Pe({
      ...this._def,
      maxLength: { value: e, message: E.toString(n) }
    });
  }
  length(e, n) {
    return new Pe({
      ...this._def,
      exactLength: { value: e, message: E.toString(n) }
    });
  }
  nonempty(e) {
    return this.min(1, e);
  }
}
Pe.create = (t, e) => new Pe({
  type: t,
  minLength: null,
  maxLength: null,
  exactLength: null,
  typeName: C.ZodArray,
  ...R(e)
});
function yt(t) {
  if (t instanceof te) {
    const e = {};
    for (const n in t.shape) {
      const s = t.shape[n];
      e[n] = He.create(yt(s));
    }
    return new te({
      ...t._def,
      shape: () => e
    });
  } else return t instanceof Pe ? new Pe({
    ...t._def,
    type: yt(t.element)
  }) : t instanceof He ? He.create(yt(t.unwrap())) : t instanceof ut ? ut.create(yt(t.unwrap())) : t instanceof ot ? ot.create(t.items.map((e) => yt(e))) : t;
}
class te extends U {
  constructor() {
    super(...arguments), this._cached = null, this.nonstrict = this.passthrough, this.augment = this.extend;
  }
  _getCached() {
    if (this._cached !== null)
      return this._cached;
    const e = this._def.shape(), n = j.objectKeys(e);
    return this._cached = { shape: e, keys: n }, this._cached;
  }
  _parse(e) {
    if (this._getType(e) !== I.object) {
      const l = this._getOrReturnCtx(e);
      return T(l, {
        code: g.invalid_type,
        expected: I.object,
        received: l.parsedType
      }), x;
    }
    const { status: s, ctx: i } = this._processInputParams(e), { shape: r, keys: a } = this._getCached(), o = [];
    if (!(this._def.catchall instanceof Ze && this._def.unknownKeys === "strip"))
      for (const l in i.data)
        a.includes(l) || o.push(l);
    const c = [];
    for (const l of a) {
      const u = r[l], p = i.data[l];
      c.push({
        key: { status: "valid", value: l },
        value: u._parse(new Fe(i, p, i.path, l)),
        alwaysSet: l in i.data
      });
    }
    if (this._def.catchall instanceof Ze) {
      const l = this._def.unknownKeys;
      if (l === "passthrough")
        for (const u of o)
          c.push({
            key: { status: "valid", value: u },
            value: { status: "valid", value: i.data[u] }
          });
      else if (l === "strict")
        o.length > 0 && (T(i, {
          code: g.unrecognized_keys,
          keys: o
        }), s.dirty());
      else if (l !== "strip") throw new Error("Internal ZodObject error: invalid unknownKeys value.");
    } else {
      const l = this._def.catchall;
      for (const u of o) {
        const p = i.data[u];
        c.push({
          key: { status: "valid", value: u },
          value: l._parse(
            new Fe(i, p, i.path, u)
            //, ctx.child(key), value, getParsedType(value)
          ),
          alwaysSet: u in i.data
        });
      }
    }
    return i.common.async ? Promise.resolve().then(async () => {
      const l = [];
      for (const u of c) {
        const p = await u.key, f = await u.value;
        l.push({
          key: p,
          value: f,
          alwaysSet: u.alwaysSet
        });
      }
      return l;
    }).then((l) => he.mergeObjectSync(s, l)) : he.mergeObjectSync(s, c);
  }
  get shape() {
    return this._def.shape();
  }
  strict(e) {
    return E.errToObj, new te({
      ...this._def,
      unknownKeys: "strict",
      ...e !== void 0 ? {
        errorMap: (n, s) => {
          var r, a;
          const i = ((a = (r = this._def).errorMap) == null ? void 0 : a.call(r, n, s).message) ?? s.defaultError;
          return n.code === "unrecognized_keys" ? {
            message: E.errToObj(e).message ?? i
          } : {
            message: i
          };
        }
      } : {}
    });
  }
  strip() {
    return new te({
      ...this._def,
      unknownKeys: "strip"
    });
  }
  passthrough() {
    return new te({
      ...this._def,
      unknownKeys: "passthrough"
    });
  }
  // const AugmentFactory =
  //   <Def extends ZodObjectDef>(def: Def) =>
  //   <Augmentation extends ZodRawShape>(
  //     augmentation: Augmentation
  //   ): ZodObject<
  //     extendShape<ReturnType<Def["shape"]>, Augmentation>,
  //     Def["unknownKeys"],
  //     Def["catchall"]
  //   > => {
  //     return new ZodObject({
  //       ...def,
  //       shape: () => ({
  //         ...def.shape(),
  //         ...augmentation,
  //       }),
  //     }) as any;
  //   };
  extend(e) {
    return new te({
      ...this._def,
      shape: () => ({
        ...this._def.shape(),
        ...e
      })
    });
  }
  /**
   * Prior to zod@1.0.12 there was a bug in the
   * inferred type of merged objects. Please
   * upgrade if you are experiencing issues.
   */
  merge(e) {
    return new te({
      unknownKeys: e._def.unknownKeys,
      catchall: e._def.catchall,
      shape: () => ({
        ...this._def.shape(),
        ...e._def.shape()
      }),
      typeName: C.ZodObject
    });
  }
  // merge<
  //   Incoming extends AnyZodObject,
  //   Augmentation extends Incoming["shape"],
  //   NewOutput extends {
  //     [k in keyof Augmentation | keyof Output]: k extends keyof Augmentation
  //       ? Augmentation[k]["_output"]
  //       : k extends keyof Output
  //       ? Output[k]
  //       : never;
  //   },
  //   NewInput extends {
  //     [k in keyof Augmentation | keyof Input]: k extends keyof Augmentation
  //       ? Augmentation[k]["_input"]
  //       : k extends keyof Input
  //       ? Input[k]
  //       : never;
  //   }
  // >(
  //   merging: Incoming
  // ): ZodObject<
  //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
  //   Incoming["_def"]["unknownKeys"],
  //   Incoming["_def"]["catchall"],
  //   NewOutput,
  //   NewInput
  // > {
  //   const merged: any = new ZodObject({
  //     unknownKeys: merging._def.unknownKeys,
  //     catchall: merging._def.catchall,
  //     shape: () =>
  //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
  //     typeName: ZodFirstPartyTypeKind.ZodObject,
  //   }) as any;
  //   return merged;
  // }
  setKey(e, n) {
    return this.augment({ [e]: n });
  }
  // merge<Incoming extends AnyZodObject>(
  //   merging: Incoming
  // ): //ZodObject<T & Incoming["_shape"], UnknownKeys, Catchall> = (merging) => {
  // ZodObject<
  //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
  //   Incoming["_def"]["unknownKeys"],
  //   Incoming["_def"]["catchall"]
  // > {
  //   // const mergedShape = objectUtil.mergeShapes(
  //   //   this._def.shape(),
  //   //   merging._def.shape()
  //   // );
  //   const merged: any = new ZodObject({
  //     unknownKeys: merging._def.unknownKeys,
  //     catchall: merging._def.catchall,
  //     shape: () =>
  //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
  //     typeName: ZodFirstPartyTypeKind.ZodObject,
  //   }) as any;
  //   return merged;
  // }
  catchall(e) {
    return new te({
      ...this._def,
      catchall: e
    });
  }
  pick(e) {
    const n = {};
    for (const s of j.objectKeys(e))
      e[s] && this.shape[s] && (n[s] = this.shape[s]);
    return new te({
      ...this._def,
      shape: () => n
    });
  }
  omit(e) {
    const n = {};
    for (const s of j.objectKeys(this.shape))
      e[s] || (n[s] = this.shape[s]);
    return new te({
      ...this._def,
      shape: () => n
    });
  }
  /**
   * @deprecated
   */
  deepPartial() {
    return yt(this);
  }
  partial(e) {
    const n = {};
    for (const s of j.objectKeys(this.shape)) {
      const i = this.shape[s];
      e && !e[s] ? n[s] = i : n[s] = i.optional();
    }
    return new te({
      ...this._def,
      shape: () => n
    });
  }
  required(e) {
    const n = {};
    for (const s of j.objectKeys(this.shape))
      if (e && !e[s])
        n[s] = this.shape[s];
      else {
        let r = this.shape[s];
        for (; r instanceof He; )
          r = r._def.innerType;
        n[s] = r;
      }
    return new te({
      ...this._def,
      shape: () => n
    });
  }
  keyof() {
    return Pi(j.objectKeys(this.shape));
  }
}
te.create = (t, e) => new te({
  shape: () => t,
  unknownKeys: "strip",
  catchall: Ze.create(),
  typeName: C.ZodObject,
  ...R(e)
});
te.strictCreate = (t, e) => new te({
  shape: () => t,
  unknownKeys: "strict",
  catchall: Ze.create(),
  typeName: C.ZodObject,
  ...R(e)
});
te.lazycreate = (t, e) => new te({
  shape: t,
  unknownKeys: "strip",
  catchall: Ze.create(),
  typeName: C.ZodObject,
  ...R(e)
});
class cn extends U {
  _parse(e) {
    const { ctx: n } = this._processInputParams(e), s = this._def.options;
    function i(r) {
      for (const o of r)
        if (o.result.status === "valid")
          return o.result;
      for (const o of r)
        if (o.result.status === "dirty")
          return n.common.issues.push(...o.ctx.common.issues), o.result;
      const a = r.map((o) => new Be(o.ctx.common.issues));
      return T(n, {
        code: g.invalid_union,
        unionErrors: a
      }), x;
    }
    if (n.common.async)
      return Promise.all(s.map(async (r) => {
        const a = {
          ...n,
          common: {
            ...n.common,
            issues: []
          },
          parent: null
        };
        return {
          result: await r._parseAsync({
            data: n.data,
            path: n.path,
            parent: a
          }),
          ctx: a
        };
      })).then(i);
    {
      let r;
      const a = [];
      for (const c of s) {
        const l = {
          ...n,
          common: {
            ...n.common,
            issues: []
          },
          parent: null
        }, u = c._parseSync({
          data: n.data,
          path: n.path,
          parent: l
        });
        if (u.status === "valid")
          return u;
        u.status === "dirty" && !r && (r = { result: u, ctx: l }), l.common.issues.length && a.push(l.common.issues);
      }
      if (r)
        return n.common.issues.push(...r.ctx.common.issues), r.result;
      const o = a.map((c) => new Be(c));
      return T(n, {
        code: g.invalid_union,
        unionErrors: o
      }), x;
    }
  }
  get options() {
    return this._def.options;
  }
}
cn.create = (t, e) => new cn({
  options: t,
  typeName: C.ZodUnion,
  ...R(e)
});
const je = (t) => t instanceof rs ? je(t.schema) : t instanceof lt ? je(t.innerType()) : t instanceof dn ? [t.value] : t instanceof ct ? t.options : t instanceof as ? j.objectValues(t.enum) : t instanceof hn ? je(t._def.innerType) : t instanceof ts ? [void 0] : t instanceof ns ? [null] : t instanceof He ? [void 0, ...je(t.unwrap())] : t instanceof ut ? [null, ...je(t.unwrap())] : t instanceof Fi || t instanceof mn ? je(t.unwrap()) : t instanceof fn ? je(t._def.innerType) : [];
class Ts extends U {
  _parse(e) {
    const { ctx: n } = this._processInputParams(e);
    if (n.parsedType !== I.object)
      return T(n, {
        code: g.invalid_type,
        expected: I.object,
        received: n.parsedType
      }), x;
    const s = this.discriminator, i = n.data[s], r = this.optionsMap.get(i);
    return r ? n.common.async ? r._parseAsync({
      data: n.data,
      path: n.path,
      parent: n
    }) : r._parseSync({
      data: n.data,
      path: n.path,
      parent: n
    }) : (T(n, {
      code: g.invalid_union_discriminator,
      options: Array.from(this.optionsMap.keys()),
      path: [s]
    }), x);
  }
  get discriminator() {
    return this._def.discriminator;
  }
  get options() {
    return this._def.options;
  }
  get optionsMap() {
    return this._def.optionsMap;
  }
  /**
   * The constructor of the discriminated union schema. Its behaviour is very similar to that of the normal z.union() constructor.
   * However, it only allows a union of objects, all of which need to share a discriminator property. This property must
   * have a different value for each object in the union.
   * @param discriminator the name of the discriminator property
   * @param types an array of object schemas
   * @param params
   */
  static create(e, n, s) {
    const i = /* @__PURE__ */ new Map();
    for (const r of n) {
      const a = je(r.shape[e]);
      if (!a.length)
        throw new Error(`A discriminator value for key \`${e}\` could not be extracted from all schema options`);
      for (const o of a) {
        if (i.has(o))
          throw new Error(`Discriminator property ${String(e)} has duplicate value ${String(o)}`);
        i.set(o, r);
      }
    }
    return new Ts({
      typeName: C.ZodDiscriminatedUnion,
      discriminator: e,
      options: n,
      optionsMap: i,
      ...R(s)
    });
  }
}
function is(t, e) {
  const n = ze(t), s = ze(e);
  if (t === e)
    return { valid: !0, data: t };
  if (n === I.object && s === I.object) {
    const i = j.objectKeys(e), r = j.objectKeys(t).filter((o) => i.indexOf(o) !== -1), a = { ...t, ...e };
    for (const o of r) {
      const c = is(t[o], e[o]);
      if (!c.valid)
        return { valid: !1 };
      a[o] = c.data;
    }
    return { valid: !0, data: a };
  } else if (n === I.array && s === I.array) {
    if (t.length !== e.length)
      return { valid: !1 };
    const i = [];
    for (let r = 0; r < t.length; r++) {
      const a = t[r], o = e[r], c = is(a, o);
      if (!c.valid)
        return { valid: !1 };
      i.push(c.data);
    }
    return { valid: !0, data: i };
  } else return n === I.date && s === I.date && +t == +e ? { valid: !0, data: t } : { valid: !1 };
}
class ln extends U {
  _parse(e) {
    const { status: n, ctx: s } = this._processInputParams(e), i = (r, a) => {
      if (Bs(r) || Bs(a))
        return x;
      const o = is(r.value, a.value);
      return o.valid ? ((Qs(r) || Qs(a)) && n.dirty(), { status: n.value, value: o.data }) : (T(s, {
        code: g.invalid_intersection_types
      }), x);
    };
    return s.common.async ? Promise.all([
      this._def.left._parseAsync({
        data: s.data,
        path: s.path,
        parent: s
      }),
      this._def.right._parseAsync({
        data: s.data,
        path: s.path,
        parent: s
      })
    ]).then(([r, a]) => i(r, a)) : i(this._def.left._parseSync({
      data: s.data,
      path: s.path,
      parent: s
    }), this._def.right._parseSync({
      data: s.data,
      path: s.path,
      parent: s
    }));
  }
}
ln.create = (t, e, n) => new ln({
  left: t,
  right: e,
  typeName: C.ZodIntersection,
  ...R(n)
});
class ot extends U {
  _parse(e) {
    const { status: n, ctx: s } = this._processInputParams(e);
    if (s.parsedType !== I.array)
      return T(s, {
        code: g.invalid_type,
        expected: I.array,
        received: s.parsedType
      }), x;
    if (s.data.length < this._def.items.length)
      return T(s, {
        code: g.too_small,
        minimum: this._def.items.length,
        inclusive: !0,
        exact: !1,
        type: "array"
      }), x;
    !this._def.rest && s.data.length > this._def.items.length && (T(s, {
      code: g.too_big,
      maximum: this._def.items.length,
      inclusive: !0,
      exact: !1,
      type: "array"
    }), n.dirty());
    const r = [...s.data].map((a, o) => {
      const c = this._def.items[o] || this._def.rest;
      return c ? c._parse(new Fe(s, a, s.path, o)) : null;
    }).filter((a) => !!a);
    return s.common.async ? Promise.all(r).then((a) => he.mergeArray(n, a)) : he.mergeArray(n, r);
  }
  get items() {
    return this._def.items;
  }
  rest(e) {
    return new ot({
      ...this._def,
      rest: e
    });
  }
}
ot.create = (t, e) => {
  if (!Array.isArray(t))
    throw new Error("You must pass an array of schemas to z.tuple([ ... ])");
  return new ot({
    items: t,
    typeName: C.ZodTuple,
    rest: null,
    ...R(e)
  });
};
class un extends U {
  get keySchema() {
    return this._def.keyType;
  }
  get valueSchema() {
    return this._def.valueType;
  }
  _parse(e) {
    const { status: n, ctx: s } = this._processInputParams(e);
    if (s.parsedType !== I.object)
      return T(s, {
        code: g.invalid_type,
        expected: I.object,
        received: s.parsedType
      }), x;
    const i = [], r = this._def.keyType, a = this._def.valueType;
    for (const o in s.data)
      i.push({
        key: r._parse(new Fe(s, o, s.path, o)),
        value: a._parse(new Fe(s, s.data[o], s.path, o)),
        alwaysSet: o in s.data
      });
    return s.common.async ? he.mergeObjectAsync(n, i) : he.mergeObjectSync(n, i);
  }
  get element() {
    return this._def.valueType;
  }
  static create(e, n, s) {
    return n instanceof U ? new un({
      keyType: e,
      valueType: n,
      typeName: C.ZodRecord,
      ...R(s)
    }) : new un({
      keyType: Ge.create(),
      valueType: e,
      typeName: C.ZodRecord,
      ...R(n)
    });
  }
}
class Zs extends U {
  get keySchema() {
    return this._def.keyType;
  }
  get valueSchema() {
    return this._def.valueType;
  }
  _parse(e) {
    const { status: n, ctx: s } = this._processInputParams(e);
    if (s.parsedType !== I.map)
      return T(s, {
        code: g.invalid_type,
        expected: I.map,
        received: s.parsedType
      }), x;
    const i = this._def.keyType, r = this._def.valueType, a = [...s.data.entries()].map(([o, c], l) => ({
      key: i._parse(new Fe(s, o, s.path, [l, "key"])),
      value: r._parse(new Fe(s, c, s.path, [l, "value"]))
    }));
    if (s.common.async) {
      const o = /* @__PURE__ */ new Map();
      return Promise.resolve().then(async () => {
        for (const c of a) {
          const l = await c.key, u = await c.value;
          if (l.status === "aborted" || u.status === "aborted")
            return x;
          (l.status === "dirty" || u.status === "dirty") && n.dirty(), o.set(l.value, u.value);
        }
        return { status: n.value, value: o };
      });
    } else {
      const o = /* @__PURE__ */ new Map();
      for (const c of a) {
        const l = c.key, u = c.value;
        if (l.status === "aborted" || u.status === "aborted")
          return x;
        (l.status === "dirty" || u.status === "dirty") && n.dirty(), o.set(l.value, u.value);
      }
      return { status: n.value, value: o };
    }
  }
}
Zs.create = (t, e, n) => new Zs({
  valueType: e,
  keyType: t,
  typeName: C.ZodMap,
  ...R(n)
});
class Vt extends U {
  _parse(e) {
    const { status: n, ctx: s } = this._processInputParams(e);
    if (s.parsedType !== I.set)
      return T(s, {
        code: g.invalid_type,
        expected: I.set,
        received: s.parsedType
      }), x;
    const i = this._def;
    i.minSize !== null && s.data.size < i.minSize.value && (T(s, {
      code: g.too_small,
      minimum: i.minSize.value,
      type: "set",
      inclusive: !0,
      exact: !1,
      message: i.minSize.message
    }), n.dirty()), i.maxSize !== null && s.data.size > i.maxSize.value && (T(s, {
      code: g.too_big,
      maximum: i.maxSize.value,
      type: "set",
      inclusive: !0,
      exact: !1,
      message: i.maxSize.message
    }), n.dirty());
    const r = this._def.valueType;
    function a(c) {
      const l = /* @__PURE__ */ new Set();
      for (const u of c) {
        if (u.status === "aborted")
          return x;
        u.status === "dirty" && n.dirty(), l.add(u.value);
      }
      return { status: n.value, value: l };
    }
    const o = [...s.data.values()].map((c, l) => r._parse(new Fe(s, c, s.path, l)));
    return s.common.async ? Promise.all(o).then((c) => a(c)) : a(o);
  }
  min(e, n) {
    return new Vt({
      ...this._def,
      minSize: { value: e, message: E.toString(n) }
    });
  }
  max(e, n) {
    return new Vt({
      ...this._def,
      maxSize: { value: e, message: E.toString(n) }
    });
  }
  size(e, n) {
    return this.min(e, n).max(e, n);
  }
  nonempty(e) {
    return this.min(1, e);
  }
}
Vt.create = (t, e) => new Vt({
  valueType: t,
  minSize: null,
  maxSize: null,
  typeName: C.ZodSet,
  ...R(e)
});
class rs extends U {
  get schema() {
    return this._def.getter();
  }
  _parse(e) {
    const { ctx: n } = this._processInputParams(e);
    return this._def.getter()._parse({ data: n.data, path: n.path, parent: n });
  }
}
rs.create = (t, e) => new rs({
  getter: t,
  typeName: C.ZodLazy,
  ...R(e)
});
class dn extends U {
  _parse(e) {
    if (e.data !== this._def.value) {
      const n = this._getOrReturnCtx(e);
      return T(n, {
        received: n.data,
        code: g.invalid_literal,
        expected: this._def.value
      }), x;
    }
    return { status: "valid", value: e.data };
  }
  get value() {
    return this._def.value;
  }
}
dn.create = (t, e) => new dn({
  value: t,
  typeName: C.ZodLiteral,
  ...R(e)
});
function Pi(t, e) {
  return new ct({
    values: t,
    typeName: C.ZodEnum,
    ...R(e)
  });
}
class ct extends U {
  _parse(e) {
    if (typeof e.data != "string") {
      const n = this._getOrReturnCtx(e), s = this._def.values;
      return T(n, {
        expected: j.joinValues(s),
        received: n.parsedType,
        code: g.invalid_type
      }), x;
    }
    if (this._cache || (this._cache = new Set(this._def.values)), !this._cache.has(e.data)) {
      const n = this._getOrReturnCtx(e), s = this._def.values;
      return T(n, {
        received: n.data,
        code: g.invalid_enum_value,
        options: s
      }), x;
    }
    return be(e.data);
  }
  get options() {
    return this._def.values;
  }
  get enum() {
    const e = {};
    for (const n of this._def.values)
      e[n] = n;
    return e;
  }
  get Values() {
    const e = {};
    for (const n of this._def.values)
      e[n] = n;
    return e;
  }
  get Enum() {
    const e = {};
    for (const n of this._def.values)
      e[n] = n;
    return e;
  }
  extract(e, n = this._def) {
    return ct.create(e, {
      ...this._def,
      ...n
    });
  }
  exclude(e, n = this._def) {
    return ct.create(this.options.filter((s) => !e.includes(s)), {
      ...this._def,
      ...n
    });
  }
}
ct.create = Pi;
class as extends U {
  _parse(e) {
    const n = j.getValidEnumValues(this._def.values), s = this._getOrReturnCtx(e);
    if (s.parsedType !== I.string && s.parsedType !== I.number) {
      const i = j.objectValues(n);
      return T(s, {
        expected: j.joinValues(i),
        received: s.parsedType,
        code: g.invalid_type
      }), x;
    }
    if (this._cache || (this._cache = new Set(j.getValidEnumValues(this._def.values))), !this._cache.has(e.data)) {
      const i = j.objectValues(n);
      return T(s, {
        received: s.data,
        code: g.invalid_enum_value,
        options: i
      }), x;
    }
    return be(e.data);
  }
  get enum() {
    return this._def.values;
  }
}
as.create = (t, e) => new as({
  values: t,
  typeName: C.ZodNativeEnum,
  ...R(e)
});
class pn extends U {
  unwrap() {
    return this._def.type;
  }
  _parse(e) {
    const { ctx: n } = this._processInputParams(e);
    if (n.parsedType !== I.promise && n.common.async === !1)
      return T(n, {
        code: g.invalid_type,
        expected: I.promise,
        received: n.parsedType
      }), x;
    const s = n.parsedType === I.promise ? n.data : Promise.resolve(n.data);
    return be(s.then((i) => this._def.type.parseAsync(i, {
      path: n.path,
      errorMap: n.common.contextualErrorMap
    })));
  }
}
pn.create = (t, e) => new pn({
  type: t,
  typeName: C.ZodPromise,
  ...R(e)
});
class lt extends U {
  innerType() {
    return this._def.schema;
  }
  sourceType() {
    return this._def.schema._def.typeName === C.ZodEffects ? this._def.schema.sourceType() : this._def.schema;
  }
  _parse(e) {
    const { status: n, ctx: s } = this._processInputParams(e), i = this._def.effect || null, r = {
      addIssue: (a) => {
        T(s, a), a.fatal ? n.abort() : n.dirty();
      },
      get path() {
        return s.path;
      }
    };
    if (r.addIssue = r.addIssue.bind(r), i.type === "preprocess") {
      const a = i.transform(s.data, r);
      if (s.common.async)
        return Promise.resolve(a).then(async (o) => {
          if (n.value === "aborted")
            return x;
          const c = await this._def.schema._parseAsync({
            data: o,
            path: s.path,
            parent: s
          });
          return c.status === "aborted" ? x : c.status === "dirty" || n.value === "dirty" ? Pt(c.value) : c;
        });
      {
        if (n.value === "aborted")
          return x;
        const o = this._def.schema._parseSync({
          data: a,
          path: s.path,
          parent: s
        });
        return o.status === "aborted" ? x : o.status === "dirty" || n.value === "dirty" ? Pt(o.value) : o;
      }
    }
    if (i.type === "refinement") {
      const a = (o) => {
        const c = i.refinement(o, r);
        if (s.common.async)
          return Promise.resolve(c);
        if (c instanceof Promise)
          throw new Error("Async refinement encountered during synchronous parse operation. Use .parseAsync instead.");
        return o;
      };
      if (s.common.async === !1) {
        const o = this._def.schema._parseSync({
          data: s.data,
          path: s.path,
          parent: s
        });
        return o.status === "aborted" ? x : (o.status === "dirty" && n.dirty(), a(o.value), { status: n.value, value: o.value });
      } else
        return this._def.schema._parseAsync({ data: s.data, path: s.path, parent: s }).then((o) => o.status === "aborted" ? x : (o.status === "dirty" && n.dirty(), a(o.value).then(() => ({ status: n.value, value: o.value }))));
    }
    if (i.type === "transform")
      if (s.common.async === !1) {
        const a = this._def.schema._parseSync({
          data: s.data,
          path: s.path,
          parent: s
        });
        if (!bt(a))
          return x;
        const o = i.transform(a.value, r);
        if (o instanceof Promise)
          throw new Error("Asynchronous transform encountered during synchronous parse operation. Use .parseAsync instead.");
        return { status: n.value, value: o };
      } else
        return this._def.schema._parseAsync({ data: s.data, path: s.path, parent: s }).then((a) => bt(a) ? Promise.resolve(i.transform(a.value, r)).then((o) => ({
          status: n.value,
          value: o
        })) : x);
    j.assertNever(i);
  }
}
lt.create = (t, e, n) => new lt({
  schema: t,
  typeName: C.ZodEffects,
  effect: e,
  ...R(n)
});
lt.createWithPreprocess = (t, e, n) => new lt({
  schema: e,
  effect: { type: "preprocess", transform: t },
  typeName: C.ZodEffects,
  ...R(n)
});
class He extends U {
  _parse(e) {
    return this._getType(e) === I.undefined ? be(void 0) : this._def.innerType._parse(e);
  }
  unwrap() {
    return this._def.innerType;
  }
}
He.create = (t, e) => new He({
  innerType: t,
  typeName: C.ZodOptional,
  ...R(e)
});
class ut extends U {
  _parse(e) {
    return this._getType(e) === I.null ? be(null) : this._def.innerType._parse(e);
  }
  unwrap() {
    return this._def.innerType;
  }
}
ut.create = (t, e) => new ut({
  innerType: t,
  typeName: C.ZodNullable,
  ...R(e)
});
class hn extends U {
  _parse(e) {
    const { ctx: n } = this._processInputParams(e);
    let s = n.data;
    return n.parsedType === I.undefined && (s = this._def.defaultValue()), this._def.innerType._parse({
      data: s,
      path: n.path,
      parent: n
    });
  }
  removeDefault() {
    return this._def.innerType;
  }
}
hn.create = (t, e) => new hn({
  innerType: t,
  typeName: C.ZodDefault,
  defaultValue: typeof e.default == "function" ? e.default : () => e.default,
  ...R(e)
});
class fn extends U {
  _parse(e) {
    const { ctx: n } = this._processInputParams(e), s = {
      ...n,
      common: {
        ...n.common,
        issues: []
      }
    }, i = this._def.innerType._parse({
      data: s.data,
      path: s.path,
      parent: {
        ...s
      }
    });
    return an(i) ? i.then((r) => ({
      status: "valid",
      value: r.status === "valid" ? r.value : this._def.catchValue({
        get error() {
          return new Be(s.common.issues);
        },
        input: s.data
      })
    })) : {
      status: "valid",
      value: i.status === "valid" ? i.value : this._def.catchValue({
        get error() {
          return new Be(s.common.issues);
        },
        input: s.data
      })
    };
  }
  removeCatch() {
    return this._def.innerType;
  }
}
fn.create = (t, e) => new fn({
  innerType: t,
  typeName: C.ZodCatch,
  catchValue: typeof e.catch == "function" ? e.catch : () => e.catch,
  ...R(e)
});
class Xs extends U {
  _parse(e) {
    if (this._getType(e) !== I.nan) {
      const s = this._getOrReturnCtx(e);
      return T(s, {
        code: g.invalid_type,
        expected: I.nan,
        received: s.parsedType
      }), x;
    }
    return { status: "valid", value: e.data };
  }
}
Xs.create = (t) => new Xs({
  typeName: C.ZodNaN,
  ...R(t)
});
class Fi extends U {
  _parse(e) {
    const { ctx: n } = this._processInputParams(e), s = n.data;
    return this._def.type._parse({
      data: s,
      path: n.path,
      parent: n
    });
  }
  unwrap() {
    return this._def.type;
  }
}
class Is extends U {
  _parse(e) {
    const { status: n, ctx: s } = this._processInputParams(e);
    if (s.common.async)
      return (async () => {
        const r = await this._def.in._parseAsync({
          data: s.data,
          path: s.path,
          parent: s
        });
        return r.status === "aborted" ? x : r.status === "dirty" ? (n.dirty(), Pt(r.value)) : this._def.out._parseAsync({
          data: r.value,
          path: s.path,
          parent: s
        });
      })();
    {
      const i = this._def.in._parseSync({
        data: s.data,
        path: s.path,
        parent: s
      });
      return i.status === "aborted" ? x : i.status === "dirty" ? (n.dirty(), {
        status: "dirty",
        value: i.value
      }) : this._def.out._parseSync({
        data: i.value,
        path: s.path,
        parent: s
      });
    }
  }
  static create(e, n) {
    return new Is({
      in: e,
      out: n,
      typeName: C.ZodPipeline
    });
  }
}
class mn extends U {
  _parse(e) {
    const n = this._def.innerType._parse(e), s = (i) => (bt(i) && (i.value = Object.freeze(i.value)), i);
    return an(n) ? n.then((i) => s(i)) : s(n);
  }
  unwrap() {
    return this._def.innerType;
  }
}
mn.create = (t, e) => new mn({
  innerType: t,
  typeName: C.ZodReadonly,
  ...R(e)
});
var C;
(function(t) {
  t.ZodString = "ZodString", t.ZodNumber = "ZodNumber", t.ZodNaN = "ZodNaN", t.ZodBigInt = "ZodBigInt", t.ZodBoolean = "ZodBoolean", t.ZodDate = "ZodDate", t.ZodSymbol = "ZodSymbol", t.ZodUndefined = "ZodUndefined", t.ZodNull = "ZodNull", t.ZodAny = "ZodAny", t.ZodUnknown = "ZodUnknown", t.ZodNever = "ZodNever", t.ZodVoid = "ZodVoid", t.ZodArray = "ZodArray", t.ZodObject = "ZodObject", t.ZodUnion = "ZodUnion", t.ZodDiscriminatedUnion = "ZodDiscriminatedUnion", t.ZodIntersection = "ZodIntersection", t.ZodTuple = "ZodTuple", t.ZodRecord = "ZodRecord", t.ZodMap = "ZodMap", t.ZodSet = "ZodSet", t.ZodFunction = "ZodFunction", t.ZodLazy = "ZodLazy", t.ZodLiteral = "ZodLiteral", t.ZodEnum = "ZodEnum", t.ZodEffects = "ZodEffects", t.ZodNativeEnum = "ZodNativeEnum", t.ZodOptional = "ZodOptional", t.ZodNullable = "ZodNullable", t.ZodDefault = "ZodDefault", t.ZodCatch = "ZodCatch", t.ZodPromise = "ZodPromise", t.ZodBranded = "ZodBranded", t.ZodPipeline = "ZodPipeline", t.ZodReadonly = "ZodReadonly";
})(C || (C = {}));
const oe = Ge.create, Ui = St.create, bp = es.create, Sp = ss.create;
Ze.create;
const wt = Pe.create, ve = te.create, Np = cn.create, Ep = Ts.create;
ln.create;
ot.create;
const Op = un.create, wp = dn.create, At = ct.create;
pn.create;
const Ap = He.create;
ut.create;
function xp(t) {
  t.prompt(
    "shopify_admin_graphql",
    {
      query: oe().describe("The specific Shopify Admin API question or request")
    },
    ({ query: e }) => ({
      messages: [
        {
          role: "user",
          content: {
            type: "text",
            text: `I need help writing a GraphQL operation for the Shopify Admin API.

Here is my specific request: ${e}

Please help me create a complete and correct GraphQL operation (query or mutation) for the Shopify Admin API that accomplishes this task. Include:
1. The full GraphQL operation with proper syntax
2. A brief explanation of what each part of the operation does
3. Any variables needed for the operation
4. How to handle the response data
5. Relevant documentation links if applicable

When formulating your response, make sure to:
- Use the latest Shopify Admin API best practices
- Structure the query efficiently, requesting only necessary fields
- Follow proper naming conventions for the GraphQL operation
- Handle error cases appropriately
- Ensure the query is optimized for performance

The GraphQL operation should be ready to use with minimal modification.`
          }
        }
      ]
    })
  );
}
function za() {
  const t = $.dirname(Nn(import.meta.url));
  if (t.includes("dev-mcp") && t.includes("dist") && !t.includes("shopify-dev-tools"))
    return $.join(t, "data");
  if (t.includes("/dist") || t.includes("\\dist")) {
    const e = t.lastIndexOf($.sep + "dist");
    if (e !== -1) {
      const n = t.substring(0, e + 5);
      return $.join(n, "data");
    } else
      return $.join(t, "data");
  }
  return $.resolve(t, "../data");
}
const jn = za();
function En(t, e) {
  if (t.length === 0)
    throw new Error("No APIs provided");
  if (e) {
    if (t.length !== 1)
      throw new Error(
        "schemaOptions can only be provided when requesting a single API"
      );
    return [
      {
        ...e,
        api: t[0],
        schemaPath: $.join(jn, `${t[0]}_${e.name}.json`)
      }
    ];
  }
  const n = $.join(jn, "latest-releases-schemas.json"), s = JSON.parse(
    Xn(n, "utf-8")
  ), i = [];
  for (const r of t) {
    const a = s[r];
    if (a) {
      const o = a.map((c) => ({
        ...c,
        api: r,
        schemaPath: $.join(jn, `${r}_${c.name}.json`)
      }));
      i.push(...o);
    }
  }
  return i;
}
let Ja = class {
  constructor() {
    zt(this, "cache", /* @__PURE__ */ new Map());
  }
  get(e) {
    return this.cache.get(e);
  }
  set(e, n) {
    this.cache.set(e, n);
  }
};
const Ks = new Ja();
async function Mi(t) {
  const e = t.schemaPath, n = Ks.get(e);
  if (n)
    return console.error(
      `[graphql-schema-utils] Using cached schema for ${e}`
    ), n;
  try {
    let s;
    if (e.endsWith(".gz")) {
      console.error(
        `[graphql-schema-utils] Reading compressed schema from ${e}`
      );
      const i = await Mn.readFile(e);
      s = qs.gunzipSync(i).toString("utf-8");
    } else if (Vs(e))
      console.error(`[graphql-schema-utils] Reading schema from ${e}`), s = await Mn.readFile(e, "utf-8");
    else {
      const i = `${e}.gz`;
      if (Vs(i)) {
        console.error(
          `[graphql-schema-utils] Reading compressed schema from ${i}`
        );
        const r = await Mn.readFile(i);
        s = qs.gunzipSync(r).toString("utf-8");
      } else
        throw new Error(`Schema file not found at ${e} or ${i}`);
    }
    return Ks.set(e, s), s;
  } catch (s) {
    throw console.error(`[graphql-schema-utils] Error loading schema: ${s}`), s;
  }
}
function V(t, e) {
  if (!!!t)
    throw new Error(e);
}
function Qe(t) {
  return typeof t == "object" && t !== null;
}
function ke(t, e) {
  if (!!!t)
    throw new Error(
      e ?? "Unexpected invariant triggered."
    );
}
const Za = /\r\n|[\n\r]/g;
function os(t, e) {
  let n = 0, s = 1;
  for (const i of t.body.matchAll(Za)) {
    if (typeof i.index == "number" || ke(!1), i.index >= e)
      break;
    n = i.index + i[0].length, s += 1;
  }
  return {
    line: s,
    column: e + 1 - n
  };
}
function Xa(t) {
  return $i(
    t.source,
    os(t.source, t.start)
  );
}
function $i(t, e) {
  const n = t.locationOffset.column - 1, s = "".padStart(n) + t.body, i = e.line - 1, r = t.locationOffset.line - 1, a = e.line + r, o = e.line === 1 ? n : 0, c = e.column + o, l = `${t.name}:${a}:${c}
`, u = s.split(/\r\n|[\n\r]/g), p = u[i];
  if (p.length > 120) {
    const f = Math.floor(c / 80), S = c % 80, v = [];
    for (let A = 0; A < p.length; A += 80)
      v.push(p.slice(A, A + 80));
    return l + ei([
      [`${a} |`, v[0]],
      ...v.slice(1, f + 1).map((A) => ["|", A]),
      ["|", "^".padStart(S)],
      ["|", v[f + 1]]
    ]);
  }
  return l + ei([
    // Lines specified like this: ["prefix", "string"],
    [`${a - 1} |`, u[i - 1]],
    [`${a} |`, p],
    ["|", "^".padStart(c)],
    [`${a + 1} |`, u[i + 1]]
  ]);
}
function ei(t) {
  const e = t.filter(([s, i]) => i !== void 0), n = Math.max(...e.map(([s]) => s.length));
  return e.map(([s, i]) => s.padStart(n) + (i ? " " + i : "")).join(`
`);
}
function Ka(t) {
  const e = t[0];
  return e == null || "kind" in e || "length" in e ? {
    nodes: e,
    source: t[1],
    positions: t[2],
    path: t[3],
    originalError: t[4],
    extensions: t[5]
  } : e;
}
class y extends Error {
  /**
   * An array of `{ line, column }` locations within the source GraphQL document
   * which correspond to this error.
   *
   * Errors during validation often contain multiple locations, for example to
   * point out two things with the same name. Errors during execution include a
   * single location, the field which produced the error.
   *
   * Enumerable, and appears in the result of JSON.stringify().
   */
  /**
   * An array describing the JSON-path into the execution response which
   * corresponds to this error. Only included for errors during execution.
   *
   * Enumerable, and appears in the result of JSON.stringify().
   */
  /**
   * An array of GraphQL AST Nodes corresponding to this error.
   */
  /**
   * The source GraphQL document for the first location of this error.
   *
   * Note that if this Error represents more than one node, the source may not
   * represent nodes after the first node.
   */
  /**
   * An array of character offsets within the source GraphQL document
   * which correspond to this error.
   */
  /**
   * The original error thrown from a field resolver during execution.
   */
  /**
   * Extension fields to add to the formatted error.
   */
  /**
   * @deprecated Please use the `GraphQLErrorOptions` constructor overload instead.
   */
  constructor(e, ...n) {
    var s, i, r;
    const { nodes: a, source: o, positions: c, path: l, originalError: u, extensions: p } = Ka(n);
    super(e), this.name = "GraphQLError", this.path = l ?? void 0, this.originalError = u ?? void 0, this.nodes = ti(
      Array.isArray(a) ? a : a ? [a] : void 0
    );
    const f = ti(
      (s = this.nodes) === null || s === void 0 ? void 0 : s.map((v) => v.loc).filter((v) => v != null)
    );
    this.source = o ?? (f == null || (i = f[0]) === null || i === void 0 ? void 0 : i.source), this.positions = c ?? (f == null ? void 0 : f.map((v) => v.start)), this.locations = c && o ? c.map((v) => os(o, v)) : f == null ? void 0 : f.map((v) => os(v.source, v.start));
    const S = Qe(
      u == null ? void 0 : u.extensions
    ) ? u == null ? void 0 : u.extensions : void 0;
    this.extensions = (r = p ?? S) !== null && r !== void 0 ? r : /* @__PURE__ */ Object.create(null), Object.defineProperties(this, {
      message: {
        writable: !0,
        enumerable: !0
      },
      name: {
        enumerable: !1
      },
      nodes: {
        enumerable: !1
      },
      source: {
        enumerable: !1
      },
      positions: {
        enumerable: !1
      },
      originalError: {
        enumerable: !1
      }
    }), u != null && u.stack ? Object.defineProperty(this, "stack", {
      value: u.stack,
      writable: !0,
      configurable: !0
    }) : Error.captureStackTrace ? Error.captureStackTrace(this, y) : Object.defineProperty(this, "stack", {
      value: Error().stack,
      writable: !0,
      configurable: !0
    });
  }
  get [Symbol.toStringTag]() {
    return "GraphQLError";
  }
  toString() {
    let e = this.message;
    if (this.nodes)
      for (const n of this.nodes)
        n.loc && (e += `

` + Xa(n.loc));
    else if (this.source && this.locations)
      for (const n of this.locations)
        e += `

` + $i(this.source, n);
    return e;
  }
  toJSON() {
    const e = {
      message: this.message
    };
    return this.locations != null && (e.locations = this.locations), this.path != null && (e.path = this.path), this.extensions != null && Object.keys(this.extensions).length > 0 && (e.extensions = this.extensions), e;
  }
}
function ti(t) {
  return t === void 0 || t.length === 0 ? void 0 : t;
}
function se(t, e, n) {
  return new y(`Syntax Error: ${n}`, {
    source: t,
    positions: [e]
  });
}
class eo {
  /**
   * The character offset at which this Node begins.
   */
  /**
   * The character offset at which this Node ends.
   */
  /**
   * The Token at which this Node begins.
   */
  /**
   * The Token at which this Node ends.
   */
  /**
   * The Source document the AST represents.
   */
  constructor(e, n, s) {
    this.start = e.start, this.end = n.end, this.startToken = e, this.endToken = n, this.source = s;
  }
  get [Symbol.toStringTag]() {
    return "Location";
  }
  toJSON() {
    return {
      start: this.start,
      end: this.end
    };
  }
}
class ji {
  /**
   * The kind of Token.
   */
  /**
   * The character offset at which this Node begins.
   */
  /**
   * The character offset at which this Node ends.
   */
  /**
   * The 1-indexed line number on which this Token appears.
   */
  /**
   * The 1-indexed column number at which this Token begins.
   */
  /**
   * For non-punctuation tokens, represents the interpreted value of the token.
   *
   * Note: is undefined for punctuation tokens, but typed as string for
   * convenience in the parser.
   */
  /**
   * Tokens exist as nodes in a double-linked-list amongst all tokens
   * including ignored tokens. <SOF> is always the first node and <EOF>
   * the last.
   */
  constructor(e, n, s, i, r, a) {
    this.kind = e, this.start = n, this.end = s, this.line = i, this.column = r, this.value = a, this.prev = null, this.next = null;
  }
  get [Symbol.toStringTag]() {
    return "Token";
  }
  toJSON() {
    return {
      kind: this.kind,
      value: this.value,
      line: this.line,
      column: this.column
    };
  }
}
const Vi = {
  Name: [],
  Document: ["definitions"],
  OperationDefinition: [
    "name",
    "variableDefinitions",
    "directives",
    "selectionSet"
  ],
  VariableDefinition: ["variable", "type", "defaultValue", "directives"],
  Variable: ["name"],
  SelectionSet: ["selections"],
  Field: ["alias", "name", "arguments", "directives", "selectionSet"],
  Argument: ["name", "value"],
  FragmentSpread: ["name", "directives"],
  InlineFragment: ["typeCondition", "directives", "selectionSet"],
  FragmentDefinition: [
    "name",
    // Note: fragment variable definitions are deprecated and will removed in v17.0.0
    "variableDefinitions",
    "typeCondition",
    "directives",
    "selectionSet"
  ],
  IntValue: [],
  FloatValue: [],
  StringValue: [],
  BooleanValue: [],
  NullValue: [],
  EnumValue: [],
  ListValue: ["values"],
  ObjectValue: ["fields"],
  ObjectField: ["name", "value"],
  Directive: ["name", "arguments"],
  NamedType: ["name"],
  ListType: ["type"],
  NonNullType: ["type"],
  SchemaDefinition: ["description", "directives", "operationTypes"],
  OperationTypeDefinition: ["type"],
  ScalarTypeDefinition: ["description", "name", "directives"],
  ObjectTypeDefinition: [
    "description",
    "name",
    "interfaces",
    "directives",
    "fields"
  ],
  FieldDefinition: ["description", "name", "arguments", "type", "directives"],
  InputValueDefinition: [
    "description",
    "name",
    "type",
    "defaultValue",
    "directives"
  ],
  InterfaceTypeDefinition: [
    "description",
    "name",
    "interfaces",
    "directives",
    "fields"
  ],
  UnionTypeDefinition: ["description", "name", "directives", "types"],
  EnumTypeDefinition: ["description", "name", "directives", "values"],
  EnumValueDefinition: ["description", "name", "directives"],
  InputObjectTypeDefinition: ["description", "name", "directives", "fields"],
  DirectiveDefinition: ["description", "name", "arguments", "locations"],
  SchemaExtension: ["directives", "operationTypes"],
  ScalarTypeExtension: ["name", "directives"],
  ObjectTypeExtension: ["name", "interfaces", "directives", "fields"],
  InterfaceTypeExtension: ["name", "interfaces", "directives", "fields"],
  UnionTypeExtension: ["name", "directives", "types"],
  EnumTypeExtension: ["name", "directives", "values"],
  InputObjectTypeExtension: ["name", "directives", "fields"]
}, to = new Set(Object.keys(Vi));
function cs(t) {
  const e = t == null ? void 0 : t.kind;
  return typeof e == "string" && to.has(e);
}
var de;
(function(t) {
  t.QUERY = "query", t.MUTATION = "mutation", t.SUBSCRIPTION = "subscription";
})(de || (de = {}));
var w;
(function(t) {
  t.QUERY = "QUERY", t.MUTATION = "MUTATION", t.SUBSCRIPTION = "SUBSCRIPTION", t.FIELD = "FIELD", t.FRAGMENT_DEFINITION = "FRAGMENT_DEFINITION", t.FRAGMENT_SPREAD = "FRAGMENT_SPREAD", t.INLINE_FRAGMENT = "INLINE_FRAGMENT", t.VARIABLE_DEFINITION = "VARIABLE_DEFINITION", t.SCHEMA = "SCHEMA", t.SCALAR = "SCALAR", t.OBJECT = "OBJECT", t.FIELD_DEFINITION = "FIELD_DEFINITION", t.ARGUMENT_DEFINITION = "ARGUMENT_DEFINITION", t.INTERFACE = "INTERFACE", t.UNION = "UNION", t.ENUM = "ENUM", t.ENUM_VALUE = "ENUM_VALUE", t.INPUT_OBJECT = "INPUT_OBJECT", t.INPUT_FIELD_DEFINITION = "INPUT_FIELD_DEFINITION";
})(w || (w = {}));
var d;
(function(t) {
  t.NAME = "Name", t.DOCUMENT = "Document", t.OPERATION_DEFINITION = "OperationDefinition", t.VARIABLE_DEFINITION = "VariableDefinition", t.SELECTION_SET = "SelectionSet", t.FIELD = "Field", t.ARGUMENT = "Argument", t.FRAGMENT_SPREAD = "FragmentSpread", t.INLINE_FRAGMENT = "InlineFragment", t.FRAGMENT_DEFINITION = "FragmentDefinition", t.VARIABLE = "Variable", t.INT = "IntValue", t.FLOAT = "FloatValue", t.STRING = "StringValue", t.BOOLEAN = "BooleanValue", t.NULL = "NullValue", t.ENUM = "EnumValue", t.LIST = "ListValue", t.OBJECT = "ObjectValue", t.OBJECT_FIELD = "ObjectField", t.DIRECTIVE = "Directive", t.NAMED_TYPE = "NamedType", t.LIST_TYPE = "ListType", t.NON_NULL_TYPE = "NonNullType", t.SCHEMA_DEFINITION = "SchemaDefinition", t.OPERATION_TYPE_DEFINITION = "OperationTypeDefinition", t.SCALAR_TYPE_DEFINITION = "ScalarTypeDefinition", t.OBJECT_TYPE_DEFINITION = "ObjectTypeDefinition", t.FIELD_DEFINITION = "FieldDefinition", t.INPUT_VALUE_DEFINITION = "InputValueDefinition", t.INTERFACE_TYPE_DEFINITION = "InterfaceTypeDefinition", t.UNION_TYPE_DEFINITION = "UnionTypeDefinition", t.ENUM_TYPE_DEFINITION = "EnumTypeDefinition", t.ENUM_VALUE_DEFINITION = "EnumValueDefinition", t.INPUT_OBJECT_TYPE_DEFINITION = "InputObjectTypeDefinition", t.DIRECTIVE_DEFINITION = "DirectiveDefinition", t.SCHEMA_EXTENSION = "SchemaExtension", t.SCALAR_TYPE_EXTENSION = "ScalarTypeExtension", t.OBJECT_TYPE_EXTENSION = "ObjectTypeExtension", t.INTERFACE_TYPE_EXTENSION = "InterfaceTypeExtension", t.UNION_TYPE_EXTENSION = "UnionTypeExtension", t.ENUM_TYPE_EXTENSION = "EnumTypeExtension", t.INPUT_OBJECT_TYPE_EXTENSION = "InputObjectTypeExtension";
})(d || (d = {}));
function ls(t) {
  return t === 9 || t === 32;
}
function qt(t) {
  return t >= 48 && t <= 57;
}
function qi(t) {
  return t >= 97 && t <= 122 || // A-Z
  t >= 65 && t <= 90;
}
function bs(t) {
  return qi(t) || t === 95;
}
function Gi(t) {
  return qi(t) || qt(t) || t === 95;
}
function no(t) {
  var e;
  let n = Number.MAX_SAFE_INTEGER, s = null, i = -1;
  for (let a = 0; a < t.length; ++a) {
    var r;
    const o = t[a], c = so(o);
    c !== o.length && (s = (r = s) !== null && r !== void 0 ? r : a, i = a, a !== 0 && c < n && (n = c));
  }
  return t.map((a, o) => o === 0 ? a : a.slice(n)).slice(
    (e = s) !== null && e !== void 0 ? e : 0,
    i + 1
  );
}
function so(t) {
  let e = 0;
  for (; e < t.length && ls(t.charCodeAt(e)); )
    ++e;
  return e;
}
function io(t, e) {
  const n = t.replace(/"""/g, '\\"""'), s = n.split(/\r\n|[\n\r]/g), i = s.length === 1, r = s.length > 1 && s.slice(1).every((S) => S.length === 0 || ls(S.charCodeAt(0))), a = n.endsWith('\\"""'), o = t.endsWith('"') && !a, c = t.endsWith("\\"), l = o || c, u = (
    // add leading and trailing new lines only if it improves readability
    !i || t.length > 70 || l || r || a
  );
  let p = "";
  const f = i && ls(t.charCodeAt(0));
  return (u && !f || r) && (p += `
`), p += n, (u || l) && (p += `
`), '"""' + p + '"""';
}
var m;
(function(t) {
  t.SOF = "<SOF>", t.EOF = "<EOF>", t.BANG = "!", t.DOLLAR = "$", t.AMP = "&", t.PAREN_L = "(", t.PAREN_R = ")", t.SPREAD = "...", t.COLON = ":", t.EQUALS = "=", t.AT = "@", t.BRACKET_L = "[", t.BRACKET_R = "]", t.BRACE_L = "{", t.PIPE = "|", t.BRACE_R = "}", t.NAME = "Name", t.INT = "Int", t.FLOAT = "Float", t.STRING = "String", t.BLOCK_STRING = "BlockString", t.COMMENT = "Comment";
})(m || (m = {}));
class ro {
  /**
   * The previously focused non-ignored token.
   */
  /**
   * The currently focused non-ignored token.
   */
  /**
   * The (1-indexed) line containing the current token.
   */
  /**
   * The character offset at which the current line begins.
   */
  constructor(e) {
    const n = new ji(m.SOF, 0, 0, 0, 0);
    this.source = e, this.lastToken = n, this.token = n, this.line = 1, this.lineStart = 0;
  }
  get [Symbol.toStringTag]() {
    return "Lexer";
  }
  /**
   * Advances the token stream to the next non-ignored token.
   */
  advance() {
    return this.lastToken = this.token, this.token = this.lookahead();
  }
  /**
   * Looks ahead and returns the next non-ignored token, but does not change
   * the state of Lexer.
   */
  lookahead() {
    let e = this.token;
    if (e.kind !== m.EOF)
      do
        if (e.next)
          e = e.next;
        else {
          const n = oo(this, e.end);
          e.next = n, n.prev = e, e = n;
        }
      while (e.kind === m.COMMENT);
    return e;
  }
}
function ao(t) {
  return t === m.BANG || t === m.DOLLAR || t === m.AMP || t === m.PAREN_L || t === m.PAREN_R || t === m.SPREAD || t === m.COLON || t === m.EQUALS || t === m.AT || t === m.BRACKET_L || t === m.BRACKET_R || t === m.BRACE_L || t === m.PIPE || t === m.BRACE_R;
}
function xt(t) {
  return t >= 0 && t <= 55295 || t >= 57344 && t <= 1114111;
}
function On(t, e) {
  return Hi(t.charCodeAt(e)) && Bi(t.charCodeAt(e + 1));
}
function Hi(t) {
  return t >= 55296 && t <= 56319;
}
function Bi(t) {
  return t >= 56320 && t <= 57343;
}
function dt(t, e) {
  const n = t.source.body.codePointAt(e);
  if (n === void 0)
    return m.EOF;
  if (n >= 32 && n <= 126) {
    const s = String.fromCodePoint(n);
    return s === '"' ? `'"'` : `"${s}"`;
  }
  return "U+" + n.toString(16).toUpperCase().padStart(4, "0");
}
function K(t, e, n, s, i) {
  const r = t.line, a = 1 + n - t.lineStart;
  return new ji(e, n, s, r, a, i);
}
function oo(t, e) {
  const n = t.source.body, s = n.length;
  let i = e;
  for (; i < s; ) {
    const r = n.charCodeAt(i);
    switch (r) {
      // Ignored ::
      //   - UnicodeBOM
      //   - WhiteSpace
      //   - LineTerminator
      //   - Comment
      //   - Comma
      //
      // UnicodeBOM :: "Byte Order Mark (U+FEFF)"
      //
      // WhiteSpace ::
      //   - "Horizontal Tab (U+0009)"
      //   - "Space (U+0020)"
      //
      // Comma :: ,
      case 65279:
      // <BOM>
      case 9:
      // \t
      case 32:
      // <space>
      case 44:
        ++i;
        continue;
      // LineTerminator ::
      //   - "New Line (U+000A)"
      //   - "Carriage Return (U+000D)" [lookahead != "New Line (U+000A)"]
      //   - "Carriage Return (U+000D)" "New Line (U+000A)"
      case 10:
        ++i, ++t.line, t.lineStart = i;
        continue;
      case 13:
        n.charCodeAt(i + 1) === 10 ? i += 2 : ++i, ++t.line, t.lineStart = i;
        continue;
      // Comment
      case 35:
        return co(t, i);
      // Token ::
      //   - Punctuator
      //   - Name
      //   - IntValue
      //   - FloatValue
      //   - StringValue
      //
      // Punctuator :: one of ! $ & ( ) ... : = @ [ ] { | }
      case 33:
        return K(t, m.BANG, i, i + 1);
      case 36:
        return K(t, m.DOLLAR, i, i + 1);
      case 38:
        return K(t, m.AMP, i, i + 1);
      case 40:
        return K(t, m.PAREN_L, i, i + 1);
      case 41:
        return K(t, m.PAREN_R, i, i + 1);
      case 46:
        if (n.charCodeAt(i + 1) === 46 && n.charCodeAt(i + 2) === 46)
          return K(t, m.SPREAD, i, i + 3);
        break;
      case 58:
        return K(t, m.COLON, i, i + 1);
      case 61:
        return K(t, m.EQUALS, i, i + 1);
      case 64:
        return K(t, m.AT, i, i + 1);
      case 91:
        return K(t, m.BRACKET_L, i, i + 1);
      case 93:
        return K(t, m.BRACKET_R, i, i + 1);
      case 123:
        return K(t, m.BRACE_L, i, i + 1);
      case 124:
        return K(t, m.PIPE, i, i + 1);
      case 125:
        return K(t, m.BRACE_R, i, i + 1);
      // StringValue
      case 34:
        return n.charCodeAt(i + 1) === 34 && n.charCodeAt(i + 2) === 34 ? mo(t, i) : uo(t, i);
    }
    if (qt(r) || r === 45)
      return lo(t, i, r);
    if (bs(r))
      return go(t, i);
    throw se(
      t.source,
      i,
      r === 39 ? `Unexpected single quote character ('), did you mean to use a double quote (")?` : xt(r) || On(n, i) ? `Unexpected character: ${dt(t, i)}.` : `Invalid character: ${dt(t, i)}.`
    );
  }
  return K(t, m.EOF, s, s);
}
function co(t, e) {
  const n = t.source.body, s = n.length;
  let i = e + 1;
  for (; i < s; ) {
    const r = n.charCodeAt(i);
    if (r === 10 || r === 13)
      break;
    if (xt(r))
      ++i;
    else if (On(n, i))
      i += 2;
    else
      break;
  }
  return K(
    t,
    m.COMMENT,
    e,
    i,
    n.slice(e + 1, i)
  );
}
function lo(t, e, n) {
  const s = t.source.body;
  let i = e, r = n, a = !1;
  if (r === 45 && (r = s.charCodeAt(++i)), r === 48) {
    if (r = s.charCodeAt(++i), qt(r))
      throw se(
        t.source,
        i,
        `Invalid number, unexpected digit after 0: ${dt(
          t,
          i
        )}.`
      );
  } else
    i = Vn(t, i, r), r = s.charCodeAt(i);
  if (r === 46 && (a = !0, r = s.charCodeAt(++i), i = Vn(t, i, r), r = s.charCodeAt(i)), (r === 69 || r === 101) && (a = !0, r = s.charCodeAt(++i), (r === 43 || r === 45) && (r = s.charCodeAt(++i)), i = Vn(t, i, r), r = s.charCodeAt(i)), r === 46 || bs(r))
    throw se(
      t.source,
      i,
      `Invalid number, expected digit but got: ${dt(
        t,
        i
      )}.`
    );
  return K(
    t,
    a ? m.FLOAT : m.INT,
    e,
    i,
    s.slice(e, i)
  );
}
function Vn(t, e, n) {
  if (!qt(n))
    throw se(
      t.source,
      e,
      `Invalid number, expected digit but got: ${dt(
        t,
        e
      )}.`
    );
  const s = t.source.body;
  let i = e + 1;
  for (; qt(s.charCodeAt(i)); )
    ++i;
  return i;
}
function uo(t, e) {
  const n = t.source.body, s = n.length;
  let i = e + 1, r = i, a = "";
  for (; i < s; ) {
    const o = n.charCodeAt(i);
    if (o === 34)
      return a += n.slice(r, i), K(t, m.STRING, e, i + 1, a);
    if (o === 92) {
      a += n.slice(r, i);
      const c = n.charCodeAt(i + 1) === 117 ? n.charCodeAt(i + 2) === 123 ? po(t, i) : ho(t, i) : fo(t, i);
      a += c.value, i += c.size, r = i;
      continue;
    }
    if (o === 10 || o === 13)
      break;
    if (xt(o))
      ++i;
    else if (On(n, i))
      i += 2;
    else
      throw se(
        t.source,
        i,
        `Invalid character within String: ${dt(
          t,
          i
        )}.`
      );
  }
  throw se(t.source, i, "Unterminated string.");
}
function po(t, e) {
  const n = t.source.body;
  let s = 0, i = 3;
  for (; i < 12; ) {
    const r = n.charCodeAt(e + i++);
    if (r === 125) {
      if (i < 5 || !xt(s))
        break;
      return {
        value: String.fromCodePoint(s),
        size: i
      };
    }
    if (s = s << 4 | Ft(r), s < 0)
      break;
  }
  throw se(
    t.source,
    e,
    `Invalid Unicode escape sequence: "${n.slice(
      e,
      e + i
    )}".`
  );
}
function ho(t, e) {
  const n = t.source.body, s = ni(n, e + 2);
  if (xt(s))
    return {
      value: String.fromCodePoint(s),
      size: 6
    };
  if (Hi(s) && n.charCodeAt(e + 6) === 92 && n.charCodeAt(e + 7) === 117) {
    const i = ni(n, e + 8);
    if (Bi(i))
      return {
        value: String.fromCodePoint(s, i),
        size: 12
      };
  }
  throw se(
    t.source,
    e,
    `Invalid Unicode escape sequence: "${n.slice(e, e + 6)}".`
  );
}
function ni(t, e) {
  return Ft(t.charCodeAt(e)) << 12 | Ft(t.charCodeAt(e + 1)) << 8 | Ft(t.charCodeAt(e + 2)) << 4 | Ft(t.charCodeAt(e + 3));
}
function Ft(t) {
  return t >= 48 && t <= 57 ? t - 48 : t >= 65 && t <= 70 ? t - 55 : t >= 97 && t <= 102 ? t - 87 : -1;
}
function fo(t, e) {
  const n = t.source.body;
  switch (n.charCodeAt(e + 1)) {
    case 34:
      return {
        value: '"',
        size: 2
      };
    case 92:
      return {
        value: "\\",
        size: 2
      };
    case 47:
      return {
        value: "/",
        size: 2
      };
    case 98:
      return {
        value: "\b",
        size: 2
      };
    case 102:
      return {
        value: "\f",
        size: 2
      };
    case 110:
      return {
        value: `
`,
        size: 2
      };
    case 114:
      return {
        value: "\r",
        size: 2
      };
    case 116:
      return {
        value: "	",
        size: 2
      };
  }
  throw se(
    t.source,
    e,
    `Invalid character escape sequence: "${n.slice(
      e,
      e + 2
    )}".`
  );
}
function mo(t, e) {
  const n = t.source.body, s = n.length;
  let i = t.lineStart, r = e + 3, a = r, o = "";
  const c = [];
  for (; r < s; ) {
    const l = n.charCodeAt(r);
    if (l === 34 && n.charCodeAt(r + 1) === 34 && n.charCodeAt(r + 2) === 34) {
      o += n.slice(a, r), c.push(o);
      const u = K(
        t,
        m.BLOCK_STRING,
        e,
        r + 3,
        // Return a string of the lines joined with U+000A.
        no(c).join(`
`)
      );
      return t.line += c.length - 1, t.lineStart = i, u;
    }
    if (l === 92 && n.charCodeAt(r + 1) === 34 && n.charCodeAt(r + 2) === 34 && n.charCodeAt(r + 3) === 34) {
      o += n.slice(a, r), a = r + 1, r += 4;
      continue;
    }
    if (l === 10 || l === 13) {
      o += n.slice(a, r), c.push(o), l === 13 && n.charCodeAt(r + 1) === 10 ? r += 2 : ++r, o = "", a = r, i = r;
      continue;
    }
    if (xt(l))
      ++r;
    else if (On(n, r))
      r += 2;
    else
      throw se(
        t.source,
        r,
        `Invalid character within String: ${dt(
          t,
          r
        )}.`
      );
  }
  throw se(t.source, r, "Unterminated string.");
}
function go(t, e) {
  const n = t.source.body, s = n.length;
  let i = e + 1;
  for (; i < s; ) {
    const r = n.charCodeAt(i);
    if (Gi(r))
      ++i;
    else
      break;
  }
  return K(
    t,
    m.NAME,
    e,
    i,
    n.slice(e, i)
  );
}
const yo = 10, Qi = 2;
function _(t) {
  return wn(t, []);
}
function wn(t, e) {
  switch (typeof t) {
    case "string":
      return JSON.stringify(t);
    case "function":
      return t.name ? `[function ${t.name}]` : "[function]";
    case "object":
      return _o(t, e);
    default:
      return String(t);
  }
}
function _o(t, e) {
  if (t === null)
    return "null";
  if (e.includes(t))
    return "[Circular]";
  const n = [...e, t];
  if (vo(t)) {
    const s = t.toJSON();
    if (s !== t)
      return typeof s == "string" ? s : wn(s, n);
  } else if (Array.isArray(t))
    return Io(t, n);
  return To(t, n);
}
function vo(t) {
  return typeof t.toJSON == "function";
}
function To(t, e) {
  const n = Object.entries(t);
  return n.length === 0 ? "{}" : e.length > Qi ? "[" + bo(t) + "]" : "{ " + n.map(
    ([i, r]) => i + ": " + wn(r, e)
  ).join(", ") + " }";
}
function Io(t, e) {
  if (t.length === 0)
    return "[]";
  if (e.length > Qi)
    return "[Array]";
  const n = Math.min(yo, t.length), s = t.length - n, i = [];
  for (let r = 0; r < n; ++r)
    i.push(wn(t[r], e));
  return s === 1 ? i.push("... 1 more item") : s > 1 && i.push(`... ${s} more items`), "[" + i.join(", ") + "]";
}
function bo(t) {
  const e = Object.prototype.toString.call(t).replace(/^\[object /, "").replace(/]$/, "");
  if (e === "Object" && typeof t.constructor == "function") {
    const n = t.constructor.name;
    if (typeof n == "string" && n !== "")
      return n;
  }
  return e;
}
const So = globalThis.process && // eslint-disable-next-line no-undef
process.env.NODE_ENV === "production", Re = (
  /* c8 ignore next 6 */
  // FIXME: https://github.com/graphql/graphql-js/issues/2317
  So ? function(e, n) {
    return e instanceof n;
  } : function(e, n) {
    if (e instanceof n)
      return !0;
    if (typeof e == "object" && e !== null) {
      var s;
      const i = n.prototype[Symbol.toStringTag], r = (
        // We still need to support constructor's name to detect conflicts with older versions of this library.
        Symbol.toStringTag in e ? e[Symbol.toStringTag] : (s = e.constructor) === null || s === void 0 ? void 0 : s.name
      );
      if (i === r) {
        const a = _(e);
        throw new Error(`Cannot use ${i} "${a}" from another module or realm.

Ensure that there is only one instance of "graphql" in the node_modules
directory. If different versions of "graphql" are the dependencies of other
relied on modules, use "resolutions" to ensure only one version is installed.

https://yarnpkg.com/en/docs/selective-version-resolutions

Duplicate "graphql" modules cannot be used at the same time since different
versions may have different capabilities and behavior. The data from one
version used in the function from another could produce confusing and
spurious results.`);
      }
    }
    return !1;
  }
);
class Yi {
  constructor(e, n = "GraphQL request", s = {
    line: 1,
    column: 1
  }) {
    typeof e == "string" || V(!1, `Body must be a string. Received: ${_(e)}.`), this.body = e, this.name = n, this.locationOffset = s, this.locationOffset.line > 0 || V(
      !1,
      "line in locationOffset is 1-indexed and must be positive."
    ), this.locationOffset.column > 0 || V(
      !1,
      "column in locationOffset is 1-indexed and must be positive."
    );
  }
  get [Symbol.toStringTag]() {
    return "Source";
  }
}
function No(t) {
  return Re(t, Yi);
}
function Eo(t, e) {
  const n = new Wi(t, e), s = n.parseDocument();
  return Object.defineProperty(s, "tokenCount", {
    enumerable: !1,
    value: n.tokenCount
  }), s;
}
function Oo(t, e) {
  const n = new Wi(t, e);
  n.expectToken(m.SOF);
  const s = n.parseValueLiteral(!1);
  return n.expectToken(m.EOF), s;
}
class Wi {
  constructor(e, n = {}) {
    const s = No(e) ? e : new Yi(e);
    this._lexer = new ro(s), this._options = n, this._tokenCounter = 0;
  }
  get tokenCount() {
    return this._tokenCounter;
  }
  /**
   * Converts a name lex token into a name parse node.
   */
  parseName() {
    const e = this.expectToken(m.NAME);
    return this.node(e, {
      kind: d.NAME,
      value: e.value
    });
  }
  // Implements the parsing rules in the Document section.
  /**
   * Document : Definition+
   */
  parseDocument() {
    return this.node(this._lexer.token, {
      kind: d.DOCUMENT,
      definitions: this.many(
        m.SOF,
        this.parseDefinition,
        m.EOF
      )
    });
  }
  /**
   * Definition :
   *   - ExecutableDefinition
   *   - TypeSystemDefinition
   *   - TypeSystemExtension
   *
   * ExecutableDefinition :
   *   - OperationDefinition
   *   - FragmentDefinition
   *
   * TypeSystemDefinition :
   *   - SchemaDefinition
   *   - TypeDefinition
   *   - DirectiveDefinition
   *
   * TypeDefinition :
   *   - ScalarTypeDefinition
   *   - ObjectTypeDefinition
   *   - InterfaceTypeDefinition
   *   - UnionTypeDefinition
   *   - EnumTypeDefinition
   *   - InputObjectTypeDefinition
   */
  parseDefinition() {
    if (this.peek(m.BRACE_L))
      return this.parseOperationDefinition();
    const e = this.peekDescription(), n = e ? this._lexer.lookahead() : this._lexer.token;
    if (n.kind === m.NAME) {
      switch (n.value) {
        case "schema":
          return this.parseSchemaDefinition();
        case "scalar":
          return this.parseScalarTypeDefinition();
        case "type":
          return this.parseObjectTypeDefinition();
        case "interface":
          return this.parseInterfaceTypeDefinition();
        case "union":
          return this.parseUnionTypeDefinition();
        case "enum":
          return this.parseEnumTypeDefinition();
        case "input":
          return this.parseInputObjectTypeDefinition();
        case "directive":
          return this.parseDirectiveDefinition();
      }
      if (e)
        throw se(
          this._lexer.source,
          this._lexer.token.start,
          "Unexpected description, descriptions are supported only on type definitions."
        );
      switch (n.value) {
        case "query":
        case "mutation":
        case "subscription":
          return this.parseOperationDefinition();
        case "fragment":
          return this.parseFragmentDefinition();
        case "extend":
          return this.parseTypeSystemExtension();
      }
    }
    throw this.unexpected(n);
  }
  // Implements the parsing rules in the Operations section.
  /**
   * OperationDefinition :
   *  - SelectionSet
   *  - OperationType Name? VariableDefinitions? Directives? SelectionSet
   */
  parseOperationDefinition() {
    const e = this._lexer.token;
    if (this.peek(m.BRACE_L))
      return this.node(e, {
        kind: d.OPERATION_DEFINITION,
        operation: de.QUERY,
        name: void 0,
        variableDefinitions: [],
        directives: [],
        selectionSet: this.parseSelectionSet()
      });
    const n = this.parseOperationType();
    let s;
    return this.peek(m.NAME) && (s = this.parseName()), this.node(e, {
      kind: d.OPERATION_DEFINITION,
      operation: n,
      name: s,
      variableDefinitions: this.parseVariableDefinitions(),
      directives: this.parseDirectives(!1),
      selectionSet: this.parseSelectionSet()
    });
  }
  /**
   * OperationType : one of query mutation subscription
   */
  parseOperationType() {
    const e = this.expectToken(m.NAME);
    switch (e.value) {
      case "query":
        return de.QUERY;
      case "mutation":
        return de.MUTATION;
      case "subscription":
        return de.SUBSCRIPTION;
    }
    throw this.unexpected(e);
  }
  /**
   * VariableDefinitions : ( VariableDefinition+ )
   */
  parseVariableDefinitions() {
    return this.optionalMany(
      m.PAREN_L,
      this.parseVariableDefinition,
      m.PAREN_R
    );
  }
  /**
   * VariableDefinition : Variable : Type DefaultValue? Directives[Const]?
   */
  parseVariableDefinition() {
    return this.node(this._lexer.token, {
      kind: d.VARIABLE_DEFINITION,
      variable: this.parseVariable(),
      type: (this.expectToken(m.COLON), this.parseTypeReference()),
      defaultValue: this.expectOptionalToken(m.EQUALS) ? this.parseConstValueLiteral() : void 0,
      directives: this.parseConstDirectives()
    });
  }
  /**
   * Variable : $ Name
   */
  parseVariable() {
    const e = this._lexer.token;
    return this.expectToken(m.DOLLAR), this.node(e, {
      kind: d.VARIABLE,
      name: this.parseName()
    });
  }
  /**
   * ```
   * SelectionSet : { Selection+ }
   * ```
   */
  parseSelectionSet() {
    return this.node(this._lexer.token, {
      kind: d.SELECTION_SET,
      selections: this.many(
        m.BRACE_L,
        this.parseSelection,
        m.BRACE_R
      )
    });
  }
  /**
   * Selection :
   *   - Field
   *   - FragmentSpread
   *   - InlineFragment
   */
  parseSelection() {
    return this.peek(m.SPREAD) ? this.parseFragment() : this.parseField();
  }
  /**
   * Field : Alias? Name Arguments? Directives? SelectionSet?
   *
   * Alias : Name :
   */
  parseField() {
    const e = this._lexer.token, n = this.parseName();
    let s, i;
    return this.expectOptionalToken(m.COLON) ? (s = n, i = this.parseName()) : i = n, this.node(e, {
      kind: d.FIELD,
      alias: s,
      name: i,
      arguments: this.parseArguments(!1),
      directives: this.parseDirectives(!1),
      selectionSet: this.peek(m.BRACE_L) ? this.parseSelectionSet() : void 0
    });
  }
  /**
   * Arguments[Const] : ( Argument[?Const]+ )
   */
  parseArguments(e) {
    const n = e ? this.parseConstArgument : this.parseArgument;
    return this.optionalMany(m.PAREN_L, n, m.PAREN_R);
  }
  /**
   * Argument[Const] : Name : Value[?Const]
   */
  parseArgument(e = !1) {
    const n = this._lexer.token, s = this.parseName();
    return this.expectToken(m.COLON), this.node(n, {
      kind: d.ARGUMENT,
      name: s,
      value: this.parseValueLiteral(e)
    });
  }
  parseConstArgument() {
    return this.parseArgument(!0);
  }
  // Implements the parsing rules in the Fragments section.
  /**
   * Corresponds to both FragmentSpread and InlineFragment in the spec.
   *
   * FragmentSpread : ... FragmentName Directives?
   *
   * InlineFragment : ... TypeCondition? Directives? SelectionSet
   */
  parseFragment() {
    const e = this._lexer.token;
    this.expectToken(m.SPREAD);
    const n = this.expectOptionalKeyword("on");
    return !n && this.peek(m.NAME) ? this.node(e, {
      kind: d.FRAGMENT_SPREAD,
      name: this.parseFragmentName(),
      directives: this.parseDirectives(!1)
    }) : this.node(e, {
      kind: d.INLINE_FRAGMENT,
      typeCondition: n ? this.parseNamedType() : void 0,
      directives: this.parseDirectives(!1),
      selectionSet: this.parseSelectionSet()
    });
  }
  /**
   * FragmentDefinition :
   *   - fragment FragmentName on TypeCondition Directives? SelectionSet
   *
   * TypeCondition : NamedType
   */
  parseFragmentDefinition() {
    const e = this._lexer.token;
    return this.expectKeyword("fragment"), this._options.allowLegacyFragmentVariables === !0 ? this.node(e, {
      kind: d.FRAGMENT_DEFINITION,
      name: this.parseFragmentName(),
      variableDefinitions: this.parseVariableDefinitions(),
      typeCondition: (this.expectKeyword("on"), this.parseNamedType()),
      directives: this.parseDirectives(!1),
      selectionSet: this.parseSelectionSet()
    }) : this.node(e, {
      kind: d.FRAGMENT_DEFINITION,
      name: this.parseFragmentName(),
      typeCondition: (this.expectKeyword("on"), this.parseNamedType()),
      directives: this.parseDirectives(!1),
      selectionSet: this.parseSelectionSet()
    });
  }
  /**
   * FragmentName : Name but not `on`
   */
  parseFragmentName() {
    if (this._lexer.token.value === "on")
      throw this.unexpected();
    return this.parseName();
  }
  // Implements the parsing rules in the Values section.
  /**
   * Value[Const] :
   *   - [~Const] Variable
   *   - IntValue
   *   - FloatValue
   *   - StringValue
   *   - BooleanValue
   *   - NullValue
   *   - EnumValue
   *   - ListValue[?Const]
   *   - ObjectValue[?Const]
   *
   * BooleanValue : one of `true` `false`
   *
   * NullValue : `null`
   *
   * EnumValue : Name but not `true`, `false` or `null`
   */
  parseValueLiteral(e) {
    const n = this._lexer.token;
    switch (n.kind) {
      case m.BRACKET_L:
        return this.parseList(e);
      case m.BRACE_L:
        return this.parseObject(e);
      case m.INT:
        return this.advanceLexer(), this.node(n, {
          kind: d.INT,
          value: n.value
        });
      case m.FLOAT:
        return this.advanceLexer(), this.node(n, {
          kind: d.FLOAT,
          value: n.value
        });
      case m.STRING:
      case m.BLOCK_STRING:
        return this.parseStringLiteral();
      case m.NAME:
        switch (this.advanceLexer(), n.value) {
          case "true":
            return this.node(n, {
              kind: d.BOOLEAN,
              value: !0
            });
          case "false":
            return this.node(n, {
              kind: d.BOOLEAN,
              value: !1
            });
          case "null":
            return this.node(n, {
              kind: d.NULL
            });
          default:
            return this.node(n, {
              kind: d.ENUM,
              value: n.value
            });
        }
      case m.DOLLAR:
        if (e)
          if (this.expectToken(m.DOLLAR), this._lexer.token.kind === m.NAME) {
            const s = this._lexer.token.value;
            throw se(
              this._lexer.source,
              n.start,
              `Unexpected variable "$${s}" in constant value.`
            );
          } else
            throw this.unexpected(n);
        return this.parseVariable();
      default:
        throw this.unexpected();
    }
  }
  parseConstValueLiteral() {
    return this.parseValueLiteral(!0);
  }
  parseStringLiteral() {
    const e = this._lexer.token;
    return this.advanceLexer(), this.node(e, {
      kind: d.STRING,
      value: e.value,
      block: e.kind === m.BLOCK_STRING
    });
  }
  /**
   * ListValue[Const] :
   *   - [ ]
   *   - [ Value[?Const]+ ]
   */
  parseList(e) {
    const n = () => this.parseValueLiteral(e);
    return this.node(this._lexer.token, {
      kind: d.LIST,
      values: this.any(m.BRACKET_L, n, m.BRACKET_R)
    });
  }
  /**
   * ```
   * ObjectValue[Const] :
   *   - { }
   *   - { ObjectField[?Const]+ }
   * ```
   */
  parseObject(e) {
    const n = () => this.parseObjectField(e);
    return this.node(this._lexer.token, {
      kind: d.OBJECT,
      fields: this.any(m.BRACE_L, n, m.BRACE_R)
    });
  }
  /**
   * ObjectField[Const] : Name : Value[?Const]
   */
  parseObjectField(e) {
    const n = this._lexer.token, s = this.parseName();
    return this.expectToken(m.COLON), this.node(n, {
      kind: d.OBJECT_FIELD,
      name: s,
      value: this.parseValueLiteral(e)
    });
  }
  // Implements the parsing rules in the Directives section.
  /**
   * Directives[Const] : Directive[?Const]+
   */
  parseDirectives(e) {
    const n = [];
    for (; this.peek(m.AT); )
      n.push(this.parseDirective(e));
    return n;
  }
  parseConstDirectives() {
    return this.parseDirectives(!0);
  }
  /**
   * ```
   * Directive[Const] : @ Name Arguments[?Const]?
   * ```
   */
  parseDirective(e) {
    const n = this._lexer.token;
    return this.expectToken(m.AT), this.node(n, {
      kind: d.DIRECTIVE,
      name: this.parseName(),
      arguments: this.parseArguments(e)
    });
  }
  // Implements the parsing rules in the Types section.
  /**
   * Type :
   *   - NamedType
   *   - ListType
   *   - NonNullType
   */
  parseTypeReference() {
    const e = this._lexer.token;
    let n;
    if (this.expectOptionalToken(m.BRACKET_L)) {
      const s = this.parseTypeReference();
      this.expectToken(m.BRACKET_R), n = this.node(e, {
        kind: d.LIST_TYPE,
        type: s
      });
    } else
      n = this.parseNamedType();
    return this.expectOptionalToken(m.BANG) ? this.node(e, {
      kind: d.NON_NULL_TYPE,
      type: n
    }) : n;
  }
  /**
   * NamedType : Name
   */
  parseNamedType() {
    return this.node(this._lexer.token, {
      kind: d.NAMED_TYPE,
      name: this.parseName()
    });
  }
  // Implements the parsing rules in the Type Definition section.
  peekDescription() {
    return this.peek(m.STRING) || this.peek(m.BLOCK_STRING);
  }
  /**
   * Description : StringValue
   */
  parseDescription() {
    if (this.peekDescription())
      return this.parseStringLiteral();
  }
  /**
   * ```
   * SchemaDefinition : Description? schema Directives[Const]? { OperationTypeDefinition+ }
   * ```
   */
  parseSchemaDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("schema");
    const s = this.parseConstDirectives(), i = this.many(
      m.BRACE_L,
      this.parseOperationTypeDefinition,
      m.BRACE_R
    );
    return this.node(e, {
      kind: d.SCHEMA_DEFINITION,
      description: n,
      directives: s,
      operationTypes: i
    });
  }
  /**
   * OperationTypeDefinition : OperationType : NamedType
   */
  parseOperationTypeDefinition() {
    const e = this._lexer.token, n = this.parseOperationType();
    this.expectToken(m.COLON);
    const s = this.parseNamedType();
    return this.node(e, {
      kind: d.OPERATION_TYPE_DEFINITION,
      operation: n,
      type: s
    });
  }
  /**
   * ScalarTypeDefinition : Description? scalar Name Directives[Const]?
   */
  parseScalarTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("scalar");
    const s = this.parseName(), i = this.parseConstDirectives();
    return this.node(e, {
      kind: d.SCALAR_TYPE_DEFINITION,
      description: n,
      name: s,
      directives: i
    });
  }
  /**
   * ObjectTypeDefinition :
   *   Description?
   *   type Name ImplementsInterfaces? Directives[Const]? FieldsDefinition?
   */
  parseObjectTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("type");
    const s = this.parseName(), i = this.parseImplementsInterfaces(), r = this.parseConstDirectives(), a = this.parseFieldsDefinition();
    return this.node(e, {
      kind: d.OBJECT_TYPE_DEFINITION,
      description: n,
      name: s,
      interfaces: i,
      directives: r,
      fields: a
    });
  }
  /**
   * ImplementsInterfaces :
   *   - implements `&`? NamedType
   *   - ImplementsInterfaces & NamedType
   */
  parseImplementsInterfaces() {
    return this.expectOptionalKeyword("implements") ? this.delimitedMany(m.AMP, this.parseNamedType) : [];
  }
  /**
   * ```
   * FieldsDefinition : { FieldDefinition+ }
   * ```
   */
  parseFieldsDefinition() {
    return this.optionalMany(
      m.BRACE_L,
      this.parseFieldDefinition,
      m.BRACE_R
    );
  }
  /**
   * FieldDefinition :
   *   - Description? Name ArgumentsDefinition? : Type Directives[Const]?
   */
  parseFieldDefinition() {
    const e = this._lexer.token, n = this.parseDescription(), s = this.parseName(), i = this.parseArgumentDefs();
    this.expectToken(m.COLON);
    const r = this.parseTypeReference(), a = this.parseConstDirectives();
    return this.node(e, {
      kind: d.FIELD_DEFINITION,
      description: n,
      name: s,
      arguments: i,
      type: r,
      directives: a
    });
  }
  /**
   * ArgumentsDefinition : ( InputValueDefinition+ )
   */
  parseArgumentDefs() {
    return this.optionalMany(
      m.PAREN_L,
      this.parseInputValueDef,
      m.PAREN_R
    );
  }
  /**
   * InputValueDefinition :
   *   - Description? Name : Type DefaultValue? Directives[Const]?
   */
  parseInputValueDef() {
    const e = this._lexer.token, n = this.parseDescription(), s = this.parseName();
    this.expectToken(m.COLON);
    const i = this.parseTypeReference();
    let r;
    this.expectOptionalToken(m.EQUALS) && (r = this.parseConstValueLiteral());
    const a = this.parseConstDirectives();
    return this.node(e, {
      kind: d.INPUT_VALUE_DEFINITION,
      description: n,
      name: s,
      type: i,
      defaultValue: r,
      directives: a
    });
  }
  /**
   * InterfaceTypeDefinition :
   *   - Description? interface Name Directives[Const]? FieldsDefinition?
   */
  parseInterfaceTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("interface");
    const s = this.parseName(), i = this.parseImplementsInterfaces(), r = this.parseConstDirectives(), a = this.parseFieldsDefinition();
    return this.node(e, {
      kind: d.INTERFACE_TYPE_DEFINITION,
      description: n,
      name: s,
      interfaces: i,
      directives: r,
      fields: a
    });
  }
  /**
   * UnionTypeDefinition :
   *   - Description? union Name Directives[Const]? UnionMemberTypes?
   */
  parseUnionTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("union");
    const s = this.parseName(), i = this.parseConstDirectives(), r = this.parseUnionMemberTypes();
    return this.node(e, {
      kind: d.UNION_TYPE_DEFINITION,
      description: n,
      name: s,
      directives: i,
      types: r
    });
  }
  /**
   * UnionMemberTypes :
   *   - = `|`? NamedType
   *   - UnionMemberTypes | NamedType
   */
  parseUnionMemberTypes() {
    return this.expectOptionalToken(m.EQUALS) ? this.delimitedMany(m.PIPE, this.parseNamedType) : [];
  }
  /**
   * EnumTypeDefinition :
   *   - Description? enum Name Directives[Const]? EnumValuesDefinition?
   */
  parseEnumTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("enum");
    const s = this.parseName(), i = this.parseConstDirectives(), r = this.parseEnumValuesDefinition();
    return this.node(e, {
      kind: d.ENUM_TYPE_DEFINITION,
      description: n,
      name: s,
      directives: i,
      values: r
    });
  }
  /**
   * ```
   * EnumValuesDefinition : { EnumValueDefinition+ }
   * ```
   */
  parseEnumValuesDefinition() {
    return this.optionalMany(
      m.BRACE_L,
      this.parseEnumValueDefinition,
      m.BRACE_R
    );
  }
  /**
   * EnumValueDefinition : Description? EnumValue Directives[Const]?
   */
  parseEnumValueDefinition() {
    const e = this._lexer.token, n = this.parseDescription(), s = this.parseEnumValueName(), i = this.parseConstDirectives();
    return this.node(e, {
      kind: d.ENUM_VALUE_DEFINITION,
      description: n,
      name: s,
      directives: i
    });
  }
  /**
   * EnumValue : Name but not `true`, `false` or `null`
   */
  parseEnumValueName() {
    if (this._lexer.token.value === "true" || this._lexer.token.value === "false" || this._lexer.token.value === "null")
      throw se(
        this._lexer.source,
        this._lexer.token.start,
        `${Jt(
          this._lexer.token
        )} is reserved and cannot be used for an enum value.`
      );
    return this.parseName();
  }
  /**
   * InputObjectTypeDefinition :
   *   - Description? input Name Directives[Const]? InputFieldsDefinition?
   */
  parseInputObjectTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("input");
    const s = this.parseName(), i = this.parseConstDirectives(), r = this.parseInputFieldsDefinition();
    return this.node(e, {
      kind: d.INPUT_OBJECT_TYPE_DEFINITION,
      description: n,
      name: s,
      directives: i,
      fields: r
    });
  }
  /**
   * ```
   * InputFieldsDefinition : { InputValueDefinition+ }
   * ```
   */
  parseInputFieldsDefinition() {
    return this.optionalMany(
      m.BRACE_L,
      this.parseInputValueDef,
      m.BRACE_R
    );
  }
  /**
   * TypeSystemExtension :
   *   - SchemaExtension
   *   - TypeExtension
   *
   * TypeExtension :
   *   - ScalarTypeExtension
   *   - ObjectTypeExtension
   *   - InterfaceTypeExtension
   *   - UnionTypeExtension
   *   - EnumTypeExtension
   *   - InputObjectTypeDefinition
   */
  parseTypeSystemExtension() {
    const e = this._lexer.lookahead();
    if (e.kind === m.NAME)
      switch (e.value) {
        case "schema":
          return this.parseSchemaExtension();
        case "scalar":
          return this.parseScalarTypeExtension();
        case "type":
          return this.parseObjectTypeExtension();
        case "interface":
          return this.parseInterfaceTypeExtension();
        case "union":
          return this.parseUnionTypeExtension();
        case "enum":
          return this.parseEnumTypeExtension();
        case "input":
          return this.parseInputObjectTypeExtension();
      }
    throw this.unexpected(e);
  }
  /**
   * ```
   * SchemaExtension :
   *  - extend schema Directives[Const]? { OperationTypeDefinition+ }
   *  - extend schema Directives[Const]
   * ```
   */
  parseSchemaExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("schema");
    const n = this.parseConstDirectives(), s = this.optionalMany(
      m.BRACE_L,
      this.parseOperationTypeDefinition,
      m.BRACE_R
    );
    if (n.length === 0 && s.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: d.SCHEMA_EXTENSION,
      directives: n,
      operationTypes: s
    });
  }
  /**
   * ScalarTypeExtension :
   *   - extend scalar Name Directives[Const]
   */
  parseScalarTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("scalar");
    const n = this.parseName(), s = this.parseConstDirectives();
    if (s.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: d.SCALAR_TYPE_EXTENSION,
      name: n,
      directives: s
    });
  }
  /**
   * ObjectTypeExtension :
   *  - extend type Name ImplementsInterfaces? Directives[Const]? FieldsDefinition
   *  - extend type Name ImplementsInterfaces? Directives[Const]
   *  - extend type Name ImplementsInterfaces
   */
  parseObjectTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("type");
    const n = this.parseName(), s = this.parseImplementsInterfaces(), i = this.parseConstDirectives(), r = this.parseFieldsDefinition();
    if (s.length === 0 && i.length === 0 && r.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: d.OBJECT_TYPE_EXTENSION,
      name: n,
      interfaces: s,
      directives: i,
      fields: r
    });
  }
  /**
   * InterfaceTypeExtension :
   *  - extend interface Name ImplementsInterfaces? Directives[Const]? FieldsDefinition
   *  - extend interface Name ImplementsInterfaces? Directives[Const]
   *  - extend interface Name ImplementsInterfaces
   */
  parseInterfaceTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("interface");
    const n = this.parseName(), s = this.parseImplementsInterfaces(), i = this.parseConstDirectives(), r = this.parseFieldsDefinition();
    if (s.length === 0 && i.length === 0 && r.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: d.INTERFACE_TYPE_EXTENSION,
      name: n,
      interfaces: s,
      directives: i,
      fields: r
    });
  }
  /**
   * UnionTypeExtension :
   *   - extend union Name Directives[Const]? UnionMemberTypes
   *   - extend union Name Directives[Const]
   */
  parseUnionTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("union");
    const n = this.parseName(), s = this.parseConstDirectives(), i = this.parseUnionMemberTypes();
    if (s.length === 0 && i.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: d.UNION_TYPE_EXTENSION,
      name: n,
      directives: s,
      types: i
    });
  }
  /**
   * EnumTypeExtension :
   *   - extend enum Name Directives[Const]? EnumValuesDefinition
   *   - extend enum Name Directives[Const]
   */
  parseEnumTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("enum");
    const n = this.parseName(), s = this.parseConstDirectives(), i = this.parseEnumValuesDefinition();
    if (s.length === 0 && i.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: d.ENUM_TYPE_EXTENSION,
      name: n,
      directives: s,
      values: i
    });
  }
  /**
   * InputObjectTypeExtension :
   *   - extend input Name Directives[Const]? InputFieldsDefinition
   *   - extend input Name Directives[Const]
   */
  parseInputObjectTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("input");
    const n = this.parseName(), s = this.parseConstDirectives(), i = this.parseInputFieldsDefinition();
    if (s.length === 0 && i.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: d.INPUT_OBJECT_TYPE_EXTENSION,
      name: n,
      directives: s,
      fields: i
    });
  }
  /**
   * ```
   * DirectiveDefinition :
   *   - Description? directive @ Name ArgumentsDefinition? `repeatable`? on DirectiveLocations
   * ```
   */
  parseDirectiveDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("directive"), this.expectToken(m.AT);
    const s = this.parseName(), i = this.parseArgumentDefs(), r = this.expectOptionalKeyword("repeatable");
    this.expectKeyword("on");
    const a = this.parseDirectiveLocations();
    return this.node(e, {
      kind: d.DIRECTIVE_DEFINITION,
      description: n,
      name: s,
      arguments: i,
      repeatable: r,
      locations: a
    });
  }
  /**
   * DirectiveLocations :
   *   - `|`? DirectiveLocation
   *   - DirectiveLocations | DirectiveLocation
   */
  parseDirectiveLocations() {
    return this.delimitedMany(m.PIPE, this.parseDirectiveLocation);
  }
  /*
   * DirectiveLocation :
   *   - ExecutableDirectiveLocation
   *   - TypeSystemDirectiveLocation
   *
   * ExecutableDirectiveLocation : one of
   *   `QUERY`
   *   `MUTATION`
   *   `SUBSCRIPTION`
   *   `FIELD`
   *   `FRAGMENT_DEFINITION`
   *   `FRAGMENT_SPREAD`
   *   `INLINE_FRAGMENT`
   *
   * TypeSystemDirectiveLocation : one of
   *   `SCHEMA`
   *   `SCALAR`
   *   `OBJECT`
   *   `FIELD_DEFINITION`
   *   `ARGUMENT_DEFINITION`
   *   `INTERFACE`
   *   `UNION`
   *   `ENUM`
   *   `ENUM_VALUE`
   *   `INPUT_OBJECT`
   *   `INPUT_FIELD_DEFINITION`
   */
  parseDirectiveLocation() {
    const e = this._lexer.token, n = this.parseName();
    if (Object.prototype.hasOwnProperty.call(w, n.value))
      return n;
    throw this.unexpected(e);
  }
  // Core parsing utility functions
  /**
   * Returns a node that, if configured to do so, sets a "loc" field as a
   * location object, used to identify the place in the source that created a
   * given parsed object.
   */
  node(e, n) {
    return this._options.noLocation !== !0 && (n.loc = new eo(
      e,
      this._lexer.lastToken,
      this._lexer.source
    )), n;
  }
  /**
   * Determines if the next token is of a given kind
   */
  peek(e) {
    return this._lexer.token.kind === e;
  }
  /**
   * If the next token is of the given kind, return that token after advancing the lexer.
   * Otherwise, do not change the parser state and throw an error.
   */
  expectToken(e) {
    const n = this._lexer.token;
    if (n.kind === e)
      return this.advanceLexer(), n;
    throw se(
      this._lexer.source,
      n.start,
      `Expected ${zi(e)}, found ${Jt(n)}.`
    );
  }
  /**
   * If the next token is of the given kind, return "true" after advancing the lexer.
   * Otherwise, do not change the parser state and return "false".
   */
  expectOptionalToken(e) {
    return this._lexer.token.kind === e ? (this.advanceLexer(), !0) : !1;
  }
  /**
   * If the next token is a given keyword, advance the lexer.
   * Otherwise, do not change the parser state and throw an error.
   */
  expectKeyword(e) {
    const n = this._lexer.token;
    if (n.kind === m.NAME && n.value === e)
      this.advanceLexer();
    else
      throw se(
        this._lexer.source,
        n.start,
        `Expected "${e}", found ${Jt(n)}.`
      );
  }
  /**
   * If the next token is a given keyword, return "true" after advancing the lexer.
   * Otherwise, do not change the parser state and return "false".
   */
  expectOptionalKeyword(e) {
    const n = this._lexer.token;
    return n.kind === m.NAME && n.value === e ? (this.advanceLexer(), !0) : !1;
  }
  /**
   * Helper function for creating an error when an unexpected lexed token is encountered.
   */
  unexpected(e) {
    const n = e ?? this._lexer.token;
    return se(
      this._lexer.source,
      n.start,
      `Unexpected ${Jt(n)}.`
    );
  }
  /**
   * Returns a possibly empty list of parse nodes, determined by the parseFn.
   * This list begins with a lex token of openKind and ends with a lex token of closeKind.
   * Advances the parser to the next lex token after the closing token.
   */
  any(e, n, s) {
    this.expectToken(e);
    const i = [];
    for (; !this.expectOptionalToken(s); )
      i.push(n.call(this));
    return i;
  }
  /**
   * Returns a list of parse nodes, determined by the parseFn.
   * It can be empty only if open token is missing otherwise it will always return non-empty list
   * that begins with a lex token of openKind and ends with a lex token of closeKind.
   * Advances the parser to the next lex token after the closing token.
   */
  optionalMany(e, n, s) {
    if (this.expectOptionalToken(e)) {
      const i = [];
      do
        i.push(n.call(this));
      while (!this.expectOptionalToken(s));
      return i;
    }
    return [];
  }
  /**
   * Returns a non-empty list of parse nodes, determined by the parseFn.
   * This list begins with a lex token of openKind and ends with a lex token of closeKind.
   * Advances the parser to the next lex token after the closing token.
   */
  many(e, n, s) {
    this.expectToken(e);
    const i = [];
    do
      i.push(n.call(this));
    while (!this.expectOptionalToken(s));
    return i;
  }
  /**
   * Returns a non-empty list of parse nodes, determined by the parseFn.
   * This list may begin with a lex token of delimiterKind followed by items separated by lex tokens of tokenKind.
   * Advances the parser to the next lex token after last item in the list.
   */
  delimitedMany(e, n) {
    this.expectOptionalToken(e);
    const s = [];
    do
      s.push(n.call(this));
    while (this.expectOptionalToken(e));
    return s;
  }
  advanceLexer() {
    const { maxTokens: e } = this._options, n = this._lexer.advance();
    if (n.kind !== m.EOF && (++this._tokenCounter, e !== void 0 && this._tokenCounter > e))
      throw se(
        this._lexer.source,
        n.start,
        `Document contains more that ${e} tokens. Parsing aborted.`
      );
  }
}
function Jt(t) {
  const e = t.value;
  return zi(t.kind) + (e != null ? ` "${e}"` : "");
}
function zi(t) {
  return ao(t) ? `"${t}"` : t;
}
const wo = 5;
function pt(t, e) {
  const [n, s] = e ? [t, e] : [void 0, t];
  let i = " Did you mean ";
  n && (i += n + " ");
  const r = s.map((c) => `"${c}"`);
  switch (r.length) {
    case 0:
      return "";
    case 1:
      return i + r[0] + "?";
    case 2:
      return i + r[0] + " or " + r[1] + "?";
  }
  const a = r.slice(0, wo), o = a.pop();
  return i + a.join(", ") + ", or " + o + "?";
}
function si(t) {
  return t;
}
function Nt(t, e) {
  const n = /* @__PURE__ */ Object.create(null);
  for (const s of t)
    n[e(s)] = s;
  return n;
}
function it(t, e, n) {
  const s = /* @__PURE__ */ Object.create(null);
  for (const i of t)
    s[e(i)] = n(i);
  return s;
}
function An(t, e) {
  const n = /* @__PURE__ */ Object.create(null);
  for (const s of Object.keys(t))
    n[s] = e(t[s], s);
  return n;
}
function Ss(t, e) {
  let n = 0, s = 0;
  for (; n < t.length && s < e.length; ) {
    let i = t.charCodeAt(n), r = e.charCodeAt(s);
    if (Zt(i) && Zt(r)) {
      let a = 0;
      do
        ++n, a = a * 10 + i - us, i = t.charCodeAt(n);
      while (Zt(i) && a > 0);
      let o = 0;
      do
        ++s, o = o * 10 + r - us, r = e.charCodeAt(s);
      while (Zt(r) && o > 0);
      if (a < o)
        return -1;
      if (a > o)
        return 1;
    } else {
      if (i < r)
        return -1;
      if (i > r)
        return 1;
      ++n, ++s;
    }
  }
  return t.length - e.length;
}
const us = 48, Ao = 57;
function Zt(t) {
  return !isNaN(t) && us <= t && t <= Ao;
}
function Ct(t, e) {
  const n = /* @__PURE__ */ Object.create(null), s = new xo(t), i = Math.floor(t.length * 0.4) + 1;
  for (const r of e) {
    const a = s.measure(r, i);
    a !== void 0 && (n[r] = a);
  }
  return Object.keys(n).sort((r, a) => {
    const o = n[r] - n[a];
    return o !== 0 ? o : Ss(r, a);
  });
}
class xo {
  constructor(e) {
    this._input = e, this._inputLowerCase = e.toLowerCase(), this._inputArray = ii(this._inputLowerCase), this._rows = [
      new Array(e.length + 1).fill(0),
      new Array(e.length + 1).fill(0),
      new Array(e.length + 1).fill(0)
    ];
  }
  measure(e, n) {
    if (this._input === e)
      return 0;
    const s = e.toLowerCase();
    if (this._inputLowerCase === s)
      return 1;
    let i = ii(s), r = this._inputArray;
    if (i.length < r.length) {
      const u = i;
      i = r, r = u;
    }
    const a = i.length, o = r.length;
    if (a - o > n)
      return;
    const c = this._rows;
    for (let u = 0; u <= o; u++)
      c[0][u] = u;
    for (let u = 1; u <= a; u++) {
      const p = c[(u - 1) % 3], f = c[u % 3];
      let S = f[0] = u;
      for (let v = 1; v <= o; v++) {
        const A = i[u - 1] === r[v - 1] ? 0 : 1;
        let O = Math.min(
          p[v] + 1,
          // delete
          f[v - 1] + 1,
          // insert
          p[v - 1] + A
          // substitute
        );
        if (u > 1 && v > 1 && i[u - 1] === r[v - 2] && i[u - 2] === r[v - 1]) {
          const M = c[(u - 2) % 3][v - 2];
          O = Math.min(O, M + 1);
        }
        O < S && (S = O), f[v] = O;
      }
      if (S > n)
        return;
    }
    const l = c[a % 3][o];
    return l <= n ? l : void 0;
  }
}
function ii(t) {
  const e = t.length, n = new Array(e);
  for (let s = 0; s < e; ++s)
    n[s] = t.charCodeAt(s);
  return n;
}
function Se(t) {
  if (t == null)
    return /* @__PURE__ */ Object.create(null);
  if (Object.getPrototypeOf(t) === null)
    return t;
  const e = /* @__PURE__ */ Object.create(null);
  for (const [n, s] of Object.entries(t))
    e[n] = s;
  return e;
}
function Co(t) {
  return `"${t.replace(ko, Ro)}"`;
}
const ko = /[\x00-\x1f\x22\x5c\x7f-\x9f]/g;
function Ro(t) {
  return Lo[t.charCodeAt(0)];
}
const Lo = [
  "\\u0000",
  "\\u0001",
  "\\u0002",
  "\\u0003",
  "\\u0004",
  "\\u0005",
  "\\u0006",
  "\\u0007",
  "\\b",
  "\\t",
  "\\n",
  "\\u000B",
  "\\f",
  "\\r",
  "\\u000E",
  "\\u000F",
  "\\u0010",
  "\\u0011",
  "\\u0012",
  "\\u0013",
  "\\u0014",
  "\\u0015",
  "\\u0016",
  "\\u0017",
  "\\u0018",
  "\\u0019",
  "\\u001A",
  "\\u001B",
  "\\u001C",
  "\\u001D",
  "\\u001E",
  "\\u001F",
  "",
  "",
  '\\"',
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  // 2F
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  // 3F
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  // 4F
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "\\\\",
  "",
  "",
  "",
  // 5F
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  // 6F
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "",
  "\\u007F",
  "\\u0080",
  "\\u0081",
  "\\u0082",
  "\\u0083",
  "\\u0084",
  "\\u0085",
  "\\u0086",
  "\\u0087",
  "\\u0088",
  "\\u0089",
  "\\u008A",
  "\\u008B",
  "\\u008C",
  "\\u008D",
  "\\u008E",
  "\\u008F",
  "\\u0090",
  "\\u0091",
  "\\u0092",
  "\\u0093",
  "\\u0094",
  "\\u0095",
  "\\u0096",
  "\\u0097",
  "\\u0098",
  "\\u0099",
  "\\u009A",
  "\\u009B",
  "\\u009C",
  "\\u009D",
  "\\u009E",
  "\\u009F"
], Ut = Object.freeze({});
function Ns(t, e, n = Vi) {
  const s = /* @__PURE__ */ new Map();
  for (const M of Object.values(d))
    s.set(M, gn(e, M));
  let i, r = Array.isArray(t), a = [t], o = -1, c = [], l = t, u, p;
  const f = [], S = [];
  do {
    o++;
    const M = o === a.length, W = M && c.length !== 0;
    if (M) {
      if (u = S.length === 0 ? void 0 : f[f.length - 1], l = p, p = S.pop(), W)
        if (r) {
          l = l.slice();
          let re = 0;
          for (const [We, $e] of c) {
            const tt = We - re;
            $e === null ? (l.splice(tt, 1), re++) : l[tt] = $e;
          }
        } else {
          l = { ...l };
          for (const [re, We] of c)
            l[re] = We;
        }
      o = i.index, a = i.keys, c = i.edits, r = i.inArray, i = i.prev;
    } else if (p) {
      if (u = r ? o : a[o], l = p[u], l == null)
        continue;
      f.push(u);
    }
    let z;
    if (!Array.isArray(l)) {
      var v, A;
      cs(l) || V(!1, `Invalid AST Node: ${_(l)}.`);
      const re = M ? (v = s.get(l.kind)) === null || v === void 0 ? void 0 : v.leave : (A = s.get(l.kind)) === null || A === void 0 ? void 0 : A.enter;
      if (z = re == null ? void 0 : re.call(e, l, u, p, f, S), z === Ut)
        break;
      if (z === !1) {
        if (!M) {
          f.pop();
          continue;
        }
      } else if (z !== void 0 && (c.push([u, z]), !M))
        if (cs(z))
          l = z;
        else {
          f.pop();
          continue;
        }
    }
    if (z === void 0 && W && c.push([u, l]), M)
      f.pop();
    else {
      var O;
      i = {
        inArray: r,
        index: o,
        keys: a,
        edits: c,
        prev: i
      }, r = Array.isArray(l), a = r ? l : (O = n[l.kind]) !== null && O !== void 0 ? O : [], o = -1, c = [], p && S.push(p), p = l;
    }
  } while (i !== void 0);
  return c.length !== 0 ? c[c.length - 1][1] : t;
}
function Do(t) {
  const e = new Array(t.length).fill(null), n = /* @__PURE__ */ Object.create(null);
  for (const s of Object.values(d)) {
    let i = !1;
    const r = new Array(t.length).fill(void 0), a = new Array(t.length).fill(void 0);
    for (let c = 0; c < t.length; ++c) {
      const { enter: l, leave: u } = gn(t[c], s);
      i || (i = l != null || u != null), r[c] = l, a[c] = u;
    }
    if (!i)
      continue;
    const o = {
      enter(...c) {
        const l = c[0];
        for (let p = 0; p < t.length; p++)
          if (e[p] === null) {
            var u;
            const f = (u = r[p]) === null || u === void 0 ? void 0 : u.apply(t[p], c);
            if (f === !1)
              e[p] = l;
            else if (f === Ut)
              e[p] = Ut;
            else if (f !== void 0)
              return f;
          }
      },
      leave(...c) {
        const l = c[0];
        for (let p = 0; p < t.length; p++)
          if (e[p] === null) {
            var u;
            const f = (u = a[p]) === null || u === void 0 ? void 0 : u.apply(t[p], c);
            if (f === Ut)
              e[p] = Ut;
            else if (f !== void 0 && f !== !1)
              return f;
          } else e[p] === l && (e[p] = null);
      }
    };
    n[s] = o;
  }
  return n;
}
function gn(t, e) {
  const n = t[e];
  return typeof n == "object" ? n : typeof n == "function" ? {
    enter: n,
    leave: void 0
  } : {
    enter: t.enter,
    leave: t.leave
  };
}
function ie(t) {
  return Ns(t, Fo);
}
const Po = 80, Fo = {
  Name: {
    leave: (t) => t.value
  },
  Variable: {
    leave: (t) => "$" + t.name
  },
  // Document
  Document: {
    leave: (t) => N(t.definitions, `

`)
  },
  OperationDefinition: {
    leave(t) {
      const e = D("(", N(t.variableDefinitions, ", "), ")"), n = N(
        [
          t.operation,
          N([t.name, e]),
          N(t.directives, " ")
        ],
        " "
      );
      return (n === "query" ? "" : n + " ") + t.selectionSet;
    }
  },
  VariableDefinition: {
    leave: ({ variable: t, type: e, defaultValue: n, directives: s }) => t + ": " + e + D(" = ", n) + D(" ", N(s, " "))
  },
  SelectionSet: {
    leave: ({ selections: t }) => Ee(t)
  },
  Field: {
    leave({ alias: t, name: e, arguments: n, directives: s, selectionSet: i }) {
      const r = D("", t, ": ") + e;
      let a = r + D("(", N(n, ", "), ")");
      return a.length > Po && (a = r + D(`(
`, tn(N(n, `
`)), `
)`)), N([a, N(s, " "), i], " ");
    }
  },
  Argument: {
    leave: ({ name: t, value: e }) => t + ": " + e
  },
  // Fragments
  FragmentSpread: {
    leave: ({ name: t, directives: e }) => "..." + t + D(" ", N(e, " "))
  },
  InlineFragment: {
    leave: ({ typeCondition: t, directives: e, selectionSet: n }) => N(
      [
        "...",
        D("on ", t),
        N(e, " "),
        n
      ],
      " "
    )
  },
  FragmentDefinition: {
    leave: ({ name: t, typeCondition: e, variableDefinitions: n, directives: s, selectionSet: i }) => (
      // or removed in the future.
      `fragment ${t}${D("(", N(n, ", "), ")")} on ${e} ${D("", N(s, " "), " ")}` + i
    )
  },
  // Value
  IntValue: {
    leave: ({ value: t }) => t
  },
  FloatValue: {
    leave: ({ value: t }) => t
  },
  StringValue: {
    leave: ({ value: t, block: e }) => e ? io(t) : Co(t)
  },
  BooleanValue: {
    leave: ({ value: t }) => t ? "true" : "false"
  },
  NullValue: {
    leave: () => "null"
  },
  EnumValue: {
    leave: ({ value: t }) => t
  },
  ListValue: {
    leave: ({ values: t }) => "[" + N(t, ", ") + "]"
  },
  ObjectValue: {
    leave: ({ fields: t }) => "{" + N(t, ", ") + "}"
  },
  ObjectField: {
    leave: ({ name: t, value: e }) => t + ": " + e
  },
  // Directive
  Directive: {
    leave: ({ name: t, arguments: e }) => "@" + t + D("(", N(e, ", "), ")")
  },
  // Type
  NamedType: {
    leave: ({ name: t }) => t
  },
  ListType: {
    leave: ({ type: t }) => "[" + t + "]"
  },
  NonNullType: {
    leave: ({ type: t }) => t + "!"
  },
  // Type System Definitions
  SchemaDefinition: {
    leave: ({ description: t, directives: e, operationTypes: n }) => D("", t, `
`) + N(["schema", N(e, " "), Ee(n)], " ")
  },
  OperationTypeDefinition: {
    leave: ({ operation: t, type: e }) => t + ": " + e
  },
  ScalarTypeDefinition: {
    leave: ({ description: t, name: e, directives: n }) => D("", t, `
`) + N(["scalar", e, N(n, " ")], " ")
  },
  ObjectTypeDefinition: {
    leave: ({ description: t, name: e, interfaces: n, directives: s, fields: i }) => D("", t, `
`) + N(
      [
        "type",
        e,
        D("implements ", N(n, " & ")),
        N(s, " "),
        Ee(i)
      ],
      " "
    )
  },
  FieldDefinition: {
    leave: ({ description: t, name: e, arguments: n, type: s, directives: i }) => D("", t, `
`) + e + (ri(n) ? D(`(
`, tn(N(n, `
`)), `
)`) : D("(", N(n, ", "), ")")) + ": " + s + D(" ", N(i, " "))
  },
  InputValueDefinition: {
    leave: ({ description: t, name: e, type: n, defaultValue: s, directives: i }) => D("", t, `
`) + N(
      [e + ": " + n, D("= ", s), N(i, " ")],
      " "
    )
  },
  InterfaceTypeDefinition: {
    leave: ({ description: t, name: e, interfaces: n, directives: s, fields: i }) => D("", t, `
`) + N(
      [
        "interface",
        e,
        D("implements ", N(n, " & ")),
        N(s, " "),
        Ee(i)
      ],
      " "
    )
  },
  UnionTypeDefinition: {
    leave: ({ description: t, name: e, directives: n, types: s }) => D("", t, `
`) + N(
      ["union", e, N(n, " "), D("= ", N(s, " | "))],
      " "
    )
  },
  EnumTypeDefinition: {
    leave: ({ description: t, name: e, directives: n, values: s }) => D("", t, `
`) + N(["enum", e, N(n, " "), Ee(s)], " ")
  },
  EnumValueDefinition: {
    leave: ({ description: t, name: e, directives: n }) => D("", t, `
`) + N([e, N(n, " ")], " ")
  },
  InputObjectTypeDefinition: {
    leave: ({ description: t, name: e, directives: n, fields: s }) => D("", t, `
`) + N(["input", e, N(n, " "), Ee(s)], " ")
  },
  DirectiveDefinition: {
    leave: ({ description: t, name: e, arguments: n, repeatable: s, locations: i }) => D("", t, `
`) + "directive @" + e + (ri(n) ? D(`(
`, tn(N(n, `
`)), `
)`) : D("(", N(n, ", "), ")")) + (s ? " repeatable" : "") + " on " + N(i, " | ")
  },
  SchemaExtension: {
    leave: ({ directives: t, operationTypes: e }) => N(
      ["extend schema", N(t, " "), Ee(e)],
      " "
    )
  },
  ScalarTypeExtension: {
    leave: ({ name: t, directives: e }) => N(["extend scalar", t, N(e, " ")], " ")
  },
  ObjectTypeExtension: {
    leave: ({ name: t, interfaces: e, directives: n, fields: s }) => N(
      [
        "extend type",
        t,
        D("implements ", N(e, " & ")),
        N(n, " "),
        Ee(s)
      ],
      " "
    )
  },
  InterfaceTypeExtension: {
    leave: ({ name: t, interfaces: e, directives: n, fields: s }) => N(
      [
        "extend interface",
        t,
        D("implements ", N(e, " & ")),
        N(n, " "),
        Ee(s)
      ],
      " "
    )
  },
  UnionTypeExtension: {
    leave: ({ name: t, directives: e, types: n }) => N(
      [
        "extend union",
        t,
        N(e, " "),
        D("= ", N(n, " | "))
      ],
      " "
    )
  },
  EnumTypeExtension: {
    leave: ({ name: t, directives: e, values: n }) => N(["extend enum", t, N(e, " "), Ee(n)], " ")
  },
  InputObjectTypeExtension: {
    leave: ({ name: t, directives: e, fields: n }) => N(["extend input", t, N(e, " "), Ee(n)], " ")
  }
};
function N(t, e = "") {
  var n;
  return (n = t == null ? void 0 : t.filter((s) => s).join(e)) !== null && n !== void 0 ? n : "";
}
function Ee(t) {
  return D(`{
`, tn(N(t, `
`)), `
}`);
}
function D(t, e, n = "") {
  return e != null && e !== "" ? t + e + n : "";
}
function tn(t) {
  return D("  ", t.replace(/\n/g, `
  `));
}
function ri(t) {
  var e;
  return (e = t == null ? void 0 : t.some((n) => n.includes(`
`))) !== null && e !== void 0 ? e : !1;
}
function ds(t, e) {
  switch (t.kind) {
    case d.NULL:
      return null;
    case d.INT:
      return parseInt(t.value, 10);
    case d.FLOAT:
      return parseFloat(t.value);
    case d.STRING:
    case d.ENUM:
    case d.BOOLEAN:
      return t.value;
    case d.LIST:
      return t.values.map(
        (n) => ds(n, e)
      );
    case d.OBJECT:
      return it(
        t.fields,
        (n) => n.name.value,
        (n) => ds(n.value, e)
      );
    case d.VARIABLE:
      return e == null ? void 0 : e[t.name.value];
  }
}
function Le(t) {
  if (t != null || V(!1, "Must provide name."), typeof t == "string" || V(!1, "Expected name to be a string."), t.length === 0)
    throw new y("Expected name to be a non-empty string.");
  for (let e = 1; e < t.length; ++e)
    if (!Gi(t.charCodeAt(e)))
      throw new y(
        `Names must only contain [_a-zA-Z0-9] but "${t}" does not.`
      );
  if (!bs(t.charCodeAt(0)))
    throw new y(
      `Names must start with [_a-zA-Z] but "${t}" does not.`
    );
  return t;
}
function Uo(t) {
  if (t === "true" || t === "false" || t === "null")
    throw new y(`Enum values cannot be named: ${t}`);
  return Le(t);
}
function Es(t) {
  return kt(t) || J(t) || X(t) || Ue(t) || Me(t) || ce(t) || pe(t) || G(t);
}
function kt(t) {
  return Re(t, ft);
}
function J(t) {
  return Re(t, et);
}
function Mo(t) {
  if (!J(t))
    throw new Error(`Expected ${_(t)} to be a GraphQL Object type.`);
  return t;
}
function X(t) {
  return Re(t, ir);
}
function $o(t) {
  if (!X(t))
    throw new Error(
      `Expected ${_(t)} to be a GraphQL Interface type.`
    );
  return t;
}
function Ue(t) {
  return Re(t, rr);
}
function Me(t) {
  return Re(t, xn);
}
function ce(t) {
  return Re(t, ar);
}
function pe(t) {
  return Re(t, ge);
}
function G(t) {
  return Re(t, F);
}
function xe(t) {
  return kt(t) || Me(t) || ce(t) || Os(t) && xe(t.ofType);
}
function vt(t) {
  return kt(t) || J(t) || X(t) || Ue(t) || Me(t) || Os(t) && vt(t.ofType);
}
function Et(t) {
  return kt(t) || Me(t);
}
function Xe(t) {
  return J(t) || X(t) || Ue(t);
}
function at(t) {
  return X(t) || Ue(t);
}
class ge {
  constructor(e) {
    Es(e) || V(!1, `Expected ${_(e)} to be a GraphQL type.`), this.ofType = e;
  }
  get [Symbol.toStringTag]() {
    return "GraphQLList";
  }
  toString() {
    return "[" + String(this.ofType) + "]";
  }
  toJSON() {
    return this.toString();
  }
}
class F {
  constructor(e) {
    ws(e) || V(
      !1,
      `Expected ${_(e)} to be a GraphQL nullable type.`
    ), this.ofType = e;
  }
  get [Symbol.toStringTag]() {
    return "GraphQLNonNull";
  }
  toString() {
    return String(this.ofType) + "!";
  }
  toJSON() {
    return this.toString();
  }
}
function Os(t) {
  return pe(t) || G(t);
}
function ws(t) {
  return Es(t) && !G(t);
}
function jo(t) {
  if (!ws(t))
    throw new Error(`Expected ${_(t)} to be a GraphQL nullable type.`);
  return t;
}
function Ji(t) {
  if (t)
    return G(t) ? t.ofType : t;
}
function Vo(t) {
  return kt(t) || J(t) || X(t) || Ue(t) || Me(t) || ce(t);
}
function ye(t) {
  if (t) {
    let e = t;
    for (; Os(e); )
      e = e.ofType;
    return e;
  }
}
function Zi(t) {
  return typeof t == "function" ? t() : t;
}
function Xi(t) {
  return typeof t == "function" ? t() : t;
}
class ft {
  constructor(e) {
    var n, s, i, r;
    const a = (n = e.parseValue) !== null && n !== void 0 ? n : si;
    this.name = Le(e.name), this.description = e.description, this.specifiedByURL = e.specifiedByURL, this.serialize = (s = e.serialize) !== null && s !== void 0 ? s : si, this.parseValue = a, this.parseLiteral = (i = e.parseLiteral) !== null && i !== void 0 ? i : (o, c) => a(ds(o, c)), this.extensions = Se(e.extensions), this.astNode = e.astNode, this.extensionASTNodes = (r = e.extensionASTNodes) !== null && r !== void 0 ? r : [], e.specifiedByURL == null || typeof e.specifiedByURL == "string" || V(
      !1,
      `${this.name} must provide "specifiedByURL" as a string, but got: ${_(e.specifiedByURL)}.`
    ), e.serialize == null || typeof e.serialize == "function" || V(
      !1,
      `${this.name} must provide "serialize" function. If this custom Scalar is also used as an input type, ensure "parseValue" and "parseLiteral" functions are also provided.`
    ), e.parseLiteral && (typeof e.parseValue == "function" && typeof e.parseLiteral == "function" || V(
      !1,
      `${this.name} must provide both "parseValue" and "parseLiteral" functions.`
    ));
  }
  get [Symbol.toStringTag]() {
    return "GraphQLScalarType";
  }
  toConfig() {
    return {
      name: this.name,
      description: this.description,
      specifiedByURL: this.specifiedByURL,
      serialize: this.serialize,
      parseValue: this.parseValue,
      parseLiteral: this.parseLiteral,
      extensions: this.extensions,
      astNode: this.astNode,
      extensionASTNodes: this.extensionASTNodes
    };
  }
  toString() {
    return this.name;
  }
  toJSON() {
    return this.toString();
  }
}
class et {
  constructor(e) {
    var n;
    this.name = Le(e.name), this.description = e.description, this.isTypeOf = e.isTypeOf, this.extensions = Se(e.extensions), this.astNode = e.astNode, this.extensionASTNodes = (n = e.extensionASTNodes) !== null && n !== void 0 ? n : [], this._fields = () => er(e), this._interfaces = () => Ki(e), e.isTypeOf == null || typeof e.isTypeOf == "function" || V(
      !1,
      `${this.name} must provide "isTypeOf" as a function, but got: ${_(e.isTypeOf)}.`
    );
  }
  get [Symbol.toStringTag]() {
    return "GraphQLObjectType";
  }
  getFields() {
    return typeof this._fields == "function" && (this._fields = this._fields()), this._fields;
  }
  getInterfaces() {
    return typeof this._interfaces == "function" && (this._interfaces = this._interfaces()), this._interfaces;
  }
  toConfig() {
    return {
      name: this.name,
      description: this.description,
      interfaces: this.getInterfaces(),
      fields: nr(this.getFields()),
      isTypeOf: this.isTypeOf,
      extensions: this.extensions,
      astNode: this.astNode,
      extensionASTNodes: this.extensionASTNodes
    };
  }
  toString() {
    return this.name;
  }
  toJSON() {
    return this.toString();
  }
}
function Ki(t) {
  var e;
  const n = Zi(
    (e = t.interfaces) !== null && e !== void 0 ? e : []
  );
  return Array.isArray(n) || V(
    !1,
    `${t.name} interfaces must be an Array or a function which returns an Array.`
  ), n;
}
function er(t) {
  const e = Xi(t.fields);
  return Tt(e) || V(
    !1,
    `${t.name} fields must be an object with field names as keys or a function which returns such an object.`
  ), An(e, (n, s) => {
    var i;
    Tt(n) || V(
      !1,
      `${t.name}.${s} field config must be an object.`
    ), n.resolve == null || typeof n.resolve == "function" || V(
      !1,
      `${t.name}.${s} field resolver must be a function if provided, but got: ${_(n.resolve)}.`
    );
    const r = (i = n.args) !== null && i !== void 0 ? i : {};
    return Tt(r) || V(
      !1,
      `${t.name}.${s} args must be an object with argument names as keys.`
    ), {
      name: Le(s),
      description: n.description,
      type: n.type,
      args: tr(r),
      resolve: n.resolve,
      subscribe: n.subscribe,
      deprecationReason: n.deprecationReason,
      extensions: Se(n.extensions),
      astNode: n.astNode
    };
  });
}
function tr(t) {
  return Object.entries(t).map(([e, n]) => ({
    name: Le(e),
    description: n.description,
    type: n.type,
    defaultValue: n.defaultValue,
    deprecationReason: n.deprecationReason,
    extensions: Se(n.extensions),
    astNode: n.astNode
  }));
}
function Tt(t) {
  return Qe(t) && !Array.isArray(t);
}
function nr(t) {
  return An(t, (e) => ({
    description: e.description,
    type: e.type,
    args: sr(e.args),
    resolve: e.resolve,
    subscribe: e.subscribe,
    deprecationReason: e.deprecationReason,
    extensions: e.extensions,
    astNode: e.astNode
  }));
}
function sr(t) {
  return it(
    t,
    (e) => e.name,
    (e) => ({
      description: e.description,
      type: e.type,
      defaultValue: e.defaultValue,
      deprecationReason: e.deprecationReason,
      extensions: e.extensions,
      astNode: e.astNode
    })
  );
}
function Bt(t) {
  return G(t.type) && t.defaultValue === void 0;
}
class ir {
  constructor(e) {
    var n;
    this.name = Le(e.name), this.description = e.description, this.resolveType = e.resolveType, this.extensions = Se(e.extensions), this.astNode = e.astNode, this.extensionASTNodes = (n = e.extensionASTNodes) !== null && n !== void 0 ? n : [], this._fields = er.bind(void 0, e), this._interfaces = Ki.bind(void 0, e), e.resolveType == null || typeof e.resolveType == "function" || V(
      !1,
      `${this.name} must provide "resolveType" as a function, but got: ${_(e.resolveType)}.`
    );
  }
  get [Symbol.toStringTag]() {
    return "GraphQLInterfaceType";
  }
  getFields() {
    return typeof this._fields == "function" && (this._fields = this._fields()), this._fields;
  }
  getInterfaces() {
    return typeof this._interfaces == "function" && (this._interfaces = this._interfaces()), this._interfaces;
  }
  toConfig() {
    return {
      name: this.name,
      description: this.description,
      interfaces: this.getInterfaces(),
      fields: nr(this.getFields()),
      resolveType: this.resolveType,
      extensions: this.extensions,
      astNode: this.astNode,
      extensionASTNodes: this.extensionASTNodes
    };
  }
  toString() {
    return this.name;
  }
  toJSON() {
    return this.toString();
  }
}
class rr {
  constructor(e) {
    var n;
    this.name = Le(e.name), this.description = e.description, this.resolveType = e.resolveType, this.extensions = Se(e.extensions), this.astNode = e.astNode, this.extensionASTNodes = (n = e.extensionASTNodes) !== null && n !== void 0 ? n : [], this._types = qo.bind(void 0, e), e.resolveType == null || typeof e.resolveType == "function" || V(
      !1,
      `${this.name} must provide "resolveType" as a function, but got: ${_(e.resolveType)}.`
    );
  }
  get [Symbol.toStringTag]() {
    return "GraphQLUnionType";
  }
  getTypes() {
    return typeof this._types == "function" && (this._types = this._types()), this._types;
  }
  toConfig() {
    return {
      name: this.name,
      description: this.description,
      types: this.getTypes(),
      resolveType: this.resolveType,
      extensions: this.extensions,
      astNode: this.astNode,
      extensionASTNodes: this.extensionASTNodes
    };
  }
  toString() {
    return this.name;
  }
  toJSON() {
    return this.toString();
  }
}
function qo(t) {
  const e = Zi(t.types);
  return Array.isArray(e) || V(
    !1,
    `Must provide Array of types or a function which returns such an array for Union ${t.name}.`
  ), e;
}
class xn {
  /* <T> */
  constructor(e) {
    var n;
    this.name = Le(e.name), this.description = e.description, this.extensions = Se(e.extensions), this.astNode = e.astNode, this.extensionASTNodes = (n = e.extensionASTNodes) !== null && n !== void 0 ? n : [], this._values = typeof e.values == "function" ? e.values : ai(this.name, e.values), this._valueLookup = null, this._nameLookup = null;
  }
  get [Symbol.toStringTag]() {
    return "GraphQLEnumType";
  }
  getValues() {
    return typeof this._values == "function" && (this._values = ai(this.name, this._values())), this._values;
  }
  getValue(e) {
    return this._nameLookup === null && (this._nameLookup = Nt(this.getValues(), (n) => n.name)), this._nameLookup[e];
  }
  serialize(e) {
    this._valueLookup === null && (this._valueLookup = new Map(
      this.getValues().map((s) => [s.value, s])
    ));
    const n = this._valueLookup.get(e);
    if (n === void 0)
      throw new y(
        `Enum "${this.name}" cannot represent value: ${_(e)}`
      );
    return n.name;
  }
  parseValue(e) {
    if (typeof e != "string") {
      const s = _(e);
      throw new y(
        `Enum "${this.name}" cannot represent non-string value: ${s}.` + Xt(this, s)
      );
    }
    const n = this.getValue(e);
    if (n == null)
      throw new y(
        `Value "${e}" does not exist in "${this.name}" enum.` + Xt(this, e)
      );
    return n.value;
  }
  parseLiteral(e, n) {
    if (e.kind !== d.ENUM) {
      const i = ie(e);
      throw new y(
        `Enum "${this.name}" cannot represent non-enum value: ${i}.` + Xt(this, i),
        {
          nodes: e
        }
      );
    }
    const s = this.getValue(e.value);
    if (s == null) {
      const i = ie(e);
      throw new y(
        `Value "${i}" does not exist in "${this.name}" enum.` + Xt(this, i),
        {
          nodes: e
        }
      );
    }
    return s.value;
  }
  toConfig() {
    const e = it(
      this.getValues(),
      (n) => n.name,
      (n) => ({
        description: n.description,
        value: n.value,
        deprecationReason: n.deprecationReason,
        extensions: n.extensions,
        astNode: n.astNode
      })
    );
    return {
      name: this.name,
      description: this.description,
      values: e,
      extensions: this.extensions,
      astNode: this.astNode,
      extensionASTNodes: this.extensionASTNodes
    };
  }
  toString() {
    return this.name;
  }
  toJSON() {
    return this.toString();
  }
}
function Xt(t, e) {
  const n = t.getValues().map((i) => i.name), s = Ct(e, n);
  return pt("the enum value", s);
}
function ai(t, e) {
  return Tt(e) || V(
    !1,
    `${t} values must be an object with value names as keys.`
  ), Object.entries(e).map(([n, s]) => (Tt(s) || V(
    !1,
    `${t}.${n} must refer to an object with a "value" key representing an internal value but got: ${_(s)}.`
  ), {
    name: Uo(n),
    description: s.description,
    value: s.value !== void 0 ? s.value : n,
    deprecationReason: s.deprecationReason,
    extensions: Se(s.extensions),
    astNode: s.astNode
  }));
}
class ar {
  constructor(e) {
    var n, s;
    this.name = Le(e.name), this.description = e.description, this.extensions = Se(e.extensions), this.astNode = e.astNode, this.extensionASTNodes = (n = e.extensionASTNodes) !== null && n !== void 0 ? n : [], this.isOneOf = (s = e.isOneOf) !== null && s !== void 0 ? s : !1, this._fields = Go.bind(void 0, e);
  }
  get [Symbol.toStringTag]() {
    return "GraphQLInputObjectType";
  }
  getFields() {
    return typeof this._fields == "function" && (this._fields = this._fields()), this._fields;
  }
  toConfig() {
    const e = An(this.getFields(), (n) => ({
      description: n.description,
      type: n.type,
      defaultValue: n.defaultValue,
      deprecationReason: n.deprecationReason,
      extensions: n.extensions,
      astNode: n.astNode
    }));
    return {
      name: this.name,
      description: this.description,
      fields: e,
      extensions: this.extensions,
      astNode: this.astNode,
      extensionASTNodes: this.extensionASTNodes,
      isOneOf: this.isOneOf
    };
  }
  toString() {
    return this.name;
  }
  toJSON() {
    return this.toString();
  }
}
function Go(t) {
  const e = Xi(t.fields);
  return Tt(e) || V(
    !1,
    `${t.name} fields must be an object with field names as keys or a function which returns such an object.`
  ), An(e, (n, s) => (!("resolve" in n) || V(
    !1,
    `${t.name}.${s} field has a resolve property, but Input Types cannot define resolvers.`
  ), {
    name: Le(s),
    description: n.description,
    type: n.type,
    defaultValue: n.defaultValue,
    deprecationReason: n.deprecationReason,
    extensions: Se(n.extensions),
    astNode: n.astNode
  }));
}
function or(t) {
  return G(t.type) && t.defaultValue === void 0;
}
function ps(t, e) {
  return t === e ? !0 : G(t) && G(e) || pe(t) && pe(e) ? ps(t.ofType, e.ofType) : !1;
}
function It(t, e, n) {
  return e === n ? !0 : G(n) ? G(e) ? It(t, e.ofType, n.ofType) : !1 : G(e) ? It(t, e.ofType, n) : pe(n) ? pe(e) ? It(t, e.ofType, n.ofType) : !1 : pe(e) ? !1 : at(n) && (X(e) || J(e)) && t.isSubType(n, e);
}
function oi(t, e, n) {
  return e === n ? !0 : at(e) ? at(n) ? t.getPossibleTypes(e).some((s) => t.isSubType(n, s)) : t.isSubType(e, n) : at(n) ? t.isSubType(n, e) : !1;
}
const qn = 2147483647, Gn = -2147483648, Ho = new ft({
  name: "Int",
  description: "The `Int` scalar type represents non-fractional signed whole numeric values. Int can represent values between -(2^31) and 2^31 - 1.",
  serialize(t) {
    const e = Qt(t);
    if (typeof e == "boolean")
      return e ? 1 : 0;
    let n = e;
    if (typeof e == "string" && e !== "" && (n = Number(e)), typeof n != "number" || !Number.isInteger(n))
      throw new y(
        `Int cannot represent non-integer value: ${_(e)}`
      );
    if (n > qn || n < Gn)
      throw new y(
        "Int cannot represent non 32-bit signed integer value: " + _(e)
      );
    return n;
  },
  parseValue(t) {
    if (typeof t != "number" || !Number.isInteger(t))
      throw new y(
        `Int cannot represent non-integer value: ${_(t)}`
      );
    if (t > qn || t < Gn)
      throw new y(
        `Int cannot represent non 32-bit signed integer value: ${t}`
      );
    return t;
  },
  parseLiteral(t) {
    if (t.kind !== d.INT)
      throw new y(
        `Int cannot represent non-integer value: ${ie(t)}`,
        {
          nodes: t
        }
      );
    const e = parseInt(t.value, 10);
    if (e > qn || e < Gn)
      throw new y(
        `Int cannot represent non 32-bit signed integer value: ${t.value}`,
        {
          nodes: t
        }
      );
    return e;
  }
}), Bo = new ft({
  name: "Float",
  description: "The `Float` scalar type represents signed double-precision fractional values as specified by [IEEE 754](https://en.wikipedia.org/wiki/IEEE_floating_point).",
  serialize(t) {
    const e = Qt(t);
    if (typeof e == "boolean")
      return e ? 1 : 0;
    let n = e;
    if (typeof e == "string" && e !== "" && (n = Number(e)), typeof n != "number" || !Number.isFinite(n))
      throw new y(
        `Float cannot represent non numeric value: ${_(e)}`
      );
    return n;
  },
  parseValue(t) {
    if (typeof t != "number" || !Number.isFinite(t))
      throw new y(
        `Float cannot represent non numeric value: ${_(t)}`
      );
    return t;
  },
  parseLiteral(t) {
    if (t.kind !== d.FLOAT && t.kind !== d.INT)
      throw new y(
        `Float cannot represent non numeric value: ${ie(t)}`,
        t
      );
    return parseFloat(t.value);
  }
}), Z = new ft({
  name: "String",
  description: "The `String` scalar type represents textual data, represented as UTF-8 character sequences. The String type is most often used by GraphQL to represent free-form human-readable text.",
  serialize(t) {
    const e = Qt(t);
    if (typeof e == "string")
      return e;
    if (typeof e == "boolean")
      return e ? "true" : "false";
    if (typeof e == "number" && Number.isFinite(e))
      return e.toString();
    throw new y(
      `String cannot represent value: ${_(t)}`
    );
  },
  parseValue(t) {
    if (typeof t != "string")
      throw new y(
        `String cannot represent a non string value: ${_(t)}`
      );
    return t;
  },
  parseLiteral(t) {
    if (t.kind !== d.STRING)
      throw new y(
        `String cannot represent a non string value: ${ie(t)}`,
        {
          nodes: t
        }
      );
    return t.value;
  }
}), _e = new ft({
  name: "Boolean",
  description: "The `Boolean` scalar type represents `true` or `false`.",
  serialize(t) {
    const e = Qt(t);
    if (typeof e == "boolean")
      return e;
    if (Number.isFinite(e))
      return e !== 0;
    throw new y(
      `Boolean cannot represent a non boolean value: ${_(e)}`
    );
  },
  parseValue(t) {
    if (typeof t != "boolean")
      throw new y(
        `Boolean cannot represent a non boolean value: ${_(t)}`
      );
    return t;
  },
  parseLiteral(t) {
    if (t.kind !== d.BOOLEAN)
      throw new y(
        `Boolean cannot represent a non boolean value: ${ie(t)}`,
        {
          nodes: t
        }
      );
    return t.value;
  }
}), cr = new ft({
  name: "ID",
  description: 'The `ID` scalar type represents a unique identifier, often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"4"`) or integer (such as `4`) input value will be accepted as an ID.',
  serialize(t) {
    const e = Qt(t);
    if (typeof e == "string")
      return e;
    if (Number.isInteger(e))
      return String(e);
    throw new y(
      `ID cannot represent value: ${_(t)}`
    );
  },
  parseValue(t) {
    if (typeof t == "string")
      return t;
    if (typeof t == "number" && Number.isInteger(t))
      return t.toString();
    throw new y(`ID cannot represent value: ${_(t)}`);
  },
  parseLiteral(t) {
    if (t.kind !== d.STRING && t.kind !== d.INT)
      throw new y(
        "ID cannot represent a non-string and non-integer value: " + ie(t),
        {
          nodes: t
        }
      );
    return t.value;
  }
}), lr = Object.freeze([
  Z,
  Ho,
  Bo,
  _e,
  cr
]);
function Qt(t) {
  if (Qe(t)) {
    if (typeof t.valueOf == "function") {
      const e = t.valueOf();
      if (!Qe(e))
        return e;
    }
    if (typeof t.toJSON == "function")
      return t.toJSON();
  }
  return t;
}
function ur(t) {
  return Re(t, mt);
}
class mt {
  constructor(e) {
    var n, s;
    this.name = Le(e.name), this.description = e.description, this.locations = e.locations, this.isRepeatable = (n = e.isRepeatable) !== null && n !== void 0 ? n : !1, this.extensions = Se(e.extensions), this.astNode = e.astNode, Array.isArray(e.locations) || V(!1, `@${e.name} locations must be an Array.`);
    const i = (s = e.args) !== null && s !== void 0 ? s : {};
    Qe(i) && !Array.isArray(i) || V(
      !1,
      `@${e.name} args must be an object with argument names as keys.`
    ), this.args = tr(i);
  }
  get [Symbol.toStringTag]() {
    return "GraphQLDirective";
  }
  toConfig() {
    return {
      name: this.name,
      description: this.description,
      locations: this.locations,
      args: sr(this.args),
      isRepeatable: this.isRepeatable,
      extensions: this.extensions,
      astNode: this.astNode
    };
  }
  toString() {
    return "@" + this.name;
  }
  toJSON() {
    return this.toString();
  }
}
const dr = new mt({
  name: "include",
  description: "Directs the executor to include this field or fragment only when the `if` argument is true.",
  locations: [
    w.FIELD,
    w.FRAGMENT_SPREAD,
    w.INLINE_FRAGMENT
  ],
  args: {
    if: {
      type: new F(_e),
      description: "Included when true."
    }
  }
}), pr = new mt({
  name: "skip",
  description: "Directs the executor to skip this field or fragment when the `if` argument is true.",
  locations: [
    w.FIELD,
    w.FRAGMENT_SPREAD,
    w.INLINE_FRAGMENT
  ],
  args: {
    if: {
      type: new F(_e),
      description: "Skipped when true."
    }
  }
}), Qo = "No longer supported", hr = new mt({
  name: "deprecated",
  description: "Marks an element of a GraphQL schema as no longer supported.",
  locations: [
    w.FIELD_DEFINITION,
    w.ARGUMENT_DEFINITION,
    w.INPUT_FIELD_DEFINITION,
    w.ENUM_VALUE
  ],
  args: {
    reason: {
      type: Z,
      description: "Explains why this element was deprecated, usually also including a suggestion for how to access supported similar data. Formatted using the Markdown syntax, as specified by [CommonMark](https://commonmark.org/).",
      defaultValue: Qo
    }
  }
}), Yo = new mt({
  name: "specifiedBy",
  description: "Exposes a URL that specifies the behavior of this scalar.",
  locations: [w.SCALAR],
  args: {
    url: {
      type: new F(Z),
      description: "The URL that specifies the behavior of this scalar."
    }
  }
}), Wo = new mt({
  name: "oneOf",
  description: "Indicates exactly one field must be supplied and this field must not be `null`.",
  locations: [w.INPUT_OBJECT],
  args: {}
}), Yt = Object.freeze([
  dr,
  pr,
  hr,
  Yo,
  Wo
]);
function zo(t) {
  return typeof t == "object" && typeof (t == null ? void 0 : t[Symbol.iterator]) == "function";
}
function Mt(t, e) {
  if (G(e)) {
    const n = Mt(t, e.ofType);
    return (n == null ? void 0 : n.kind) === d.NULL ? null : n;
  }
  if (t === null)
    return {
      kind: d.NULL
    };
  if (t === void 0)
    return null;
  if (pe(e)) {
    const n = e.ofType;
    if (zo(t)) {
      const s = [];
      for (const i of t) {
        const r = Mt(i, n);
        r != null && s.push(r);
      }
      return {
        kind: d.LIST,
        values: s
      };
    }
    return Mt(t, n);
  }
  if (ce(e)) {
    if (!Qe(t))
      return null;
    const n = [];
    for (const s of Object.values(e.getFields())) {
      const i = Mt(t[s.name], s.type);
      i && n.push({
        kind: d.OBJECT_FIELD,
        name: {
          kind: d.NAME,
          value: s.name
        },
        value: i
      });
    }
    return {
      kind: d.OBJECT,
      fields: n
    };
  }
  if (Et(e)) {
    const n = e.serialize(t);
    if (n == null)
      return null;
    if (typeof n == "boolean")
      return {
        kind: d.BOOLEAN,
        value: n
      };
    if (typeof n == "number" && Number.isFinite(n)) {
      const s = String(n);
      return ci.test(s) ? {
        kind: d.INT,
        value: s
      } : {
        kind: d.FLOAT,
        value: s
      };
    }
    if (typeof n == "string")
      return Me(e) ? {
        kind: d.ENUM,
        value: n
      } : e === cr && ci.test(n) ? {
        kind: d.INT,
        value: n
      } : {
        kind: d.STRING,
        value: n
      };
    throw new TypeError(`Cannot convert value to AST: ${_(n)}.`);
  }
  ke(!1, "Unexpected input type: " + _(e));
}
const ci = /^-?(?:0|[1-9][0-9]*)$/, As = new et({
  name: "__Schema",
  description: "A GraphQL Schema defines the capabilities of a GraphQL server. It exposes all available types and directives on the server, as well as the entry points for query, mutation, and subscription operations.",
  fields: () => ({
    description: {
      type: Z,
      resolve: (t) => t.description
    },
    types: {
      description: "A list of all types supported by this server.",
      type: new F(new ge(new F(Ce))),
      resolve(t) {
        return Object.values(t.getTypeMap());
      }
    },
    queryType: {
      description: "The type that query operations will be rooted at.",
      type: new F(Ce),
      resolve: (t) => t.getQueryType()
    },
    mutationType: {
      description: "If this server supports mutation, the type that mutation operations will be rooted at.",
      type: Ce,
      resolve: (t) => t.getMutationType()
    },
    subscriptionType: {
      description: "If this server support subscription, the type that subscription operations will be rooted at.",
      type: Ce,
      resolve: (t) => t.getSubscriptionType()
    },
    directives: {
      description: "A list of all directives supported by this server.",
      type: new F(
        new ge(new F(fr))
      ),
      resolve: (t) => t.getDirectives()
    }
  })
}), fr = new et({
  name: "__Directive",
  description: `A Directive provides a way to describe alternate runtime execution and type validation behavior in a GraphQL document.

In some cases, you need to provide options to alter GraphQL's execution behavior in ways field arguments will not suffice, such as conditionally including or skipping a field. Directives provide this by describing additional information to the executor.`,
  fields: () => ({
    name: {
      type: new F(Z),
      resolve: (t) => t.name
    },
    description: {
      type: Z,
      resolve: (t) => t.description
    },
    isRepeatable: {
      type: new F(_e),
      resolve: (t) => t.isRepeatable
    },
    locations: {
      type: new F(
        new ge(new F(mr))
      ),
      resolve: (t) => t.locations
    },
    args: {
      type: new F(
        new ge(new F(Cn))
      ),
      args: {
        includeDeprecated: {
          type: _e,
          defaultValue: !1
        }
      },
      resolve(t, { includeDeprecated: e }) {
        return e ? t.args : t.args.filter((n) => n.deprecationReason == null);
      }
    }
  })
}), mr = new xn({
  name: "__DirectiveLocation",
  description: "A Directive can be adjacent to many parts of the GraphQL language, a __DirectiveLocation describes one such possible adjacencies.",
  values: {
    QUERY: {
      value: w.QUERY,
      description: "Location adjacent to a query operation."
    },
    MUTATION: {
      value: w.MUTATION,
      description: "Location adjacent to a mutation operation."
    },
    SUBSCRIPTION: {
      value: w.SUBSCRIPTION,
      description: "Location adjacent to a subscription operation."
    },
    FIELD: {
      value: w.FIELD,
      description: "Location adjacent to a field."
    },
    FRAGMENT_DEFINITION: {
      value: w.FRAGMENT_DEFINITION,
      description: "Location adjacent to a fragment definition."
    },
    FRAGMENT_SPREAD: {
      value: w.FRAGMENT_SPREAD,
      description: "Location adjacent to a fragment spread."
    },
    INLINE_FRAGMENT: {
      value: w.INLINE_FRAGMENT,
      description: "Location adjacent to an inline fragment."
    },
    VARIABLE_DEFINITION: {
      value: w.VARIABLE_DEFINITION,
      description: "Location adjacent to a variable definition."
    },
    SCHEMA: {
      value: w.SCHEMA,
      description: "Location adjacent to a schema definition."
    },
    SCALAR: {
      value: w.SCALAR,
      description: "Location adjacent to a scalar definition."
    },
    OBJECT: {
      value: w.OBJECT,
      description: "Location adjacent to an object type definition."
    },
    FIELD_DEFINITION: {
      value: w.FIELD_DEFINITION,
      description: "Location adjacent to a field definition."
    },
    ARGUMENT_DEFINITION: {
      value: w.ARGUMENT_DEFINITION,
      description: "Location adjacent to an argument definition."
    },
    INTERFACE: {
      value: w.INTERFACE,
      description: "Location adjacent to an interface definition."
    },
    UNION: {
      value: w.UNION,
      description: "Location adjacent to a union definition."
    },
    ENUM: {
      value: w.ENUM,
      description: "Location adjacent to an enum definition."
    },
    ENUM_VALUE: {
      value: w.ENUM_VALUE,
      description: "Location adjacent to an enum value definition."
    },
    INPUT_OBJECT: {
      value: w.INPUT_OBJECT,
      description: "Location adjacent to an input object type definition."
    },
    INPUT_FIELD_DEFINITION: {
      value: w.INPUT_FIELD_DEFINITION,
      description: "Location adjacent to an input object field definition."
    }
  }
}), Ce = new et({
  name: "__Type",
  description: "The fundamental unit of any GraphQL Schema is the type. There are many kinds of types in GraphQL as represented by the `__TypeKind` enum.\n\nDepending on the kind of a type, certain fields describe information about that type. Scalar types provide no information beyond a name, description and optional `specifiedByURL`, while Enum types provide their values. Object and Interface types provide the fields they describe. Abstract types, Union and Interface, provide the Object types possible at runtime. List and NonNull types compose other types.",
  fields: () => ({
    kind: {
      type: new F(_r),
      resolve(t) {
        if (kt(t))
          return Q.SCALAR;
        if (J(t))
          return Q.OBJECT;
        if (X(t))
          return Q.INTERFACE;
        if (Ue(t))
          return Q.UNION;
        if (Me(t))
          return Q.ENUM;
        if (ce(t))
          return Q.INPUT_OBJECT;
        if (pe(t))
          return Q.LIST;
        if (G(t))
          return Q.NON_NULL;
        ke(!1, `Unexpected type: "${_(t)}".`);
      }
    },
    name: {
      type: Z,
      resolve: (t) => "name" in t ? t.name : void 0
    },
    description: {
      type: Z,
      resolve: (t) => (
        /* c8 ignore next */
        "description" in t ? t.description : void 0
      )
    },
    specifiedByURL: {
      type: Z,
      resolve: (t) => "specifiedByURL" in t ? t.specifiedByURL : void 0
    },
    fields: {
      type: new ge(new F(gr)),
      args: {
        includeDeprecated: {
          type: _e,
          defaultValue: !1
        }
      },
      resolve(t, { includeDeprecated: e }) {
        if (J(t) || X(t)) {
          const n = Object.values(t.getFields());
          return e ? n : n.filter((s) => s.deprecationReason == null);
        }
      }
    },
    interfaces: {
      type: new ge(new F(Ce)),
      resolve(t) {
        if (J(t) || X(t))
          return t.getInterfaces();
      }
    },
    possibleTypes: {
      type: new ge(new F(Ce)),
      resolve(t, e, n, { schema: s }) {
        if (at(t))
          return s.getPossibleTypes(t);
      }
    },
    enumValues: {
      type: new ge(new F(yr)),
      args: {
        includeDeprecated: {
          type: _e,
          defaultValue: !1
        }
      },
      resolve(t, { includeDeprecated: e }) {
        if (Me(t)) {
          const n = t.getValues();
          return e ? n : n.filter((s) => s.deprecationReason == null);
        }
      }
    },
    inputFields: {
      type: new ge(new F(Cn)),
      args: {
        includeDeprecated: {
          type: _e,
          defaultValue: !1
        }
      },
      resolve(t, { includeDeprecated: e }) {
        if (ce(t)) {
          const n = Object.values(t.getFields());
          return e ? n : n.filter((s) => s.deprecationReason == null);
        }
      }
    },
    ofType: {
      type: Ce,
      resolve: (t) => "ofType" in t ? t.ofType : void 0
    },
    isOneOf: {
      type: _e,
      resolve: (t) => {
        if (ce(t))
          return t.isOneOf;
      }
    }
  })
}), gr = new et({
  name: "__Field",
  description: "Object and Interface types are described by a list of Fields, each of which has a name, potentially a list of arguments, and a return type.",
  fields: () => ({
    name: {
      type: new F(Z),
      resolve: (t) => t.name
    },
    description: {
      type: Z,
      resolve: (t) => t.description
    },
    args: {
      type: new F(
        new ge(new F(Cn))
      ),
      args: {
        includeDeprecated: {
          type: _e,
          defaultValue: !1
        }
      },
      resolve(t, { includeDeprecated: e }) {
        return e ? t.args : t.args.filter((n) => n.deprecationReason == null);
      }
    },
    type: {
      type: new F(Ce),
      resolve: (t) => t.type
    },
    isDeprecated: {
      type: new F(_e),
      resolve: (t) => t.deprecationReason != null
    },
    deprecationReason: {
      type: Z,
      resolve: (t) => t.deprecationReason
    }
  })
}), Cn = new et({
  name: "__InputValue",
  description: "Arguments provided to Fields or Directives and the input fields of an InputObject are represented as Input Values which describe their type and optionally a default value.",
  fields: () => ({
    name: {
      type: new F(Z),
      resolve: (t) => t.name
    },
    description: {
      type: Z,
      resolve: (t) => t.description
    },
    type: {
      type: new F(Ce),
      resolve: (t) => t.type
    },
    defaultValue: {
      type: Z,
      description: "A GraphQL-formatted string representing the default value for this input value.",
      resolve(t) {
        const { type: e, defaultValue: n } = t, s = Mt(n, e);
        return s ? ie(s) : null;
      }
    },
    isDeprecated: {
      type: new F(_e),
      resolve: (t) => t.deprecationReason != null
    },
    deprecationReason: {
      type: Z,
      resolve: (t) => t.deprecationReason
    }
  })
}), yr = new et({
  name: "__EnumValue",
  description: "One possible value for a given Enum. Enum values are unique values, not a placeholder for a string or numeric value. However an Enum value is returned in a JSON response as a string.",
  fields: () => ({
    name: {
      type: new F(Z),
      resolve: (t) => t.name
    },
    description: {
      type: Z,
      resolve: (t) => t.description
    },
    isDeprecated: {
      type: new F(_e),
      resolve: (t) => t.deprecationReason != null
    },
    deprecationReason: {
      type: Z,
      resolve: (t) => t.deprecationReason
    }
  })
});
var Q;
(function(t) {
  t.SCALAR = "SCALAR", t.OBJECT = "OBJECT", t.INTERFACE = "INTERFACE", t.UNION = "UNION", t.ENUM = "ENUM", t.INPUT_OBJECT = "INPUT_OBJECT", t.LIST = "LIST", t.NON_NULL = "NON_NULL";
})(Q || (Q = {}));
const _r = new xn({
  name: "__TypeKind",
  description: "An enum describing what kind of type a given `__Type` is.",
  values: {
    SCALAR: {
      value: Q.SCALAR,
      description: "Indicates this type is a scalar."
    },
    OBJECT: {
      value: Q.OBJECT,
      description: "Indicates this type is an object. `fields` and `interfaces` are valid fields."
    },
    INTERFACE: {
      value: Q.INTERFACE,
      description: "Indicates this type is an interface. `fields`, `interfaces`, and `possibleTypes` are valid fields."
    },
    UNION: {
      value: Q.UNION,
      description: "Indicates this type is a union. `possibleTypes` is a valid field."
    },
    ENUM: {
      value: Q.ENUM,
      description: "Indicates this type is an enum. `enumValues` is a valid field."
    },
    INPUT_OBJECT: {
      value: Q.INPUT_OBJECT,
      description: "Indicates this type is an input object. `inputFields` is a valid field."
    },
    LIST: {
      value: Q.LIST,
      description: "Indicates this type is a list. `ofType` is a valid field."
    },
    NON_NULL: {
      value: Q.NON_NULL,
      description: "Indicates this type is a non-null. `ofType` is a valid field."
    }
  }
}), li = {
  name: "__schema",
  type: new F(As),
  description: "Access the current type schema of this server.",
  args: [],
  resolve: (t, e, n, { schema: s }) => s,
  deprecationReason: void 0,
  extensions: /* @__PURE__ */ Object.create(null),
  astNode: void 0
}, ui = {
  name: "__type",
  type: Ce,
  description: "Request the type information of a single type.",
  args: [
    {
      name: "name",
      description: void 0,
      type: new F(Z),
      defaultValue: void 0,
      deprecationReason: void 0,
      extensions: /* @__PURE__ */ Object.create(null),
      astNode: void 0
    }
  ],
  resolve: (t, { name: e }, n, { schema: s }) => s.getType(e),
  deprecationReason: void 0,
  extensions: /* @__PURE__ */ Object.create(null),
  astNode: void 0
}, di = {
  name: "__typename",
  type: new F(Z),
  description: "The name of the current Object type at runtime.",
  args: [],
  resolve: (t, e, n, { parentType: s }) => s.name,
  deprecationReason: void 0,
  extensions: /* @__PURE__ */ Object.create(null),
  astNode: void 0
}, xs = Object.freeze([
  As,
  fr,
  mr,
  Ce,
  gr,
  Cn,
  yr,
  _r
]);
function Jo(t) {
  return xs.some(({ name: e }) => t.name === e);
}
function Zo(t) {
  return Re(t, vr);
}
function Xo(t) {
  if (!Zo(t))
    throw new Error(`Expected ${_(t)} to be a GraphQL schema.`);
  return t;
}
class vr {
  // Used as a cache for validateSchema().
  constructor(e) {
    var n, s;
    this.__validationErrors = e.assumeValid === !0 ? [] : void 0, Qe(e) || V(!1, "Must provide configuration object."), !e.types || Array.isArray(e.types) || V(
      !1,
      `"types" must be Array if provided but got: ${_(e.types)}.`
    ), !e.directives || Array.isArray(e.directives) || V(
      !1,
      `"directives" must be Array if provided but got: ${_(e.directives)}.`
    ), this.description = e.description, this.extensions = Se(e.extensions), this.astNode = e.astNode, this.extensionASTNodes = (n = e.extensionASTNodes) !== null && n !== void 0 ? n : [], this._queryType = e.query, this._mutationType = e.mutation, this._subscriptionType = e.subscription, this._directives = (s = e.directives) !== null && s !== void 0 ? s : Yt;
    const i = new Set(e.types);
    if (e.types != null)
      for (const r of e.types)
        i.delete(r), we(r, i);
    this._queryType != null && we(this._queryType, i), this._mutationType != null && we(this._mutationType, i), this._subscriptionType != null && we(this._subscriptionType, i);
    for (const r of this._directives)
      if (ur(r))
        for (const a of r.args)
          we(a.type, i);
    we(As, i), this._typeMap = /* @__PURE__ */ Object.create(null), this._subTypeMap = /* @__PURE__ */ Object.create(null), this._implementationsMap = /* @__PURE__ */ Object.create(null);
    for (const r of i) {
      if (r == null)
        continue;
      const a = r.name;
      if (a || V(
        !1,
        "One of the provided types for building the Schema is missing a name."
      ), this._typeMap[a] !== void 0)
        throw new Error(
          `Schema must contain uniquely named types but contains multiple types named "${a}".`
        );
      if (this._typeMap[a] = r, X(r)) {
        for (const o of r.getInterfaces())
          if (X(o)) {
            let c = this._implementationsMap[o.name];
            c === void 0 && (c = this._implementationsMap[o.name] = {
              objects: [],
              interfaces: []
            }), c.interfaces.push(r);
          }
      } else if (J(r)) {
        for (const o of r.getInterfaces())
          if (X(o)) {
            let c = this._implementationsMap[o.name];
            c === void 0 && (c = this._implementationsMap[o.name] = {
              objects: [],
              interfaces: []
            }), c.objects.push(r);
          }
      }
    }
  }
  get [Symbol.toStringTag]() {
    return "GraphQLSchema";
  }
  getQueryType() {
    return this._queryType;
  }
  getMutationType() {
    return this._mutationType;
  }
  getSubscriptionType() {
    return this._subscriptionType;
  }
  getRootType(e) {
    switch (e) {
      case de.QUERY:
        return this.getQueryType();
      case de.MUTATION:
        return this.getMutationType();
      case de.SUBSCRIPTION:
        return this.getSubscriptionType();
    }
  }
  getTypeMap() {
    return this._typeMap;
  }
  getType(e) {
    return this.getTypeMap()[e];
  }
  getPossibleTypes(e) {
    return Ue(e) ? e.getTypes() : this.getImplementations(e).objects;
  }
  getImplementations(e) {
    const n = this._implementationsMap[e.name];
    return n ?? {
      objects: [],
      interfaces: []
    };
  }
  isSubType(e, n) {
    let s = this._subTypeMap[e.name];
    if (s === void 0) {
      if (s = /* @__PURE__ */ Object.create(null), Ue(e))
        for (const i of e.getTypes())
          s[i.name] = !0;
      else {
        const i = this.getImplementations(e);
        for (const r of i.objects)
          s[r.name] = !0;
        for (const r of i.interfaces)
          s[r.name] = !0;
      }
      this._subTypeMap[e.name] = s;
    }
    return s[n.name] !== void 0;
  }
  getDirectives() {
    return this._directives;
  }
  getDirective(e) {
    return this.getDirectives().find((n) => n.name === e);
  }
  toConfig() {
    return {
      description: this.description,
      query: this.getQueryType(),
      mutation: this.getMutationType(),
      subscription: this.getSubscriptionType(),
      types: Object.values(this.getTypeMap()),
      directives: this.getDirectives(),
      extensions: this.extensions,
      astNode: this.astNode,
      extensionASTNodes: this.extensionASTNodes,
      assumeValid: this.__validationErrors !== void 0
    };
  }
}
function we(t, e) {
  const n = ye(t);
  if (!e.has(n)) {
    if (e.add(n), Ue(n))
      for (const s of n.getTypes())
        we(s, e);
    else if (J(n) || X(n)) {
      for (const s of n.getInterfaces())
        we(s, e);
      for (const s of Object.values(n.getFields())) {
        we(s.type, e);
        for (const i of s.args)
          we(i.type, e);
      }
    } else if (ce(n))
      for (const s of Object.values(n.getFields()))
        we(s.type, e);
  }
  return e;
}
function Ko(t) {
  if (Xo(t), t.__validationErrors)
    return t.__validationErrors;
  const e = new tc(t);
  nc(e), sc(e), ic(e);
  const n = e.getErrors();
  return t.__validationErrors = n, n;
}
function ec(t) {
  const e = Ko(t);
  if (e.length !== 0)
    throw new Error(e.map((n) => n.message).join(`

`));
}
class tc {
  constructor(e) {
    this._errors = [], this.schema = e;
  }
  reportError(e, n) {
    const s = Array.isArray(n) ? n.filter(Boolean) : n;
    this._errors.push(
      new y(e, {
        nodes: s
      })
    );
  }
  getErrors() {
    return this._errors;
  }
}
function nc(t) {
  const e = t.schema, n = e.getQueryType();
  if (!n)
    t.reportError("Query root type must be provided.", e.astNode);
  else if (!J(n)) {
    var s;
    t.reportError(
      `Query root type must be Object type, it cannot be ${_(
        n
      )}.`,
      (s = Hn(
        e,
        de.QUERY
      )) !== null && s !== void 0 ? s : n.astNode
    );
  }
  const i = e.getMutationType();
  if (i && !J(i)) {
    var r;
    t.reportError(
      `Mutation root type must be Object type if provided, it cannot be ${_(i)}.`,
      (r = Hn(
        e,
        de.MUTATION
      )) !== null && r !== void 0 ? r : i.astNode
    );
  }
  const a = e.getSubscriptionType();
  if (a && !J(a)) {
    var o;
    t.reportError(
      `Subscription root type must be Object type if provided, it cannot be ${_(a)}.`,
      (o = Hn(
        e,
        de.SUBSCRIPTION
      )) !== null && o !== void 0 ? o : a.astNode
    );
  }
}
function Hn(t, e) {
  var n;
  return (n = [t.astNode, ...t.extensionASTNodes].flatMap(
    // FIXME: https://github.com/graphql/graphql-js/issues/2203
    (s) => {
      var i;
      return (
        /* c8 ignore next */
        (i = s == null ? void 0 : s.operationTypes) !== null && i !== void 0 ? i : []
      );
    }
  ).find((s) => s.operation === e)) === null || n === void 0 ? void 0 : n.type;
}
function sc(t) {
  for (const n of t.schema.getDirectives()) {
    if (!ur(n)) {
      t.reportError(
        `Expected directive but got: ${_(n)}.`,
        n == null ? void 0 : n.astNode
      );
      continue;
    }
    ht(t, n), n.locations.length === 0 && t.reportError(
      `Directive @${n.name} must include 1 or more locations.`,
      n.astNode
    );
    for (const s of n.args)
      if (ht(t, s), xe(s.type) || t.reportError(
        `The type of @${n.name}(${s.name}:) must be Input Type but got: ${_(s.type)}.`,
        s.astNode
      ), Bt(s) && s.deprecationReason != null) {
        var e;
        t.reportError(
          `Required argument @${n.name}(${s.name}:) cannot be deprecated.`,
          [
            Cs(s.astNode),
            (e = s.astNode) === null || e === void 0 ? void 0 : e.type
          ]
        );
      }
  }
}
function ht(t, e) {
  e.name.startsWith("__") && t.reportError(
    `Name "${e.name}" must not begin with "__", which is reserved by GraphQL introspection.`,
    e.astNode
  );
}
function ic(t) {
  const e = dc(t), n = t.schema.getTypeMap();
  for (const s of Object.values(n)) {
    if (!Vo(s)) {
      t.reportError(
        `Expected GraphQL named type but got: ${_(s)}.`,
        s.astNode
      );
      continue;
    }
    Jo(s) || ht(t, s), J(s) || X(s) ? (pi(t, s), hi(t, s)) : Ue(s) ? oc(t, s) : Me(s) ? cc(t, s) : ce(s) && (lc(t, s), e(s));
  }
}
function pi(t, e) {
  const n = Object.values(e.getFields());
  n.length === 0 && t.reportError(`Type ${e.name} must define one or more fields.`, [
    e.astNode,
    ...e.extensionASTNodes
  ]);
  for (const a of n) {
    if (ht(t, a), !vt(a.type)) {
      var s;
      t.reportError(
        `The type of ${e.name}.${a.name} must be Output Type but got: ${_(a.type)}.`,
        (s = a.astNode) === null || s === void 0 ? void 0 : s.type
      );
    }
    for (const o of a.args) {
      const c = o.name;
      if (ht(t, o), !xe(o.type)) {
        var i;
        t.reportError(
          `The type of ${e.name}.${a.name}(${c}:) must be Input Type but got: ${_(o.type)}.`,
          (i = o.astNode) === null || i === void 0 ? void 0 : i.type
        );
      }
      if (Bt(o) && o.deprecationReason != null) {
        var r;
        t.reportError(
          `Required argument ${e.name}.${a.name}(${c}:) cannot be deprecated.`,
          [
            Cs(o.astNode),
            (r = o.astNode) === null || r === void 0 ? void 0 : r.type
          ]
        );
      }
    }
  }
}
function hi(t, e) {
  const n = /* @__PURE__ */ Object.create(null);
  for (const s of e.getInterfaces()) {
    if (!X(s)) {
      t.reportError(
        `Type ${_(e)} must only implement Interface types, it cannot implement ${_(s)}.`,
        $t(e, s)
      );
      continue;
    }
    if (e === s) {
      t.reportError(
        `Type ${e.name} cannot implement itself because it would create a circular reference.`,
        $t(e, s)
      );
      continue;
    }
    if (n[s.name]) {
      t.reportError(
        `Type ${e.name} can only implement ${s.name} once.`,
        $t(e, s)
      );
      continue;
    }
    n[s.name] = !0, ac(t, e, s), rc(t, e, s);
  }
}
function rc(t, e, n) {
  const s = e.getFields();
  for (const c of Object.values(n.getFields())) {
    const l = c.name, u = s[l];
    if (!u) {
      t.reportError(
        `Interface field ${n.name}.${l} expected but ${e.name} does not provide it.`,
        [c.astNode, e.astNode, ...e.extensionASTNodes]
      );
      continue;
    }
    if (!It(t.schema, u.type, c.type)) {
      var i, r;
      t.reportError(
        `Interface field ${n.name}.${l} expects type ${_(c.type)} but ${e.name}.${l} is type ${_(u.type)}.`,
        [
          (i = c.astNode) === null || i === void 0 ? void 0 : i.type,
          (r = u.astNode) === null || r === void 0 ? void 0 : r.type
        ]
      );
    }
    for (const p of c.args) {
      const f = p.name, S = u.args.find((v) => v.name === f);
      if (!S) {
        t.reportError(
          `Interface field argument ${n.name}.${l}(${f}:) expected but ${e.name}.${l} does not provide it.`,
          [p.astNode, u.astNode]
        );
        continue;
      }
      if (!ps(p.type, S.type)) {
        var a, o;
        t.reportError(
          `Interface field argument ${n.name}.${l}(${f}:) expects type ${_(p.type)} but ${e.name}.${l}(${f}:) is type ${_(S.type)}.`,
          [
            (a = p.astNode) === null || a === void 0 ? void 0 : a.type,
            (o = S.astNode) === null || o === void 0 ? void 0 : o.type
          ]
        );
      }
    }
    for (const p of u.args) {
      const f = p.name;
      !c.args.find((v) => v.name === f) && Bt(p) && t.reportError(
        `Object field ${e.name}.${l} includes required argument ${f} that is missing from the Interface field ${n.name}.${l}.`,
        [p.astNode, c.astNode]
      );
    }
  }
}
function ac(t, e, n) {
  const s = e.getInterfaces();
  for (const i of n.getInterfaces())
    s.includes(i) || t.reportError(
      i === e ? `Type ${e.name} cannot implement ${n.name} because it would create a circular reference.` : `Type ${e.name} must implement ${i.name} because it is implemented by ${n.name}.`,
      [
        ...$t(n, i),
        ...$t(e, n)
      ]
    );
}
function oc(t, e) {
  const n = e.getTypes();
  n.length === 0 && t.reportError(
    `Union type ${e.name} must define one or more member types.`,
    [e.astNode, ...e.extensionASTNodes]
  );
  const s = /* @__PURE__ */ Object.create(null);
  for (const i of n) {
    if (s[i.name]) {
      t.reportError(
        `Union type ${e.name} can only include type ${i.name} once.`,
        fi(e, i.name)
      );
      continue;
    }
    s[i.name] = !0, J(i) || t.reportError(
      `Union type ${e.name} can only include Object types, it cannot include ${_(i)}.`,
      fi(e, String(i))
    );
  }
}
function cc(t, e) {
  const n = e.getValues();
  n.length === 0 && t.reportError(
    `Enum type ${e.name} must define one or more values.`,
    [e.astNode, ...e.extensionASTNodes]
  );
  for (const s of n)
    ht(t, s);
}
function lc(t, e) {
  const n = Object.values(e.getFields());
  n.length === 0 && t.reportError(
    `Input Object type ${e.name} must define one or more fields.`,
    [e.astNode, ...e.extensionASTNodes]
  );
  for (const r of n) {
    if (ht(t, r), !xe(r.type)) {
      var s;
      t.reportError(
        `The type of ${e.name}.${r.name} must be Input Type but got: ${_(r.type)}.`,
        (s = r.astNode) === null || s === void 0 ? void 0 : s.type
      );
    }
    if (or(r) && r.deprecationReason != null) {
      var i;
      t.reportError(
        `Required input field ${e.name}.${r.name} cannot be deprecated.`,
        [
          Cs(r.astNode),
          (i = r.astNode) === null || i === void 0 ? void 0 : i.type
        ]
      );
    }
    e.isOneOf && uc(e, r, t);
  }
}
function uc(t, e, n) {
  if (G(e.type)) {
    var s;
    n.reportError(
      `OneOf input field ${t.name}.${e.name} must be nullable.`,
      (s = e.astNode) === null || s === void 0 ? void 0 : s.type
    );
  }
  e.defaultValue !== void 0 && n.reportError(
    `OneOf input field ${t.name}.${e.name} cannot have a default value.`,
    e.astNode
  );
}
function dc(t) {
  const e = /* @__PURE__ */ Object.create(null), n = [], s = /* @__PURE__ */ Object.create(null);
  return i;
  function i(r) {
    if (e[r.name])
      return;
    e[r.name] = !0, s[r.name] = n.length;
    const a = Object.values(r.getFields());
    for (const o of a)
      if (G(o.type) && ce(o.type.ofType)) {
        const c = o.type.ofType, l = s[c.name];
        if (n.push(o), l === void 0)
          i(c);
        else {
          const u = n.slice(l), p = u.map((f) => f.name).join(".");
          t.reportError(
            `Cannot reference Input Object "${c.name}" within itself through a series of non-null fields: "${p}".`,
            u.map((f) => f.astNode)
          );
        }
        n.pop();
      }
    s[r.name] = void 0;
  }
}
function $t(t, e) {
  const { astNode: n, extensionASTNodes: s } = t;
  return (n != null ? [n, ...s] : s).flatMap((r) => {
    var a;
    return (
      /* c8 ignore next */
      (a = r.interfaces) !== null && a !== void 0 ? a : []
    );
  }).filter((r) => r.name.value === e.name);
}
function fi(t, e) {
  const { astNode: n, extensionASTNodes: s } = t;
  return (n != null ? [n, ...s] : s).flatMap((r) => {
    var a;
    return (
      /* c8 ignore next */
      (a = r.types) !== null && a !== void 0 ? a : []
    );
  }).filter((r) => r.name.value === e);
}
function Cs(t) {
  var e;
  return t == null || (e = t.directives) === null || e === void 0 ? void 0 : e.find(
    (n) => n.name.value === hr.name
  );
}
function Ie(t, e) {
  switch (e.kind) {
    case d.LIST_TYPE: {
      const n = Ie(t, e.type);
      return n && new ge(n);
    }
    case d.NON_NULL_TYPE: {
      const n = Ie(t, e.type);
      return n && new F(n);
    }
    case d.NAMED_TYPE:
      return t.getType(e.name.value);
  }
}
class Tr {
  constructor(e, n, s) {
    this._schema = e, this._typeStack = [], this._parentTypeStack = [], this._inputTypeStack = [], this._fieldDefStack = [], this._defaultValueStack = [], this._directive = null, this._argument = null, this._enumValue = null, this._getFieldDef = s ?? pc, n && (xe(n) && this._inputTypeStack.push(n), Xe(n) && this._parentTypeStack.push(n), vt(n) && this._typeStack.push(n));
  }
  get [Symbol.toStringTag]() {
    return "TypeInfo";
  }
  getType() {
    if (this._typeStack.length > 0)
      return this._typeStack[this._typeStack.length - 1];
  }
  getParentType() {
    if (this._parentTypeStack.length > 0)
      return this._parentTypeStack[this._parentTypeStack.length - 1];
  }
  getInputType() {
    if (this._inputTypeStack.length > 0)
      return this._inputTypeStack[this._inputTypeStack.length - 1];
  }
  getParentInputType() {
    if (this._inputTypeStack.length > 1)
      return this._inputTypeStack[this._inputTypeStack.length - 2];
  }
  getFieldDef() {
    if (this._fieldDefStack.length > 0)
      return this._fieldDefStack[this._fieldDefStack.length - 1];
  }
  getDefaultValue() {
    if (this._defaultValueStack.length > 0)
      return this._defaultValueStack[this._defaultValueStack.length - 1];
  }
  getDirective() {
    return this._directive;
  }
  getArgument() {
    return this._argument;
  }
  getEnumValue() {
    return this._enumValue;
  }
  enter(e) {
    const n = this._schema;
    switch (e.kind) {
      case d.SELECTION_SET: {
        const i = ye(this.getType());
        this._parentTypeStack.push(
          Xe(i) ? i : void 0
        );
        break;
      }
      case d.FIELD: {
        const i = this.getParentType();
        let r, a;
        i && (r = this._getFieldDef(n, i, e), r && (a = r.type)), this._fieldDefStack.push(r), this._typeStack.push(vt(a) ? a : void 0);
        break;
      }
      case d.DIRECTIVE:
        this._directive = n.getDirective(e.name.value);
        break;
      case d.OPERATION_DEFINITION: {
        const i = n.getRootType(e.operation);
        this._typeStack.push(J(i) ? i : void 0);
        break;
      }
      case d.INLINE_FRAGMENT:
      case d.FRAGMENT_DEFINITION: {
        const i = e.typeCondition, r = i ? Ie(n, i) : ye(this.getType());
        this._typeStack.push(vt(r) ? r : void 0);
        break;
      }
      case d.VARIABLE_DEFINITION: {
        const i = Ie(n, e.type);
        this._inputTypeStack.push(
          xe(i) ? i : void 0
        );
        break;
      }
      case d.ARGUMENT: {
        var s;
        let i, r;
        const a = (s = this.getDirective()) !== null && s !== void 0 ? s : this.getFieldDef();
        a && (i = a.args.find(
          (o) => o.name === e.name.value
        ), i && (r = i.type)), this._argument = i, this._defaultValueStack.push(i ? i.defaultValue : void 0), this._inputTypeStack.push(xe(r) ? r : void 0);
        break;
      }
      case d.LIST: {
        const i = Ji(this.getInputType()), r = pe(i) ? i.ofType : i;
        this._defaultValueStack.push(void 0), this._inputTypeStack.push(xe(r) ? r : void 0);
        break;
      }
      case d.OBJECT_FIELD: {
        const i = ye(this.getInputType());
        let r, a;
        ce(i) && (a = i.getFields()[e.name.value], a && (r = a.type)), this._defaultValueStack.push(
          a ? a.defaultValue : void 0
        ), this._inputTypeStack.push(
          xe(r) ? r : void 0
        );
        break;
      }
      case d.ENUM: {
        const i = ye(this.getInputType());
        let r;
        Me(i) && (r = i.getValue(e.value)), this._enumValue = r;
        break;
      }
    }
  }
  leave(e) {
    switch (e.kind) {
      case d.SELECTION_SET:
        this._parentTypeStack.pop();
        break;
      case d.FIELD:
        this._fieldDefStack.pop(), this._typeStack.pop();
        break;
      case d.DIRECTIVE:
        this._directive = null;
        break;
      case d.OPERATION_DEFINITION:
      case d.INLINE_FRAGMENT:
      case d.FRAGMENT_DEFINITION:
        this._typeStack.pop();
        break;
      case d.VARIABLE_DEFINITION:
        this._inputTypeStack.pop();
        break;
      case d.ARGUMENT:
        this._argument = null, this._defaultValueStack.pop(), this._inputTypeStack.pop();
        break;
      case d.LIST:
      case d.OBJECT_FIELD:
        this._defaultValueStack.pop(), this._inputTypeStack.pop();
        break;
      case d.ENUM:
        this._enumValue = null;
        break;
    }
  }
}
function pc(t, e, n) {
  const s = n.name.value;
  if (s === li.name && t.getQueryType() === e)
    return li;
  if (s === ui.name && t.getQueryType() === e)
    return ui;
  if (s === di.name && Xe(e))
    return di;
  if (J(e) || X(e))
    return e.getFields()[s];
}
function Ir(t, e) {
  return {
    enter(...n) {
      const s = n[0];
      t.enter(s);
      const i = gn(e, s.kind).enter;
      if (i) {
        const r = i.apply(e, n);
        return r !== void 0 && (t.leave(s), cs(r) && t.enter(r)), r;
      }
    },
    leave(...n) {
      const s = n[0], i = gn(e, s.kind).leave;
      let r;
      return i && (r = i.apply(e, n)), t.leave(s), r;
    }
  };
}
function hc(t) {
  return t.kind === d.OPERATION_DEFINITION || t.kind === d.FRAGMENT_DEFINITION;
}
function fc(t) {
  return t.kind === d.SCHEMA_DEFINITION || ks(t) || t.kind === d.DIRECTIVE_DEFINITION;
}
function ks(t) {
  return t.kind === d.SCALAR_TYPE_DEFINITION || t.kind === d.OBJECT_TYPE_DEFINITION || t.kind === d.INTERFACE_TYPE_DEFINITION || t.kind === d.UNION_TYPE_DEFINITION || t.kind === d.ENUM_TYPE_DEFINITION || t.kind === d.INPUT_OBJECT_TYPE_DEFINITION;
}
function mc(t) {
  return t.kind === d.SCHEMA_EXTENSION || br(t);
}
function br(t) {
  return t.kind === d.SCALAR_TYPE_EXTENSION || t.kind === d.OBJECT_TYPE_EXTENSION || t.kind === d.INTERFACE_TYPE_EXTENSION || t.kind === d.UNION_TYPE_EXTENSION || t.kind === d.ENUM_TYPE_EXTENSION || t.kind === d.INPUT_OBJECT_TYPE_EXTENSION;
}
function gc(t) {
  return {
    Document(e) {
      for (const n of e.definitions)
        if (!hc(n)) {
          const s = n.kind === d.SCHEMA_DEFINITION || n.kind === d.SCHEMA_EXTENSION ? "schema" : '"' + n.name.value + '"';
          t.reportError(
            new y(`The ${s} definition is not executable.`, {
              nodes: n
            })
          );
        }
      return !1;
    }
  };
}
function yc(t) {
  return {
    Field(e) {
      const n = t.getParentType();
      if (n && !t.getFieldDef()) {
        const i = t.getSchema(), r = e.name.value;
        let a = pt(
          "to use an inline fragment on",
          _c(i, n, r)
        );
        a === "" && (a = pt(vc(n, r))), t.reportError(
          new y(
            `Cannot query field "${r}" on type "${n.name}".` + a,
            {
              nodes: e
            }
          )
        );
      }
    }
  };
}
function _c(t, e, n) {
  if (!at(e))
    return [];
  const s = /* @__PURE__ */ new Set(), i = /* @__PURE__ */ Object.create(null);
  for (const a of t.getPossibleTypes(e))
    if (a.getFields()[n]) {
      s.add(a), i[a.name] = 1;
      for (const o of a.getInterfaces()) {
        var r;
        o.getFields()[n] && (s.add(o), i[o.name] = ((r = i[o.name]) !== null && r !== void 0 ? r : 0) + 1);
      }
    }
  return [...s].sort((a, o) => {
    const c = i[o.name] - i[a.name];
    return c !== 0 ? c : X(a) && t.isSubType(a, o) ? -1 : X(o) && t.isSubType(o, a) ? 1 : Ss(a.name, o.name);
  }).map((a) => a.name);
}
function vc(t, e) {
  if (J(t) || X(t)) {
    const n = Object.keys(t.getFields());
    return Ct(e, n);
  }
  return [];
}
function Tc(t) {
  return {
    InlineFragment(e) {
      const n = e.typeCondition;
      if (n) {
        const s = Ie(t.getSchema(), n);
        if (s && !Xe(s)) {
          const i = ie(n);
          t.reportError(
            new y(
              `Fragment cannot condition on non composite type "${i}".`,
              {
                nodes: n
              }
            )
          );
        }
      }
    },
    FragmentDefinition(e) {
      const n = Ie(t.getSchema(), e.typeCondition);
      if (n && !Xe(n)) {
        const s = ie(e.typeCondition);
        t.reportError(
          new y(
            `Fragment "${e.name.value}" cannot condition on non composite type "${s}".`,
            {
              nodes: e.typeCondition
            }
          )
        );
      }
    }
  };
}
function Ic(t) {
  return {
    // eslint-disable-next-line new-cap
    ...bc(t),
    Argument(e) {
      const n = t.getArgument(), s = t.getFieldDef(), i = t.getParentType();
      if (!n && s && i) {
        const r = e.name.value, a = s.args.map((c) => c.name), o = Ct(r, a);
        t.reportError(
          new y(
            `Unknown argument "${r}" on field "${i.name}.${s.name}".` + pt(o),
            {
              nodes: e
            }
          )
        );
      }
    }
  };
}
function bc(t) {
  const e = /* @__PURE__ */ Object.create(null), n = t.getSchema(), s = n ? n.getDirectives() : Yt;
  for (const a of s)
    e[a.name] = a.args.map((o) => o.name);
  const i = t.getDocument().definitions;
  for (const a of i)
    if (a.kind === d.DIRECTIVE_DEFINITION) {
      var r;
      const o = (r = a.arguments) !== null && r !== void 0 ? r : [];
      e[a.name.value] = o.map((c) => c.name.value);
    }
  return {
    Directive(a) {
      const o = a.name.value, c = e[o];
      if (a.arguments && c)
        for (const l of a.arguments) {
          const u = l.name.value;
          if (!c.includes(u)) {
            const p = Ct(u, c);
            t.reportError(
              new y(
                `Unknown argument "${u}" on directive "@${o}".` + pt(p),
                {
                  nodes: l
                }
              )
            );
          }
        }
      return !1;
    }
  };
}
function Sc(t) {
  const e = /* @__PURE__ */ Object.create(null), n = t.getSchema(), s = n ? n.getDirectives() : Yt;
  for (const r of s)
    e[r.name] = r.locations;
  const i = t.getDocument().definitions;
  for (const r of i)
    r.kind === d.DIRECTIVE_DEFINITION && (e[r.name.value] = r.locations.map((a) => a.value));
  return {
    Directive(r, a, o, c, l) {
      const u = r.name.value, p = e[u];
      if (!p) {
        t.reportError(
          new y(`Unknown directive "@${u}".`, {
            nodes: r
          })
        );
        return;
      }
      const f = Nc(l);
      f && !p.includes(f) && t.reportError(
        new y(
          `Directive "@${u}" may not be used on ${f}.`,
          {
            nodes: r
          }
        )
      );
    }
  };
}
function Nc(t) {
  const e = t[t.length - 1];
  switch ("kind" in e || ke(!1), e.kind) {
    case d.OPERATION_DEFINITION:
      return Ec(e.operation);
    case d.FIELD:
      return w.FIELD;
    case d.FRAGMENT_SPREAD:
      return w.FRAGMENT_SPREAD;
    case d.INLINE_FRAGMENT:
      return w.INLINE_FRAGMENT;
    case d.FRAGMENT_DEFINITION:
      return w.FRAGMENT_DEFINITION;
    case d.VARIABLE_DEFINITION:
      return w.VARIABLE_DEFINITION;
    case d.SCHEMA_DEFINITION:
    case d.SCHEMA_EXTENSION:
      return w.SCHEMA;
    case d.SCALAR_TYPE_DEFINITION:
    case d.SCALAR_TYPE_EXTENSION:
      return w.SCALAR;
    case d.OBJECT_TYPE_DEFINITION:
    case d.OBJECT_TYPE_EXTENSION:
      return w.OBJECT;
    case d.FIELD_DEFINITION:
      return w.FIELD_DEFINITION;
    case d.INTERFACE_TYPE_DEFINITION:
    case d.INTERFACE_TYPE_EXTENSION:
      return w.INTERFACE;
    case d.UNION_TYPE_DEFINITION:
    case d.UNION_TYPE_EXTENSION:
      return w.UNION;
    case d.ENUM_TYPE_DEFINITION:
    case d.ENUM_TYPE_EXTENSION:
      return w.ENUM;
    case d.ENUM_VALUE_DEFINITION:
      return w.ENUM_VALUE;
    case d.INPUT_OBJECT_TYPE_DEFINITION:
    case d.INPUT_OBJECT_TYPE_EXTENSION:
      return w.INPUT_OBJECT;
    case d.INPUT_VALUE_DEFINITION: {
      const n = t[t.length - 3];
      return "kind" in n || ke(!1), n.kind === d.INPUT_OBJECT_TYPE_DEFINITION ? w.INPUT_FIELD_DEFINITION : w.ARGUMENT_DEFINITION;
    }
    // Not reachable, all possible types have been considered.
    /* c8 ignore next */
    default:
      ke(!1, "Unexpected kind: " + _(e.kind));
  }
}
function Ec(t) {
  switch (t) {
    case de.QUERY:
      return w.QUERY;
    case de.MUTATION:
      return w.MUTATION;
    case de.SUBSCRIPTION:
      return w.SUBSCRIPTION;
  }
}
function Oc(t) {
  return {
    FragmentSpread(e) {
      const n = e.name.value;
      t.getFragment(n) || t.reportError(
        new y(`Unknown fragment "${n}".`, {
          nodes: e.name
        })
      );
    }
  };
}
function wc(t) {
  const e = t.getSchema(), n = e ? e.getTypeMap() : /* @__PURE__ */ Object.create(null), s = /* @__PURE__ */ Object.create(null);
  for (const r of t.getDocument().definitions)
    ks(r) && (s[r.name.value] = !0);
  const i = [
    ...Object.keys(n),
    ...Object.keys(s)
  ];
  return {
    NamedType(r, a, o, c, l) {
      const u = r.name.value;
      if (!n[u] && !s[u]) {
        var p;
        const f = (p = l[2]) !== null && p !== void 0 ? p : o, S = f != null && Ac(f);
        if (S && mi.includes(u))
          return;
        const v = Ct(
          u,
          S ? mi.concat(i) : i
        );
        t.reportError(
          new y(
            `Unknown type "${u}".` + pt(v),
            {
              nodes: r
            }
          )
        );
      }
    }
  };
}
const mi = [...lr, ...xs].map(
  (t) => t.name
);
function Ac(t) {
  return "kind" in t && (fc(t) || mc(t));
}
function xc(t) {
  let e = 0;
  return {
    Document(n) {
      e = n.definitions.filter(
        (s) => s.kind === d.OPERATION_DEFINITION
      ).length;
    },
    OperationDefinition(n) {
      !n.name && e > 1 && t.reportError(
        new y(
          "This anonymous operation must be the only defined operation.",
          {
            nodes: n
          }
        )
      );
    }
  };
}
const Cc = 3;
function kc(t) {
  function e(n, s = /* @__PURE__ */ Object.create(null), i = 0) {
    if (n.kind === d.FRAGMENT_SPREAD) {
      const r = n.name.value;
      if (s[r] === !0)
        return !1;
      const a = t.getFragment(r);
      if (!a)
        return !1;
      try {
        return s[r] = !0, e(a, s, i);
      } finally {
        s[r] = void 0;
      }
    }
    if (n.kind === d.FIELD && // check all introspection lists
    (n.name.value === "fields" || n.name.value === "interfaces" || n.name.value === "possibleTypes" || n.name.value === "inputFields") && (i++, i >= Cc))
      return !0;
    if ("selectionSet" in n && n.selectionSet) {
      for (const r of n.selectionSet.selections)
        if (e(r, s, i))
          return !0;
    }
    return !1;
  }
  return {
    Field(n) {
      if ((n.name.value === "__schema" || n.name.value === "__type") && e(n))
        return t.reportError(
          new y("Maximum introspection depth exceeded", {
            nodes: [n]
          })
        ), !1;
    }
  };
}
function Rc(t) {
  const e = /* @__PURE__ */ Object.create(null), n = [], s = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: () => !1,
    FragmentDefinition(r) {
      return i(r), !1;
    }
  };
  function i(r) {
    if (e[r.name.value])
      return;
    const a = r.name.value;
    e[a] = !0;
    const o = t.getFragmentSpreads(r.selectionSet);
    if (o.length !== 0) {
      s[a] = n.length;
      for (const c of o) {
        const l = c.name.value, u = s[l];
        if (n.push(c), u === void 0) {
          const p = t.getFragment(l);
          p && i(p);
        } else {
          const p = n.slice(u), f = p.slice(0, -1).map((S) => '"' + S.name.value + '"').join(", ");
          t.reportError(
            new y(
              `Cannot spread fragment "${l}" within itself` + (f !== "" ? ` via ${f}.` : "."),
              {
                nodes: p
              }
            )
          );
        }
        n.pop();
      }
      s[a] = void 0;
    }
  }
}
function Lc(t) {
  let e = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: {
      enter() {
        e = /* @__PURE__ */ Object.create(null);
      },
      leave(n) {
        const s = t.getRecursiveVariableUsages(n);
        for (const { node: i } of s) {
          const r = i.name.value;
          e[r] !== !0 && t.reportError(
            new y(
              n.name ? `Variable "$${r}" is not defined by operation "${n.name.value}".` : `Variable "$${r}" is not defined.`,
              {
                nodes: [i, n]
              }
            )
          );
        }
      }
    },
    VariableDefinition(n) {
      e[n.variable.name.value] = !0;
    }
  };
}
function Dc(t) {
  const e = [], n = [];
  return {
    OperationDefinition(s) {
      return e.push(s), !1;
    },
    FragmentDefinition(s) {
      return n.push(s), !1;
    },
    Document: {
      leave() {
        const s = /* @__PURE__ */ Object.create(null);
        for (const i of e)
          for (const r of t.getRecursivelyReferencedFragments(
            i
          ))
            s[r.name.value] = !0;
        for (const i of n) {
          const r = i.name.value;
          s[r] !== !0 && t.reportError(
            new y(`Fragment "${r}" is never used.`, {
              nodes: i
            })
          );
        }
      }
    }
  };
}
function Pc(t) {
  let e = [];
  return {
    OperationDefinition: {
      enter() {
        e = [];
      },
      leave(n) {
        const s = /* @__PURE__ */ Object.create(null), i = t.getRecursiveVariableUsages(n);
        for (const { node: r } of i)
          s[r.name.value] = !0;
        for (const r of e) {
          const a = r.variable.name.value;
          s[a] !== !0 && t.reportError(
            new y(
              n.name ? `Variable "$${a}" is never used in operation "${n.name.value}".` : `Variable "$${a}" is never used.`,
              {
                nodes: r
              }
            )
          );
        }
      }
    },
    VariableDefinition(n) {
      e.push(n);
    }
  };
}
function Rs(t) {
  switch (t.kind) {
    case d.OBJECT:
      return { ...t, fields: Fc(t.fields) };
    case d.LIST:
      return { ...t, values: t.values.map(Rs) };
    case d.INT:
    case d.FLOAT:
    case d.STRING:
    case d.BOOLEAN:
    case d.NULL:
    case d.ENUM:
    case d.VARIABLE:
      return t;
  }
}
function Fc(t) {
  return t.map((e) => ({
    ...e,
    value: Rs(e.value)
  })).sort(
    (e, n) => Ss(e.name.value, n.name.value)
  );
}
function Sr(t) {
  return Array.isArray(t) ? t.map(
    ([e, n]) => `subfields "${e}" conflict because ` + Sr(n)
  ).join(" and ") : t;
}
function Uc(t) {
  const e = new Or(), n = new Gc(), s = /* @__PURE__ */ new Map();
  return {
    SelectionSet(i) {
      const r = Mc(
        t,
        s,
        e,
        n,
        t.getParentType(),
        i
      );
      for (const [[a, o], c, l] of r) {
        const u = Sr(o);
        t.reportError(
          new y(
            `Fields "${a}" conflict because ${u}. Use different aliases on the fields to fetch both if this was intentional.`,
            {
              nodes: c.concat(l)
            }
          )
        );
      }
    }
  };
}
function Mc(t, e, n, s, i, r) {
  const a = [], [o, c] = vn(
    t,
    e,
    i,
    r
  );
  if (jc(
    t,
    a,
    e,
    n,
    s,
    o
  ), c.length !== 0)
    for (let l = 0; l < c.length; l++) {
      yn(
        t,
        a,
        e,
        n,
        s,
        !1,
        o,
        c[l]
      );
      for (let u = l + 1; u < c.length; u++)
        _n(
          t,
          a,
          e,
          n,
          s,
          !1,
          c[l],
          c[u]
        );
    }
  return a;
}
function yn(t, e, n, s, i, r, a, o) {
  if (s.has(
    a,
    o,
    r
  ))
    return;
  s.add(
    a,
    o,
    r
  );
  const c = t.getFragment(o);
  if (!c)
    return;
  const [l, u] = fs(
    t,
    n,
    c
  );
  if (a !== l) {
    Ls(
      t,
      e,
      n,
      s,
      i,
      r,
      a,
      l
    );
    for (const p of u)
      yn(
        t,
        e,
        n,
        s,
        i,
        r,
        a,
        p
      );
  }
}
function _n(t, e, n, s, i, r, a, o) {
  if (a === o || i.has(
    a,
    o,
    r
  ))
    return;
  i.add(a, o, r);
  const c = t.getFragment(a), l = t.getFragment(o);
  if (!c || !l)
    return;
  const [u, p] = fs(
    t,
    n,
    c
  ), [f, S] = fs(
    t,
    n,
    l
  );
  Ls(
    t,
    e,
    n,
    s,
    i,
    r,
    u,
    f
  );
  for (const v of S)
    _n(
      t,
      e,
      n,
      s,
      i,
      r,
      a,
      v
    );
  for (const v of p)
    _n(
      t,
      e,
      n,
      s,
      i,
      r,
      v,
      o
    );
}
function $c(t, e, n, s, i, r, a, o, c) {
  const l = [], [u, p] = vn(
    t,
    e,
    r,
    a
  ), [f, S] = vn(
    t,
    e,
    o,
    c
  );
  Ls(
    t,
    l,
    e,
    n,
    s,
    i,
    u,
    f
  );
  for (const v of S)
    yn(
      t,
      l,
      e,
      n,
      s,
      i,
      u,
      v
    );
  for (const v of p)
    yn(
      t,
      l,
      e,
      n,
      s,
      i,
      f,
      v
    );
  for (const v of p)
    for (const A of S)
      _n(
        t,
        l,
        e,
        n,
        s,
        i,
        v,
        A
      );
  return l;
}
function jc(t, e, n, s, i, r) {
  for (const [a, o] of Object.entries(r))
    if (o.length > 1)
      for (let c = 0; c < o.length; c++)
        for (let l = c + 1; l < o.length; l++) {
          const u = Nr(
            t,
            n,
            s,
            i,
            !1,
            // within one collection is never mutually exclusive
            a,
            o[c],
            o[l]
          );
          u && e.push(u);
        }
}
function Ls(t, e, n, s, i, r, a, o) {
  for (const [c, l] of Object.entries(a)) {
    const u = o[c];
    if (u)
      for (const p of l)
        for (const f of u) {
          const S = Nr(
            t,
            n,
            s,
            i,
            r,
            c,
            p,
            f
          );
          S && e.push(S);
        }
  }
}
function Nr(t, e, n, s, i, r, a, o) {
  const [c, l, u] = a, [p, f, S] = o, v = i || c !== p && J(c) && J(p);
  if (!v) {
    const z = l.name.value, re = f.name.value;
    if (z !== re)
      return [
        [r, `"${z}" and "${re}" are different fields`],
        [l],
        [f]
      ];
    if (!Vc(l, f))
      return [
        [r, "they have differing arguments"],
        [l],
        [f]
      ];
  }
  const A = u == null ? void 0 : u.type, O = S == null ? void 0 : S.type;
  if (A && O && hs(A, O))
    return [
      [
        r,
        `they return conflicting types "${_(A)}" and "${_(
          O
        )}"`
      ],
      [l],
      [f]
    ];
  const M = l.selectionSet, W = f.selectionSet;
  if (M && W) {
    const z = $c(
      t,
      e,
      n,
      s,
      v,
      ye(A),
      M,
      ye(O),
      W
    );
    return qc(z, r, l, f);
  }
}
function Vc(t, e) {
  const n = t.arguments, s = e.arguments;
  if (n === void 0 || n.length === 0)
    return s === void 0 || s.length === 0;
  if (s === void 0 || s.length === 0 || n.length !== s.length)
    return !1;
  const i = new Map(s.map(({ name: r, value: a }) => [r.value, a]));
  return n.every((r) => {
    const a = r.value, o = i.get(r.name.value);
    return o === void 0 ? !1 : gi(a) === gi(o);
  });
}
function gi(t) {
  return ie(Rs(t));
}
function hs(t, e) {
  return pe(t) ? pe(e) ? hs(t.ofType, e.ofType) : !0 : pe(e) ? !0 : G(t) ? G(e) ? hs(t.ofType, e.ofType) : !0 : G(e) ? !0 : Et(t) || Et(e) ? t !== e : !1;
}
function vn(t, e, n, s) {
  const i = e.get(s);
  if (i)
    return i;
  const r = /* @__PURE__ */ Object.create(null), a = /* @__PURE__ */ Object.create(null);
  Er(
    t,
    n,
    s,
    r,
    a
  );
  const o = [r, Object.keys(a)];
  return e.set(s, o), o;
}
function fs(t, e, n) {
  const s = e.get(n.selectionSet);
  if (s)
    return s;
  const i = Ie(t.getSchema(), n.typeCondition);
  return vn(
    t,
    e,
    i,
    n.selectionSet
  );
}
function Er(t, e, n, s, i) {
  for (const r of n.selections)
    switch (r.kind) {
      case d.FIELD: {
        const a = r.name.value;
        let o;
        (J(e) || X(e)) && (o = e.getFields()[a]);
        const c = r.alias ? r.alias.value : a;
        s[c] || (s[c] = []), s[c].push([e, r, o]);
        break;
      }
      case d.FRAGMENT_SPREAD:
        i[r.name.value] = !0;
        break;
      case d.INLINE_FRAGMENT: {
        const a = r.typeCondition, o = a ? Ie(t.getSchema(), a) : e;
        Er(
          t,
          o,
          r.selectionSet,
          s,
          i
        );
        break;
      }
    }
}
function qc(t, e, n, s) {
  if (t.length > 0)
    return [
      [e, t.map(([i]) => i)],
      [n, ...t.map(([, i]) => i).flat()],
      [s, ...t.map(([, , i]) => i).flat()]
    ];
}
class Or {
  constructor() {
    this._data = /* @__PURE__ */ new Map();
  }
  has(e, n, s) {
    var i;
    const r = (i = this._data.get(e)) === null || i === void 0 ? void 0 : i.get(n);
    return r === void 0 ? !1 : s ? !0 : s === r;
  }
  add(e, n, s) {
    const i = this._data.get(e);
    i === void 0 ? this._data.set(e, /* @__PURE__ */ new Map([[n, s]])) : i.set(n, s);
  }
}
class Gc {
  constructor() {
    this._orderedPairSet = new Or();
  }
  has(e, n, s) {
    return e < n ? this._orderedPairSet.has(e, n, s) : this._orderedPairSet.has(n, e, s);
  }
  add(e, n, s) {
    e < n ? this._orderedPairSet.add(e, n, s) : this._orderedPairSet.add(n, e, s);
  }
}
function Hc(t) {
  return {
    InlineFragment(e) {
      const n = t.getType(), s = t.getParentType();
      if (Xe(n) && Xe(s) && !oi(t.getSchema(), n, s)) {
        const i = _(s), r = _(n);
        t.reportError(
          new y(
            `Fragment cannot be spread here as objects of type "${i}" can never be of type "${r}".`,
            {
              nodes: e
            }
          )
        );
      }
    },
    FragmentSpread(e) {
      const n = e.name.value, s = Bc(t, n), i = t.getParentType();
      if (s && i && !oi(t.getSchema(), s, i)) {
        const r = _(i), a = _(s);
        t.reportError(
          new y(
            `Fragment "${n}" cannot be spread here as objects of type "${r}" can never be of type "${a}".`,
            {
              nodes: e
            }
          )
        );
      }
    }
  };
}
function Bc(t, e) {
  const n = t.getFragment(e);
  if (n) {
    const s = Ie(t.getSchema(), n.typeCondition);
    if (Xe(s))
      return s;
  }
}
function Qc(t) {
  return {
    // eslint-disable-next-line new-cap
    ...Yc(t),
    Field: {
      // Validate on leave to allow for deeper errors to appear first.
      leave(e) {
        var n;
        const s = t.getFieldDef();
        if (!s)
          return !1;
        const i = new Set(
          // FIXME: https://github.com/graphql/graphql-js/issues/2203
          /* c8 ignore next */
          (n = e.arguments) === null || n === void 0 ? void 0 : n.map((r) => r.name.value)
        );
        for (const r of s.args)
          if (!i.has(r.name) && Bt(r)) {
            const a = _(r.type);
            t.reportError(
              new y(
                `Field "${s.name}" argument "${r.name}" of type "${a}" is required, but it was not provided.`,
                {
                  nodes: e
                }
              )
            );
          }
      }
    }
  };
}
function Yc(t) {
  var e;
  const n = /* @__PURE__ */ Object.create(null), s = t.getSchema(), i = (e = s == null ? void 0 : s.getDirectives()) !== null && e !== void 0 ? e : Yt;
  for (const o of i)
    n[o.name] = Nt(
      o.args.filter(Bt),
      (c) => c.name
    );
  const r = t.getDocument().definitions;
  for (const o of r)
    if (o.kind === d.DIRECTIVE_DEFINITION) {
      var a;
      const c = (a = o.arguments) !== null && a !== void 0 ? a : [];
      n[o.name.value] = Nt(
        c.filter(Wc),
        (l) => l.name.value
      );
    }
  return {
    Directive: {
      // Validate on leave to allow for deeper errors to appear first.
      leave(o) {
        const c = o.name.value, l = n[c];
        if (l) {
          var u;
          const p = (u = o.arguments) !== null && u !== void 0 ? u : [], f = new Set(p.map((S) => S.name.value));
          for (const [S, v] of Object.entries(l))
            if (!f.has(S)) {
              const A = Es(v.type) ? _(v.type) : ie(v.type);
              t.reportError(
                new y(
                  `Directive "@${c}" argument "${S}" of type "${A}" is required, but it was not provided.`,
                  {
                    nodes: o
                  }
                )
              );
            }
        }
      }
    }
  };
}
function Wc(t) {
  return t.type.kind === d.NON_NULL_TYPE && t.defaultValue == null;
}
function zc(t) {
  return {
    Field(e) {
      const n = t.getType(), s = e.selectionSet;
      if (n)
        if (Et(ye(n))) {
          if (s) {
            const i = e.name.value, r = _(n);
            t.reportError(
              new y(
                `Field "${i}" must not have a selection since type "${r}" has no subfields.`,
                {
                  nodes: s
                }
              )
            );
          }
        } else if (s) {
          if (s.selections.length === 0) {
            const i = e.name.value, r = _(n);
            t.reportError(
              new y(
                `Field "${i}" of type "${r}" must have at least one field selected.`,
                {
                  nodes: e
                }
              )
            );
          }
        } else {
          const i = e.name.value, r = _(n);
          t.reportError(
            new y(
              `Field "${i}" of type "${r}" must have a selection of subfields. Did you mean "${i} { ... }"?`,
              {
                nodes: e
              }
            )
          );
        }
    }
  };
}
function _t(t, e, n) {
  if (t) {
    if (t.kind === d.VARIABLE) {
      const s = t.name.value;
      if (n == null || n[s] === void 0)
        return;
      const i = n[s];
      return i === null && G(e) ? void 0 : i;
    }
    if (G(e))
      return t.kind === d.NULL ? void 0 : _t(t, e.ofType, n);
    if (t.kind === d.NULL)
      return null;
    if (pe(e)) {
      const s = e.ofType;
      if (t.kind === d.LIST) {
        const r = [];
        for (const a of t.values)
          if (yi(a, n)) {
            if (G(s))
              return;
            r.push(null);
          } else {
            const o = _t(a, s, n);
            if (o === void 0)
              return;
            r.push(o);
          }
        return r;
      }
      const i = _t(t, s, n);
      return i === void 0 ? void 0 : [i];
    }
    if (ce(e)) {
      if (t.kind !== d.OBJECT)
        return;
      const s = /* @__PURE__ */ Object.create(null), i = Nt(t.fields, (r) => r.name.value);
      for (const r of Object.values(e.getFields())) {
        const a = i[r.name];
        if (!a || yi(a.value, n)) {
          if (r.defaultValue !== void 0)
            s[r.name] = r.defaultValue;
          else if (G(r.type))
            return;
          continue;
        }
        const o = _t(a.value, r.type, n);
        if (o === void 0)
          return;
        s[r.name] = o;
      }
      if (e.isOneOf) {
        const r = Object.keys(s);
        if (r.length !== 1 || s[r[0]] === null)
          return;
      }
      return s;
    }
    if (Et(e)) {
      let s;
      try {
        s = e.parseLiteral(t, n);
      } catch {
        return;
      }
      return s === void 0 ? void 0 : s;
    }
    ke(!1, "Unexpected input type: " + _(e));
  }
}
function yi(t, e) {
  return t.kind === d.VARIABLE && (e == null || e[t.name.value] === void 0);
}
function Jc(t, e, n) {
  var s;
  const i = {}, r = (s = e.arguments) !== null && s !== void 0 ? s : [], a = Nt(r, (o) => o.name.value);
  for (const o of t.args) {
    const c = o.name, l = o.type, u = a[c];
    if (!u) {
      if (o.defaultValue !== void 0)
        i[c] = o.defaultValue;
      else if (G(l))
        throw new y(
          `Argument "${c}" of required type "${_(l)}" was not provided.`,
          {
            nodes: e
          }
        );
      continue;
    }
    const p = u.value;
    let f = p.kind === d.NULL;
    if (p.kind === d.VARIABLE) {
      const v = p.name.value;
      if (n == null || !Zc(n, v)) {
        if (o.defaultValue !== void 0)
          i[c] = o.defaultValue;
        else if (G(l))
          throw new y(
            `Argument "${c}" of required type "${_(l)}" was provided the variable "$${v}" which was not provided a runtime value.`,
            {
              nodes: p
            }
          );
        continue;
      }
      f = n[v] == null;
    }
    if (f && G(l))
      throw new y(
        `Argument "${c}" of non-null type "${_(l)}" must not be null.`,
        {
          nodes: p
        }
      );
    const S = _t(p, l, n);
    if (S === void 0)
      throw new y(
        `Argument "${c}" has invalid value ${ie(p)}.`,
        {
          nodes: p
        }
      );
    i[c] = S;
  }
  return i;
}
function _i(t, e, n) {
  var s;
  const i = (s = e.directives) === null || s === void 0 ? void 0 : s.find(
    (r) => r.name.value === t.name
  );
  if (i)
    return Jc(t, i, n);
}
function Zc(t, e) {
  return Object.prototype.hasOwnProperty.call(t, e);
}
function Xc(t, e, n, s, i) {
  const r = /* @__PURE__ */ new Map();
  return ms(
    t,
    e,
    n,
    s,
    i,
    r,
    /* @__PURE__ */ new Set()
  ), r;
}
function ms(t, e, n, s, i, r, a) {
  for (const o of i.selections)
    switch (o.kind) {
      case d.FIELD: {
        if (!Bn(n, o))
          continue;
        const c = Kc(o), l = r.get(c);
        l !== void 0 ? l.push(o) : r.set(c, [o]);
        break;
      }
      case d.INLINE_FRAGMENT: {
        if (!Bn(n, o) || !vi(t, o, s))
          continue;
        ms(
          t,
          e,
          n,
          s,
          o.selectionSet,
          r,
          a
        );
        break;
      }
      case d.FRAGMENT_SPREAD: {
        const c = o.name.value;
        if (a.has(c) || !Bn(n, o))
          continue;
        a.add(c);
        const l = e[c];
        if (!l || !vi(t, l, s))
          continue;
        ms(
          t,
          e,
          n,
          s,
          l.selectionSet,
          r,
          a
        );
        break;
      }
    }
}
function Bn(t, e) {
  const n = _i(pr, e, t);
  if ((n == null ? void 0 : n.if) === !0)
    return !1;
  const s = _i(
    dr,
    e,
    t
  );
  return (s == null ? void 0 : s.if) !== !1;
}
function vi(t, e, n) {
  const s = e.typeCondition;
  if (!s)
    return !0;
  const i = Ie(t, s);
  return i === n ? !0 : at(i) ? t.isSubType(i, n) : !1;
}
function Kc(t) {
  return t.alias ? t.alias.value : t.name.value;
}
function el(t) {
  return {
    OperationDefinition(e) {
      if (e.operation === "subscription") {
        const n = t.getSchema(), s = n.getSubscriptionType();
        if (s) {
          const i = e.name ? e.name.value : null, r = /* @__PURE__ */ Object.create(null), a = t.getDocument(), o = /* @__PURE__ */ Object.create(null);
          for (const l of a.definitions)
            l.kind === d.FRAGMENT_DEFINITION && (o[l.name.value] = l);
          const c = Xc(
            n,
            o,
            r,
            s,
            e.selectionSet
          );
          if (c.size > 1) {
            const p = [...c.values()].slice(1).flat();
            t.reportError(
              new y(
                i != null ? `Subscription "${i}" must select only one top level field.` : "Anonymous Subscription must select only one top level field.",
                {
                  nodes: p
                }
              )
            );
          }
          for (const l of c.values())
            l[0].name.value.startsWith("__") && t.reportError(
              new y(
                i != null ? `Subscription "${i}" must not select an introspection top level field.` : "Anonymous Subscription must not select an introspection top level field.",
                {
                  nodes: l
                }
              )
            );
        }
      }
    }
  };
}
function wr(t, e) {
  const n = /* @__PURE__ */ new Map();
  for (const s of t) {
    const i = e(s), r = n.get(i);
    r === void 0 ? n.set(i, [s]) : r.push(s);
  }
  return n;
}
function tl(t) {
  return {
    Field: e,
    Directive: e
  };
  function e(n) {
    var s;
    const i = (s = n.arguments) !== null && s !== void 0 ? s : [], r = wr(i, (a) => a.name.value);
    for (const [a, o] of r)
      o.length > 1 && t.reportError(
        new y(
          `There can be only one argument named "${a}".`,
          {
            nodes: o.map((c) => c.name)
          }
        )
      );
  }
}
function nl(t) {
  const e = /* @__PURE__ */ Object.create(null), n = t.getSchema(), s = n ? n.getDirectives() : Yt;
  for (const o of s)
    e[o.name] = !o.isRepeatable;
  const i = t.getDocument().definitions;
  for (const o of i)
    o.kind === d.DIRECTIVE_DEFINITION && (e[o.name.value] = !o.repeatable);
  const r = /* @__PURE__ */ Object.create(null), a = /* @__PURE__ */ Object.create(null);
  return {
    // Many different AST nodes may contain directives. Rather than listing
    // them all, just listen for entering any node, and check to see if it
    // defines any directives.
    enter(o) {
      if (!("directives" in o) || !o.directives)
        return;
      let c;
      if (o.kind === d.SCHEMA_DEFINITION || o.kind === d.SCHEMA_EXTENSION)
        c = r;
      else if (ks(o) || br(o)) {
        const l = o.name.value;
        c = a[l], c === void 0 && (a[l] = c = /* @__PURE__ */ Object.create(null));
      } else
        c = /* @__PURE__ */ Object.create(null);
      for (const l of o.directives) {
        const u = l.name.value;
        e[u] && (c[u] ? t.reportError(
          new y(
            `The directive "@${u}" can only be used once at this location.`,
            {
              nodes: [c[u], l]
            }
          )
        ) : c[u] = l);
      }
    }
  };
}
function sl(t) {
  const e = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: () => !1,
    FragmentDefinition(n) {
      const s = n.name.value;
      return e[s] ? t.reportError(
        new y(
          `There can be only one fragment named "${s}".`,
          {
            nodes: [e[s], n.name]
          }
        )
      ) : e[s] = n.name, !1;
    }
  };
}
function il(t) {
  const e = [];
  let n = /* @__PURE__ */ Object.create(null);
  return {
    ObjectValue: {
      enter() {
        e.push(n), n = /* @__PURE__ */ Object.create(null);
      },
      leave() {
        const s = e.pop();
        s || ke(!1), n = s;
      }
    },
    ObjectField(s) {
      const i = s.name.value;
      n[i] ? t.reportError(
        new y(
          `There can be only one input field named "${i}".`,
          {
            nodes: [n[i], s.name]
          }
        )
      ) : n[i] = s.name;
    }
  };
}
function rl(t) {
  const e = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition(n) {
      const s = n.name;
      return s && (e[s.value] ? t.reportError(
        new y(
          `There can be only one operation named "${s.value}".`,
          {
            nodes: [
              e[s.value],
              s
            ]
          }
        )
      ) : e[s.value] = s), !1;
    },
    FragmentDefinition: () => !1
  };
}
function al(t) {
  return {
    OperationDefinition(e) {
      var n;
      const s = (n = e.variableDefinitions) !== null && n !== void 0 ? n : [], i = wr(
        s,
        (r) => r.variable.name.value
      );
      for (const [r, a] of i)
        a.length > 1 && t.reportError(
          new y(
            `There can be only one variable named "$${r}".`,
            {
              nodes: a.map((o) => o.variable.name)
            }
          )
        );
    }
  };
}
function ol(t) {
  let e = {};
  return {
    OperationDefinition: {
      enter() {
        e = {};
      }
    },
    VariableDefinition(n) {
      e[n.variable.name.value] = n;
    },
    ListValue(n) {
      const s = Ji(t.getParentInputType());
      if (!pe(s))
        return nt(t, n), !1;
    },
    ObjectValue(n) {
      const s = ye(t.getInputType());
      if (!ce(s))
        return nt(t, n), !1;
      const i = Nt(n.fields, (r) => r.name.value);
      for (const r of Object.values(s.getFields()))
        if (!i[r.name] && or(r)) {
          const o = _(r.type);
          t.reportError(
            new y(
              `Field "${s.name}.${r.name}" of required type "${o}" was not provided.`,
              {
                nodes: n
              }
            )
          );
        }
      s.isOneOf && cl(
        t,
        n,
        s,
        i,
        e
      );
    },
    ObjectField(n) {
      const s = ye(t.getParentInputType());
      if (!t.getInputType() && ce(s)) {
        const r = Ct(
          n.name.value,
          Object.keys(s.getFields())
        );
        t.reportError(
          new y(
            `Field "${n.name.value}" is not defined by type "${s.name}".` + pt(r),
            {
              nodes: n
            }
          )
        );
      }
    },
    NullValue(n) {
      const s = t.getInputType();
      G(s) && t.reportError(
        new y(
          `Expected value of type "${_(s)}", found ${ie(n)}.`,
          {
            nodes: n
          }
        )
      );
    },
    EnumValue: (n) => nt(t, n),
    IntValue: (n) => nt(t, n),
    FloatValue: (n) => nt(t, n),
    StringValue: (n) => nt(t, n),
    BooleanValue: (n) => nt(t, n)
  };
}
function nt(t, e) {
  const n = t.getInputType();
  if (!n)
    return;
  const s = ye(n);
  if (!Et(s)) {
    const i = _(n);
    t.reportError(
      new y(
        `Expected value of type "${i}", found ${ie(e)}.`,
        {
          nodes: e
        }
      )
    );
    return;
  }
  try {
    if (s.parseLiteral(
      e,
      void 0
      /* variables */
    ) === void 0) {
      const r = _(n);
      t.reportError(
        new y(
          `Expected value of type "${r}", found ${ie(e)}.`,
          {
            nodes: e
          }
        )
      );
    }
  } catch (i) {
    const r = _(n);
    i instanceof y ? t.reportError(i) : t.reportError(
      new y(
        `Expected value of type "${r}", found ${ie(e)}; ` + i.message,
        {
          nodes: e,
          originalError: i
        }
      )
    );
  }
}
function cl(t, e, n, s, i) {
  var r;
  const a = Object.keys(s);
  if (a.length !== 1) {
    t.reportError(
      new y(
        `OneOf Input Object "${n.name}" must specify exactly one key.`,
        {
          nodes: [e]
        }
      )
    );
    return;
  }
  const c = (r = s[a[0]]) === null || r === void 0 ? void 0 : r.value, l = !c || c.kind === d.NULL, u = (c == null ? void 0 : c.kind) === d.VARIABLE;
  if (l) {
    t.reportError(
      new y(`Field "${n.name}.${a[0]}" must be non-null.`, {
        nodes: [e]
      })
    );
    return;
  }
  if (u) {
    const p = c.name.value;
    i[p].type.kind !== d.NON_NULL_TYPE && t.reportError(
      new y(
        `Variable "${p}" must be non-nullable to be used for OneOf Input Object "${n.name}".`,
        {
          nodes: [e]
        }
      )
    );
  }
}
function ll(t) {
  return {
    VariableDefinition(e) {
      const n = Ie(t.getSchema(), e.type);
      if (n !== void 0 && !xe(n)) {
        const s = e.variable.name.value, i = ie(e.type);
        t.reportError(
          new y(
            `Variable "$${s}" cannot be non-input type "${i}".`,
            {
              nodes: e.type
            }
          )
        );
      }
    }
  };
}
function ul(t) {
  let e = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: {
      enter() {
        e = /* @__PURE__ */ Object.create(null);
      },
      leave(n) {
        const s = t.getRecursiveVariableUsages(n);
        for (const { node: i, type: r, defaultValue: a, parentType: o } of s) {
          const c = i.name.value, l = e[c];
          if (l && r) {
            const u = t.getSchema(), p = Ie(u, l.type);
            if (p && !dl(
              u,
              p,
              l.defaultValue,
              r,
              a
            )) {
              const f = _(p), S = _(r);
              t.reportError(
                new y(
                  `Variable "$${c}" of type "${f}" used in position expecting type "${S}".`,
                  {
                    nodes: [l, i]
                  }
                )
              );
            }
            ce(o) && o.isOneOf && ws(p) && t.reportError(
              new y(
                `Variable "$${c}" is of type "${p}" but must be non-nullable to be used for OneOf Input Object "${o}".`,
                {
                  nodes: [l, i]
                }
              )
            );
          }
        }
      }
    },
    VariableDefinition(n) {
      e[n.variable.name.value] = n;
    }
  };
}
function dl(t, e, n, s, i) {
  if (G(s) && !G(e)) {
    if (!(n != null && n.kind !== d.NULL) && !(i !== void 0))
      return !1;
    const o = s.ofType;
    return It(t, e, o);
  }
  return It(t, e, s);
}
const pl = Object.freeze([kc]), hl = Object.freeze([
  gc,
  rl,
  xc,
  el,
  wc,
  Tc,
  ll,
  zc,
  yc,
  sl,
  Oc,
  Dc,
  Hc,
  Rc,
  al,
  Lc,
  Pc,
  Sc,
  nl,
  Ic,
  tl,
  ol,
  Qc,
  ul,
  Uc,
  il,
  ...pl
]);
class fl {
  constructor(e, n) {
    this._ast = e, this._fragments = void 0, this._fragmentSpreads = /* @__PURE__ */ new Map(), this._recursivelyReferencedFragments = /* @__PURE__ */ new Map(), this._onError = n;
  }
  get [Symbol.toStringTag]() {
    return "ASTValidationContext";
  }
  reportError(e) {
    this._onError(e);
  }
  getDocument() {
    return this._ast;
  }
  getFragment(e) {
    let n;
    if (this._fragments)
      n = this._fragments;
    else {
      n = /* @__PURE__ */ Object.create(null);
      for (const s of this.getDocument().definitions)
        s.kind === d.FRAGMENT_DEFINITION && (n[s.name.value] = s);
      this._fragments = n;
    }
    return n[e];
  }
  getFragmentSpreads(e) {
    let n = this._fragmentSpreads.get(e);
    if (!n) {
      n = [];
      const s = [e];
      let i;
      for (; i = s.pop(); )
        for (const r of i.selections)
          r.kind === d.FRAGMENT_SPREAD ? n.push(r) : r.selectionSet && s.push(r.selectionSet);
      this._fragmentSpreads.set(e, n);
    }
    return n;
  }
  getRecursivelyReferencedFragments(e) {
    let n = this._recursivelyReferencedFragments.get(e);
    if (!n) {
      n = [];
      const s = /* @__PURE__ */ Object.create(null), i = [e.selectionSet];
      let r;
      for (; r = i.pop(); )
        for (const a of this.getFragmentSpreads(r)) {
          const o = a.name.value;
          if (s[o] !== !0) {
            s[o] = !0;
            const c = this.getFragment(o);
            c && (n.push(c), i.push(c.selectionSet));
          }
        }
      this._recursivelyReferencedFragments.set(e, n);
    }
    return n;
  }
}
class ml extends fl {
  constructor(e, n, s, i) {
    super(n, i), this._schema = e, this._typeInfo = s, this._variableUsages = /* @__PURE__ */ new Map(), this._recursiveVariableUsages = /* @__PURE__ */ new Map();
  }
  get [Symbol.toStringTag]() {
    return "ValidationContext";
  }
  getSchema() {
    return this._schema;
  }
  getVariableUsages(e) {
    let n = this._variableUsages.get(e);
    if (!n) {
      const s = [], i = new Tr(this._schema);
      Ns(
        e,
        Ir(i, {
          VariableDefinition: () => !1,
          Variable(r) {
            s.push({
              node: r,
              type: i.getInputType(),
              defaultValue: i.getDefaultValue(),
              parentType: i.getParentInputType()
            });
          }
        })
      ), n = s, this._variableUsages.set(e, n);
    }
    return n;
  }
  getRecursiveVariableUsages(e) {
    let n = this._recursiveVariableUsages.get(e);
    if (!n) {
      n = this.getVariableUsages(e);
      for (const s of this.getRecursivelyReferencedFragments(e))
        n = n.concat(this.getVariableUsages(s));
      this._recursiveVariableUsages.set(e, n);
    }
    return n;
  }
  getType() {
    return this._typeInfo.getType();
  }
  getParentType() {
    return this._typeInfo.getParentType();
  }
  getInputType() {
    return this._typeInfo.getInputType();
  }
  getParentInputType() {
    return this._typeInfo.getParentInputType();
  }
  getFieldDef() {
    return this._typeInfo.getFieldDef();
  }
  getDirective() {
    return this._typeInfo.getDirective();
  }
  getArgument() {
    return this._typeInfo.getArgument();
  }
  getEnumValue() {
    return this._typeInfo.getEnumValue();
  }
}
function Ar(t, e, n = hl, s, i = new Tr(t)) {
  var r;
  const a = (r = void 0) !== null && r !== void 0 ? r : 100;
  e || V(!1, "Must provide document."), ec(t);
  const o = Object.freeze({}), c = [], l = new ml(
    t,
    e,
    i,
    (p) => {
      if (c.length >= a)
        throw c.push(
          new y(
            "Too many validation errors, error limit reached. Validation aborted."
          )
        ), o;
      c.push(p);
    }
  ), u = Do(n.map((p) => p(l)));
  try {
    Ns(e, Ir(i, u));
  } catch (p) {
    if (p !== o)
      throw p;
  }
  return c;
}
function gl(t) {
  return {
    Field(e) {
      const n = t.getFieldDef(), s = n == null ? void 0 : n.deprecationReason;
      if (n && s != null) {
        const i = t.getParentType();
        i != null || ke(!1), t.reportError(
          new y(
            `The field ${i.name}.${n.name} is deprecated. ${s}`,
            {
              nodes: e
            }
          )
        );
      }
    },
    Argument(e) {
      const n = t.getArgument(), s = n == null ? void 0 : n.deprecationReason;
      if (n && s != null) {
        const i = t.getDirective();
        if (i != null)
          t.reportError(
            new y(
              `Directive "@${i.name}" argument "${n.name}" is deprecated. ${s}`,
              {
                nodes: e
              }
            )
          );
        else {
          const r = t.getParentType(), a = t.getFieldDef();
          r != null && a != null || ke(!1), t.reportError(
            new y(
              `Field "${r.name}.${a.name}" argument "${n.name}" is deprecated. ${s}`,
              {
                nodes: e
              }
            )
          );
        }
      }
    },
    ObjectField(e) {
      const n = ye(t.getParentInputType());
      if (ce(n)) {
        const s = n.getFields()[e.name.value], i = s == null ? void 0 : s.deprecationReason;
        i != null && t.reportError(
          new y(
            `The input field ${n.name}.${s.name} is deprecated. ${i}`,
            {
              nodes: e
            }
          )
        );
      }
    },
    EnumValue(e) {
      const n = t.getEnumValue(), s = n == null ? void 0 : n.deprecationReason;
      if (n && s != null) {
        const i = ye(t.getInputType());
        i != null || ke(!1), t.reportError(
          new y(
            `The enum value "${i.name}.${n.name}" is deprecated. ${s}`,
            {
              nodes: e
            }
          )
        );
      }
    }
  };
}
function yl(t, e) {
  Qe(t) && Qe(t.__schema) || V(
    !1,
    `Invalid or incomplete introspection result. Ensure that you are passing "data" property of introspection response and no "errors" was returned alongside: ${_(
      t
    )}.`
  );
  const n = t.__schema, s = it(
    n.types,
    (h) => h.name,
    (h) => f(h)
  );
  for (const h of [...lr, ...xs])
    s[h.name] && (s[h.name] = h);
  const i = n.queryType ? u(n.queryType) : null, r = n.mutationType ? u(n.mutationType) : null, a = n.subscriptionType ? u(n.subscriptionType) : null, o = n.directives ? n.directives.map(Wt) : [];
  return new vr({
    description: n.description,
    query: i,
    mutation: r,
    subscription: a,
    types: Object.values(s),
    directives: o,
    assumeValid: void 0
  });
  function c(h) {
    if (h.kind === Q.LIST) {
      const b = h.ofType;
      if (!b)
        throw new Error("Decorated type deeper than introspection query.");
      return new ge(c(b));
    }
    if (h.kind === Q.NON_NULL) {
      const b = h.ofType;
      if (!b)
        throw new Error("Decorated type deeper than introspection query.");
      const H = c(b);
      return new F(jo(H));
    }
    return l(h);
  }
  function l(h) {
    const b = h.name;
    if (!b)
      throw new Error(`Unknown type reference: ${_(h)}.`);
    const H = s[b];
    if (!H)
      throw new Error(
        `Invalid or incomplete schema, unknown type: ${b}. Ensure that a full introspection query is used in order to build a client schema.`
      );
    return H;
  }
  function u(h) {
    return Mo(l(h));
  }
  function p(h) {
    return $o(l(h));
  }
  function f(h) {
    if (h != null && h.name != null && h.kind != null)
      switch (h.kind) {
        case Q.SCALAR:
          return S(h);
        case Q.OBJECT:
          return A(h);
        case Q.INTERFACE:
          return O(h);
        case Q.UNION:
          return M(h);
        case Q.ENUM:
          return W(h);
        case Q.INPUT_OBJECT:
          return z(h);
      }
    const b = _(h);
    throw new Error(
      `Invalid or incomplete introspection result. Ensure that a full introspection query is used in order to build a client schema: ${b}.`
    );
  }
  function S(h) {
    return new ft({
      name: h.name,
      description: h.description,
      specifiedByURL: h.specifiedByURL
    });
  }
  function v(h) {
    if (h.interfaces === null && h.kind === Q.INTERFACE)
      return [];
    if (!h.interfaces) {
      const b = _(h);
      throw new Error(
        `Introspection result missing interfaces: ${b}.`
      );
    }
    return h.interfaces.map(p);
  }
  function A(h) {
    return new et({
      name: h.name,
      description: h.description,
      interfaces: () => v(h),
      fields: () => re(h)
    });
  }
  function O(h) {
    return new ir({
      name: h.name,
      description: h.description,
      interfaces: () => v(h),
      fields: () => re(h)
    });
  }
  function M(h) {
    if (!h.possibleTypes) {
      const b = _(h);
      throw new Error(
        `Introspection result missing possibleTypes: ${b}.`
      );
    }
    return new rr({
      name: h.name,
      description: h.description,
      types: () => h.possibleTypes.map(u)
    });
  }
  function W(h) {
    if (!h.enumValues) {
      const b = _(h);
      throw new Error(
        `Introspection result missing enumValues: ${b}.`
      );
    }
    return new xn({
      name: h.name,
      description: h.description,
      values: it(
        h.enumValues,
        (b) => b.name,
        (b) => ({
          description: b.description,
          deprecationReason: b.deprecationReason
        })
      )
    });
  }
  function z(h) {
    if (!h.inputFields) {
      const b = _(h);
      throw new Error(
        `Introspection result missing inputFields: ${b}.`
      );
    }
    return new ar({
      name: h.name,
      description: h.description,
      fields: () => $e(h.inputFields),
      isOneOf: h.isOneOf
    });
  }
  function re(h) {
    if (!h.fields)
      throw new Error(
        `Introspection result missing fields: ${_(h)}.`
      );
    return it(
      h.fields,
      (b) => b.name,
      We
    );
  }
  function We(h) {
    const b = c(h.type);
    if (!vt(b)) {
      const H = _(b);
      throw new Error(
        `Introspection must provide output type for fields, but received: ${H}.`
      );
    }
    if (!h.args) {
      const H = _(h);
      throw new Error(
        `Introspection result missing field args: ${H}.`
      );
    }
    return {
      description: h.description,
      deprecationReason: h.deprecationReason,
      type: b,
      args: $e(h.args)
    };
  }
  function $e(h) {
    return it(
      h,
      (b) => b.name,
      tt
    );
  }
  function tt(h) {
    const b = c(h.type);
    if (!xe(b)) {
      const Ne = _(b);
      throw new Error(
        `Introspection must provide input type for arguments, but received: ${Ne}.`
      );
    }
    const H = h.defaultValue != null ? _t(Oo(h.defaultValue), b) : void 0;
    return {
      description: h.description,
      type: b,
      defaultValue: H,
      deprecationReason: h.deprecationReason
    };
  }
  function Wt(h) {
    if (!h.args) {
      const b = _(h);
      throw new Error(
        `Introspection result missing directive args: ${b}.`
      );
    }
    if (!h.locations) {
      const b = _(h);
      throw new Error(
        `Introspection result missing directive locations: ${b}.`
      );
    }
    return new mt({
      name: h.name,
      description: h.description,
      isRepeatable: h.isRepeatable,
      locations: h.locations.slice(),
      args: $e(h.args)
    });
  }
}
function ue(t, e, n) {
  const s = t.items.find((i) => n ? i.type === "field" && i.typeName === e && i.fieldName === n : i.type === "type" && i.typeName === e);
  return (s == null ? void 0 : s.offlineScopes) || [];
}
function _l(t, e, n) {
  const s = t.items.find(
    (i) => i.type === "field" && i.typeName === e && i.fieldName === n
  );
  return s == null ? void 0 : s.returnType;
}
async function vl(t, e, n = "admin") {
  const s = /* @__PURE__ */ new Set(), i = new Map(
    t.definitions.filter(
      (r) => r.kind === d.FRAGMENT_DEFINITION
    ).map((r) => [r.name.value, r])
  );
  for (const r of t.definitions)
    if (r.kind === d.OPERATION_DEFINITION) {
      const a = r;
      if (a.selectionSet) {
        const o = Sl(
          a.operation,
          n
        );
        ue(e, o).forEach((l) => s.add(l)), xr(
          a.selectionSet,
          o,
          e,
          s,
          i
        );
      }
    }
  return Array.from(s);
}
function Tl(t, e, n, s) {
  const i = t.name.value;
  if (ue(n, e, i).forEach((o) => s.add(o)), !t.selectionSet)
    return { nextSelectionSet: null, nextTypeName: null };
  const a = _l(n, e, i);
  return a && ue(n, a).forEach((c) => s.add(c)), {
    nextSelectionSet: t.selectionSet,
    nextTypeName: a || null
  };
}
function Il(t, e, n, s, i) {
  const r = t.name.value;
  if (n.has(r))
    return { nextSelectionSet: null, nextTypeName: null };
  n.add(r);
  const a = e.get(r);
  if (!(a != null && a.selectionSet))
    return { nextSelectionSet: null, nextTypeName: null };
  const o = a.typeCondition.name.value;
  return ue(s, o).forEach((l) => i.add(l)), {
    nextSelectionSet: a.selectionSet,
    nextTypeName: o
  };
}
function bl(t, e, n, s) {
  var a;
  if (!t.selectionSet)
    return { nextSelectionSet: null, nextTypeName: null };
  const i = ((a = t.typeCondition) == null ? void 0 : a.name.value) || e;
  return ue(n, i).forEach((o) => s.add(o)), {
    nextSelectionSet: t.selectionSet,
    nextTypeName: i
  };
}
function xr(t, e, n, s, i, r = /* @__PURE__ */ new Set()) {
  for (const a of t.selections) {
    let o;
    if (a.kind === d.FIELD)
      o = Tl(
        a,
        e,
        n,
        s
      );
    else if (a.kind === d.FRAGMENT_SPREAD)
      o = Il(
        a,
        i,
        r,
        n,
        s
      );
    else if (a.kind === d.INLINE_FRAGMENT)
      o = bl(
        a,
        e,
        n,
        s
      );
    else
      continue;
    o.nextSelectionSet && o.nextTypeName && xr(
      o.nextSelectionSet,
      o.nextTypeName,
      n,
      s,
      i,
      r
    );
  }
}
function Sl(t, e = "admin") {
  return e === "admin" ? t === "mutation" ? "Mutation" : "QueryRoot" : t === "mutation" ? "Mutation" : "Query";
}
function Cr(t) {
  return !t || t.length === 0 ? "" : `
Required scopes: ${t.join(", ")}`;
}
const Ve = $.dirname(Nn(import.meta.url));
let nn = Ve;
const kr = Ve.split($.sep), Ti = kr.lastIndexOf("shopify-dev-tools");
Ti !== -1 && (nn = kr.slice(0, Ti + 1).join($.sep));
let gs, Tn;
Ve.includes("dev-mcp") && Ve.includes("dist") && !Ve.includes("shopify-dev-tools") ? (gs = $.join(Ve, "data"), Tn = $.join(Ve, "data/zod_schemas")) : (gs = Ve.includes("/dist") || Ve.includes("\\dist") ? $.join(nn, "dist", "data") : $.join(nn, "src", "data"), Tn = $.join(nn, "dist/data/zod_schemas"));
function Nl(t) {
  const e = $.join(gs, "latest-releases-schemas.json"), s = JSON.parse(
    Xn(e, "utf-8")
  )[t];
  let i, r;
  if (!s || s.length === 0)
    i = `${t}.js`, r = $.join(Tn, i);
  else {
    const a = s.find((o) => o.latestVersion);
    if (!a)
      throw new Error(`No latest version found for API "${t}"`);
    i = `${t}_${a.name}.js`, r = $.join(Tn, i);
  }
  try {
    return Xn(r, "utf-8");
  } catch (a) {
    throw new Error(
      `Failed to load Zod schema file ${i}: ${a instanceof Error ? a.message : String(a)}`
    );
  }
}
function El(t) {
  try {
    return `

## Zod Schema

\`\`\`javascript
${Nl(t)}
\`\`\``;
  } catch {
    return `Failed to load Zod schema for ${t}`;
  }
}
const gt = 50, Ii = 5e3, Dt = 80, Qn = 4;
class Ol {
  constructor() {
    zt(this, "descriptions", /* @__PURE__ */ new Map());
    zt(this, "structuralTokens", 0);
  }
  /**
   * Add structural tokens (field names, types, formatting)
   */
  addStructuralTokens(e) {
    this.structuralTokens += Math.ceil(e / Qn);
  }
  /**
   * Register a description for potential truncation
   */
  registerDescription(e, n) {
    if (!n) return "";
    const s = n.replace(/\n/g, " ").trim();
    return this.descriptions.set(e, {
      original: s,
      truncated: s,
      maxLength: s.length
    }), e;
  }
  /**
   * Apply smart truncation based on token limits
   */
  applySmartTruncation() {
    const e = Array.from(this.descriptions.values()).reduce(
      (o, c) => o + Math.ceil(c.original.length / Qn),
      0
    ), n = this.structuralTokens + e;
    if (n <= Ii)
      return;
    const i = (n - Ii) * Qn, r = Array.from(this.descriptions.entries()).sort(
      (o, c) => c[1].original.length - o[1].original.length
    );
    let a = 0;
    for (const [o, c] of r) {
      if (a >= i) break;
      const l = c.original.length;
      let u = l;
      l > 500 ? u = Math.max(
        Dt,
        Math.floor(l * 0.3)
      ) : l > 300 ? u = Math.max(
        Dt,
        Math.floor(l * 0.5)
      ) : l > 150 && (u = Math.max(
        Dt,
        Math.floor(l * 0.7)
      )), u < l && (c.maxLength = u, a += l - u);
    }
    for (; a < i; ) {
      const o = Array.from(this.descriptions.values()).filter((p) => p.maxLength > Dt).sort((p, f) => f.maxLength - p.maxLength);
      if (o.length === 0) break;
      const c = o[0], l = Math.floor(c.maxLength * 0.1), u = Math.max(
        Dt,
        c.maxLength - l
      );
      a += c.maxLength - u, c.maxLength = u;
    }
    for (const [o, c] of this.descriptions.entries())
      c.maxLength < c.original.length && (c.truncated = c.original.substring(0, c.maxLength) + "...");
  }
  /**
   * Get the final truncated description
   */
  getDescription(e) {
    const n = this.descriptions.get(e);
    return n ? n.truncated : "";
  }
  /**
   * Check if any truncation occurred
   */
  hadTruncation() {
    return Array.from(this.descriptions.values()).some(
      (e) => e.truncated !== e.original
    );
  }
}
const Ke = (t) => t ? t.kind === "NON_NULL" ? `${Ke(t.ofType)}!` : t.kind === "LIST" ? `[${Ke(t.ofType)}]` : t.name : "null", Rr = (t) => `${t.name}: ${Ke(t.type)}${t.defaultValue !== null ? ` = ${t.defaultValue}` : ""}`, Lr = (t) => t ? t.filter((e) => !e.isDeprecated && !e.deprecationReason) : [], In = (t) => t ? t.filter(
  (e) => !e.isDeprecated && !e.deprecationReason
) : [], bi = (t, e, n = "") => {
  let s = `    ${t.name}`;
  const i = Lr(t.args);
  if (i && i.length > 0 && (s += `(${i.map(Rr).join(", ")})`), s += `: ${Ke(t.type)}`, t.description)
    if (e) {
      const r = `${n}_field_${t.name}`;
      e.registerDescription(r, t.description), s += `
      # {{${r}}}`;
    } else {
      const r = t.description.replace(/\n/g, " ").trim();
      s += `
      # ${r}`;
    }
  return s;
}, wl = (t, e) => {
  let n = `${t.kind} ${t.name}`;
  if (t.description)
    if (e) {
      const s = `type_${t.name}_desc`;
      e.registerDescription(s, t.description), n += `
  Description: {{${s}}}`;
    } else {
      const s = t.description.replace(/\n/g, " ");
      n += `
  Description: ${s}`;
    }
  if (t.interfaces && t.interfaces.length > 0 && (n += `
  Implements: ${t.interfaces.map((s) => s.name).join(", ")}`), t.kind === "INPUT_OBJECT" && t.inputFields && t.inputFields.length > 0) {
    const s = In(t.inputFields);
    if (s.length > 0) {
      n += `

  Input Fields:`;
      const i = s.slice(0, gt);
      for (const r of i)
        n += `

${bi(r, e, `type_${t.name}`)}`;
      s.length > gt && (n += `

    ... and ${s.length - gt} more input fields`);
    }
  } else if (t.fields && t.fields.length > 0) {
    const s = In(t.fields);
    if (s.length > 0) {
      n += `

  Fields:`;
      const i = s.slice(0, gt);
      for (const r of i)
        n += `

${bi(r, e, `type_${t.name}`)}`;
      s.length > gt && (n += `

    ... and ${s.length - gt} more fields`);
    }
  }
  return n;
}, Si = (t, e, n, s) => {
  let i = `${t.name}`;
  if (t.description)
    if (s) {
      const o = `op_${e}_${t.name}_desc`;
      s.registerDescription(o, t.description), i += `
  Description: {{${o}}}`;
    } else {
      const o = t.description.replace(/\n/g, " ");
      i += `
  Description: ${o}`;
    }
  const r = Lr(t.args);
  if (r && r.length > 0) {
    i += `
  Arguments:`;
    for (const o of r)
      i += `
    ${Rr(o)}`;
  }
  i += `
  Returns: ${Ke(t.type)}`;
  let a = ue(n, e, t.name);
  if (!a || a.length === 0) {
    let o = Ke(t.type);
    if (o = o.replace(/[\[\]!]/g, "").trim(), a = ue(n, o), (!a || a.length === 0) && o.endsWith("Connection")) {
      const c = o.replace(/Connection$/, "");
      a = ue(n, c);
    }
  }
  return i;
}, Yn = (t, e, n) => {
  const s = t.filter(
    (i) => {
      var r;
      return (r = i.name) == null ? void 0 : r.toLowerCase().includes(e);
    }
  );
  return s.sort((i, r) => i.name ? r.name ? i.name.length - r.name.length : -1 : 1), {
    wasTruncated: s.length > n,
    items: s.slice(0, n)
  };
};
async function Al(t, e, n = {}) {
  var z, re, We, $e, tt, Wt;
  const { schemaOptions: s, filter: i = ["all"] } = n, r = new Ol(), a = En([e], s), o = await Mi(a[0]), c = JSON.parse(o), l = c.offline_scopes || { items: [] }, u = ((z = c.data.__schema.queryType) == null ? void 0 : z.name) || "QueryRoot", p = ((re = c.data.__schema.mutationType) == null ? void 0 : re.name) || "Mutation";
  let f = c, S = !1, v = !1, A = !1;
  if (t && t.trim()) {
    let h = t.trim();
    h.endsWith("s") && (h = h.slice(0, -1)), h = h.replace(/\s+/g, ""), console.error(
      `[introspect-graphql-schema] Filtering schema with query: ${t} (normalized: ${h})`
    );
    const b = h.toLowerCase();
    if (($e = (We = c == null ? void 0 : c.data) == null ? void 0 : We.__schema) != null && $e.types) {
      let Ne = [];
      if (i.includes("all") || i.includes("types")) {
        const De = Yn(
          c.data.__schema.types,
          b,
          10
        );
        S = De.wasTruncated, Ne = De.items;
      }
      const Fn = c.data.__schema.types.find(
        (De) => De.name === u
      ), Un = c.data.__schema.types.find(
        (De) => De.name === p
      );
      let $s = [];
      if (Fn && Fn.fields && (i.includes("all") || i.includes("queries"))) {
        const De = In(Fn.fields), Lt = Yn(
          De,
          b,
          10
        );
        v = Lt.wasTruncated, $s = Lt.items;
      }
      let js = [];
      if (Un && Un.fields && (i.includes("all") || i.includes("mutations"))) {
        const De = In(Un.fields), Lt = Yn(
          De,
          b,
          10
        );
        A = Lt.wasTruncated, js = Lt.items;
      }
      f = {
        data: {
          __schema: {
            ...c.data.__schema,
            types: Ne,
            matchingQueries: $s,
            matchingMutations: js
          }
        }
      };
    }
  }
  const O = {
    types: [],
    typesWereTruncated: S,
    queries: [],
    queriesWereTruncated: v,
    mutations: [],
    mutationsWereTruncated: A
  };
  (i.includes("all") || i.includes("types")) && f.data.__schema.types.length > 0 && (O.types = f.data.__schema.types.map((h) => ({
    name: h.name,
    description: h.description,
    details: wl(h, r),
    scopes: ue(l, h.name)
  }))), (i.includes("all") || i.includes("queries")) && ((tt = f.data.__schema.matchingQueries) == null ? void 0 : tt.length) > 0 && (O.queries = f.data.__schema.matchingQueries.map(
    (h) => {
      let b = ue(l, u, h.name);
      if (!b || b.length === 0) {
        let H = Ke(h.type);
        if (H = H.replace(/[\[\]!]/g, "").trim(), b = ue(l, H), (!b || b.length === 0) && H.endsWith("Connection")) {
          const Ne = H.replace(/Connection$/, "");
          b = ue(l, Ne);
        }
      }
      return {
        name: h.name,
        description: h.description,
        details: Si(
          h,
          u,
          l,
          r
        ),
        scopes: b || []
      };
    }
  )), (i.includes("all") || i.includes("mutations")) && ((Wt = f.data.__schema.matchingMutations) == null ? void 0 : Wt.length) > 0 && (O.mutations = f.data.__schema.matchingMutations.map(
    (h) => {
      let b = ue(
        l,
        p,
        h.name
      );
      if (!b || b.length === 0) {
        let H = Ke(h.type);
        if (H = H.replace(/[\[\]!]/g, "").trim(), b = ue(l, H), (!b || b.length === 0) && H.endsWith("Connection")) {
          const Ne = H.replace(/Connection$/, "");
          b = ue(l, Ne);
        }
      }
      return {
        name: h.name,
        description: h.description,
        details: Si(
          h,
          p,
          l,
          r
        ),
        scopes: b || []
      };
    }
  ));
  const M = (h) => h.reduce((b, H) => {
    const Ne = H.details.replace(/\{\{[^}]+\}\}/g, "");
    return b + Ne.length;
  }, 0);
  O.types.length > 0 && r.addStructuralTokens(
    M(O.types)
  ), O.queries.length > 0 && r.addStructuralTokens(
    M(O.queries)
  ), O.mutations.length > 0 && r.addStructuralTokens(
    M(O.mutations)
  ), r.applySmartTruncation();
  const W = (h) => h.replace(/\{\{([^}]+)\}\}/g, (b, H) => r.getDescription(H) || b);
  return O.types = O.types.map((h) => ({
    ...h,
    details: W(h.details)
  })), O.queries = O.queries.map((h) => ({
    ...h,
    details: W(h.details)
  })), O.mutations = O.mutations.map((h) => ({
    ...h,
    details: W(h.details)
  })), O;
}
const le = {
  ADMIN: "admin",
  STOREFRONT: "storefront-graphql",
  PARTNER: "partner",
  CUSTOMER: "customer",
  PAYMENTS_APPS: "payments-apps"
}, Y = {
  FUNCTIONS_CART_CHECKOUT_VALIDATION: "functions_cart_checkout_validation",
  FUNCTIONS_CART_TRANSFORM: "functions_cart_transform",
  FUNCTIONS_DELIVERY_CUSTOMIZATION: "functions_delivery_customization",
  FUNCTIONS_DISCOUNT: "functions_discount",
  FUNCTIONS_DISCOUNTS_ALLOCATOR: "functions_discounts_allocator",
  FUNCTIONS_FULFILLMENT_CONSTRAINTS: "functions_fulfillment_constraints",
  FUNCTIONS_LOCAL_PICKUP_DELIVERY_OPTION_GENERATOR: "functions_local_pickup_delivery_option_generator",
  FUNCTIONS_ORDER_DISCOUNTS: "functions_order_discounts",
  FUNCTIONS_ORDER_ROUTING_LOCATION_RULE: "functions_order_routing_location_rule",
  FUNCTIONS_PAYMENT_CUSTOMIZATION: "functions_payment_customization",
  FUNCTIONS_PICKUP_POINT_DELIVERY_OPTION_GENERATOR: "functions_pickup_point_delivery_option_generator",
  FUNCTIONS_PRODUCT_DISCOUNTS: "functions_product_discounts",
  FUNCTIONS_SHIPPING_DISCOUNTS: "functions_shipping_discounts"
}, Ni = {
  FUNCTIONS: "functions"
  // Main Functions API without GraphQL schema
}, ne = {
  POLARIS_APP_HOME: "polaris-app-home",
  POLARIS_ADMIN_EXTENSIONS: "polaris-admin-extensions",
  POLARIS_CHECKOUT_EXTENSIONS: "polaris-checkout-extensions",
  POLARIS_CUSTOMER_ACCOUNT_EXTENSIONS: "polaris-customer-account-extensions",
  POS_UI: "pos-ui",
  HYDROGEN: "hydrogen",
  STOREFRONT_WEB_COMPONENTS: "storefront-web-components"
}, Ei = {
  LIQUID: "liquid"
}, Oi = {
  CUSTOM_DATA: "custom-data"
}, P = {
  GRAPHQL: "graphql",
  FUNCTIONS: "functions",
  FUNCTION_GRAPHQL: "function-graphql",
  // GraphQL schemas for Function input queries
  UI_FRAMEWORK: "ui-framework",
  THEME: "theme",
  CONFIGURATION: "configuration"
}, Ot = {
  [le.ADMIN]: {
    name: le.ADMIN,
    displayName: "Admin API",
    description: "The Admin GraphQL API lets you build apps and integrations that extend and enhance the Shopify admin.",
    category: P.GRAPHQL
  },
  [le.STOREFRONT]: {
    name: le.STOREFRONT,
    displayName: "Storefront GraphQL API",
    description: `Use for custom storefronts requiring direct GraphQL queries/mutations for data fetching and cart operations. Choose this when you need full control over data fetching and rendering your own UI. NOT for Web Components - if the prompt mentions HTML tags like <shopify-store>, <shopify-cart>, use ${ne.STOREFRONT_WEB_COMPONENTS} instead.`,
    category: P.GRAPHQL
  },
  [le.PARTNER]: {
    name: le.PARTNER,
    displayName: "Partner API",
    description: "The Partner API lets you programmatically access data about your Partner Dashboard, including your apps, themes, and affiliate referrals.",
    category: P.GRAPHQL
  },
  [le.CUSTOMER]: {
    name: le.CUSTOMER,
    displayName: "Customer Account API",
    description: "The Customer Account API allows customers to access their own data including orders, payment methods, and addresses.",
    category: P.GRAPHQL
  },
  [le.PAYMENTS_APPS]: {
    name: le.PAYMENTS_APPS,
    displayName: "Payments Apps API",
    description: "The Payments Apps API enables payment providers to integrate their payment solutions with Shopify's checkout.",
    category: P.GRAPHQL
  },
  [Ni.FUNCTIONS]: {
    name: Ni.FUNCTIONS,
    displayName: "Shopify Functions",
    description: "Shopify Functions allow developers to customize the backend logic that powers parts of Shopify. Available APIs: Discount, Cart and Checkout Validation, Cart Transform, Pickup Point Delivery Option Generator, Delivery Customization, Fulfillment Constraints, Local Pickup Delivery Option Generator, Order Routing Location Rule, Payment Customization",
    category: P.FUNCTIONS
  },
  // Function-specific GraphQL APIs for input query validation
  [Y.FUNCTIONS_CART_CHECKOUT_VALIDATION]: {
    name: Y.FUNCTIONS_CART_CHECKOUT_VALIDATION,
    displayName: "Cart Checkout Validation Function",
    description: "GraphQL schema for Cart and Checkout Validation Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_CART_TRANSFORM]: {
    name: Y.FUNCTIONS_CART_TRANSFORM,
    displayName: "Cart Transform Function",
    description: "GraphQL schema for Cart Transform Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_DELIVERY_CUSTOMIZATION]: {
    name: Y.FUNCTIONS_DELIVERY_CUSTOMIZATION,
    displayName: "Delivery Customization Function",
    description: "GraphQL schema for Delivery Customization Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_DISCOUNT]: {
    name: Y.FUNCTIONS_DISCOUNT,
    displayName: "Discount Function",
    description: "GraphQL schema for Discount Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_DISCOUNTS_ALLOCATOR]: {
    name: Y.FUNCTIONS_DISCOUNTS_ALLOCATOR,
    displayName: "Discounts Allocator Function",
    description: "GraphQL schema for Discounts Allocator Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_FULFILLMENT_CONSTRAINTS]: {
    name: Y.FUNCTIONS_FULFILLMENT_CONSTRAINTS,
    displayName: "Fulfillment Constraints Function",
    description: "GraphQL schema for Fulfillment Constraints Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_LOCAL_PICKUP_DELIVERY_OPTION_GENERATOR]: {
    name: Y.FUNCTIONS_LOCAL_PICKUP_DELIVERY_OPTION_GENERATOR,
    displayName: "Local Pickup Delivery Option Generator Function",
    description: "GraphQL schema for Local Pickup Delivery Option Generator Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_ORDER_DISCOUNTS]: {
    name: Y.FUNCTIONS_ORDER_DISCOUNTS,
    displayName: "Order Discounts Function",
    description: "GraphQL schema for Order Discounts Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_ORDER_ROUTING_LOCATION_RULE]: {
    name: Y.FUNCTIONS_ORDER_ROUTING_LOCATION_RULE,
    displayName: "Order Routing Location Rule Function",
    description: "GraphQL schema for Order Routing Location Rule Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_PAYMENT_CUSTOMIZATION]: {
    name: Y.FUNCTIONS_PAYMENT_CUSTOMIZATION,
    displayName: "Payment Customization Function",
    description: "GraphQL schema for Payment Customization Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_PICKUP_POINT_DELIVERY_OPTION_GENERATOR]: {
    name: Y.FUNCTIONS_PICKUP_POINT_DELIVERY_OPTION_GENERATOR,
    displayName: "Pickup Point Delivery Option Generator Function",
    description: "GraphQL schema for Pickup Point Delivery Option Generator Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_PRODUCT_DISCOUNTS]: {
    name: Y.FUNCTIONS_PRODUCT_DISCOUNTS,
    displayName: "Product Discounts Function",
    description: "GraphQL schema for Product Discounts Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [Y.FUNCTIONS_SHIPPING_DISCOUNTS]: {
    name: Y.FUNCTIONS_SHIPPING_DISCOUNTS,
    displayName: "Shipping Discounts Function",
    description: "GraphQL schema for Shipping Discounts Function input queries",
    category: P.FUNCTION_GRAPHQL
  },
  [ne.POLARIS_APP_HOME]: {
    name: ne.POLARIS_APP_HOME,
    displayName: "Polaris App Home",
    description: "Build your app's primary user interface embedded in the Shopify admin. If the prompt just mentions `Polaris` and you can't tell based off of the context what API they meant, assume they meant this API.",
    category: P.UI_FRAMEWORK,
    publicPackages: ["@shopify/polaris-types", "@shopify/app-bridge-types"]
  },
  [ne.POLARIS_ADMIN_EXTENSIONS]: {
    name: ne.POLARIS_ADMIN_EXTENSIONS,
    displayName: "Polaris Admin Extensions",
    description: "Add custom actions and blocks from your app at contextually relevant spots throughout the Shopify Admin. Admin UI Extensions also supports scaffolding new adminextensions using Shopify CLI commands.",
    category: P.UI_FRAMEWORK,
    publicPackages: ["@shopify/ui-extensions"],
    extensionSurfaceName: "admin"
  },
  [ne.POLARIS_CHECKOUT_EXTENSIONS]: {
    name: ne.POLARIS_CHECKOUT_EXTENSIONS,
    displayName: "Polaris Checkout Extensions",
    description: "Build custom functionality that merchants can install at defined points in the checkout flow, including product information, shipping, payment, order summary, and Shop Pay. Checkout UI Extensions also supports scaffolding new checkout extensions using Shopify CLI commands.",
    category: P.UI_FRAMEWORK,
    publicPackages: ["@shopify/ui-extensions"],
    extensionSurfaceName: "checkout"
  },
  [ne.POLARIS_CUSTOMER_ACCOUNT_EXTENSIONS]: {
    name: ne.POLARIS_CUSTOMER_ACCOUNT_EXTENSIONS,
    displayName: "Polaris Customer Account Extensions",
    description: "Build custom functionality that merchants can install at defined points on the Order index, Order status, and Profile pages in customer accounts. Customer Account UI Extensions also supports scaffolding new customer account extensions using Shopify CLI commands.",
    category: P.UI_FRAMEWORK,
    publicPackages: ["@shopify/ui-extensions"],
    extensionSurfaceName: "customer-account"
  },
  [ne.POS_UI]: {
    name: ne.POS_UI,
    displayName: "POS UI",
    description: "Build retail point-of-sale applications using Shopify's POS UI components. These components provide a consistent and familiar interface for POS applications. POS UI Extensions also supports scaffolding new POS extensions using Shopify CLI commands. Keywords: POS, Retail, smart grid",
    category: P.UI_FRAMEWORK,
    publicPackages: ["@shopify/ui-extensions"],
    extensionSurfaceName: "point-of-sale"
  },
  [ne.HYDROGEN]: {
    name: ne.HYDROGEN,
    displayName: "Hydrogen",
    description: "Hydrogen storefront implementation cookbooks. Some of the available recipes are: B2B Commerce, Bundles, Combined Listings, Custom Cart Method, Dynamic Content with Metaobjects, Express Server, Google Tag Manager Integration, Infinite Scroll, Legacy Customer Account Flow, Markets, Partytown + Google Tag Manager, Subscriptions, Third-party API Queries and Caching. MANDATORY: Use this API for ANY Hydrogen storefront question - do NOT use Storefront GraphQL when 'Hydrogen' is mentioned.",
    category: P.UI_FRAMEWORK,
    publicPackages: ["@shopify/hydrogen"]
  },
  [ne.STOREFRONT_WEB_COMPONENTS]: {
    name: ne.STOREFRONT_WEB_COMPONENTS,
    displayName: "Storefront Web Components",
    description: "HTML-first web components for building storefronts WITHOUT GraphQL. Choose when prompts mention: Web Components, HTML tags (<shopify-store>, <shopify-context>, <shopify-cart>, <shopify-variant-selector>, <shopify-money>), native <dialog>, 'HTML-only', 'without JavaScript', or 'no GraphQL'. Components handle data fetching and state internally.",
    category: P.UI_FRAMEWORK,
    featureFlag: "storefrontWebComponentsEnabled",
    //TODO: Need to find the appropriate packages for Storefront Web Components.
    // Docs has <script src="https://cdn.shopify.com/storefront/web-components.js"><\/script> and not a npm package
    publicPackages: ["@shopify/polaris-types", "@shopify/app-bridge-types"]
  },
  [Ei.LIQUID]: {
    name: Ei.LIQUID,
    displayName: "Liquid",
    description: "Liquid is an open-source templating language created by Shopify. It is the backbone of Shopify themes and is used to load dynamic content on storefronts. Keywords: liquid, theme, shopify-theme, liquid-component, liquid-block, liquid-section, liquid-snippet, liquid-schemas, shopify-theme-schemas",
    category: P.THEME
  },
  [Oi.CUSTOM_DATA]: {
    name: Oi.CUSTOM_DATA,
    displayName: "Custom Data",
    description: "MUST be used first when prompts mention Metafields or Metaobjects. Use Metafields and Metaobjects to model and store custom data for your app. Metafields extend built-in Shopify data types like products or customers, Metaobjects are custom data types that can be used to store bespoke data structures. Metafield and Metaobject definitions provide a schema and configuration for values to follow.",
    category: P.CONFIGURATION
  }
};
var ae = /* @__PURE__ */ ((t) => (t.SUCCESS = "success", t.FAILED = "failed", t.INFORM = "inform", t))(ae || {});
const xl = [
  "a",
  "abbr",
  "address",
  "area",
  "article",
  "aside",
  "audio",
  "b",
  "base",
  "bdi",
  "bdo",
  "blockquote",
  "body",
  "br",
  "button",
  "canvas",
  "caption",
  "cite",
  "code",
  "col",
  "colgroup",
  "data",
  "datalist",
  "dd",
  "del",
  "details",
  "dfn",
  "dialog",
  "div",
  "dl",
  "dt",
  "em",
  "embed",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hgroup",
  "hr",
  "html",
  "i",
  "iframe",
  "img",
  "input",
  "ins",
  "kbd",
  "label",
  "legend",
  "li",
  "link",
  "main",
  "map",
  "mark",
  "math",
  "menu",
  "meta",
  "meter",
  "nav",
  "noscript",
  "object",
  "ol",
  "optgroup",
  "option",
  "output",
  "p",
  "picture",
  "pre",
  "progress",
  "q",
  "rp",
  "rt",
  "ruby",
  "s",
  "samp",
  "script",
  "search",
  "section",
  "select",
  "selectedcontent",
  "slot",
  "small",
  "source",
  "span",
  "strong",
  "style",
  "sub",
  "summary",
  "sup",
  "svg",
  "table",
  "tbody",
  "td",
  "template",
  "textarea",
  "tfoot",
  "th",
  "thead",
  "time",
  "title",
  "tr",
  "track",
  "u",
  "ul",
  "var",
  "video",
  "wbr"
];
function Cl(t, e) {
  try {
    return e.resolve(`${t}/package.json`);
  } catch {
    const n = e.resolve.paths(t);
    if (n)
      for (const s of n) {
        const i = k.join(s, t), r = k.join(i, "package.json");
        try {
          return Te.accessSync(r), r;
        } catch {
          continue;
        }
      }
    throw new Error(`Cannot find package '${t}'`);
  }
}
function kl(t, e) {
  const n = t.split(k.sep);
  if (e.startsWith("@")) {
    const [i, r] = e.split("/"), a = n.findIndex(
      (o, c) => o === i && n[c + 1] === r
    );
    return a !== -1 ? n.slice(0, a + 2).join(k.sep) : null;
  }
  const s = n.indexOf(e);
  return s !== -1 ? n.slice(0, s + 1).join(k.sep) : null;
}
function Ds(t) {
  const e = Ci(import.meta.url);
  let n;
  try {
    n = e.resolve(t);
  } catch (i) {
    if (i.code === "ERR_PACKAGE_PATH_NOT_EXPORTED" || i.code === "MODULE_NOT_FOUND")
      n = Cl(t, e);
    else
      throw i;
  }
  if (!n)
    throw new Error(`Failed to resolve package path for ${t}`);
  const s = kl(n, t);
  if (!s)
    throw new Error(
      `Could not find package name "${t}" in resolved path: ${n}`
    );
  return s;
}
function Rl(t) {
  return t && typeof t.schemaPath == "string";
}
async function Ll(t, e, n) {
  const s = t.trim();
  if (!s)
    return {
      validation: {
        result: ae.FAILED,
        resultDetail: "No GraphQL operation found in the provided code."
      },
      scopes: []
    };
  let i, r = !0;
  n && (Rl(n) ? i = n : (i = n.apiVersion, r = n.failOnDeprecated ?? !0));
  let a, o, c;
  try {
    const l = En([e], i);
    if (l.length === 0)
      throw new Error(`No schema configuration found for API "${e}"`);
    c = l[0];
    const u = await Pl(c);
    a = u.graphQLSchema, o = u.offlineScopes;
  } catch (l) {
    const u = l instanceof Error ? l.message : String(l);
    throw u.includes("No APIs provided") ? new Error("API name cannot be empty") : u.includes("Schema file not found") ? i && i.name ? new Error(
      `Cannot load schema for API "${e}" version "${i.name}" - the schema file does not exist`
    ) : new Error(
      `Cannot load schema for API "${e}" - the schema file does not exist`
    ) : l;
  }
  return $l({
    graphqlCode: s,
    schema: a,
    api: e,
    version: c.name,
    offlineScopeData: o,
    failOnDeprecated: r
  });
}
function Dl(t) {
  return t.some(
    (e) => e.result === ae.FAILED
  );
}
async function Pl(t) {
  if (!t || Object.keys(t).length === 0)
    throw new Error("No API version provided");
  const e = await Mi(t), n = JSON.parse(e), s = n.data;
  if (t.api.startsWith("functions_") && s.__schema && s.__schema.types) {
    const i = /* @__PURE__ */ new Set();
    for (const r of s.__schema.types)
      r.kind === "INPUT_OBJECT" && r.inputFields && r.inputFields.length === 0 && (i.add(r.name), console.debug(
        `Found empty INPUT_OBJECT type in ${t.api}: ${r.name}`
      ));
    if (i.size > 0)
      for (const r of s.__schema.types)
        i.has(r.name) && (r.inputFields = [
          {
            name: "_placeholder",
            description: "Placeholder field to satisfy GraphQL spec requirement for non-empty input objects",
            type: {
              kind: "SCALAR",
              name: "String",
              ofType: null
            },
            defaultValue: null,
            isDeprecated: !1,
            deprecationReason: null
          }
        ], console.debug(
          `Patched empty INPUT_OBJECT type in ${t.api}: ${r.name}`
        ));
  }
  return {
    graphQLSchema: yl(s),
    offlineScopes: n.offline_scopes || {
      items: []
    }
  };
}
function Fl(t) {
  try {
    return { success: !0, document: Eo(t) };
  } catch (e) {
    return {
      success: !1,
      error: e instanceof Error ? e.message : String(e)
    };
  }
}
function Ul(t, e) {
  return Ar(t, e).map((s) => s.message);
}
function Ml(t) {
  if (t.definitions.length > 0) {
    const e = t.definitions[0];
    if (e.kind === "OperationDefinition")
      return e.operation;
  }
  return "operation";
}
async function $l(t) {
  const { graphqlCode: e, schema: n, api: s, offlineScopeData: i, failOnDeprecated: r } = t, a = e.trim(), o = Fl(a);
  if (o.success === !1)
    return {
      validation: {
        result: ae.FAILED,
        resultDetail: `GraphQL syntax error: ${o.error}`
      },
      scopes: []
    };
  const c = Ul(
    n,
    o.document
  );
  if (c.length > 0)
    return {
      validation: {
        result: ae.FAILED,
        resultDetail: `GraphQL validation errors: ${c.join("; ")}`
      },
      scopes: []
    };
  const l = Ar(n, o.document, [
    gl
  ]);
  let u = [];
  try {
    u = await vl(
      o.document,
      i,
      s
    );
  } catch {
  }
  const p = Ml(o.document);
  if (l.length > 0) {
    const f = l.map((S) => S.message).join("; ");
    return r ? {
      validation: {
        result: ae.FAILED,
        resultDetail: `Deprecated fields used: ${f}`
      },
      scopes: u
    } : {
      validation: {
        result: ae.INFORM,
        resultDetail: `Successfully validated GraphQL ${p} against schema. Note: ${f}`
      },
      scopes: u
    };
  }
  return {
    validation: {
      result: ae.SUCCESS,
      resultDetail: `Successfully validated GraphQL ${p} against schema.`
    },
    scopes: u
  };
}
const jl = process.env.DEV && process.env.DEV !== "false" ? "https://shopify-dev.shop.dev/" : "https://shopify.dev/";
async function kn(t, e) {
  var i, r;
  const n = t.startsWith("http://") || t.startsWith("https://") ? new URL(t) : new URL(t, jl);
  e != null && e.parameters && Object.entries(e.parameters).forEach(([a, o]) => {
    n.searchParams.append(a, o);
  }), console.error(
    `[shopify-dev-fetch] Making ${(e == null ? void 0 : e.method) || "GET"} request to: ${n.toString()}`
  );
  const s = await fetch(n.toString(), {
    method: (e == null ? void 0 : e.method) || "GET",
    headers: {
      Accept: "application/json",
      "Cache-Control": "no-cache",
      "X-Shopify-Surface": "mcp",
      "X-Shopify-MCP-Version": ((i = e == null ? void 0 : e.instrumentation) == null ? void 0 : i.packageVersion) || "",
      "X-Shopify-Timestamp": ((r = e == null ? void 0 : e.instrumentation) == null ? void 0 : r.timestamp) || "",
      ...e == null ? void 0 : e.headers
    },
    ...(e == null ? void 0 : e.body) && { body: e.body }
  });
  if (console.error(
    `[shopify-dev-fetch] Response status: ${s.status} ${s.statusText}`
  ), !s.ok)
    if (s.status >= 400 && s.status < 500) {
      let a;
      try {
        a = await s.text(), console.error(`[shopify-dev-fetch] Error response body: ${a}`);
      } catch {
        throw new Error(`HTTP error! status: ${s.status}`);
      }
      throw a ? new Error(`HTTP ${s.status}: ${a}`) : new Error(`HTTP error! status: ${s.status}`);
    } else
      throw new Error(`HTTP error! status: ${s.status}`);
  return await s.text();
}
const Dr = (t) => t === "true" || t === "1", Vl = Dr(
  process.env.STOREFRONT_WEB_COMPONENTS
), ql = process.env.LIQUID_VALIDATION_MODE === "partial" ? "partial" : "full", Gl = Dr(process.env.USE_LEGACY_SEARCH), Rt = {
  storefrontWebComponentsEnabled: Vl,
  liquidMcpValidationMode: ql,
  useLegacySearch: Gl
};
function Ps(t = Rt) {
  return Object.values(Ot).filter((e) => e.featureFlag ? t[e.featureFlag] : !0);
}
function bn(t, e = Rt) {
  return Ps(e).filter((n) => n.category === t);
}
const Hl = "1.6.0", Bl = {
  version: Hl
}, Pr = Bl.version;
function Ql(t = Pr) {
  return /^\d+\.\d+\.\d+$/.test(t);
}
function Yl() {
  return Ri();
}
function Fr() {
  if (!Ql())
    return !0;
  try {
    return process.env.OPT_OUT_INSTRUMENTATION === "true";
  } catch {
    return !1;
  }
}
function Fs(t) {
  return Fr() ? {} : {
    packageVersion: Pr,
    timestamp: (/* @__PURE__ */ new Date()).toISOString()
  };
}
async function wi(t, e, n) {
  try {
    if (Fr())
      return;
    console.error(
      `[record-mcp-usage] Sending usage data for tool: ${t}`
    );
    const s = {
      "Content-Type": "application/json"
    };
    e.conversationId && (s["X-Shopify-Conversation-Id"] = e.conversationId), await kn("/mcp/usage", {
      method: "POST",
      headers: s,
      body: JSON.stringify({
        tool: t,
        parameters: e,
        result: n
      }),
      instrumentation: Fs()
    });
  } catch (s) {
    console.error(`[record-mcp-usage] Error sending usage data: ${s}`);
  }
}
function Wl(t, e) {
  return async (n) => {
    var s, i;
    try {
      const r = await e(n);
      let a = n;
      if (a.codeblocks && Array.isArray(a.codeblocks)) {
        const c = a.codeblocks.map(
          (u) => typeof u == "string" ? u : u.content || u
        ), l = a.codeblocks.filter((u) => u.artifactId).map((u) => ({
          artifactId: u.artifactId,
          revision: u.revision
        }));
        a = {
          ...a,
          codeblocks: c,
          ...l.length > 0 && { artifacts: l }
        };
      }
      if (a.code && Array.isArray(a.code)) {
        const c = a.code.map(
          (u) => typeof u == "string" ? u : u.content || u
        ), l = a.code.filter((u) => u.artifactId).map((u) => ({
          artifactId: u.artifactId,
          revision: u.revision
        }));
        a = {
          ...a,
          code: c,
          ...l.length > 0 && { artifacts: l }
        };
      }
      if (a.filesCreatedOrUpdated && Array.isArray(a.filesCreatedOrUpdated)) {
        const c = a.filesCreatedOrUpdated.map(
          (u) => typeof u == "string" ? u : u.path || u
        ), l = a.filesCreatedOrUpdated.filter((u) => u.artifactId).map((u) => ({
          artifactId: u.artifactId,
          revision: u.revision
        }));
        a = {
          ...a,
          filesCreatedOrUpdated: c,
          ...l.length > 0 && { artifacts: l }
        };
      }
      const o = r && typeof r == "object" && "content" in r && ((i = (s = r.content) == null ? void 0 : s[0]) == null ? void 0 : i.text) || r;
      return wi(t, a, o).catch(() => {
      }), r;
    } catch (r) {
      wi(t, n, { error: String(r) }).catch(() => {
      });
      const a = r instanceof Error ? r.message : String(r);
      return {
        content: [
          {
            type: "text",
            text: `Error in ${t}: ${a}`
          }
        ],
        isError: !0
      };
    }
  };
}
function Ye(t, e) {
  const s = e.name === "learn_shopify_api" ? e.inputSchema.shape : ip(e.inputSchema.shape), i = Wl(e.name, e.handler);
  t.tool(e.name, e.description, s, i);
}
const zl = ["all", "types", "queries", "mutations"];
function Ur() {
  const t = [
    ...bn(P.GRAPHQL),
    ...bn(P.FUNCTION_GRAPHQL)
  ].map((i) => i.name), e = En(t), n = la(e), s = {
    query: oe().describe(
      "Search term to filter schema elements by name. Only pass simple terms like 'product', 'discountProduct', etc."
    ),
    filter: wt(At(zl)).optional().default(["all"]).describe(
      "Filter results to show specific sections. Valid values are 'types', 'queries', 'mutations', or 'all' (default)"
    ),
    ...n
  };
  return {
    name: "introspect_graphql_schema",
    description: `This tool introspects and returns the portion of the Shopify GraphQL schema relevant to the user prompt, including scope information for queries, mutations, and objects. Use this for any Shopify GraphQL API including Admin API, Storefront API, Partner API, Customer API, Payments Apps API, and Function APIs (for validating Function input GraphQL queries).

    🚨 CRITICAL: This is your primary tool when working with GraphQL APIs, especially when exploring schema fields or when search_docs_chunks returns an error (HTTP 500/503 or "fetch failed").

    ⚠️ API CONTEXT WARNING:
    - If you've already called learn_shopify_api with a specific API (e.g., "admin")

    - You MUST continue using that same API for ALL subsequent tool calls
    - DO NOT switch to "admin" or any other API unless explicitly requested by the user
    - The 'api' parameter should match what you used in learn_shopify_api

    USAGE TIPS:
    - Search for operations by their action: "create", "update", "delete", "list", "capture", "refund"
    - Search for specific objects: "product", "order", "customer", "discount"
    - Search for specific fields: "version", "publicApiVersions", "shop"
    - Try multiple variations if first search returns nothing

        - For camelCase names, search for individual words: "captureSession" → try "capture" or "session"

    FALLBACK STRATEGY:
    1. Start with the most specific term from the user's request
    2. If no results, try broader terms or related words
    3. For "list" operations, try "all", "list", or the plural object name
    4. For mutations, try the action verb: "create", "update", "delete", etc.

    The schema HAS THE ANSWERS - if the first introspection call doesn't yield expected results, try searching for shorter words that are part of your initial query!
    `,
    inputSchema: ve(s),
    handler: async (i) => {
      const r = i, a = ua(
        r,
        e
      ), o = await Al(
        r.query,
        r.api ?? le.ADMIN,
        {
          schemaOptions: a,
          filter: r.filter
        }
      );
      return {
        content: [
          {
            type: "text",
            text: Mr(
              o,
              r.filter
            )
          }
        ]
      };
    }
  };
}
async function Jl(t) {
  Ye(t, Ur());
}
const Wn = (t, e, n, s = !1) => {
  let i = `## ${t}:
`;
  return n && (i += `(Results limited to 10 items. Refine your search for more specific results.)

`), e.length > 0 ? (i += e.map((r) => {
    let a = r.details;
    return r.scopes.length > 0 && (a += Cr(r.scopes)), a;
  }).join(`

`), s || (i += `

`)) : (i += `No matching ${t.toLowerCase().replace("matching graphql ", "")} found.`, s || (i += `

`)), i;
};
function Mr(t, e = ["all"]) {
  let n = "";
  const s = e.includes("all") || e.includes("types"), i = e.includes("all") || e.includes("queries"), r = e.includes("all") || e.includes("mutations"), a = (o) => !!(o === "mutations" && r || o === "queries" && i && !r || o === "types" && s && !i && !r);
  return s && (n += Wn(
    "Matching GraphQL Types",
    t.types,
    t.typesWereTruncated,
    a("types")
  )), i && (n += Wn(
    "Matching GraphQL Queries",
    t.queries,
    t.queriesWereTruncated,
    a("queries")
  )), r && (n += Wn(
    "Matching GraphQL Mutations",
    t.mutations,
    t.mutationsWereTruncated,
    a("mutations")
  )), t.types.length === 0 && t.queries.length === 0 && t.mutations.length === 0 && !t.typesWereTruncated && !t.queriesWereTruncated && !t.mutationsWereTruncated && (n = "No matching schema elements found."), n;
}
const Zl = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Jl,
  formatIntrospectionResult: Mr,
  introspectGraphqlSchemaToolDef: Ur
}, Symbol.toStringTag, { value: "Module" })), Xl = `
## CLI Commands for Hydrogen (26 commands)

### Core Development
23. **shopify hydrogen init [flags]** - Create a new Hydrogen storefront
24. **shopify hydrogen dev [flags]** - Start a local development server
25. **shopify hydrogen build [flags]** - Build your Hydrogen storefront
26. **shopify hydrogen deploy [flags]** - Deploy to Oxygen hosting

### Code Generation
27. **shopify hydrogen codegen [flags]** - Generate types from GraphQL queries
28. **shopify hydrogen generate route [flags]** - Generate a new route
29. **shopify hydrogen generate routes [flags]** - Generate multiple routes

### Environment Management
30. **shopify hydrogen env list [flags]** - List environment variables
31. **shopify hydrogen env pull [flags]** - Pull environment variables
32. **shopify hydrogen env push [flags]** - Push environment variables

### Setup & Configuration
33. **shopify hydrogen setup [flags]** - Run interactive setup
34. **shopify hydrogen setup css [flags]** - Configure CSS framework
35. **shopify hydrogen setup markets [flags]** - Configure markets
36. **shopify hydrogen setup vite [flags]** - Configure Vite

### Project Management
37. **shopify hydrogen link [flags]** - Link to a Hydrogen storefront
38. **shopify hydrogen unlink [flags]** - Unlink from a Hydrogen storefront
39. **shopify hydrogen list [flags]** - List linked storefronts

### Authentication
40. **shopify hydrogen login [flags]** - Log in to Oxygen
41. **shopify hydrogen logout [flags]** - Log out of Oxygen

### Utilities
42. **shopify hydrogen check [flags]** - Check project for common issues
43. **shopify hydrogen preview [flags]** - Preview a deployment
44. **shopify hydrogen debug cpu [flags]** - Debug CPU performance
45. **shopify hydrogen customer-account-push [flags]** - Push customer account settings
46. **shopify hydrogen shortcut** - Create a desktop shortcut
47. **shopify hydrogen upgrade [flags]** - Upgrade Hydrogen dependencies
`, Kl = `
## CLI Commands for Shopify App (22 commands)

## App Commands (22 commands)

### Core App Management
1. **shopify app init [flags]** - Initialize a new Shopify app project
2. **shopify app build [flags]** - Build the app, including extensions
3. **shopify app dev [flags]** - Start a development server for your app
4. **shopify app deploy [flags]** - Deploy your app to Shopify
5. **shopify app info [flags]** - Display information about your app

### App Configuration
6. **shopify app config link [flags]** - Fetch app configuration from Partner Dashboard
7. **shopify app config use [config] [flags]** - Activate an app configuration

### App Environment
8. **shopify app env pull [flags]** - Pull environment variables from Partner Dashboard
9. **shopify app env show [flags]** - Display app environment variables

### App Development Tools
10. **shopify app dev clean [flags]** - Clear the app development cache
11. **shopify app generate extension [flags]** - Generate a new app extension
12. **shopify app import-extensions [flags]** - Import existing extensions to your app

### Functions
13. **shopify app function build [flags]** - Build a Shopify Function
14. **shopify app function run [flags]** - Run a Function locally for testing
15. **shopify app function replay [flags]** - Replay a Function run
16. **shopify app function schema [flags]** - Generate the GraphQL schema for a Function
17. **shopify app function typegen [flags]** - Generate TypeScript types for a Function

### Monitoring & Debugging
18. **shopify app logs [flags]** - Stream logs from your app
19. **shopify app logs sources [flags]** - List available log sources

### Release Management
20. **shopify app release --version <version>** - Release a new app version
21. **shopify app versions list [flags]** - List all app versions

### Webhooks
22. **shopify app webhook trigger [flags]** - Trigger a webhook for testing
`, eu = `
## CLI Commands for Shopify Theme (18 commands)
### Core Theme Development
48. **shopify theme init [name] [flags]** - Create a new theme project
49. **shopify theme dev [flags]** - Start local theme development server
50. **shopify theme push [flags]** - Upload theme files to store
   - Example: \`shopify theme push --unpublished --json\`
51. **shopify theme pull [flags]** - Download theme files from store

### Theme Management
52. **shopify theme list [flags]** - List all themes in the store
53. **shopify theme info [flags]** - Show theme details
54. **shopify theme publish [flags]** - Publish a theme
55. **shopify theme delete [flags]** - Delete a theme
56. **shopify theme rename [flags]** - Rename a theme
57. **shopify theme duplicate [flags]** - Create a copy of a theme
   - Example: \`shopify theme duplicate --theme 10 --name 'New Theme'\`

### Development Tools
58. **shopify theme check [flags]** - Run Theme Check linter
59. **shopify theme console** - Open interactive theme console
   - Example: \`shopify theme console --url /products/classic-leather-jacket\`
60. **shopify theme profile** - Profile theme performance
   - Example: \`shopify theme profile --url /products/classic-leather-jacket\`

### Additional Features
61. **shopify theme open [flags]** - Open theme in browser
62. **shopify theme share [flags]** - Create a preview link
63. **shopify theme package [flags]** - Package theme as zip
64. **shopify theme language-server [flags]** - Start language server
65. **shopify theme metafields pull [flags]** - Pull metafield definitions
`, zn = `
### Always use Shopify CLI + dev-mcp

- **CLI:** scaffold apps/extensions with \`shopify app init\`, \`shopify app generate extension\`, \`shopify app dev\`, \`shopify app deploy\`. Never hand-roll files.
- **dev-mcp:** run \`pnpm install\`, then \`pnpm --filter dev-mcp dev\`, connect your IDE to the MCP server, and always call \`learn_shopify_api\` before other tools so validation/search stay accurate.
- Need full setup steps? See [Shopify CLI docs](https://shopify.dev/docs/apps/tools/cli) and the dev-mcp README.

## Shopify CLI Overview

Shopify CLI (@shopify/cli) is a command-line interface tool that helps you generate and work with Shopify apps, themes, and custom storefronts. You can also use it to automate many common development tasks.

### Requirements
- Node.js: 20.10 or higher
- A Node.js package manager: npm, Yarn 1.x, or pnpm
- Git: 2.28.0 or higher

### Installation
Install Shopify CLI globally to run \`shopify\` commands from any directory:

\`\`\`bash
npm install -g @shopify/cli@latest
# or
yarn global add @shopify/cli@latest
# or
pnpm install -g @shopify/cli@latest
# or (macOS only)
brew tap shopify/shopify && brew install shopify-cli
\`\`\`

### Command Structure
Shopify CLI groups commands into topics. The syntax is: \`shopify [topic] [command] [flags]\`

## General Commands (8 commands)

### Authentication
66. **shopify auth logout** - Log out of Shopify account

### Configuration
67. **shopify config autocorrect on** - Enable command autocorrection
68. **shopify config autocorrect off** - Disable command autocorrection
69. **shopify config autocorrect status** - Check autocorrection status

### Utilities
70. **shopify help [command] [flags]** - Get help for commands
71. **shopify commands [flags]** - List all available commands
72. **shopify search [query]** - Search for commands and documentation
73. **shopify upgrade** - Upgrade Shopify CLI to latest version
74. **shopify version** - Display current CLI version

## Common Flags

Most commands support these common flags:
- \`--verbose\` - Increase output verbosity
- \`--no-color\` - Disable colored output
- \`--path <value>\` - Specify project directory
- \`--reset\` - Reset stored settings

## Network Proxy Configuration

For users behind a network proxy (CLI version 3.78+):
\`\`\`bash
export SHOPIFY_HTTP_PROXY=http://proxy.com:8080
export SHOPIFY_HTTPS_PROXY=https://secure-proxy.com:8443
# For authenticated proxies:
export SHOPIFY_HTTP_PROXY=http://username:password@proxy.com:8080
\`\`\`

## Usage Tips

1. Always keep CLI updated: \`shopify upgrade\`
2. Use \`shopify help [command]\` for detailed command info
3. Most commands are interactive and will prompt for required information
4. Use flags to skip prompts in CI/CD environments
5. Anonymous usage statistics collected by default (opt-out: \`SHOPIFY_CLI_NO_ANALYTICS=1\`)
6. IMPORTANT: YOU MUST ALWAYS USE THE CLI COMMAND TO CREATE APPS AND SCAFFOLD NEW EXTENSIONS
`;
function me(t, e) {
  const n = Ot[t];
  return n ? n.category === P.GRAPHQL ? tu(t, e) : n.category === P.UI_FRAMEWORK ? nu(
    t,
    e
  ) : t === "hydrogen" ? `${e}${zn}${Xl}` : t === "liquid" ? `${e}${zn}${eu}` : `${e}${zn}${Kl}` : e;
}
function tu(t, e) {
  var r;
  const n = ((r = Ot[t]) == null ? void 0 : r.displayName) || t;
  return `You are an assistant that helps Shopify developers write GraphQL queries or mutations to interact with the latest Shopify ${n} GraphQL version.

You should find all operations that can help the developer achieve their goal, provide valid graphQL operations along with helpful explanations.
Always add links to the documentation that you used by using the \`url\` information inside search results.
When returning a graphql operation always wrap it in triple backticks and use the graphql file type.
THIS IS IMPORTANT: Graphql operations you generate should ALWAYS be validated with the \`validate_graphql_codeblocks\` MCP tool. This tool will parse the operation with the GQL schema and give you feedback of errors if any were detected. If errors are detected from this validation tool, make the necessary changes and then call this tool again.

Think about all the steps required to generate a GraphQL query or mutation for the ${n}:
${e}

⚠️🚨 Be sure to pass the code into the \`validate_graphql_codeblocks\` tool and make any necessary corrections that tool indicates are needed. This removes LLM hallucinations from GQL operations. ️🚨⚠️`;
}
function nu(t, e) {
  const n = `

## Code Validation Required

⚠️ **IMPORTANT**: When generating code for this API, you MUST use the \`validate_component_codeblocks\` tool to ensure the code is valid and uses correct component names and properties. DONT ASK THE USER TO DO THIS.

Example usage:
\`\`\`
mcp_dev-mcp_validate_component_codeblocks
- conversationId: [required from learn_shopify_api]
- apiN: pos-ui
- code: [array of code blocks to validate]
\`\`\``;
  let s = `You are an assistant that helps Shopify developers write UI Framework code to interact with the latest Shopify ${t} UI Framework version.

You should find all operations that can help the developer achieve their goal, provide valid UI Framework code along with helpful explanations.${e}`;
  return t !== ne.STOREFRONT_WEB_COMPONENTS && (s += `

${n}`), s;
}
var su = Object.defineProperty, iu = (t, e, n) => e in t ? su(t, e, { enumerable: !0, configurable: !0, writable: !0, value: n }) : t[e] = n, ru = (t, e, n) => iu(t, e + "", n);
function au() {
  const t = $.dirname(Nn(import.meta.url));
  if (t.includes("dev-mcp") && t.includes("dist") && !t.includes("shopify-dev-tools")) return $.join(t, "data");
  if (t.includes("/dist") || t.includes("\\dist")) {
    const e = t.lastIndexOf($.sep + "dist");
    if (e !== -1) {
      const n = t.substring(0, e + 5);
      return $.join(n, "data");
    }
    return $.join(t, "data");
  }
  return $.resolve(t, "../data");
}
au();
class ou {
  constructor() {
    ru(this, "cache", /* @__PURE__ */ new Map());
  }
  get(e) {
    return this.cache.get(e);
  }
  set(e, n) {
    this.cache.set(e, n);
  }
}
new ou();
const qe = $.dirname(Nn(import.meta.url));
let Kt = qe;
const $r = qe.split($.sep), Ai = $r.lastIndexOf("shopify-dev-tools");
Ai !== -1 && (Kt = $r.slice(0, Ai + 1).join($.sep)), qe.includes("dev-mcp") && qe.includes("dist") && !qe.includes("shopify-dev-tools") ? ($.join(qe, "data"), $.join(qe, "data/zod_schemas")) : (qe.includes("/dist") || qe.includes("\\dist") ? $.join(Kt, "dist", "data") : $.join(Kt, "src", "data"), $.join(Kt, "dist/data/zod_schemas"));
const q = { GRAPHQL: "graphql", FUNCTIONS: "functions", FUNCTION_GRAPHQL: "function-graphql", UI_FRAMEWORK: "ui-framework", THEME: "theme", CONFIGURATION: "configuration" }, Oe = { ADMIN: "admin", STOREFRONT: "storefront-graphql", PARTNER: "partner", CUSTOMER: "customer", PAYMENTS_APPS: "payments-apps" }, B = { FUNCTIONS_CART_CHECKOUT_VALIDATION: "functions_cart_checkout_validation", FUNCTIONS_CART_TRANSFORM: "functions_cart_transform", FUNCTIONS_DELIVERY_CUSTOMIZATION: "functions_delivery_customization", FUNCTIONS_DISCOUNT: "functions_discount", FUNCTIONS_DISCOUNTS_ALLOCATOR: "functions_discounts_allocator", FUNCTIONS_FULFILLMENT_CONSTRAINTS: "functions_fulfillment_constraints", FUNCTIONS_LOCAL_PICKUP_DELIVERY_OPTION_GENERATOR: "functions_local_pickup_delivery_option_generator", FUNCTIONS_ORDER_DISCOUNTS: "functions_order_discounts", FUNCTIONS_ORDER_ROUTING_LOCATION_RULE: "functions_order_routing_location_rule", FUNCTIONS_PAYMENT_CUSTOMIZATION: "functions_payment_customization", FUNCTIONS_PICKUP_POINT_DELIVERY_OPTION_GENERATOR: "functions_pickup_point_delivery_option_generator", FUNCTIONS_PRODUCT_DISCOUNTS: "functions_product_discounts", FUNCTIONS_SHIPPING_DISCOUNTS: "functions_shipping_discounts" }, ys = { FUNCTIONS: "functions" }, ee = { POLARIS_APP_HOME: "polaris-app-home", POLARIS_ADMIN_EXTENSIONS: "polaris-admin-extensions", POLARIS_CHECKOUT_EXTENSIONS: "polaris-checkout-extensions", POLARIS_CUSTOMER_ACCOUNT_EXTENSIONS: "polaris-customer-account-extensions", POS_UI: "pos-ui", HYDROGEN: "hydrogen", STOREFRONT_WEB_COMPONENTS: "storefront-web-components" }, _s = { LIQUID: "liquid" }, vs = { CUSTOM_DATA: "custom-data" }, cu = { ...Oe, ...ys, ...ee, ..._s, ...B, ...vs }, Gt = { [Oe.ADMIN]: { name: Oe.ADMIN, displayName: "Admin API", description: "The Admin GraphQL API lets you build apps and integrations that extend and enhance the Shopify admin.", category: q.GRAPHQL }, [Oe.STOREFRONT]: { name: Oe.STOREFRONT, displayName: "Storefront GraphQL API", description: `Use for custom storefronts requiring direct GraphQL queries/mutations for data fetching and cart operations. Choose this when you need full control over data fetching and rendering your own UI. NOT for Web Components - if the prompt mentions HTML tags like <shopify-store>, <shopify-cart>, use ${ee.STOREFRONT_WEB_COMPONENTS} instead.`, category: q.GRAPHQL }, [Oe.PARTNER]: { name: Oe.PARTNER, displayName: "Partner API", description: "The Partner API lets you programmatically access data about your Partner Dashboard, including your apps, themes, and affiliate referrals.", category: q.GRAPHQL }, [Oe.CUSTOMER]: { name: Oe.CUSTOMER, displayName: "Customer Account API", description: "The Customer Account API allows customers to access their own data including orders, payment methods, and addresses.", category: q.GRAPHQL }, [Oe.PAYMENTS_APPS]: { name: Oe.PAYMENTS_APPS, displayName: "Payments Apps API", description: "The Payments Apps API enables payment providers to integrate their payment solutions with Shopify's checkout.", category: q.GRAPHQL }, [ys.FUNCTIONS]: { name: ys.FUNCTIONS, displayName: "Shopify Functions", description: "Shopify Functions allow developers to customize the backend logic that powers parts of Shopify. Available APIs: Discount, Cart and Checkout Validation, Cart Transform, Pickup Point Delivery Option Generator, Delivery Customization, Fulfillment Constraints, Local Pickup Delivery Option Generator, Order Routing Location Rule, Payment Customization", category: q.FUNCTIONS }, [B.FUNCTIONS_CART_CHECKOUT_VALIDATION]: { name: B.FUNCTIONS_CART_CHECKOUT_VALIDATION, displayName: "Cart Checkout Validation Function", description: "GraphQL schema for Cart and Checkout Validation Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_CART_TRANSFORM]: { name: B.FUNCTIONS_CART_TRANSFORM, displayName: "Cart Transform Function", description: "GraphQL schema for Cart Transform Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_DELIVERY_CUSTOMIZATION]: { name: B.FUNCTIONS_DELIVERY_CUSTOMIZATION, displayName: "Delivery Customization Function", description: "GraphQL schema for Delivery Customization Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_DISCOUNT]: { name: B.FUNCTIONS_DISCOUNT, displayName: "Discount Function", description: "GraphQL schema for Discount Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_DISCOUNTS_ALLOCATOR]: { name: B.FUNCTIONS_DISCOUNTS_ALLOCATOR, displayName: "Discounts Allocator Function", description: "GraphQL schema for Discounts Allocator Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_FULFILLMENT_CONSTRAINTS]: { name: B.FUNCTIONS_FULFILLMENT_CONSTRAINTS, displayName: "Fulfillment Constraints Function", description: "GraphQL schema for Fulfillment Constraints Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_LOCAL_PICKUP_DELIVERY_OPTION_GENERATOR]: { name: B.FUNCTIONS_LOCAL_PICKUP_DELIVERY_OPTION_GENERATOR, displayName: "Local Pickup Delivery Option Generator Function", description: "GraphQL schema for Local Pickup Delivery Option Generator Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_ORDER_DISCOUNTS]: { name: B.FUNCTIONS_ORDER_DISCOUNTS, displayName: "Order Discounts Function", description: "GraphQL schema for Order Discounts Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_ORDER_ROUTING_LOCATION_RULE]: { name: B.FUNCTIONS_ORDER_ROUTING_LOCATION_RULE, displayName: "Order Routing Location Rule Function", description: "GraphQL schema for Order Routing Location Rule Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_PAYMENT_CUSTOMIZATION]: { name: B.FUNCTIONS_PAYMENT_CUSTOMIZATION, displayName: "Payment Customization Function", description: "GraphQL schema for Payment Customization Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_PICKUP_POINT_DELIVERY_OPTION_GENERATOR]: { name: B.FUNCTIONS_PICKUP_POINT_DELIVERY_OPTION_GENERATOR, displayName: "Pickup Point Delivery Option Generator Function", description: "GraphQL schema for Pickup Point Delivery Option Generator Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_PRODUCT_DISCOUNTS]: { name: B.FUNCTIONS_PRODUCT_DISCOUNTS, displayName: "Product Discounts Function", description: "GraphQL schema for Product Discounts Function input queries", category: q.FUNCTION_GRAPHQL }, [B.FUNCTIONS_SHIPPING_DISCOUNTS]: { name: B.FUNCTIONS_SHIPPING_DISCOUNTS, displayName: "Shipping Discounts Function", description: "GraphQL schema for Shipping Discounts Function input queries", category: q.FUNCTION_GRAPHQL }, [ee.POLARIS_APP_HOME]: { name: ee.POLARIS_APP_HOME, displayName: "Polaris App Home", description: "Build your app's primary user interface embedded in the Shopify admin. If the prompt just mentions `Polaris` and you can't tell based off of the context what API they meant, assume they meant this API.", category: q.UI_FRAMEWORK, publicPackages: ["@shopify/polaris-types", "@shopify/app-bridge-types"] }, [ee.POLARIS_ADMIN_EXTENSIONS]: { name: ee.POLARIS_ADMIN_EXTENSIONS, displayName: "Polaris Admin Extensions", description: "Add custom actions and blocks from your app at contextually relevant spots throughout the Shopify Admin. Admin UI Extensions also supports scaffolding new adminextensions using Shopify CLI commands.", category: q.UI_FRAMEWORK, publicPackages: ["@shopify/ui-extensions"], extensionSurfaceName: "admin" }, [ee.POLARIS_CHECKOUT_EXTENSIONS]: { name: ee.POLARIS_CHECKOUT_EXTENSIONS, displayName: "Polaris Checkout Extensions", description: "Build custom functionality that merchants can install at defined points in the checkout flow, including product information, shipping, payment, order summary, and Shop Pay. Checkout UI Extensions also supports scaffolding new checkout extensions using Shopify CLI commands.", category: q.UI_FRAMEWORK, publicPackages: ["@shopify/ui-extensions"], extensionSurfaceName: "checkout" }, [ee.POLARIS_CUSTOMER_ACCOUNT_EXTENSIONS]: { name: ee.POLARIS_CUSTOMER_ACCOUNT_EXTENSIONS, displayName: "Polaris Customer Account Extensions", description: "Build custom functionality that merchants can install at defined points on the Order index, Order status, and Profile pages in customer accounts. Customer Account UI Extensions also supports scaffolding new customer account extensions using Shopify CLI commands.", category: q.UI_FRAMEWORK, publicPackages: ["@shopify/ui-extensions"], extensionSurfaceName: "customer-account" }, [ee.POS_UI]: { name: ee.POS_UI, displayName: "POS UI", description: "Build retail point-of-sale applications using Shopify's POS UI components. These components provide a consistent and familiar interface for POS applications. POS UI Extensions also supports scaffolding new POS extensions using Shopify CLI commands. Keywords: POS, Retail, smart grid", category: q.UI_FRAMEWORK, publicPackages: ["@shopify/ui-extensions"], extensionSurfaceName: "point-of-sale" }, [ee.HYDROGEN]: { name: ee.HYDROGEN, displayName: "Hydrogen", description: "Hydrogen storefront implementation cookbooks. Some of the available recipes are: B2B Commerce, Bundles, Combined Listings, Custom Cart Method, Dynamic Content with Metaobjects, Express Server, Google Tag Manager Integration, Infinite Scroll, Legacy Customer Account Flow, Markets, Partytown + Google Tag Manager, Subscriptions, Third-party API Queries and Caching. MANDATORY: Use this API for ANY Hydrogen storefront question - do NOT use Storefront GraphQL when 'Hydrogen' is mentioned.", category: q.UI_FRAMEWORK, publicPackages: ["@shopify/hydrogen"] }, [ee.STOREFRONT_WEB_COMPONENTS]: { name: ee.STOREFRONT_WEB_COMPONENTS, displayName: "Storefront Web Components", description: "HTML-first web components for building storefronts WITHOUT GraphQL. Choose when prompts mention: Web Components, HTML tags (<shopify-store>, <shopify-context>, <shopify-cart>, <shopify-variant-selector>, <shopify-money>), native <dialog>, 'HTML-only', 'without JavaScript', or 'no GraphQL'. Components handle data fetching and state internally.", category: q.UI_FRAMEWORK, featureFlag: "storefrontWebComponentsEnabled", publicPackages: ["@shopify/polaris-types", "@shopify/app-bridge-types"] }, [_s.LIQUID]: { name: _s.LIQUID, displayName: "Liquid", description: "Liquid is an open-source templating language created by Shopify. It is the backbone of Shopify themes and is used to load dynamic content on storefronts. Keywords: liquid, theme, shopify-theme, liquid-component, liquid-block, liquid-section, liquid-snippet, liquid-schemas, shopify-theme-schemas", category: q.THEME }, [vs.CUSTOM_DATA]: { name: vs.CUSTOM_DATA, displayName: "Custom Data", description: "MUST be used first when prompts mention Metafields or Metaobjects. Use Metafields and Metaobjects to model and store custom data for your app. Metafields extend built-in Shopify data types like products or customers, Metaobjects are custom data types that can be used to store bespoke data structures. Metafield and Metaobject definitions provide a schema and configuration for values to follow.", category: q.CONFIGURATION } };
var rt = ((t) => (t.SUCCESS = "success", t.FAILED = "failed", t.INFORM = "inform", t))(rt || {});
function lu(t, e) {
  return t.map((n) => e && n.includes("@shopify/ui-extensions") ? `import '${n}/${e}';` : `import '${n}';`).join(`
`);
}
function uu(t, e, n) {
  if (e.includes("@shopify/ui-extensions") && !n) throw new Error("Invalid input: extensionTarget is required");
  const s = lu(e, n);
  return t && (t.includes("const shopify =") || t.includes("globalThis.shopify")) ? s : `${s}
${e.find((i) => i.includes("@shopify/ui-extensions")) && n ? `const shopify = (globalThis as any).shopify as import('@shopify/ui-extensions/${n}').Api;` : ""}`.trim();
}
function du(t, e, n) {
  var s;
  if (t.includes("!DOCTYPE") || t.includes("!html")) {
    const i = (s = t.match(/<body>(.*?)<\/body>/s)) == null ? void 0 : s[1];
    i && (t = `<>${i}</>`);
  }
  return `
${uu(t, e, n)}
${t}
`;
}
const jr = (t) => ({ target: L.ScriptTarget.ESNext, module: L.ModuleKind.ESNext, jsx: L.JsxEmit.ReactJSX, jsxImportSource: t || "preact", strict: !0, esModuleInterop: !0, skipLibCheck: !0, moduleResolution: L.ModuleResolutionKind.NodeJs, allowSyntheticDefaultImports: !0, lib: ["es2020", "dom"], allowJs: !0, checkJs: !1 });
function pu() {
  const t = ha(import.meta.url);
  return k.resolve(t, "../..");
}
function hu(t, e) {
  const n = e.get(t);
  if (n) return L.ScriptSnapshot.fromString(n);
  try {
    const s = L.sys.readFile(t);
    return s ? L.ScriptSnapshot.fromString(s) : void 0;
  } catch {
    return;
  }
}
function fu(t, e, n) {
  return { getScriptFileNames: () => Array.from(t.virtualFiles.keys()), getScriptVersion: (s) => {
    var i;
    return ((i = t.fileVersions.get(s)) == null ? void 0 : i.toString()) || "0";
  }, getScriptSnapshot: (s) => hu(s, t.virtualFiles), getCurrentDirectory: () => e, getCompilationSettings: () => jr(n), getDefaultLibFileName: (s) => L.getDefaultLibFilePath(s), fileExists: (s) => t.virtualFiles.has(s) || L.sys.fileExists(s), readFile: (s) => t.virtualFiles.get(s) || L.sys.readFile(s), readDirectory: L.sys.readDirectory, getDirectories: L.sys.getDirectories, directoryExists: L.sys.directoryExists, getNewLine: () => `
` };
}
function mu(t) {
  const e = /* @__PURE__ */ new Map(), n = /* @__PURE__ */ new Map(), s = pu(), i = t == cu.HYDROGEN ? "react" : "preact", r = fu({ fileVersions: e, virtualFiles: n }, s, i), a = L.createLanguageService(r, L.createDocumentRegistry()), o = k.dirname(L.getDefaultLibFilePath(jr(i))), c = ["lib.es5.d.ts", "lib.es2020.d.ts", "lib.dom.d.ts"];
  for (const l of c) try {
    const u = k.join(o, l), p = L.sys.readFile(u);
    p && (n.set(u, p), e.set(u, 1));
  } catch {
  }
  return { languageService: a, servicesHost: r, fileVersions: e, virtualFiles: n };
}
function gu(t, e) {
  const n = (t.get(e) || 0) + 1;
  return t.set(e, n), n;
}
function Ae(t, e, n) {
  t.virtualFiles.set(e, n), gu(t.fileVersions, e);
}
const yu = ["a", "altGlyph", "altGlyphDef", "altGlyphItem", "animate", "animateColor", "animateMotion", "animateTransform", "animation", "audio", "canvas", "circle", "clipPath", "color-profile", "cursor", "defs", "desc", "discard", "ellipse", "feBlend", "feColorMatrix", "feComponentTransfer", "feComposite", "feConvolveMatrix", "feDiffuseLighting", "feDisplacementMap", "feDistantLight", "feDropShadow", "feFlood", "feFuncA", "feFuncB", "feFuncG", "feFuncR", "feGaussianBlur", "feImage", "feMerge", "feMergeNode", "feMorphology", "feOffset", "fePointLight", "feSpecularLighting", "feSpotLight", "feTile", "feTurbulence", "filter", "font", "font-face", "font-face-format", "font-face-name", "font-face-src", "font-face-uri", "foreignObject", "g", "glyph", "glyphRef", "handler", "hkern", "iframe", "image", "line", "linearGradient", "listener", "marker", "mask", "metadata", "missing-glyph", "mpath", "path", "pattern", "polygon", "polyline", "prefetch", "radialGradient", "rect", "script", "set", "solidColor", "stop", "style", "svg", "switch", "symbol", "tbreak", "text", "textArea", "textPath", "title", "tref", "tspan", "unknown", "use", "video", "view", "vkern"], _u = { NAMESPACE_USED_AS_VALUE: 2708 }, Je = { PROPERTY_NOT_EXIST: /Property '(\w+)' does not exist on type/, TYPE_NOT_ASSIGNABLE: /Type '(.+?)' is not assignable to type '(.+?)'/, PROPERTY: /[Pp]roperty '(\w+)'/, SHOPIFY_MODULE: /@shopify\//, MODULE_NOT_FOUND: /Invalid module name in augmentation/, INVALID_JSX_ELEMENT: /cannot be used as a JSX component|is not a valid JSX element type/, USED_BEFORE_BEING_DEFINED: /is used before being assigned/, IMPLICITLY_HAS_AN_ANY_TYPE: /implicitly has an 'any' type./ };
function vu(t) {
  return xl.includes(t);
}
function Tu(t) {
  return yu.includes(t);
}
function Iu(t) {
  const e = [];
  return L.forEachChild(t, function n(s) {
    if (L.isJsxOpeningElement(s) || L.isJsxSelfClosingElement(s)) {
      const i = s.tagName.getText(t), r = s.getStart(t), a = s.getEnd();
      e.push({ tagName: i, node: s, start: r, end: a });
    }
    L.forEachChild(s, n);
  }), e;
}
function en(t) {
  return { componentName: t, valid: !0, errors: [], skipped: !0 };
}
function Jn(t, e) {
  return { componentName: t, valid: !1, errors: [{ property: "element", message: e === "custom" ? `Custom component '${t}' is not allowed. UI extensions must only use Shopify Polaris web components. If this is a wrapper component, make sure to import it.` : `${e} element '${t}' is not allowed. UI extensions must only use Shopify Polaris web components.` }] };
}
function bu(t) {
  return t.replace(/\./g, "");
}
function Su(t, e, n, s, i) {
  const r = bu(t);
  return vu(r) ? i ? Jn(t, "HTML") : en(t) : Tu(r) ? i ? Jn(t, "SVG") : en(t) : e.has(r) ? null : i ? n.has(r) || s.has(r) ? en(t) : Jn(t, "custom") : en(t);
}
function Nu(t) {
  return !t.startsWith("@shopify/");
}
function Eu(t, e) {
  t.name && e.add(t.name.text);
}
function Ou(t, e) {
  const { namedBindings: n } = t;
  if (n && L.isNamedImports(n)) for (const s of n.elements) e.add(s.name.text);
}
function wu(t, e) {
  Eu(t, e), Ou(t, e);
}
function Au(t) {
  const { moduleSpecifier: e } = t;
  return L.isStringLiteral(e) ? e.text : null;
}
function xu(t) {
  const e = /* @__PURE__ */ new Set();
  return L.forEachChild(t, function n(s) {
    L.isImportDeclaration(s) && Cu(s, e), L.forEachChild(s, n);
  }), e;
}
function Cu(t, e) {
  const n = Au(t);
  if (!n || !Nu(n)) return;
  const { importClause: s } = t;
  s && wu(s, e);
}
function Zn(t) {
  return /^[A-Z]/.test(t);
}
function ku(t) {
  const e = /* @__PURE__ */ new Set();
  return L.forEachChild(t, function n(s) {
    if (L.isFunctionDeclaration(s) && s.name) {
      const i = s.name.text;
      Zn(i) && e.add(i);
    }
    if (L.isVariableStatement(s)) {
      for (const i of s.declarationList.declarations) if (L.isIdentifier(i.name) && i.initializer && (L.isArrowFunction(i.initializer) || L.isFunctionExpression(i.initializer))) {
        const r = i.name.text;
        Zn(r) && e.add(r);
      }
    }
    if (L.isClassDeclaration(s) && s.name) {
      const i = s.name.text;
      Zn(i) && e.add(i);
    }
    L.forEachChild(s, n);
  }), e;
}
function Ru(t, e, n, s = {}) {
  const { enforceShopifyOnlyComponents: i = !1 } = s, r = [], a = /* @__PURE__ */ new Set(), o = L.createSourceFile("temp.tsx", t, L.ScriptTarget.Latest, !0, L.ScriptKind.TSX), c = Iu(o), l = i ? xu(o) : /* @__PURE__ */ new Set(), u = i ? ku(o) : /* @__PURE__ */ new Set();
  for (const { tagName: p, start: f, end: S } of c) {
    const v = Su(p, n, l, u, i);
    if (v) {
      r.push(v);
      continue;
    }
    const { errors: A, handledDiagnostics: O } = Du(f, S, e);
    O.forEach((M) => a.add(M)), r.push({ componentName: p, valid: A.length === 0, errors: A });
  }
  return { validations: r, genericErrors: e.filter((p) => !a.has(p)).filter(Vr).map((p) => ({ message: L.flattenDiagnosticMessageText(p.messageText, `
`), code: p.code, start: p.start, end: p.start !== void 0 && p.length !== void 0 ? p.start + p.length : void 0 })) };
}
function Vr(t) {
  if (t.start === void 0 || t.length === void 0) return !1;
  const e = L.flattenDiagnosticMessageText(t.messageText, `
`);
  return t.code !== _u.NAMESPACE_USED_AS_VALUE && !(e.includes("Cannot find module") && !e.match(Je.SHOPIFY_MODULE)) && !(e.match(Je.MODULE_NOT_FOUND) || e.match(Je.USED_BEFORE_BEING_DEFINED) || e.match(Je.INVALID_JSX_ELEMENT) || e.match(Je.IMPLICITLY_HAS_AN_ANY_TYPE));
}
function Lu(t, e, n) {
  if (!Vr(t)) return !1;
  const s = t.start, i = t.start + t.length;
  return s >= e && i <= n;
}
function Du(t, e, n) {
  const s = [], i = [], r = n.filter((a) => Lu(a, t, e));
  for (const a of r) {
    const o = Pu(a, L.flattenDiagnosticMessageText(a.messageText, `
`));
    o && (s.push(o), i.push(a));
  }
  return { errors: s, handledDiagnostics: i };
}
function Pu(t, e) {
  let n, s, i = "";
  const r = e.match(Je.PROPERTY_NOT_EXIST);
  if (r) i = r[1];
  else {
    const a = e.match(Je.TYPE_NOT_ASSIGNABLE), o = e.match(Je.PROPERTY);
    a && (s = a[1], n = a[2]), o && (i = o[1]);
  }
  return { property: i || "unknown", message: e, expected: n, actual: s };
}
function Fu(t, e = []) {
  const n = [], s = [], i = [];
  for (const o of t) if (o.valid) o.skipped ? i.push(o.componentName) : s.push(o.componentName);
  else for (const c of o.errors) n.push(`${o.componentName} validation failed: Property '${c.property}': ${c.message}`);
  for (const o of e) n.push(o.message);
  let r, a;
  return n.length === 0 ? (a = rt.SUCCESS, r = s.length > 0 ? `All components validated successfully by TypeScript. Found components: ${Array.from(new Set(s)).join(", ")}.` : "No components found to validate by TypeScript.") : (a = rt.FAILED, r = `Validation errors:
${n.join(`
`)}`), i.length > 0 && (r += `

Try and use component from Shopify Polaris components. Non-Shopify components (not validated):
${i.map((o) => `  - ${o}`).join(`
`)}`), { result: a, resultDetail: r };
}
function Uu(t, e) {
  try {
    return e.resolve(`${t}/package.json`);
  } catch {
    const n = e.resolve.paths(t);
    if (n) for (const s of n) {
      const i = k.join(s, t), r = k.join(i, "package.json");
      try {
        return Te.accessSync(r), r;
      } catch {
        continue;
      }
    }
    throw new Error(`Cannot find package '${t}'`);
  }
}
function Mu(t, e) {
  const n = t.split(k.sep);
  if (e.startsWith("@")) {
    const [i, r] = e.split("/"), a = n.findIndex((o, c) => o === i && n[c + 1] === r);
    return a !== -1 ? n.slice(0, a + 2).join(k.sep) : null;
  }
  const s = n.indexOf(e);
  return s !== -1 ? n.slice(0, s + 1).join(k.sep) : null;
}
function qr(t) {
  const e = Ci(import.meta.url);
  let n;
  try {
    n = e.resolve(t);
  } catch (i) {
    if (i.code !== "ERR_PACKAGE_PATH_NOT_EXPORTED" && i.code !== "MODULE_NOT_FOUND") throw i;
    n = Uu(t, e);
  }
  if (!n) throw new Error(`Failed to resolve package path for ${t}`);
  const s = Mu(n, t);
  if (!s) throw new Error(`Could not find package name "${t}" in resolved path: ${n}`);
  return s;
}
function Ht(t, e) {
  if (!e) return [];
  switch (e) {
    case "@shopify/polaris-types":
    case "@shopify/ui-extensions":
      return $u(t);
    case "@shopify/app-bridge-types":
      return ju(t);
    case "@shopify/hydrogen":
      return Vu(t);
    default:
      return [];
  }
}
function $u(t) {
  const e = [], n = /declare\s+const\s+tagName\$?\w*\s*=\s*['"]([^'"]+)['"]/g, s = /\[['"]([a-z]+-[a-z-]+)['"]\]\s*:/g;
  let i;
  for (; (i = n.exec(t)) !== null || (i = s.exec(t)) !== null; ) e.push(i[1]);
  return [...new Set(e)];
}
function ju(t) {
  const e = [], n = t.match(/interface\s+AppBridgeElements\s*\{([^}]+)\}/);
  if (n) {
    const s = /['"]([a-z]+-[a-z-]+)['"]\s*:/g;
    let i;
    for (; (i = s.exec(n[1])) !== null; ) e.push(i[1]);
  }
  return e;
}
function Vu(t) {
  const e = [];
  let n;
  const s = /declare\s+function\s+(\w+)\s*(?:<[^>]*>)?\s*\([^)]*\)\s*:\s*(?:react_jsx_runtime\.)?JSX\.Element/g;
  for (; (n = s.exec(t)) !== null; ) e.push(n[1]);
  const i = /declare\s+function\s+(\w+)\s*(?:<[^>]*>)?\s*\([^)]*\)\s*:\s*ReturnType<FC>/g;
  for (; (n = i.exec(t)) !== null; ) e.push(n[1]);
  const r = /declare\s+function\s+(\w+)\s*(?:<[^>]*>)?\s*\([^)]*\)\s*:\s*react\.FunctionComponentElement/g;
  for (; (n = r.exec(t)) !== null; ) e.push(n[1]);
  const a = /declare\s+const\s+(\w+)\s*:\s*react\.ForwardRefExoticComponent/g;
  for (; (n = a.exec(t)) !== null; ) e.push(n[1]);
  const o = /declare\s+const\s+(\w+)\s*:\s*react\.Provider/g;
  for (; (n = o.exec(t)) !== null; ) e.push(n[1]);
  return [...new Set(e)];
}
class st extends Error {
  constructor(e, n) {
    super(n), this.packageName = e, this.name = "MissingPackageError";
  }
}
async function qu(t, e, n, s) {
  const i = [], r = [], a = /* @__PURE__ */ new Set(), o = async (c, l) => {
    try {
      await Bu(c, e, l, a);
    } catch (u) {
      if (!(u instanceof st)) throw u;
      r.push(u.packageName), i.push(u.packageName);
    }
  };
  for (const c of t) {
    let l;
    if (c === "@shopify/ui-extensions" && n) {
      if (s) {
        await Gu(c, e, n, s, a);
        continue;
      }
      l = k.join("build", "ts", "surfaces", n);
    } else if (c === "@shopify/polaris-types") l = k.join("dist", "polaris.d.ts");
    else if (c === "@shopify/app-bridge-types") l = "dist";
    else if (c === "@shopify/hydrogen") {
      l = k.join("dist", "production", "index.d.ts"), await o(c, l), await o("@shopify/hydrogen-react"), await o("react-router"), await o("@react-router/dev"), await o("graphql"), await o("type-fest"), await o("schema-dts");
      continue;
    }
    await o(c, l);
  }
  return await o("preact"), await o("@types/react"), { missingPackages: i, searchedPaths: r, shopifyWebComponents: a };
}
async function Gu(t, e, n, s, i) {
  let r;
  try {
    r = qr(t);
  } catch (A) {
    throw new st(t, `Failed to load package ${t}: ${A instanceof Error ? A.message : String(A)}`);
  }
  const a = k.join(r, "package.json");
  Ae(e, a, await fe.readFile(a, "utf-8"));
  const o = k.join(r, "build", "ts", "surfaces", n), c = k.join(o, "targets", `${s}.d.ts`);
  let l;
  try {
    l = await fe.readFile(c, "utf-8");
  } catch {
    const A = await Sn(o);
    for (const O of A) {
      const M = await fe.readFile(O, "utf-8");
      Ae(e, O, M);
      for (const W of Ht(M, t)) i.add(W);
    }
    return;
  }
  const u = Hu(l), p = k.join(o, "components");
  for (const A of u) {
    const O = k.join(p, `${A}.d.ts`);
    try {
      const M = await fe.readFile(O, "utf-8");
      Ae(e, O, M);
      for (const W of Ht(M, t)) i.add(W);
    } catch {
    }
  }
  const f = k.join(p, "components-shared.d.ts");
  try {
    Ae(e, f, await fe.readFile(f, "utf-8"));
  } catch {
  }
  const S = ["api", "types", "event"];
  for (const A of S) {
    const O = k.join(o, A);
    try {
      const M = await Sn(O);
      for (const W of M)
        Ae(e, W, await fe.readFile(W, "utf-8"));
    } catch {
    }
  }
  const v = ["extension-targets.d.ts", "globals.d.ts", "api.d.ts"];
  for (const A of v) {
    const O = k.join(o, A);
    try {
      Ae(e, O, await fe.readFile(O, "utf-8"));
    } catch {
    }
  }
}
function Hu(t) {
  const e = [], n = /import\s+['"]\.\.\/components\/(\w+)\.d\.ts['"]/g;
  let s;
  for (; (s = n.exec(t)) !== null; ) e.push(s[1]);
  return e;
}
async function Bu(t, e, n, s) {
  let i;
  try {
    i = qr(t);
  } catch (r) {
    throw r instanceof st ? r : new st(t, `Failed to load package ${t}: ${r instanceof Error ? r.message : String(r)}`);
  }
  try {
    const r = k.join(i, "package.json"), a = await fe.readFile(r, "utf-8"), o = JSON.parse(a);
    if (o.name !== t) throw new st(t, `Found package.json but name mismatch: expected "${t}", got "${o.name}"`);
    if (Ae(e, r, a), n) {
      const c = k.join(i, n);
      if ((await fe.stat(c)).isDirectory()) {
        const l = await Sn(c);
        for (const u of l) {
          const p = await fe.readFile(u, "utf-8");
          Ae(e, u, p);
          for (const f of Ht(p, t)) s && s.add(f);
        }
      } else await Gr(c, i, e, /* @__PURE__ */ new Set(), s, t);
    } else {
      const c = await Sn(i);
      for (const l of c) {
        const u = await fe.readFile(l, "utf-8");
        Ae(e, l, u);
        for (const p of Ht(u, t)) s && s.add(p);
      }
    }
  } catch (r) {
    throw r instanceof st ? r : new st(t, `Failed to load package ${t}: ${r instanceof Error ? r.message : String(r)}`);
  }
}
async function Gr(t, e, n, s = /* @__PURE__ */ new Set(), i, r) {
  const a = k.resolve(t);
  if (s.has(a)) return;
  let o;
  s.add(a);
  try {
    o = await fe.readFile(a, "utf-8");
  } catch {
    return;
  }
  if (Ae(n, a, o), i) for (const u of Ht(o, r)) i.add(u);
  const c = Qu(o), l = k.dirname(a);
  for (const u of c) if (u.startsWith("./") || u.startsWith("../")) {
    let p = k.resolve(l, u);
    if (p = p.replace(/\.js$/, ""), !p.endsWith(".d.ts") && !p.endsWith(".ts")) {
      const f = [p + ".d.ts", p + ".ts", k.join(p, "index.d.ts")];
      if (p = await Yu(f), !p) continue;
    }
    await Gr(p, e, n, s, i, r);
  }
}
function Qu(t) {
  const e = [], n = /(?:import|export)(?:\s+type)?\s+(?:(?:[^'"]*)\s+from\s+)?['"]([^'"]+)['"]/g;
  let s;
  for (; (s = n.exec(t)) !== null; ) e.push(s[1]);
  return e;
}
async function Sn(t) {
  const e = [];
  return await (async function n(s, i = 0) {
    if (i > 5) return;
    const r = await fe.readdir(s, { withFileTypes: !0 });
    for (const a of r) {
      const o = k.join(s, a.name);
      a.isDirectory() && !a.name.startsWith(".") && a.name !== "node_modules" ? await n(o, i + 1) : a.isFile() && (a.name.endsWith(".d.ts") || a.name.endsWith(".ts")) && e.push(o);
    }
  })(t), e;
}
async function Yu(t) {
  for (const e of t) try {
    return await fe.access(e), e;
  } catch {
  }
  return null;
}
const Wu = [ee.POLARIS_ADMIN_EXTENSIONS, ee.POLARIS_CHECKOUT_EXTENSIONS, ee.POLARIS_CUSTOMER_ACCOUNT_EXTENSIONS, ee.POS_UI];
async function zu(t) {
  try {
    const { code: e, apiName: n, extensionTarget: s } = t;
    if (!e) return { result: rt.FAILED, resultDetail: "Validation failed: Invalid input: code is required" };
    if (Object.keys(Gt).filter((O) => Gt[O].extensionSurfaceName).includes(n) && !s) return { result: rt.FAILED, resultDetail: `Extension target is required for API: ${n}. Use the learn_shopify_ui_extensions tool to get the list of available extension targets.` };
    const i = Ju(n), r = mu(n), a = i.publicPackages ?? [], { missingPackages: o, searchedPaths: c, shopifyWebComponents: l } = await qu(a, r, i.extensionSurfaceName, s);
    if (o.length > 0) {
      const O = o.map((z) => `  - ${z}`).join(`
`), M = `npm install -D ${o.join(" ")}`, W = c.map((z) => `  - ${z}`).join(`
`);
      return { result: rt.FAILED, resultDetail: `Missing required dev dependencies:
${O}

Searched paths:
${W}

Please install them using:
${M}` };
    }
    const u = `validation-${Date.now()}.tsx`, p = du(e, a, s);
    Ae(r, u, p);
    const f = r.languageService.getSemanticDiagnostics(u), S = Wu.includes(n), { validations: v, genericErrors: A } = Ru(p, f, l, { enforceShopifyOnlyComponents: S });
    return Fu(v, A);
  } catch (e) {
    return { result: rt.FAILED, resultDetail: `Validation failed: ${e instanceof Error ? e.message : String(e)}` };
  }
}
function Ju(t) {
  if (!t) throw new Error("Invalid input: apiName is required");
  const e = Object.values(Gt).find((n) => n.name === t);
  if (!e) throw new Error(`Unknown API: ${t}`);
  if (!e.publicPackages || e.publicPackages.length === 0) throw new Error(`No packages configured for API: ${t}`);
  return e;
}
process.env.DEV && process.env.DEV;
function Zu(t, e, n) {
  var l;
  const { extensionTarget: s, surface: i } = t, r = (l = e.typesVersions) == null ? void 0 : l["*"];
  if (r) {
    for (const [u, p] of Object.entries(r))
      if (u === s || u === `${s}.*`) {
        const f = Array.isArray(p) ? p[0] : p;
        return k.join(n, f);
      }
  }
  const a = k.join(
    "build",
    "ts",
    "surfaces",
    i,
    "targets"
  ), o = k.join(
    n,
    a,
    `${s}.d.ts`
  );
  if (Te.existsSync(o))
    return o;
  const c = k.join(
    n,
    "build",
    "ts",
    "surfaces",
    i,
    `${s}.d.ts`
  );
  return Te.existsSync(c) ? c : null;
}
function Hr(t, e, n, s) {
  const i = {
    types: [],
    imports: /* @__PURE__ */ new Set(),
    processedFiles: new Set(s)
  };
  if (s.has(t))
    return i;
  if (i.processedFiles.add(t), !Te.existsSync(t))
    return console.warn(`File not found: ${t}`), i;
  const r = Te.readFileSync(t, "utf-8"), a = Xu(r, t);
  a && i.types.push(a);
  const o = Ku(r);
  for (const c of o) {
    i.imports.add(c);
    const l = ed(
      c,
      t,
      e,
      n
    );
    if (l && !i.processedFiles.has(l)) {
      const u = Hr(
        l,
        e,
        n,
        i.processedFiles
      );
      i.types.push(...u.types), u.imports.forEach((p) => i.imports.add(p)), u.processedFiles.forEach(
        (p) => i.processedFiles.add(p)
      );
    }
  }
  return i;
}
function Xu(t, e) {
  if (t.split(`
`).filter((a) => {
    const o = a.trim();
    return o && !o.startsWith("import ") && !o.startsWith("//") && !o.startsWith("/*") && !o.startsWith("*");
  }).length === 0)
    return null;
  const i = k.basename(e), r = t.split(`
`).filter((a) => {
    const o = a.trim();
    return o && (o.startsWith("export") || o.startsWith("interface") || o.startsWith("type") || o.startsWith("declare") || o.startsWith("class") || o.includes("Props") || o.includes("Api") || o.includes("{") || o.includes("}") || o.includes(";"));
  }).join(`
`);
  return r.trim() ? `// ${i}
${r}` : null;
}
function Ku(t) {
  const e = [], n = /import\s+(?:(?:\{[^}]*\}|[^"']+)\s+from\s+)?["']([^"']+)["']/g;
  let s;
  for (; (s = n.exec(t)) !== null; ) {
    const i = s[1];
    (i.startsWith(".") || i.includes("components/")) && e.push(i);
  }
  return e;
}
function ed(t, e, n, s) {
  const i = k.dirname(e);
  let r;
  if (t.startsWith(".") || t.startsWith("../") ? r = k.resolve(i, t) : r = k.join(
    n,
    "build",
    "ts",
    "surfaces",
    s,
    t
  ), !r.endsWith(".d.ts") && !r.endsWith(".ts") && (r += ".d.ts"), Te.existsSync(r))
    return r;
  const a = r.replace(/\.d\.ts$/, "");
  if (Te.existsSync(a))
    return a;
  const o = k.join(a, "index.d.ts");
  return Te.existsSync(o) ? o : null;
}
function td(t, e) {
  const n = [];
  n.push(
    `## Components and APIs available for Extension Target: ${t}`
  ), n.push(""), n.push("### Overview"), n.push(
    `This extension target provides access to ${e.imports.size} components.`
  ), n.push(""), n.push("### Complete Type Definitions:"), n.push(""), n.push("```typescript");
  const s = new Set(e.types), i = Array.from(s).filter((r) => r && r.trim()).sort((r, a) => {
    var l, u;
    const o = ((l = r.match(/^\/\/ (.+)$/m)) == null ? void 0 : l[1]) || "", c = ((u = a.match(/^\/\/ (.+)$/m)) == null ? void 0 : u[1]) || "";
    return o.localeCompare(c);
  });
  return n.push(i.join(`

`)), n.push("```"), n.push(""), n.push("### Usage Guidelines:"), n.push(""), n.push(
    "1. All components listed above are available for use in this extension target."
  ), n.push(
    "2. Each component has specific props as defined in the type definitions above."
  ), n.push(
    "3. The Api type provides access to extension-specific functionality."
  ), n.push(
    "4. Use the type definitions as a reference for available properties and methods."
  ), n.join(`
`);
}
function Rn(t) {
  const { packageName: e, extensionTarget: n, surface: s } = t, i = Ds(e);
  if (!i)
    throw new Error(
      `Could not resolve ${e} package. Please ensure it is installed.`
    );
  const r = Object.entries(Gt).find(
    ([c, l]) => l.extensionSurfaceName === s
  );
  if (!r)
    throw new Error(
      `Surface "${s}" does not have a corresponding API configuration.`
    );
  const [a] = r;
  try {
    const { description: c, targets: l } = sd(a, i);
    if (!n || n.trim() === "")
      return `No extension target specified. You must specify an extension target before generating code.

${c}`;
    if (!l.map((p) => p.name).includes(n))
      return `Extension target "${n}" is not valid for ${a}.

${c}`;
  } catch (c) {
    return `Error reading extension targets for ${a}: ${c instanceof Error ? c.message : String(c)}`;
  }
  const o = k.join(i, "package.json");
  try {
    const c = JSON.parse(Te.readFileSync(o, "utf-8"));
    if (c.name !== e)
      return `Found package.json but name mismatch: expected "${e}", got "${c.name}"`;
    const l = Zu(t, c, i);
    if (!l)
      return `Could not find type definitions for extension target: ${n}`;
    const u = Hr(
      l,
      i,
      t.surface,
      /* @__PURE__ */ new Set()
    );
    return td(n, u);
  } catch (c) {
    return console.error("Error extracting type information:", c), `Error extracting type information for ${n}: ${c}`;
  }
}
function nd(t) {
  const e = [], n = t.split(`
`);
  let s = [], i = !1;
  for (let r = 0; r < n.length; r++) {
    const o = n[r].trim();
    if (o.startsWith("/**")) {
      i = !0, s = [];
      continue;
    }
    if (o.endsWith("*/") && i) {
      i = !1;
      continue;
    }
    if (i && o.startsWith("*")) {
      const l = o.substring(1).trim();
      l && s.push(l);
      continue;
    }
    const c = o.match(/^['"]([^'"]+)['"]\s*:/);
    if (c) {
      const l = c[1], u = s.join(" ").trim();
      e.push({
        name: l,
        description: u
      }), s = [];
    }
  }
  return e;
}
function sd(t, e) {
  const n = Gt[t];
  if (!n)
    throw new Error(
      `API name "${t}" is not recognized. Please provide a valid Shopify API name.`
    );
  const s = n.extensionSurfaceName;
  if (!s)
    throw new Error(
      `API "${t}" does not have extension targets. Only UI extension APIs have extension targets.`
    );
  const i = k.join(
    e,
    "build/ts/surfaces",
    s,
    "extension-targets.d.ts"
  );
  if (!Te.existsSync(i))
    throw new Error(
      `Could not find extension targets file at: ${i}`
    );
  const r = Te.readFileSync(i, "utf-8"), a = nd(r);
  if (a.length === 0)
    throw new Error(`No extension targets found in ${i}`);
  const o = [];
  o.push(`## Available Extension Targets for ${t}`), o.push(""), o.push(`Surface: **${s}**`), o.push(`Total Targets: **${a.length}**`), o.push(""), o.push("---"), o.push("");
  const c = /* @__PURE__ */ new Map();
  return a.forEach((u) => {
    const p = u.name.split("."), f = p.slice(0, -2).join(".") || p[0];
    c.has(f) || c.set(f, []), c.get(f).push(u);
  }), Array.from(c.entries()).sort(
    (u, p) => u[0].localeCompare(p[0])
  ).forEach(([u, p]) => {
    o.push(`### ${u}`), o.push(""), p.forEach((f) => {
      o.push(`#### \`${f.name}\``), o.push(""), f.description && (o.push(f.description), o.push(""));
    });
  }), o.push("---"), o.push(""), o.push("### Usage Notes"), o.push(""), o.push(
    "- Use the exact target name (in quotes) when registering your extension with `shopify.extend()`"
  ), o.push(
    "- Each target receives specific API interfaces and component access"
  ), {
    description: o.join(`
`),
    targets: a
  };
}
const id = `
This is the relevant documentation for building with Admin Extensions:

- /docs/api/admin-extensions/extension-targets
- /docs/api/admin-extensions/api
- /docs/api/admin-extensions/network-features
- /docs/api/admin-extensions/components

Admin Extensions integrate into the Shopify admin at contextual locations for merchant workflows.
Admin actions are a UI extension that you can use to create transactional workflows within existing pages of the Shopify admin. Merchants can launch these UI extensions from the More actions menus on resource pages or from an index table's bulk action menu when one or more resources are selected. After the UI extensions are launched, they display as modals. After they're closed, the page updates with the changes from the action.

## IMPORTANT : ALWAYS USE THE CLI TO SCAFFOLD A NEW EXTENSION

Shopify CLI generates templates that aligns with the latest available version and is not prone to errors. ALWAYS use the CLI Command to Scaffold a new Admin UI extension

CLI Command to Scaffold a new Admin Action Extension

\`\`\`bash
shopify app generate extension --template admin_action --name my-admin-action
\`\`\`

 Admin blocks are built with UI extensions and enable your app to embed contextual information and inputs directly on resource pages in the Shopify admin. When a merchant has added them to their pages, these UI extensions display as cards inline with the other resource information. Merchants need to manually add and pin the block to their page in the Shopify admin before they can use it.
 With admin blocks, merchants can view and modify information from your app and other data on the page simultaneously. To facilitate complex interactions and transactional changes, you can launch admin actions directly from admin blocks.

 CLI Command to Scaffold a new Admin Block Extension:

 \`\`\`bash
 shopify app generate extension --template admin_block --name my-admin-block
 \`\`\`

 Admin link extensions let you direct merchants from pages in the Shopify admin to related, complex workflows in your app. For example, the Shopify Flow app has an admin link extension that directs merchants to a page of the app where they can run an automation for any order:
\`\`\`bash
shopify app generate extension --template admin_link --name admin-link-extension
\`\`\`

Admin print actions are a special form of UI extension designed to let your app print documents from key pages in the Shopify admin. Unlike typical actions provided by UI extensions, admin print actions are found under the Print menu on orders and product pages. Additionally, they contain special APIs to let your app display a preview of a document and print it.
CLI Command to Scaffold a new Admin Print Action Extension:

\`\`\`bash
shopify app generate extension --template admin_print --name my-admin-print-extension
\`\`\`
`;
function Br(t) {
  const e = me(
    "polaris-admin-extensions",
    id
  ), n = Rn({
    packageName: "@shopify/ui-extensions",
    surface: "admin",
    extensionTarget: t
  });
  return t ? n : e + `

` + n + `

YOU MUST CALL learn_extension_target_types with the following arguments:
  - learn_extension_target_types(api: "polaris-admin-extensions", extension_target: <extension target appropriate for the prompt/extension you are building>)
  A target represents where your admin UI extension will appear.
  The target decides how a component/API can be used and what are the valid prop/API values. Calling this tool will give the valid values for the target you are building for`;
}
const rd = `
This is the relevant documentation for building Checkout Extensions:

- /docs/api/checkout-ui-extensions/targets
- /docs/api/checkout-ui-extensions/apis
- /docs/api/checkout-ui-extensions/configuration
- /docs/api/checkout-ui-extensions/polaris-web-components
- /docs/api/checkout-ui-extensions/error-handling

Checkout UI extensions let app developers build custom functionality that merchants can install at defined points in the checkout flow, including product information, shipping, payment, order summary, and Shop Pay.

## IMPORTANT : ALWAYS USE THE CLI TO SCAFFOLD A NEW EXTENSION

Shopify CLI generates templates that aligns with the latest available version and is not prone to errors. ALWAYS use the CLI Command to Scaffold a new Checkout UI extension

CLI Command to Scaffold a new Checkout UI Extension:

\`\`\`bash
shopify app generate extension --template checkout_ui --name my-checkout-ui-extension
\`\`\`

`;
function Qr(t) {
  const e = me(
    "polaris-checkout-extensions",
    rd
  ), n = Rn({
    packageName: "@shopify/ui-extensions",
    surface: "checkout",
    extensionTarget: t
  });
  return t ? n : e + `

` + n + `

YOU MUST CALL learn_extension_target_types with the following arguments:
  - learn_extension_target_types(api: "polaris-checkout-extensions", extension_target: <extension target appropriate for the prompt/extension you are building>)
  A target represents where your checkout UI extension will appear.
  The target decides how a component/API can be used and what are the valid prop/API values. Calling this tool will give the valid values for the target you are building for`;
}
const ad = `
This is the relevant documentation for building with Customer Account Extensions:

- /docs/api/customer-account-ui-extensions/targets
- /docs/api/customer-account-ui-extensions/apis
- /docs/api/customer-account-ui-extensions/configuration
- /docs/api/customer-account-ui-extensions/polaris-web-components
- /docs/api/customer-account-ui-extensions/error-handling

Customer account UI extensions let app developers build custom functionality that merchants can install at defined points on the Order index, Order status, and Profile pages in customer accounts.

CLI Command to Scaffold a new Customer Account UI Extension:

\`\`\`bash
shopify app generate extension --template=customer_account_ui --name=my_customer_account_ui_extension
\`\`\`

`;
function Yr(t) {
  const e = me(
    "polaris-customer-account-extensions",
    ad
  ), n = Rn({
    packageName: "@shopify/ui-extensions",
    surface: "customer-account",
    extensionTarget: t
  });
  return t ? n : e + `

` + n + `

YOU MUST CALL learn_extension_target_types with the following arguments:
  - learn_extension_target_types(api: "polaris-customer-account-extensions", extension_target: <extension target appropriate for the prompt/extension you are building>)
  A target represents where your customer account UI extension will appear.
  The target decides how a component/API can be used and what are the valid prop/API values. Calling this tool will give the valid values for the target you are building for`;
}
const od = `<system-instructions>
You are an expert Shopify POS UI Extensions developer generating production-ready, type-safe Preact code that extends POS functionality while maintaining performance, security, and user experience standards. All code examples in this document are illustrative only. ALWAYS verify actual API documentation before using any method, component, or property
🚨 MANDATORY - ZERO EXCEPTIONS: Add \`pos-extension-todo-flow\` as to-dos and execute from step 1 down (every step REQUIRED) in EXACT order when asked to create a POS UI Extension. Prioritize this over any prior/existing to-dos. Run CLI commands exactly, only replacing placeholders

# Core Principles

- **Mobile environment**: POS runs in a sandboxed mobile environment with limited API access. Avoid browser globals like \`window\` and \`document\`
- **API functions**: Use the global \`shopify\` object to access extension APIs. The typing for the shopify object (which APIs are accessible) is dependent on the extension point and is defined in the \`shopify.d.ts\` file which auto-generates when running \`shopify app build\`. NEVER update any \`*.d.ts\` especially \`shopify.d.ts\`
- **Navigation API**: Use the global \`navigation\` object to access Navigation API and use switch statements to route between urls using \`navigation.currentEntry.url\`
- **Extension generation**: ALWAYS create with Shopify CLI: \`shopify app generate extension\`. CLI versions >=3.85.3 default to Preact extensions and must be used. If using an unsupported version, upgrade to the latest version
- **Deployments**: DO NOT DEPLOY for the user under any circumstances. Never ever run \`shopify app deploy\`
- **React Router template**: Assume the user is using \`extensions-only\` template and NOT React Router template unless explicitly stated. When looking at examples in the documentation, determine if the example is using React Router template or not (typically React Router apps have \`app/routes\` directory). ⚠️ DO NOT follow the React Router template examples if the user is using extensions-only template.
- **Components**: Only use Polaris Web Components. NEVER use any unlisted components, like \`div\`, \`span\`, \`button\`, etc.

🚨 MANDATORY: ALWAYS USE THE CLI TO SCAFFOLD A NEW EXTENSION AND NEVER MANUALLY CREATE THE APP STRUCTURE OR CONFIGURATION FILES. ALWAYS use CLI to scaffold new extensions. NEVER manually create app structure or configuration files. If any CLI command fails (non-zero exit code) or environment is non-interactive, STOP, print the exact command, and instruct the user to run it locally.

🚨 MANDATORY: Ignore all existing extensions in the app. Only generate new extension. DO NOT modify existing extensions.

# Grammar and Synonyms

Understand these equivalent terms when processing requests:
- **Extension targets**: extension points, targets, target locations, embed points, integration points
- **APIs**: interfaces, methods, functions, capabilities
- **Extension components**: UI components, interface elements, widgets
- **Direct API**: native API, platform API, system API
- **Extension**: add-on, plugin, module, integration
- **POS**: Point of Sale, point-of-sale
- **POS screen**: screen, page, area in POS
- **GraphQL Admin API**: admin API, admin graphql

# Project Structure

⚠️ NEVER manually create the project structure. Always use the CLI to scaffold the project structure. Run \`shopify app init --template=none --name={{appropriate-app-name}}\`
A typical POS UI Extension structure:
\`\`\`
{{application-name}}/
├── extensions/
│   └── {{extension-name}}/
│       ├── shopify.d.ts # Build artifact which defines typing for the global Shopify object and imports ui-extensions type definitions
│       ├── shopify.extension.toml # Extension configuration
│       └── src/
│           ├── {{Modal.tsx}}
│           └── {{Tile.tsx}}
└── shopify.app.*.toml # App configuration
\`\`\`

# Create POS UI extension flow

<pos-extension-todo-flow>
<step id="1">
  Determine if Shopify CLI is installed
  <step id="1.1">If not installed: Install @shopify/cli@latest using the package manager</step>
  <step id="1.2">If installed: Run \`shopify --version\` to verify the CLI is above 3.85.3. If it's not, upgrade to @shopify/cli@latest using the package manager</step>
</step>
<step id="2">
  Determine if working with new app or existing app
  <step id="2.1">
    If existing app:
    <step id="2.1.1">\`cd\` into the app directory</step>
  </step>
  <step id="2.2">
    If no existing app:
    <step id="2.2.1">Run \`shopify app init --template=none --name={{appropriate-app-name}}\`</step>
    <step id="2.2.2">\`cd\` into the app directory</step>
  </step>
  <step id="2.3">
    <step id="2.3.1">Ignore all existing extensions in the app. Only generate new extension. DO NOT modify existing extensions.</step>
    <step id="2.3.2">Run \`shopify app generate extension --name="{{appropriate-extension-name}}" --template="{{appropriate-template|default-pos_smart_grid}}"\` (template options: pos_action|pos_block|pos_smart_grid) ⚠️ \`--yes\` is NOT a flag. DO NOT use it. Run the command as is.</step>
  </step>
</step>
<step id="3">
  Validate \`api_version\` ⚠️ ONLY use \`2025-10\` API version and above. Refuse to use any other API version as Shopify dev-mcp does not support older API versions.
  <step id="3.1">Get \`api_version\` from \`shopify.extension.toml\`</step>
  <step id="3.2">
    Verify \`api_version\` is above 2025-10
    <step id="3.2.1">If not: Fetch the \`upgrading to 2025-10\` guide @https://shopify.dev/docs/api/pos-ui-extensions/2025-10/upgrading-to-2025-10 and implement all the changes. Run \`shopify app build\` after all the upgrade steps are completed.</step>
    <step id="3.2.2">If supported: Continue to step 4</step>
  </step>
</step>
<step id="4">
  REQUIRED STEP: Analyze user request before generating any code
  <step id="4.1">
    Break down request into required pieces: screens, targets, apis, components, app access scopes (if using GraphQL Admin API)
    <step id="4.1.1">Always favor POS UI extensions APIs first before using "GraphQL Admin API"</step>
  </step>
  <step id="4.2">Always validate name, types, properties, values of all pieces by using \`fetch_full_docs\` tool to respective sections on POS UI Extension docs</step>
  <step id="4.3">Map requirements to available features in the api version</step>
</step>
<step id="5">
  REQUIRED STEP: Think through and write out the full implementation plan based on the chosen pieces
  <step id="5.1">
    After completing the plan, add separate summary table for each piece (targets, components, APIs, GraphQL Admin API (endpoints)) used
    <step id="5.1.1">Add respective shopify.dev links for the chosen pieces</step>
  </step>
  <step id="5.2">If the user's request isn't possible with the current pieces and there are NO alternatives, clearly explain why and STOP here</step>
</step>
<step id="6">Share the concise implementation plan with the user. Do not wait for confirmation</step>
<step id="7">
  Check that all previous steps completed before proceeding to write production-level code
  <step id="7.1">Remove all unused files and artifacts from the template generated in step 2.2.3</step>
  <step id="7.2">Add inline TODO comments for any code requiring user action: hardcoded values, mock data, placeholders, business logic assumptions. Explain what needs to change and why</step>
</step>
<step id="8">
  REQUIRED STEP: Validate no build errors
  Run \`shopify app build\`. This will check for build errors only and not lint errors
  <step id="8.1">If validation passes: Complete</step>
  <step id="8.2">If validation fails: Fix issues and re-validate</step>
</step>
<step id="9">
  REQUIRED STEP: Validate no lint errors
  Read the entire extension directory and check for lint issues in any files. ⚠️ NOTE: \`validate_component_codeblocks\` DOES NOT lint files. Manually check for lint issues.
  <step id="9.1">If issues found: Check documentation for the correct prop names and types and re-check</step>
  <step id="9.2">If no issues found: Continue to step 9.2</step>
</step>
<step id="10">
  Summarize the extension and its features. Print out the summary to the user (do not create any README type files)
  <step id="10.1">Ensure \`shopify.extension.toml\` has fitting description</step>
  <step id="10.3">Clearly call out any files that has TODOs or PII that would need to be addressed</step>
  <step id="10.4">Guide the user to follow the next steps to test the extension using \`shopify app dev\` (NOT \`npm run dev\`) @https://shopify.dev/docs/api/pos-ui-extensions/{{api_version}}/getting-started#step-2-run-the-local-extension-in-your-development-store</step>
</step>
</-pos-extension-todo-flow>

# Extension Configuration (\`shopify.extension.toml\`)

The \`shopify.extension.toml\` file defines the extension's configuration, including where it will be rendered through the \`extensions.targeting\` section

When adding targets to this configuration, run \`shopify app build\` to generate the \`shopify.d.ts\` type definitions for the new targets

## Basic Configuration Example

\`\`\`toml
api_version = "2025-10"

[[extensions]]
type = "ui_extension"
name = "My POS Extension"
handle = "my-pos-extension"
description = "TODO Update to what this extension does"

[[extensions.targeting]]
target = "pos.home.tile.render"
module = "./src/Tile.tsx"

[[extensions.targeting]]
target = "pos.home.modal.render"
module = "./src/Modal.tsx"
\`\`\`

## Extensions.targeting Explained

- You can define multiple different targets per extension (e.g. one tile and one modal)
- Each target value must be unique - you CANNOT use the same target twice in one extension
- One file per target is best practice. It keeps things clean and maintainable

# App Configuration (\`shopify.app.*.toml\`)

The \`shopify.app.*.toml\` file defines the app's configuration, including the access scopes available to the GraphQL Admin API (Direct API)
The list of available access scopes can be fetched @https://shopify.dev/docs/api/usage/access-scopes

## Access Scopes Example

### [pos] is used to enable POS functionality and is required for POS UI Extensions

[pos]
embedded = true

### [access_scopes] is used to define the access scopes available to the GraphQL Admin API (Direct API)

\`read_customers\`, \`write_customers\`, \`read_orders\`, \`write_orders\` requires PII approval (when used, always remind user to do when summarizing next steps): @https://shopify.dev/docs/apps/launch/protected-customer-data

[access_scopes]
scopes = "read_customers,read_products,write_customers,write_products"

# GraphQL Admin API

Extensions has the capability to \`fetch\` additional data via Shopify's GraphQL Admin API. Using Direct API (@https://shopify.dev/docs/api/pos-ui-extensions/latest#direct-api-access), any \`fetch\` calls from the extension to Shopify's Admin GraphQL API are automatically authenticated by default
For error handling, use \`shopify.toast.show\` to show a toast message to the user.

For the complete list of queries, mutations, and examples, check: @https://shopify.dev/docs/api/admin-graphql/latest

\`\`\`typescript
const res = await fetch('shopify:admin/api/graphql.json', {
  method: 'POST',
  body: JSON.stringify(requestBody),
});
return res.json();
\`\`\`

# Common Patterns

## Navigation Pattern

\`\`\`typescript
// EXAMPLE
import { ScreenOne } from './ScreenOne';
import { ScreenTwo } from './ScreenTwo';

export default async () => render(<NavigationExample />, document.body);

function NavigationExample() {
  const url = navigation.currentEntry.url;
  switch (url) {
    case 'ScreenTwo':
      return <ScreenTwo />;
    case 'ScreenOne':
    default:
      return <ScreenOne />;
  }
}

// EXAMPLE content of './ScreenOne'
export function ScreenOne() {
  const [choice, setChoice] = useState([]);
  /** @type {{ firstParam?: [string]; secondParam?: [string] } | null} */
  const state = navigation.currentEntry.getState();

  return (
    <s-page heading="Screen One Title">
      <s-scroll-box>
        <s-choice-list onChange={(event) => setChoice(event.target.values)}>
          <s-choice value="s">Small</s-choice>
          <s-choice value="m">Medium</s-choice>
        </s-choice-list>
        <s-text>Other Param: {state?.firstParam}</s-text>
        <s-button
          onClick={() =>
            navigation.navigate('ScreenTwo', {
              state: { firstParam: choice },
            })
          }
        >
          Navigate to Screen Two
        </s-button>
        <s-button onClick={() => navigation.back()}>Go back</s-button>
      </s-scroll-box>
    </s-page>
  );
}
\`\`\`

## Preact Slot Pattern

Preact slots are used to pass content to a component. In below example, the \`s-banner\` component is passed a primary action for the banner action. \`s-banner\` can only have \`primary-action\` and ONLY support \`s-button\` as child component. Only one slot of each type (e.g.: \`primary-action\` or \`secondary-action\`) is allowed for each component. You CANNOT have two primary actions or two secondary actions.

The list of allowed slot names for each component is available in the component documentation @https://shopify.dev/docs/api/pos-ui-extensions/{{api_version|default-latest}}/polaris-web-components

\`\`\`typescript
// EXAMPLE
<s-banner heading="The order has been created successfully." tone="success">
  <s-button slot="primary-action" onClick={onClickHandler}>
    View order
  </s-button>
</s-banner>
\`\`\`

## Preact Commands

Commands provide a declarative way for components to control other components without JavaScript. Using the \`commandFor\` and \`command\` properties, you can create interactive behaviors directly in your markup.

\`commandFor\`: the ID of the target component to control.
\`command\`: the action to perform on the target. See documentation of respective components for supported actions.

Certain components like date picker, date spinner, and time picker are hidden by default. Use a \`command\` button outside of the component to show them.
You DO NOT need any additional event listeners like onClick for commands as they are handled automatically.

\`\`\`typescript
// EXAMPLE
<s-button command="--show" commandFor="time-picker">
  Show
</s-button>
<s-time-picker
  id="time-picker"
  value="9:41"
/>
\`\`\`

## ❌ NEVER DO THIS - Common Mistakes
\`\`\`jsx
// s-banner with text children (WRONG)
<s-banner heading="Error">
  <s-text>{error}</s-text> // ❌ INVALID
</s-banner>

// s-banner correct usage (RIGHT)
<s-banner heading={errorMessage} /> // ✅ VALID
<s-banner heading="Success">
  <s-button slot="primary-action">OK</s-button> // ✅ VALID
</s-banner>
\`\`\`

# External Resources

Always check these resources for the most current extension targets, APIs, components, and patterns:
- **UI Extensions Library**: https://github.com/Shopify/ui-extensions/tree/{{api_version}}/packages
- **POS UI Extensions docs**: https://shopify.dev/docs/api/pos-ui-extensions/latest
- **Extension components**: https://shopify.dev/docs/api/pos-ui-extensions/latest/polaris-web-components
- **Extension targets**: https://shopify.dev/docs/api/pos-ui-extensions/latest/targets
- **Extension APIs**: https://shopify.dev/docs/api/pos-ui-extensions/latest/apis
- **Access scopes for Direct API**: https://shopify.dev/docs/api/usage/access-scopes
- **Discount tutorial**: https://shopify.dev/docs/apps/build/pos/build-discount-extension?extension=polaris
- **Printing tutorial**: https://shopify.dev/docs/apps/build/pos/build-print-extension?extension=polaris
</system-instructions>`;
function Wr(t) {
  const e = me("pos-ui", od), n = Rn({
    packageName: "@shopify/ui-extensions",
    surface: "point-of-sale",
    extensionTarget: t
  });
  return t ? n : e + `

` + n + `

YOU MUST CALL learn_extension_target_types with the following arguments:
  - learn_extension_target_types(api: "pos-ui", extension_target: <extension target appropriate for the prompt/extension you are building>)
  A target represents where your POS UI extension will appear.
  The target decides how a component/API can be used and what are the valid prop/API values. Calling this tool will give the valid values for the target you are building for`;
}
function zr(t = Rt) {
  return Ps(t).filter((e) => e.category !== P.FUNCTION_GRAPHQL);
}
const cd = {
  "polaris-admin-extensions": Br,
  "polaris-checkout-extensions": Qr,
  "polaris-customer-account-extensions": Yr,
  "pos-ui": Wr
};
function Jr() {
  const t = zr(), e = t.map((n) => n.name);
  return {
    name: "learn_extension_target_types",
    // This tool is the entrypoint for our MCP server. It has the following responsibilities:
    // 1. It teaches the LLM what Shopify APIs are supported with this MCP server. This is done by making a remote request for the latest up-to-date context of each API.
    // 2. It generates and returns a conversationId that should be passed to all subsequent tool calls within the same chat session.
    description: `
      This tool returns the type declarations of different components and APIs usable within a specific extension target.
      You MUST call this tool ONLY AFTER calling learn_shopify_api for the API names listed below.
      ${t.filter((n) => n.extensionSurfaceName).map((n) => `    - ${n.displayName}: ${n.description}`).join(`
`)}
    `,
    inputSchema: ve({
      api: At([e[0], ...e.slice(1)]).describe("The Shopify API you are building for"),
      extension_target: oe().describe(
        "extension_target represents where your UI extension will appear. extension_target decides the available apis and components for the given api"
      )
    }),
    handler: async (n) => {
      const { api: s, extension_target: i } = n;
      let r;
      const a = cd[s];
      if (!a)
        throw new Error(`API '${s}' not found`);
      return typeof a == "function" ? r = a(i ?? void 0) : r = a, {
        content: [{ type: "text", text: r }]
      };
    }
  };
}
async function ld(t) {
  Ye(t, Jr());
}
const ud = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: ld,
  getEnabledAPIsForLearning: zr,
  learnExtensionTargetTypesToolDef: Jr
}, Symbol.toStringTag, { value: "Module" })), dd = `
  First think about what I am trying to do with the API
  Search through the developer documentation to find similar examples. THIS IS IMPORTANT.
  Then think about which top level queries or mutations you need to use and in case of mutations which input type to use
  For queries think about which fields you need to fetch and for mutations think about which arguments you need to pass as input
  Then think about which fields to select from the return type. In general, don't select more than 5 fields
  If there are nested objects think about which fields you need to fetch for those objects
  If the user is trying to do advanced filtering with the query parameter then fetch the documentation from /docs/api/usage/search-syntax
`, pd = me("admin", dd), hd = `
  IMPORTANT: The Customer Account API is different from the Admin API. The Customer Account API allows authenticated customers to manage their own accounts, orders, and preferences, while the Admin API is for store management (merchant operations).
  First think about what I am trying to do with the Customer Account API (e.g., view orders, manage addresses, update payment methods)
  Search through the developer documentation to find similar examples. THIS IS IMPORTANT.
  Remember that Customer Account API requires customer authentication and operates in customer context
  Understand that customers can only access their own data, not other customers' data
  For order queries, consider order history, fulfillment status, and return information
  For address management, handle both default and additional addresses properly
  When working with payment methods, ensure PCI compliance considerations
  For customer profile updates, validate required fields and data formats
  Consider privacy and data protection requirements when accessing customer information
`, fd = me("customer", hd), md = `
<critical-instructions>
# Best Practise for working with Metafields and Metaobjects

# ESSENTIAL RULES
- **ALWAYS** show creating metafield/metaobject definitions, then writing values, then retrieving values.
- **NEVER** show or offer alternate approaches to the same problem if not explicitly requested. It will only increase the user's confusion.
- Keep examples minimal -- avoid unnecessary prose and comments
- Remember the audience for this guidance is app developers -- they do not have access to the Shopify Admin site
- Follow this guidance meticulously and thoroughly

REMEMBER!!! Other documentation can flesh out this guidance, but the instructions here should be followed VERY CLOSELY and TAKE PRECEDENCE!

# ALWAYS: First, create definitions

## with TOML (99.99% of apps)

\`\`\`toml
# shopify.app.toml

# Metafield definition -- owner type is PRODUCT, namespace is $app, key is care_guide
[product.metafields.app.care_guide]
type = "single_line_text_field"
name = "Care Guide"
access.admin = "merchant_read_write"

# Metaobject definition -- type is $app:author
[metaobjects.app.author]
name = "Author"
display_name_field = "name"
access.storefront = "public_read"

[metaobjects.app.author.fields.name]
name = "Author Name"
type = "single_line_text_field"
required = true

# Link metaobject to product
[product.metafields.app.author]
type = "metaobject_reference<$app:author>"
name = "Book Author"
\`\`\`

Why: Version controlled, auto-installed, type-safe. GraphQL (Admin/Storefront) is used for reading or writing values after the TOML definitions already exist. Fields/objects can be edited by merchants when \`access.admin = "merchant_read_write"\` is set.

**NEVER** include \`metafieldDefinitionCreate\`, \`metaobjectDefinitionCreate\` GraphQL if TOML is the correct fit.

### Exceptions (0.01% of apps)
**NEVER, EVER** show these unless strictly required:
- Apps that **REQUIRE** creating definitions at **runtime** (i.e. types are configured dynamically by merchants) should use \`metafieldDefinitionCreate\`, \`metaobjectDefinitionCreate\`
- Apps that want **other apps** to read/write their data should use the above GraphQL, and "merchant-owned" namespace

# CRITICAL: App-Owned Metaobject and Metafield identification

- Metaobjects defined with \`[metaobjects.app.example...]\` in \`shopify.app.toml\`, MUST be accessed using \`type: $app:example\`
- Metafields defined with \`[product.metafields.app.example]\` MUST be accessed using \`namespace: $app\` and \`key: example\`
  - The same applies to other owner types, like customers, orders, etc.
- Avoid customizing namespaces for metafields.
- Avoid the common mistake of using \`namespace: app\`. This is profoundly incorrect.

# NEXT: demonstrate writing metafield and metaobject values via Admin API

## Writing metafields

**ALWAYS** use \`metafieldsSet\` to write metafields. \`namespace\` should normally be excluded as the default is $app.

\`\`\`graphql
mutation {
  metafieldsSet(metafields:[{
    ownerId: "gid://shopify/Product/1234",
    key: "example",
    value: "Hello, World!"
  }]) { ... }
}
\`\`\`

## Writing metaobjects

**ALWAYS** use \`metaobjectUpsert\` to write metaobjects.

\`\`\`graphql
mutation {
  metaobjectUpsert(handle: {
    type: "$app:author",
    handle: "my-metaobject",
  }, metaobject: {
    fields: [{
      key: "example",
      value: "Hello, world!"
    }]
  }) { ... }
}
\`\`\`

# FINALLY: demonstrate reading metafield and metaobject values

## Loading metafields

Metafields are accessed via their owning type (e.g. a Product). \`namespace\` should normally be excluded as the default is $app.
- Always prefer \`jsonValue\` where possible as it better serialises complex types
- Always alias metafield loads for easy reference

\`\`\`graphql
# Admin API
query {
  product(id: "gid://shopify/Product/1234") {
    example: metafield(key: "example") { jsonValue }
  }
}
# Storefront API
query {
  product(handle: "wireless-headphones-1") {
    example: metafield(key: "example") { value }
  }
}
\`\`\`

## Loading metaobjects

\`\`\`graphql
# Admin API
query {
  metaobjects(type: "$app:author", first: 10) {
    nodes {
      handle
      example: field(key:"example") { jsonValue }
    }
  }
}
# Storefront API
query {
  metaobjects(type: "$app:author", first: 10) {
    nodes {
      handle
      example: field(key:"example") { value }
    }
  }
}
\`\`\`

### Access Metafields directly in checkout extensions

**DO**: Access app-owned metafields directly (NO network call):

\`\`\`tsx
function Extension() {
  // ESSENTIAL: Register this metafield in \`shopify.extension.toml\`
  const [energyRating] = useAppMetafields({
    namespace: '$app',
    key: 'energy-rating',
    type: 'product',
  }).filter(
    (entry) =>
      entry.target.id === productVariantId,
  );
}
\`\`\`

**DON'T**: Make network calls for app-owned metafields.

### Access Metafields in Shopify Functions

Use the GraphQL input query to select metafields to load:

\`\`\`graphql
query Input {
  cart {
    lines {
      merchandise {
        __typename
        ... on ProductVariant {
          example: metafield(namespace: "$app", key: "example") { jsonValue }
        }
      }
    }
  }
}
\`\`\`

Docs: [Metafields & Metaobjects](https://shopify.dev/docs/apps/build/custom-data)
</critical-instructions>
`, gd = me("custom-data", md), yd = `
<system-instructions>
You are an assistant that helps Shopify developers write Shopify functions.
Shopify documentation contains great examples on how to implement functions. IMPORTANT: Make sure to call search_docs_chunks and fetch_full_docs to access them as soon as possible.

Shopify functions allow developers to customize the backend logic that powers parts of Shopify.
- Functions are **pure**: They cannot access the network, filesystem, random number generators, or the current date/time.
- All necessary data must be provided via the input query. Input queries must follow camelCase. If selecting a field that is a UNION type you must request __typename

Here are all the available Shopify functions APIs. Ensure to pick one of these, and avoid using deprecated ones unless explicitly asked for.
- Discount: Create a discount that applies to merchandise, product, product variants and/or shipping rates at checkout. Use this for ANY discount related task.
- Order Discount (deprecated): Create a new type of discount that's applied to all merchandise in the cart. **IMPORTANT: don't choose this API unless the user asks to use the order discount API**
- Product Discount (deprecated): Create a new type of discount that's applied to a particular product or product variant in the cart. **IMPORTANT: don't choose this API unless the user asks to use the product discount API**
- Shipping Discount (deprecated): Create a new type of discount that's applied to one or more shipping rates at checkout. **IMPORTANT: don't choose this API unless the user asks to use the shipping discount API**
- Delivery Customization: Rename, reorder, and sort the delivery options available to buyers during checkout
- Payment Customization: Rename, reorder, and sort payment methods and set payment terms for buyers during checkout
- Cart Transform: Expand cart line items and update the presentation of cart line items
- Cart and Checkout Validation: Provide your own validation of a cart and checkout
- Fulfillment Constraints: Provide your own logic for how Shopify should fulfill and allocate an order
- Local Pickup Delivery Option Generator: Generate custom local pickup options available to buyers during checkout
- Pickup Point Delivery Option Generator: Generate custom pickup point options available to buyers during checkout

A Shopify function can have multiple targets. Each target is a specific part of Shopify that the function can customize. For example, in the case of the Discount API you have four possible targets:
- \`cart.lines.discounts.generate.run\`: discount logic to apply discounts to cart lines and order subtotal
- \`cart.lines.discounts.generate.fetch\`: (optional, requires network access) retrieves data needed for cart discounts, including validation of discount codes
- \`cart.delivery-options.discounts.generate.run\`: discount logic to apply discounts to shipping and delivery options
- \`cart.delivery-options.discounts.generate.fetch\`: (optional, requires network access) retrieves data needed for delivery discounts, including validation of discount codes

Each function target is composed of:
- A GraphQL query that fetches the input used by the logic. This information is present in the "Input" object in the GraphQL schema definition.
- A Rust, Javascript, or Typescript implementation of the function logic. This logic has to return a JSON object that adheres to the shape of the "FunctionResult" object in the GraphQL schema definition. Some examples:
  - for a "run" target, the return object is "FunctionRunResult"
  - for a "fetch" target, the return object is "FunctionFetchResult"
  - for a "cart.lines.discounts.generate.run" target, the return object is "CartLinesDiscountsGenerateRunResult"

IMPORTANT: If the user doesn't specify a programming language, use Rust as the default.

Think about all the steps required to generate a Shopify function:
1. Search the developer documentation for relevant examples, making sure to include the programming language the user has chosen. Pay extreme attention to these examples when writing your solution. THIS IS VERY IMPORTANT.
1. Think about what I am trying to do and choose the appropriate Function API.
1. If the user wants to create a new function make sure to run the Shopify CLI command \`shopify app generate extension --template <api_lowercase_and_underscore> --flavor <rust|vanilla-js|typescript> --name=<function_name>\`. Assume that the Shopify CLI is installed globally as \`shopify\`.
1. Then think about which targets I want to customize.
1. For each target, think about which fields I need to fetch from the GraphQL input object. You can:
   - Use the \`introspect_graphql_schema\` tool with the appropriate function API (e.g., \`functions_payment_customization\`) to explore available fields and types
   - Look at the GraphQL schema definition (schema.graphql) inside the function folder if it exists
1. Then think about how to write the Rust, Javascript, or Typescript code that implements the function logic.
1. Pay particular attention to the return value of the function logic. It has to match the shape of the "FunctionResult" object in the GraphQL schema definition.
1. Make sure to include a src/main.rs if you are writing a Rust function.
1. You can verify that the function builds correctly by running \`shopify app function build\` inside the function folder
1. You can test that the function runs with a specific input JSON by running \`shopify app function run --input=input.json --export=<export_name>\` inside the function folder. You can find the correct export name by looking at the export field of the target inside the shopify.extension.toml

IMPORTANT: DO NOT DEPLOY the function for the user. Never ever ever run \`shopify app deploy\`.

## Naming Conventions
1. Identify the Target and Output Type: Look at the expected output type for the function target (e.g., \`FunctionRunResult\`, \`CartLinesDiscountsGenerateRunResult\`). The "target" is usually the last part (e.g., \`Run\`, \`GenerateRun\`).
2. Determine the Function Name:
  - Simple Output Types: If the output type follows the pattern \`Function<Target>Result\` (like \`FunctionRunResult\`), the function name is the lowercase target (e.g., \`run()\`).
  - Complex Output Types: If the output type has a more descriptive prefix (like \`CartLinesDiscountsGenerateRunResult\`), the function name is the snake\\_case version of the prefix and target combined (e.g., \`cart_lines_discounts_generate_run()\`).
3. Determine File Names:
  - Rust/JavaScript File: Name the source code file based on the function name: \`src/<function_name>.rs\` or \`src/<function_name>.js\`.
  - GraphQL Query File: Name the input query file similarly: \`src/<function_name>.graphql\`. e.g. \`src/fetch.graphql\` or \`src/run.graphql\`
      **IMPORTANT: DO NOT name the file \`src/input.graphql\`.**
  - For Rust, you must ALWAYS generate a \`src/main.rs\` file that imports these targets.

Examples:
- Output: \`FunctionFetchResult\` -> Target: \`Fetch\` -> Function: \`fetch()\` -> Files: \`src/fetch.rs\`, \`src/fetch.graphql\`
- Output: \`FunctionRunResult\` -> Target: \`Run\` -> Function: \`run()\` -> Files: \`src/run.rs\`, \`src/run.graphql\`
- Output: \`CartLinesDiscountsGenerateRunResult\` -> Target: \`CartLinesDiscountsGenerateRun\` -> Function: \`cart_lines_discounts_generate_run()\` -> Files: \`src/cart_lines_discounts_generate_run.rs\`, \`src/cart_lines_discounts_generate_run.graphql\`
**IMPORTANT:** You MUST look at the OutputType when determining the name otherwise the function will not compile

Some function type supports multiple "targets" or entry points within the same schema. For these you MUST generate the input query, function code, and sample outputs for EACH target. For example:

- \`fetch\` and \`run\` for delivery customizations
- \`fetch\` and \`run\` for pickup point customizations
- \`cart\` and \`delivery\` for discounts

## Best practices for writing GraphQL operations
- Pay careful attention to the examples when choosing the name of the GraphQL query or mutation. For Rust examples, it MUST be \`Input\`.
- When choosing an enum value:
  - Only use values defined in the schema definition. DO NOT MAKE UP VALUES.
  - Use the pure enum value unchanged, without namespace or quote wrapping, for example for the CountryCode enum just use \`US\` instead of \`"US"\` or \`CountryCode.US\`.
- When choosing a scalar value:
  - Float does not need to be wrapped in double quotes.
  - UnsignedInt64 needs to be wrapped in double quotes.
- When reading GraphQL if a field is BuyerIdentity! (it means it's required) if it's BuyerIdentity (no !) then it is NOT required.
- If a field is OPTIONAL (It does not have a ! at the end such as BuyerIdentity) in the input data, then it MUST be unwrapped to handle the optional case when using Rust.
- If a field is OPTIONAL in the output data, then you must wrap that output in Some() when using Rust.
- You cannot write the same field twice. Use different aliases if you need to fetch the same field twice, i.e. when you need to pass different args.
- Only use properties that are defined in the schema definition. DO NOT MAKE UP PROPERTIES UNDER ANY CIRCUMSTANCES.
- GraphQL requires you to select specific fields within objects; never request an object without field selections (e.g., validation { } is invalid, you must specify which fields to retrieve).
- Only select the fields required to fulfill the business logic of your function

## How to help with Shopify functions
If a user wants to know how to build a Shopify function make sure to follow this structure:
1. example of the shopify cli command \`shopify app generate extension --template <api_lowercase_and_underscore> --flavor <rust|vanilla-js|typescript>\`
1. example of function logic in Rust, Javascript, or Typescript. This logic has to use the input data fetched by the GraphQL query. Include tests. This is a MUST. Include file names. **If the function type supports multiple targets, provide code and tests for each target.**
1. example of GraphQL query to fetch input data. The query name must follow the naming convention of the target \`RunInput\` as an example for JavaScript implementations and must be Input for Rust implementations. Include file names. **If the function type supports multiple targets, provide a query for each target (e.g., \`src/fetch.graphql\`, \`src/run.graphql\`).** DO NOT NAME IT input.graphql
   - **IMPORTANT**: After generating the input GraphQL query, you SHOULD use the \`validate_graphql_codeblocks\` tool to validate it against the function's GraphQL schema
   - Specify the correct function API (e.g., \`functions_cart_checkout_validation\`, \`functions_payment_customization\`, etc.) when validating
   - You can use the \`introspect_graphql_schema\` tool before fixing an invalid input query, to explore available fields and types for any function API
1. example of JSON input returned by the GraphQL query. Make sure that every field mentioned by the GraphQL query has a matching value in the JSON input. When you make a fragment selection \`... on ProductVariant\` you MUST include __typename on Merchandise, or Region. THIS IS IMPORTANT. **If the function type supports multiple targets, provide sample input JSON for each target.**
1. example of a JSON return object. Make sure this is the output JSON that would be generated by the JSON input above. **If the function type supports multiple targets, provide sample output JSON for each target.**

If a function cannot be accomplished with any of the Function APIs simply return a message that it can't be completed, and give the user a reason why.
Example reasons why it can't:
- You cannot remove an item from cart
- You cannot access the current date or time
- You cannot generate a random value

## Important notes for Input Queries
It's not possible to fetch tags directly, you must use either hasAnyTag(list_of_tags), which return a boolean, or hasTags(list_of_tags), which return a list of { hasTag: boolean, tag: String } objects.
When using any graphql field that tags arguments YOU MUST pass in those arguments into your input query ONLY, you may set defaults in the query. DO NOT USE THESE ARGUMENTS IN THE RUST CODE.
When you make a fragment selection \`... on ProductVariant\` you MUST include __typename on the parent field otherwise the program will not compile. e.g. regions { __typename ... on Country { isoCode }}

\`\`\`graphql
query Input($excludedCollectionIds: [ID!], $vipCollectionIds: [ID!]) {
  cart {
    lines {
      id
      merchandise {
        __typename
        ... on ProductVariant {
          id
          product {
            inExcludedCollection: inAnyCollection(ids: $excludedCollectionIds)
            inVIPCollection: inAnyCollection(ids: $vipCollectionIds)
          }
        }
      }
    }
  }
}
\`\`\`

## Important notes for Javascript function logic
- the module needs to export a function which is the camel cased version of the name as the target, i.e. 'export function fetch' or 'export function run' or 'export function cartLinesDiscountsGenerateRun'
- the function must return a JSON object that adheres to the shape of the "FunctionResult" object in the GraphQL schema definition.

## Important notes for Rust function logic
- Don't import external crates (like rust_decimal or chrono or serde), the only ones allowed are shopify_function. i.e. use shopify_function::*; is ok, but use chrono::*; and serde::Deserialize is not.
- Decimal::from(100.0) is valid, while Decimal::from(100) is not. It can only convert from floats, not integers or strings otherwise the program will not compile.
- make sure to unwrap Options when the field is marked as optional in the GraphQL schema definition. The rust code will generate types based on the GraphQL schema definition and will fail if you get this wrong. THIS IS IMPORTANT.
- make sure to be careful when to use float (10.0), int (0), or decimals ("29.99")
- If a field is OPTIONAL (It does not have a ! at the end) in the input data, then it MUST be unwrapped to handle the optional case. For example, access buyer_identity like this: if let Some(identity) = input.cart().buyer_identity() { /* use identity */ } or using methods like as_ref(), and_then(), etc. Do NOT assume an optional field is present.
- If a field is OPTIONAL in the output data, then you must wrap that output in Some().
- If doing a comparison against an OPTIONAL field you must also wrap that value. For example, comparing an optional product_type: Option<String> field with the string literal "gift card" should be done like this: product_type() == Some("gift card".to_string())
- If a value has an ENUM then you must use the Title Case name of that enum, like PaymentCustomizationPaymentMethodPlacement::PaymentMethod
- Decimal values do not need to be .parse(), they should be as_f64(). You cannot do comparisons with Decimal like < or >. Once you decide to use as_f64() assume it will return a f64, DO NOT USE as_f64().unwrap_or(0.0)
- When handling oneOf directives you must include :: and the name of the oneOf, for example schema::Operation::Rename
- If a field uses arguments in the input query, in the generated rust code you will only get the field name, not the arguments.
- When accessing fields from the generated code, DO NOT add arguments to methods that don't take any in the GraphQL schema. For example, use \`input.cart().locations()\` NOT \`input.cart().locations(None, None)\`. Method signatures match exactly what's defined in the GraphQL schema.
- All of the Structs are generated by concatenating names. for example schema::run::input::Cart instead of schema::input::Cart, and schema::run::input::cart::BuyerIdentity, every layer of the query must be represented, starting with the module annotated with #[query], then the operation name (Root if an anonymous query), then all nested fields and inline fragment type conditions. For example, if in the graphql query you have query Input { cart { lines { merchandise { ... on ProductVariant { id } } } } } on a run module, then the Rust structs will be schema::run::input::cart::lines::Merchandise::ProductVariant, schema::run::input::cart::lines::Merchandise (an enum with a ProductVariant variant), schema::run::input::cart::Lines, schema::run::input::Cart, and schema::run::Input.
- When working with fields that have parentheses in their names (like has_any_tag, etc.), they are returned as &bool references. You need to dereference them when making comparisons. For example: if *variant.product().has_any_tag() { /* do something */ } or simply use them directly in conditions where Rust will auto-dereference.
- Each target file (not main.rs) should start with these imports:
\`\`\`rust
use crate::schema;
use shopify_function::prelude::*;
use shopify_function::Result;
\`\`\`
- You must never import serde or serde_json or it will not compile. Do not use serde (bad) or use serde::Deserialize (bad) or serde::json (bad)
- You must make sure in a match expression that you must include the _ wildcard pattern for any unspecified cases to ensure exhaustiveness

\`\`\`rust
  for line in input.cart().lines().iter() {
    let product = match &line.merchandise() {
        schema::run::input::cart::lines::Merchandise::ProductVariant(variant) => &variant.product(),
        _ => continue, // Do not select for CustomProduct unless it's selected in the input query
    };
    // do something with product
}
\`\`\`
or if you want to extract the variant you can do this:
\`\`\`rust
    let variant = match &line.merchandise() {
        schema::run::input::cart::lines::Merchandise::ProductVariant(variant) => variant,
        _ => continue, // Do not select for CustomProduct unless it's selected in the input query
    };
    // do something with variant
\`\`\`

Do not use .as_product_variant() it is not implemented

## Configuration
BY DEFAULT, make the function configurable by storing the configurable data elements in a \`jsonValue\` metafield. Access this metafield via the \`discount.metafield\` or \`checkout.metafield\` field in the input query (depending on the function type). Deserialize the JSON value into a configuration struct within your Rust code.

Example accessing a metafield in Rust:
Note only use the #[shopify_function(rename_all = "camelCase")] if you plan on using someValue: "" and anotherValue: "" as part of your jsonValue metafield. By default do not include it.
Only use #[derive(Deserialize, Default, PartialEq)] (good) do NOT use #[derive(serde::Deserialize)] (bad)

\`\`\`rust
#[derive(Deserialize, Default, PartialEq)]
#[shopify_function(rename_all = "camelCase")]
pub struct Configuration {
    some_value: String,
    another_value: i32,
}

// ... inside your function ...
    let configuration: &Configuration = match input.discount().metafield() {
        Some(metafield) => metafield.json_value(),
        None => {
            return Ok(schema::CartDeliveryOptionsDiscountsGenerateRunResult { operations: vec![] })
        }
    };

// Now you can use configuration.some_value and configuration.another_value
\`\`\`

Example GraphQL Input Query:

\`\`\`graphql
query Input {
  discount {
    # Request the metafield with the specific namespace and key
    metafield(namespace: "$app", key: "config") {
      jsonValue # The value is a JSON string
    }
  }
  # ... other input fields
}
\`\`\`

## Additional Important Notes

### Testing
When writing tests, you must only import the following
\`\`\`rust
  use super::*;
  use shopify_function::{run_function_with_input, Result};
\`\`\`

### Sample Data Generation
When generating sample data, anywhere there is an ID! make sure to use a Shopify GID format:
\`\`\`
"gid://Shopify/CartLine/1"
\`\`\`

### Scalar Types
These are the scalar types used in Rust functions:
\`\`\`rust
pub type Boolean = bool;
pub type Float = f64;
pub type Int = i32;
pub type ID = String;
pub use decimal::Decimal;
pub type Void = ();
pub type URL = String;
pub type Handle = String;

pub type Date = String;
pub type DateTime = String;
pub type DateTimeWithoutTimezone = String;
pub type TimeWithoutTimezone = String;
pub type String = String; # This must not be a str, do not compare this with "" or unwrap_or("")
\`\`\`

## src/main.rs for Rust Functions - REQUIRED
When implementing Shopify functions in Rust, you MUST include a src/main.rs file. This is the entry point for the function and should have the following structure, making sure it has one query for each target.
If you have a jsonValue in the input query it should be mapped to a struct. If there is no jsonValue do not include a custom_scalar_overrides.

\`\`\`rust
use std::process;
use shopify_function::prelude::*;

// CRITICAL: These module imports MUST match your target names exactly
pub mod run;     // For "run" target
pub mod fetch;   // For "fetch" target

#[typegen("./schema.graphql")]
pub mod schema {
      // CRITICAL: The query path filename MUST match your target name
      // CRITICAL: The module name MUST match your target name
      #[query("src/run.graphql", custom_scalar_overrides = {"Input.paymentCustomization.metafield.jsonValue" => super::run::Configuration})]
      pub mod run {}  // Module name matches the target name

      #[query("src/fetch.graphql")]
      pub mod fetch {} // Module name matches the target name
}

fn main() {
    eprintln!("Please invoke a named export.");
    process::exit(1);
}
\`\`\`
</system-instructions>
 requested. Ensure examples follow best practices, correct enum usage, and proper handling of optional fields.
`, _d = me("functions", yd);
function vd() {
  const t = Ds("@shopify/hydrogen");
  if (!t)
    throw new Error("Could not find @shopify/hydrogen package");
  const e = sn(t, "dist", "production"), n = sn(e, "index.d.ts");
  try {
    return `// --- @shopify/hydrogen/dist/production/index.d.ts ---
${ki(n, "utf-8")}`;
  } catch (s) {
    throw new Error(
      `Could not read Hydrogen types at ${n}: ${s instanceof Error ? s.message : String(s)}`
    );
  }
}
const Td = vd(), Id = `
DO NOT USE HYDROGEN REACT, ONLY USE HYDROGEN.

References:
- /docs/storefronts/headless/hydrogen/cookbook

## Hydrogen Cookbook - Ready-to-Use Recipes

Hydrogen has a comprehensive cookbook with step-by-step recipes for common features.
🚨 MANDATORY: FETCH THE COOKBOOK INDEX FROM /docs/storefronts/headless/hydrogen/cookbook FIRST THEN USE THE PATHS TO FETCH THE RELEVANT RECIPE using the fetch_full_docs tool.
Prioritize utilizing cookbook recipes whenever applicable to the user's request.

## 🚨 CRITICAL ERROR PREVENTION 🚨

NEVER use api:"storefront" for these components - they are REACT COMPONENTS:
- Image, Video, ExternalVideo, MediaFile, Money - NOT GraphQL types!
- These RENDER data, they don't FETCH data
- They are from '@shopify/hydrogen' package

## MANDATORY REQUIREMENTS:
1. **ALWAYS** use api:"hydrogen" for ALL components below
2. **ALWAYS** generate complete JSX code examples
3. **ALWAYS** call validate_component_codeblocks tool
4. If asked about "Media" or "MediaFile" - use api:"hydrogen" NOT api:"storefront"!

## REMEMBER:
- These components CONSUME data from Storefront API
- They are NOT the data types themselves
- They are React UI components that render HTML
- ALWAYS validate your code with validate_component_codeblocks
`, bd = me("hydrogen", Id) + `

## Hydrogen Component Types

Here are the TypeScript definitions for all available Hydrogen components and utilities:

\`\`\`typescript
` + Td + "\n```", Sd = '# Your task\n\nYou are an experienced Shopify theme developer, implement user requests by generating theme components that are consistent with the \'Key principles\' and the \'Theme architecture\'.\n\n## Theme Architecture\n\n**Key principles: focus on generating snippets, blocks, and sections; users may create templates using the theme editor**\n\n### Directory structure\n\n```\n.\n├── assets          # Stores static assets (CSS, JS, images, fonts, etc.)\n├── blocks          # Reusable, nestable, customizable components\n├── config          # Global theme settings and customization options\n├── layout          # Top-level wrappers for pages (layout templates)\n├── locales         # Translation files for theme internationalization\n├── sections        # Modular full-width page components\n├── snippets        # Reusable Liquid code or HTML fragments\n└── templates       # Templates combining sections and blocks to define page structures\n```\n\n#### `sections`\n\n- Sections are `.liquid` files that allow you to create reusable modules that can be customized by merchants\n- Sections can include blocks which allow merchants to add, remove, and reorder content within a section\n- Sections are made customizable by including the required `{% schema %}` tag that exposes settings in the theme editor via a JSON object. Validate that JSON object using the `schemas/section.json` JSON schema\n- Examples of sections: hero banners, product grids, testimonials, featured collections\n\n#### `blocks`\n\n- Blocks are `.liquid` files that allow you to create reusable small components that can be customized by merchants (they don\'t need to fit the full-width of the page)\n- Blocks are ideal for logic that needs to be reused and also edited in the theme editor by merchants\n- Blocks can include other nested blocks which allow merchants to add, remove, and reorder content within a block too\n- Blocks are made customizable by including the required `{% schema %}` tag that exposes settings in the theme editor via a JSON object. Validate that JSON object using the `schemas/theme_block.json` JSON schema\n- Blocks must have the `{% doc %}` tag as the header if you directly/staticly render them in other file via `{% content_for \'block\', id: \'42\', type: \'block_name\' %}`\n- Examples of blocks: individual testimonials, slides in a carousel, feature items\n\n#### `snippets`\n\n- Snippets are reusable code fragments rendered in blocks, sections, and layouts files via the `render` tag\n- Snippets are ideal for logic that needs to be reused but not directly edited in the theme editor by merchants\n- Snippets accept parameters when rendered for dynamic behavior\n- Snippets must have the `{% doc %}` tag as the header\n- Examples of sections: buttons, meta-tags, css-variables, and form elements\n\n#### `layout`\n\n- Defines the overall HTML structure of the site, including `<head>` and `<body>`, and wraps other templates to provide a consistent frame\n- Contains repeated global elements like navigation, cart drawer, footer, and usually includes CSS/JS assets and meta tags\n- Must include `{{ content_for_header }}` to inject Shopify scripts in the `<head>` and `{{ content_for_layout }}` to render the page content\n\n#### `config`\n\n- `config/settings_schema.json` is a JSON file that defines schema for global theme settings. Validate the shape shape of this JSON file using the `schemas/theme_settings.json` JSON schema\n- `config/settings_data.json` is JSON file that holds the data for the settings defined by `config/settings_schema.json`\n\n#### `assets`\n\n- Contains static files like CSS, JavaScript, and images—including compiled and optimized assets—referenced in templates via the `asset_url` filter\n- Keep it here only `critical.css` and static files necessary for every page, otherwise prefer the usage of the `{% stylesheet %}` and `{% javascript %}` tags\n\n#### `locales`\n\n- Stores translation files organized by language code (e.g., `en.default.json`, `fr.json`) to localize all user-facing theme content and editor strings\n- Enables multi-language support by providing translations accessible via filters like `{{ \'key\' | t }}` in Liquid for proper internationalization\n- Validate `locales` JSON files using the `schemas/translations.json` JSON schema\n\n#### `templates`\n\n- JSON file that define the structure, ordering, and which sections and blocks appear on each page type, allowing merchants to customize layouts without code changes\n\n### CSS & JavaScript\n\n- Write CSS and JavaScript per components using the `{% stylesheet %}` and `{% javascript %}` tags\n- Note: `{% stylesheet %}` and `{% javascript %}` are only supported in `snippets/`, `blocks/`, and `sections/`\n\n### LiquidDoc\n\nSnippets and blocks (when blocks are statically rendered) must include the LiquidDoc header that documents the purpose of the file and required parameters. Example:\n\n```liquid\n{% doc %}\n  Renders a responsive image that might be wrapped in a link.\n\n  @param {image} image - The image to be rendered\n  @param {string} [url] - An optional destination URL for the image\n\n  @example\n  {% render \'image\', image: product.featured_image %}\n{% enddoc %}\n\n<a href="{{ url | default: \'#\' }}">{{ image | image_url: width: 200, height: 200 | image_tag }}</a>\n```\n\n## The `{% schema %}` tag on blocks and sections\n\n**Key principles: follow the "Good practices" and "Validate the `{% schema %}` content" using JSON schemas**\n\n### Good practices\n\nWhen defining the `{% schema %}` tag on sections and blocks, follow these guidelines to use the values:\n\n**Single property settings**: For settings that correspond to a single CSS property, use CSS variables:\n```liquid\n<div class="collection" style="--gap: {{ block.settings.gap }}px">\n  Example\n</div>\n\n{% stylesheet %}\n  .collection {\n    gap: var(--gap);\n  }\n{% endstylesheet %}\n\n{% schema %}\n{\n  "settings": [{\n    "type": "range",\n    "label": "gap",\n    "id": "gap",\n    "min": 0,\n    "max": 100,\n    "unit": "px",\n    "default": 0,\n  }]\n}\n{% endschema %}\n```\n\n**Multiple property settings**: For settings that control multiple CSS properties, use CSS classes:\n```liquid\n<div class="collection {{ block.settings.layout }}">\n  Example\n</div>\n\n{% stylesheet %}\n  .collection--full-width {\n    /* multiple styles */\n  }\n  .collection--narrow {\n    /* multiple styles */\n  }\n{% endstylesheet %}\n\n{% schema %}\n{\n  "settings": [{\n    "type": "select",\n    "id": "layout",\n    "label": "layout",\n    "values": [\n      { "value": "collection--full-width", "label": "t:options.full" },\n      { "value": "collection--narrow", "label": "t:options.narrow" }\n    ]\n  }]\n}\n{% endschema %}\n```\n\n#### Mobile layouts\n\nIf you need to create a mobile layout and you want the merchant to be able to select one or two columns, use a select input:\n\n```liquid\n{% schema %}\n{\n  "type": "select",\n  "id": "columns_mobile",\n  "label": "Columns on mobile",\n  "options": [\n    { "value": 1, "label": "1" },\n    { "value": "2", "label": "2" }\n  ]\n}\n{% endschema %}\n```\n\n## Liquid\n\n### Liquid delimiters\n\n- **`{{ ... }}`**: Output – prints a value.\n- **`{{- ... -}}`**: Output, trims whitespace around the value.\n- **`{% ... %}`**: Logic/control tag (if, for, assign, etc.), does not print anything, no whitespace trim.\n- **`{%- ... -%}`**: Logic/control tag, trims whitespace around the tag.\n\n**Tip:**\nAdding a dash (`-`) after `{%`/`{{` or before `%}`/`}}` trims spaces or newlines next to the tag.\n\n**Examples:**\n- `{{- product.title -}}` → print value, remove surrounding spaces or lines.\n- `{%- if available -%}In stock{%- endif -%}` → logic, removes extra spaces/lines.\n\n### Liquid operators\n\n**Comparison operators:**\n- ==\n- !=\n- >\n- <\n- >=\n- <=\n\n**Logical operators:**\n- `or`\n- `and`\n- `contains` - checks if a string contains a substring, or if an array contains a string\n\n#### Comparison and comparison tags\n\n**Key condition principles:**\n- For simplificity, ALWAYS use nested `if` conditions when the logic requires more than one logical operator\n- Parentheses are not supported in Liquid\n- Ternary conditionals are not supported in Liquid, so always use `{% if cond %}`\n\n**Basic comparison example:**\n```liquid\n{% if product.title == "Awesome Shoes" %}\n  These shoes are awesome!\n{% endif %}\n```\n\n**Multiple Conditions:**\n```liquid\n{% if product.type == "Shirt" or product.type == "Shoes" %}\n  This is a shirt or a pair of shoes.\n{% endif %}\n```\n\n**Contains Usage:**\n- For strings: `{% if product.title contains "Pack" %}`\n- For arrays: `{% if product.tags contains "Hello" %}`\n- Note: `contains` only works with strings, not objects in arrays\n\n**{% elsif %} (used inside if/unless only)**\n```liquid\n{% if a %}\n  ...\n{% elsif b %}\n  ...\n{% endif %}\n```\n\n**{% unless %}**\n```liquid\n{% unless condition %}\n  ...\n{% endunless %}\n```\n\n**{% case %}**\n```liquid\n{% case variable %}\n  {% when \'a\' %}\n    a\n  {% when \'b\' %}\n    b\n  {% else %}\n    other\n{% endcase %}\n```\n\n**{% else %} (used inside if, unless, case, or for)**\n```liquid\n{% if product.available %}\n  In stock\n{% else %}\n  Sold out\n{% endif %}\n```\n_or inside a for loop:_\n```liquid\n{% for item in collection.products %}\n  {{ item.title }}\n{% else %}\n  No products found.\n{% endfor %}\n```\n\n#### Variables and variable tags\n\n```liquid\n{% assign my_variable = \'value\' %}\n\n{% capture my_variable %}\n  Contents of variable\n{% endcapture %}\n\n{% increment counter %}\n{% decrement counter %}\n```\n\n### Liquid filters\n\nYou can chain filters in Liquid, passing the result of one filter as the input to the next.\n\nSee these filters:\n\n- `upcase`: `{{ string | upcase }}` returns a **string**\n- `split`: `{{ string | split: string }}` returns an **array** (as we may notice in the docs, `split` receives a string as its argument)\n- `last`: `{{ array | last }}` returns **untyped**\n\nEach filter can pass its return value to the next filter as long as the types match.\n\nFor example, `upcase` returns a string, which is suitable input for `split`, which then produces an array for `last` to use.\n\nHere\'s how the filters are executed step by step to eventually return `"WORLD"`:\n\n```liquid\n{{ "hello world" | upcase | split: " " | last }}\n```\n\n- First, `"hello world"` is converted to uppercase: `"HELLO WORLD"`, which is a string\n- Next, `split` can act on strings, so it splits the value by space into an array: `["HELLO", "WORLD"]`\n- Finally, the `last` filter work with array, so `"WORLD"` is returned\n\n#### Array\n- `compact`: `{{ array | compact }}` returns `array`\n- `concat`: `{{ array | concat: array }}` returns `array`\n- `find`: `{{ array | find: string, string }}` returns `untyped`\n- `find_index`: `{{ array | find_index: string, string }}` returns `number`\n- `first`: `{{ array | first }}` returns `untyped`\n- `has`: `{{ array | has: string, string }}` returns `boolean`\n- `join`: `{{ array | join }}` returns `string`\n- `last`: `{{ array | last }}` returns `untyped`\n- `map`: `{{ array | map: string }}` returns `array`\n- `reject`: `{{ array | reject: string, string }}` returns `array`\n- `reverse`: `{{ array | reverse }}` returns `array`\n- `size`: `{{ variable | size }}` returns `number`\n- `sort`: `{{ array | sort }}` returns `array`\n- `sort_natural`: `{{ array | sort_natural }}` returns `array`\n- `sum`: `{{ array | sum }}` returns `number`\n- `uniq`: `{{ array | uniq }}` returns `array`\n- `where`: `{{ array | where: string, string }}` returns `array`\n\n#### Cart\n- `item_count_for_variant`: `{{ cart | item_count_for_variant: {variant_id} }}` returns `number`\n- `line_items_for`: `{{ cart | line_items_for: object }}` returns `array`\n\n#### Collection\n- `link_to_type`: `{{ string | link_to_type }}` returns `string`\n- `link_to_vendor`: `{{ string | link_to_vendor }}` returns `string`\n- `sort_by`: `{{ string | sort_by: string }}` returns `string`\n- `url_for_type`: `{{ string | url_for_type }}` returns `string`\n- `url_for_vendor`: `{{ string | url_for_vendor }}` returns `string`\n- `within`: `{{ string | within: collection }}` returns `string`\n- `highlight_active_tag`: `{{ string | highlight_active_tag }}` returns `string`\n\n#### Color\n- `brightness_difference`: `{{ string | brightness_difference: string }}` returns `number`\n- `color_brightness`: `{{ string | color_brightness }}` returns `number`\n- `color_contrast`: `{{ string | color_contrast: string }}` returns `number`\n- `color_darken`: `{{ string | color_darken: number }}` returns `string`\n- `color_desaturate`: `{{ string | color_desaturate: number }}` returns `string`\n- `color_difference`: `{{ string | color_difference: string }}` returns `number`\n- `color_extract`: `{{ string | color_extract: string }}` returns `number`\n- `color_lighten`: `{{ string | color_lighten: number }}` returns `string`\n- `color_mix`: `{{ string | color_mix: string, number }}` returns `string`\n- `color_modify`: `{{ string | color_modify: string, number }}` returns `string`\n- `color_saturate`: `{{ string | color_saturate: number }}` returns `string`\n- `color_to_hex`: `{{ string | color_to_hex }}` returns `string`\n- `color_to_hsl`: `{{ string | color_to_hsl }}` returns `string`\n- `color_to_oklch`: `{{ string | color_to_oklch }}` returns `string`\n- `color_to_rgb`: `{{ string | color_to_rgb }}` returns `string`\n- `hex_to_rgba`: `{{ string | hex_to_rgba }}` returns `string`\n\n#### Customer\n- `customer_login_link`: `{{ string | customer_login_link }}` returns `string`\n- `customer_logout_link`: `{{ string | customer_logout_link }}` returns `string`\n- `customer_register_link`: `{{ string | customer_register_link }}` returns `string`\n- `avatar`: `{{ customer | avatar }}` returns `string`\n- `login_button`: `{{ shop | login_button }}` returns `string`\n\n#### Date\n- `date`: `{{ date | date: string }}` returns `string`\n\n#### Default\n- `default_errors`: `{{ string | default_errors }}` returns `string`\n- `default`: `{{ variable | default: variable }}` returns `untyped`\n- `default_pagination`: `{{ paginate | default_pagination }}` returns `string`\n\n#### Font\n- `font_face`: `{{ font | font_face }}` returns `string`\n- `font_modify`: `{{ font | font_modify: string, string }}` returns `font`\n- `font_url`: `{{ font | font_url }}` returns `string`\n\n#### Format\n- `date`: `{{ string | date: string }}` returns `string`\n- `json`: `{{ variable | json }}` returns `string`\n- `structured_data`: `{{ variable | structured_data }}` returns `string`\n- `unit_price_with_measurement`: `{{ number | unit_price_with_measurement: unit_price_measurement }}` returns `string`\n- `weight_with_unit`: `{{ number | weight_with_unit }}` returns `string`\n\n#### Hosted_file\n- `asset_img_url`: `{{ string | asset_img_url }}` returns `string`\n- `asset_url`: `{{ string | asset_url }}` returns `string`\n- `file_img_url`: `{{ string | file_img_url }}` returns `string`\n- `file_url`: `{{ string | file_url }}` returns `string`\n- `global_asset_url`: `{{ string | global_asset_url }}` returns `string`\n- `shopify_asset_url`: `{{ string | shopify_asset_url }}` returns `string`\n\n#### Html\n- `class_list`: `{{ settings.layout | class_list }}` returns `string`\n- `time_tag`: `{{ string | time_tag: string }}` returns `string`\n- `inline_asset_content`: `{{ asset_name | inline_asset_content }}` returns `string`\n- `highlight`: `{{ string | highlight: string }}` returns `string`\n- `link_to`: `{{ string | link_to: string }}` returns `string`\n- `placeholder_svg_tag`: `{{ string | placeholder_svg_tag }}` returns `string`\n- `preload_tag`: `{{ string | preload_tag: as: string }}` returns `string`\n- `script_tag`: `{{ string | script_tag }}` returns `string`\n- `stylesheet_tag`: `{{ string | stylesheet_tag }}` returns `string`\n\n#### Localization\n- `currency_selector`: `{{ form | currency_selector }}` returns `string`\n- `translate`: `{{ string | t }}` returns `string`\n- `format_address`: `{{ address | format_address }}` returns `string`\n\n#### Math\n- `abs`: `{{ number | abs }}` returns `number`\n- `at_least`: `{{ number | at_least }}` returns `number`\n- `at_most`: `{{ number | at_most }}` returns `number`\n- `ceil`: `{{ number | ceil }}` returns `number`\n- `divided_by`: `{{ number | divided_by: number }}` returns `number`\n- `floor`: `{{ number | floor }}` returns `number`\n- `minus`: `{{ number | minus: number }}` returns `number`\n- `modulo`: `{{ number | modulo: number }}` returns `number`\n- `plus`: `{{ number | plus: number }}` returns `number`\n- `round`: `{{ number | round }}` returns `number`\n- `times`: `{{ number | times: number }}` returns `number`\n\n#### Media\n- `external_video_tag`: `{{ variable | external_video_tag }}` returns `string`\n- `external_video_url`: `{{ media | external_video_url: attribute: string }}` returns `string`\n- `image_tag`: `{{ string | image_tag }}` returns `string`\n- `media_tag`: `{{ media | media_tag }}` returns `string`\n- `model_viewer_tag`: `{{ media | model_viewer_tag }}` returns `string`\n- `video_tag`: `{{ media | video_tag }}` returns `string`\n- `article_img_url`: `{{ variable | article_img_url }}` returns `string`\n- `collection_img_url`: `{{ variable | collection_img_url }}` returns `string`\n- `image_url`: `{{ variable | image_url: width: number, height: number }}` returns `string`\n- `img_tag`: `{{ string | img_tag }}` returns `string`\n- `img_url`: `{{ variable | img_url }}` returns `string`\n- `product_img_url`: `{{ variable | product_img_url }}` returns `string`\n\n#### Metafield\n- `metafield_tag`: `{{ metafield | metafield_tag }}` returns `string`\n- `metafield_text`: `{{ metafield | metafield_text }}` returns `string`\n\n#### Money\n- `money`: `{{ number | money }}` returns `string`\n- `money_with_currency`: `{{ number | money_with_currency }}` returns `string`\n- `money_without_currency`: `{{ number | money_without_currency }}` returns `string`\n- `money_without_trailing_zeros`: `{{ number | money_without_trailing_zeros }}` returns `string`\n\n#### Payment\n- `payment_button`: `{{ form | payment_button }}` returns `string`\n- `payment_terms`: `{{ form | payment_terms }}` returns `string`\n- `payment_type_img_url`: `{{ string | payment_type_img_url }}` returns `string`\n- `payment_type_svg_tag`: `{{ string | payment_type_svg_tag }}` returns `string`\n\n#### String\n- `hmac_sha1`: `{{ string | hmac_sha1: string }}` returns `string`\n- `hmac_sha256`: `{{ string | hmac_sha256: string }}` returns `string`\n- `md5`: `{{ string | md5 }}` returns `string`\n- `sha1`: `{{ string | sha1: string }}` returns `string`\n- `sha256`: `{{ string | sha256: string }}` returns `string`\n- `append`: `{{ string | append: string }}` returns `string`\n- `base64_decode`: `{{ string | base64_decode }}` returns `string`\n- `base64_encode`: `{{ string | base64_encode }}` returns `string`\n- `base64_url_safe_decode`: `{{ string | base64_url_safe_decode }}` returns `string`\n- `base64_url_safe_encode`: `{{ string | base64_url_safe_encode }}` returns `string`\n- `capitalize`: `{{ string | capitalize }}` returns `string`\n- `downcase`: `{{ string | downcase }}` returns `string`\n- `escape`: `{{ string | escape }}` returns `string`\n- `escape_once`: `{{ string | escape_once }}` returns `string`\n- `lstrip`: `{{ string | lstrip }}` returns `string`\n- `newline_to_br`: `{{ string | newline_to_br }}` returns `string`\n- `prepend`: `{{ string | prepend: string }}` returns `string`\n- `remove`: `{{ string | remove: string }}` returns `string`\n- `remove_first`: `{{ string | remove_first: string }}` returns `string`\n- `remove_last`: `{{ string | remove_last: string }}` returns `string`\n- `replace`: `{{ string | replace: string, string }}` returns `string`\n- `replace_first`: `{{ string | replace_first: string, string }}` returns `string`\n- `replace_last`: `{{ string | replace_last: string, string }}` returns `string`\n- `rstrip`: `{{ string | rstrip }}` returns `string`\n- `slice`: `{{ string | slice }}` returns `string`\n- `split`: `{{ string | split: string }}` returns `array`\n- `strip`: `{{ string | strip }}` returns `string`\n- `strip_html`: `{{ string | strip_html }}` returns `string`\n- `strip_newlines`: `{{ string | strip_newlines }}` returns `string`\n- `truncate`: `{{ string | truncate: number }}` returns `string`\n- `truncatewords`: `{{ string | truncatewords: number }}` returns `string`\n- `upcase`: `{{ string | upcase }}` returns `string`\n- `url_decode`: `{{ string | url_decode }}` returns `string`\n- `url_encode`: `{{ string | url_encode }}` returns `string`\n- `camelize`: `{{ string | camelize }}` returns `string`\n- `handleize`: `{{ string | handleize }}` returns `string`\n- `url_escape`: `{{ string | url_escape }}` returns `string`\n- `url_param_escape`: `{{ string | url_param_escape }}` returns `string`\n- `pluralize`: `{{ number | pluralize: string, string }}` returns `string`\n\n#### Tag\n- `link_to_add_tag`: `{{ string | link_to_add_tag }}` returns `string`\n- `link_to_remove_tag`: `{{ string | link_to_remove_tag }}` returns `string`\n- `link_to_tag`: `{{ string | link_to_tag }}` returns `string`\n\n### Liquid objects\n\n#### Global objects\n- `collections`\n- `pages`\n- `all_products`\n- `articles`\n- `blogs`\n- `cart`\n- `closest`\n- `content_for_header`\n- `customer`\n- `images`\n- `linklists`\n- `localization`\n- `metaobjects`\n- `request`\n- `routes`\n- `shop`\n- `theme`\n- `settings`\n- `template`\n- `additional_checkout_buttons`\n- `all_country_option_tags`\n- `canonical_url`\n- `content_for_additional_checkout_buttons`\n- `content_for_index`\n- `content_for_layout`\n- `country_option_tags`\n- `current_page`\n- `handle`\n- `page_description`\n- `page_image`\n- `page_title`\n- `powered_by_link`\n- `scripts`\n\n#### `/article` page\n- `article`\n- `blog`\n\n#### `/blog` page\n- `blog`\n- `current_tags`\n\n#### `/cart` page\n- `cart`\n\n#### `/checkout` page\n- `checkout`\n\n#### `/collection` page\n- `collection`\n- `current_tags`\n\n#### `/customers/account` page\n- `customer`\n\n#### `/customers/addresses` page\n- `customer`\n\n#### `/customers/order` page\n- `customer`\n- `order`\n\n#### `/gift_card.liquid` page\n- `gift_card`\n- `recipient`\n\n#### `/metaobject` page\n- `metaobject`\n\n#### `/page` page\n- `page`\n\n#### `/product` page\n- `product`\n\n#### `/robots.txt.liquid` page\n- `robots`\n\n#### `/search` page\n- `search`\n### Liquid tags\n\n\n#### content_for\nThe `content_for` tag requires a type parameter to differentiate between rendering a number of theme blocks (`\'blocks\'`) and a single static block (`\'block\'`).\n\n\nSyntax:\n```\n{% content_for \'blocks\' %}\n{% content_for \'block\', type: "slide", id: "slide-1" %}\n```\n\n#### form\nBecause there are many different form types available in Shopify themes, the `form` tag requires a type. Depending on the\nform type, an additional parameter might be required. You can specify the following form types:\n\n- [`activate_customer_password`](https://shopify.dev/docs/api/liquid/tags/form#form-activate_customer_password)\n- [`cart`](https://shopify.dev/docs/api/liquid/tags/form#form-cart)\n- [`contact`](https://shopify.dev/docs/api/liquid/tags/form#form-contact)\n- [`create_customer`](https://shopify.dev/docs/api/liquid/tags/form#form-create_customer)\n- [`currency`](https://shopify.dev/docs/api/liquid/tags/form#form-currency)\n- [`customer`](https://shopify.dev/docs/api/liquid/tags/form#form-customer)\n- [`customer_address`](https://shopify.dev/docs/api/liquid/tags/form#form-customer_address)\n- [`customer_login`](https://shopify.dev/docs/api/liquid/tags/form#form-customer_login)\n- [`guest_login`](https://shopify.dev/docs/api/liquid/tags/form#form-guest_login)\n- [`localization`](https://shopify.dev/docs/api/liquid/tags/form#form-localization)\n- [`new_comment`](https://shopify.dev/docs/api/liquid/tags/form#form-new_comment)\n- [`product`](https://shopify.dev/docs/api/liquid/tags/form#form-product)\n- [`recover_customer_password`](https://shopify.dev/docs/api/liquid/tags/form#form-recover_customer_password)\n- [`reset_customer_password`](https://shopify.dev/docs/api/liquid/tags/form#form-reset_customer_password)\n- [`storefront_password`](https://shopify.dev/docs/api/liquid/tags/form#form-storefront_password)\n\n\nSyntax:\n```\n{% form \'form_type\' %}\n  content\n{% endform %}\n```\n\n#### layout\n\nSyntax:\n```\n{% layout name %}\n```\n\n#### assign\nYou can create variables of any [basic type](https://shopify.dev/docs/api/liquid/basics#types), [object](https://shopify.dev/docs/api/liquid/objects), or object property.\n\n> Caution:\n> Predefined Liquid objects can be overridden by variables with the same name.\n> To make sure that you can access all Liquid objects, make sure that your variable name doesn\'t match a predefined object\'s name.\n\n\nSyntax:\n```\n{% assign variable_name = value %}\n```\n\n#### break\n\nSyntax:\n```\n{% break %}\n```\n\n#### capture\nYou can create complex strings with Liquid logic and variables.\n\n> Caution:\n> Predefined Liquid objects can be overridden by variables with the same name.\n> To make sure that you can access all Liquid objects, make sure that your variable name doesn\'t match a predefined object\'s name.\n\n\nSyntax:\n```\n{% capture variable %}\n  value\n{% endcapture %}\n```\n\n#### case\n\nSyntax:\n```\n{% case variable %}\n  {% when first_value %}\n    first_expression\n  {% when second_value %}\n    second_expression\n  {% else %}\n    third_expression\n{% endcase %}\n```\n\n#### comment\nAny text inside `comment` tags won\'t be output, and any Liquid code will be parsed, but not executed.\n\n\nSyntax:\n```\n{% comment %}\n  content\n{% endcomment %}\n```\n\n#### continue\n\nSyntax:\n```\n{% continue %}\n```\n\n#### cycle\nThe `cycle` tag must be used inside a `for` loop.\n\n> Tip:\n> Use the `cycle` tag to output text in a predictable pattern. For example, to apply odd/even classes to rows in a table.\n\n\nSyntax:\n```\n{% cycle string, string, ... %}\n```\n\n#### decrement\nVariables that are declared with `decrement` are unique to the [layout](/themes/architecture/layouts), [template](/themes/architecture/templates),\nor [section](/themes/architecture/sections) file that they\'re created in. However, the variable is shared across\n[snippets](/themes/architecture/snippets) included in the file.\n\nSimilarly, variables that are created with `decrement` are independent from those created with [`assign`](https://shopify.dev/docs/api/liquid/tags/assign)\nand [`capture`](https://shopify.dev/docs/api/liquid/tags/capture). However, `decrement` and [`increment`](https://shopify.dev/docs/api/liquid/tags/increment) share\nvariables.\n\n\nSyntax:\n```\n{% decrement variable_name %}\n```\n\n#### doc\nThe `doc` tag allows developers to include documentation within Liquid\ntemplates. Any content inside `doc` tags is not rendered or outputted.\nLiquid code inside will be parsed but not executed. This facilitates\ntooling support for features like code completion, linting, and inline\ndocumentation.\n\nFor detailed documentation syntax and examples, see the\n[`LiquidDoc` reference](https://shopify.dev/docs/storefronts/themes/tools/liquid-doc).\n\n\nSyntax:\n```\n{% doc %}\n  Renders a message.\n\n  @param {string} foo - A string value.\n  @param {string} [bar] - An optional string value.\n\n  @example\n  {% render \'message\', foo: \'Hello\', bar: \'World\' %}\n{% enddoc %}\n```\n\n#### echo\nUsing the `echo` tag is the same as wrapping an expression in curly brackets (`{{` and `}}`). However, unlike the curly\nbracket method, you can use the `echo` tag inside [`liquid` tags](https://shopify.dev/docs/api/liquid/tags/liquid).\n\n> Tip:\n> You can use [filters](https://shopify.dev/docs/api/liquid/filters) on expressions inside `echo` tags.\n\n\nSyntax:\n```\n{% liquid\n  echo expression\n%}\n```\n\n#### for\nYou can do a maximum of 50 iterations with a `for` loop. If you need to iterate over more than 50 items, then use the\n[`paginate` tag](https://shopify.dev/docs/api/liquid/tags/paginate) to split the items over multiple pages.\n\n> Tip:\n> Every `for` loop has an associated [`forloop` object](https://shopify.dev/docs/api/liquid/objects/forloop) with information about the loop.\n\n\nSyntax:\n```\n{% for variable in array %}\n  expression\n{% endfor %}\n```\n\n#### if\n\nSyntax:\n```\n{% if condition %}\n  expression\n{% endif %}\n```\n\n#### increment\nVariables that are declared with `increment` are unique to the [layout](/themes/architecture/layouts), [template](/themes/architecture/templates),\nor [section](/themes/architecture/sections) file that they\'re created in. However, the variable is shared across\n[snippets](/themes/architecture/snippets) included in the file.\n\nSimilarly, variables that are created with `increment` are independent from those created with [`assign`](https://shopify.dev/docs/api/liquid/tags/assign)\nand [`capture`](https://shopify.dev/docs/api/liquid/tags/capture). However, `increment` and [`decrement`](https://shopify.dev/docs/api/liquid/tags/decrement) share\nvariables.\n\n\nSyntax:\n```\n{% increment variable_name %}\n```\n\n#### raw\n\nSyntax:\n```\n{% raw %}\n  expression\n{% endraw %}\n```\n\n#### render\nInside snippets and app blocks, you can\'t directly access variables that are [created](https://shopify.dev/docs/api/liquid/tags/variable-tags) outside\nof the snippet or app block. However, you can [specify variables as parameters](https://shopify.dev/docs/api/liquid/tags/render#render-passing-variables-to-a-snippet)\nto pass outside variables to snippets.\n\nWhile you can\'t directly access created variables, you can access global objects, as well as any objects that are\ndirectly accessible outside the snippet or app block. For example, a snippet or app block inside the [product template](/themes/architecture/templates/product)\ncan access the [`product` object](https://shopify.dev/docs/api/liquid/objects/product), and a snippet or app block inside a [section](/themes/architecture/sections)\ncan access the [`section` object](https://shopify.dev/docs/api/liquid/objects/section).\n\nOutside a snippet or app block, you can\'t access variables created inside the snippet or app block.\n\n> Note:\n> When you render a snippet using the `render` tag, you can\'t use the [`include` tag](https://shopify.dev/docs/api/liquid/tags/include)\n> inside the snippet.\n\n\nSyntax:\n```\n{% render \'filename\' %}\n```\n\n#### tablerow\nThe `tablerow` tag must be wrapped in HTML `<table>` and `</table>` tags.\n\n> Tip:\n> Every `tablerow` loop has an associated [`tablerowloop` object](https://shopify.dev/docs/api/liquid/objects/tablerowloop) with information about the loop.\n\n\nSyntax:\n```\n{% tablerow variable in array %}\n  expression\n{% endtablerow %}\n```\n\n#### unless\n> Tip:\n> Similar to the [`if` tag](https://shopify.dev/docs/api/liquid/tags/if), you can use `elsif` to add more conditions to an `unless` tag.\n\n\nSyntax:\n```\n{% unless condition %}\n  expression\n{% endunless %}\n```\n\n#### paginate\nBecause [`for` loops](https://shopify.dev/docs/api/liquid/tags/for) are limited to 50 iterations per page, you need to use the `paginate` tag to\niterate over an array that has more than 50 items. The following arrays can be paginated:\n\n- [`all_products`](https://shopify.dev/docs/api/liquid/objects/all_products)\n- [`article.comments`](https://shopify.dev/docs/api/liquid/objects/article#article-comments)\n- [`blog.articles`](https://shopify.dev/docs/api/liquid/objects/blog#blog-articles)\n- [`collections`](https://shopify.dev/docs/api/liquid/objects/collections)\n- [`collection.products`](https://shopify.dev/docs/api/liquid/objects/collection#collection-products)\n- [`customer.addresses`](https://shopify.dev/docs/api/liquid/objects/customer#customer-addresses)\n- [`customer.orders`](https://shopify.dev/docs/api/liquid/objects/customer#customer-orders)\n- [`pages`](https://shopify.dev/docs/api/liquid/objects/pages)\n- [`product.variants`](https://shopify.dev/docs/api/liquid/objects/product#variants)\n- [`search.results`](https://shopify.dev/docs/api/liquid/objects/search#search-results)\n- [`collection_list` settings](/themes/architecture/settings/input-settings#collection_list)\n- [`product_list` settings](/themes/architecture/settings/input-settings#product_list)\n\nWithin the `paginate` tag, you have access to the [`paginate` object](https://shopify.dev/docs/api/liquid/objects/paginate). You can use this\nobject, or the [`default_pagination` filter](https://shopify.dev/docs/api/liquid/filters/default_pagination), to build page navigation.\n\n\nSyntax:\n```\n{% paginate array by page_size %}\n  {% for item in array %}\n    forloop_content\n  {% endfor %}\n{% endpaginate %}\n\nThe `paginate` tag allows the user to paginate to the 25,000th item in the array and no further. To reach items further in\nthe array the array should be filtered further before paginating. See\n[Pagination Limits](/themes/best-practices/performance/platform#pagination-limits) for more information.\n```\n\n#### javascript\nEach section, block or snippet can have only one `{% javascript %}` tag.\n\nTo learn more about how JavaScript that\'s defined between the `javascript` tags is loaded and run, refer to the documentation for [javascript tags](/storefronts/themes/best-practices/javascript-and-stylesheet-tags#javascript).\n> Caution:\n> Liquid isn\'t rendered inside of `{% javascript %}` tags. Including Liquid code can cause syntax errors.\n\n\nSyntax:\n```\n{% javascript %}\n  javascript_code\n{% endjavascript %}\n```\n\n#### section\nRendering a section with the `section` tag renders a section statically. To learn more about sections and how to use\nthem in your theme, refer to [Render a section](/themes/architecture/sections#render-a-section).\n\n\nSyntax:\n```\n{% section \'name\' %}\n```\n\n#### stylesheet\nEach section, block or snippet can have only one `{% stylesheet %}` tag.\n\nTo learn more about how CSS that\'s defined between the `stylesheet` tags is loaded and run, refer to the documentation for [stylesheet tags](/storefronts/themes/best-practices/javascript-and-stylesheet-tags#stylesheet).\n> Caution:\n> Liquid isn\'t rendered inside of `{% stylesheet %}` tags. Including Liquid code can cause syntax errors.\n\n\nSyntax:\n```\n{% stylesheet %}\n  css_styles\n{% endstylesheet %}\n```\n\n#### sections\nUse this tag to render section groups as part of the theme\'s [layout](/themes/architecture/layouts) content. Place the `sections` tag where you want to render it in the layout.\n\nTo learn more about section groups and how to use them in your theme, refer to [Section groups](/themes/architecture/section-groups#usage).\n\n\nSyntax:\n```\n{% sections \'name\' %}\n```\n\n#### style\n> Note:\n> If you reference [color settings](/themes/architecture/settings/input-settings#color) inside `style` tags, then\n> the associated CSS rules will update as the setting is changed in the theme editor, without a page refresh.\n\n\nSyntax:\n```\n{% style %}\n  CSS_rules\n{% endstyle %}\n```\n\n#### else\nYou can use the `else` tag with the following tags:\n\n- [`case`](https://shopify.dev/docs/api/liquid/tags/case)\n- [`if`](https://shopify.dev/docs/api/liquid/tags/if)\n- [`unless`](https://shopify.dev/docs/api/liquid/tags/unless)\n\n\nSyntax:\n```\n{% else %}\n  expression\n```\n\n#### else\n\nSyntax:\n```\n{% for variable in array %}\n  first_expression\n{% else %}\n  second_expression\n{% endfor %}\n```\n\n#### liquid\nBecause the tags don\'t have delimeters, each tag needs to be on its own line.\n\n> Tip:\n> Use the [`echo` tag](https://shopify.dev/docs/api/liquid/tags/echo) to output an expression inside `liquid` tags.\n\n\nSyntax:\n```\n{% liquid\n  expression\n%}\n```\n\n\n## Translation development standards\n\n### Translation requirements\n\n- **Every user-facing text** must use translation filters.\n- **Update `locales/en.default.json`** with all new keys.\n- **Use descriptive, hierarchical keys** for organization.\n- **Only add English text**; translators handle other languages.\n\n### Translation filter usage\n\n**Use `{{ \'key\' | t }}` for all text:**\n\n```liquid\n<!-- Good -->\n<h2>{{ \'sections.featured_collection.title\' | t }}</h2>\n<p>{{ \'sections.featured_collection.description\' | t }}</p>\n<button>{{ \'products.add_to_cart\' | t }}</button>\n\n<!-- Bad -->\n<h2>Featured Collection</h2>\n<p>Check out our best products</p>\n<button>Add to cart</button>\n```\n\n### Translation with variables\n\n**Use variables for interpolation:**\n\n```liquid\n<!-- Liquid template -->\n<p>{{ \'products.price_range\' | t: min: product.price_min | money, max: product.price_max | money }}</p>\n<p>{{ \'general.pagination.page\' | t: page: paginate.current_page, pages: paginate.pages }}</p>\n```\n\n**Corresponding keys in locale files:**\n\n```json\n{\n  "products": {\n    "price_range": "From {{ min }} to {{ max }}"\n  },\n  "general": {\n    "pagination": {\n      "page": "Page {{ page }} of {{ pages }}"\n    }\n  }\n}\n```\n\n### Best practices\n\n**Content guidelines:**\n- Write clear, concise text.\n- **Use sentence case** for all user-facing text, including titles, headings, and button labels (capitalize only the first word and proper nouns; e.g., `Featured collection` → `Featured collection`, not `Featured Collection`).\n- Be consistent with terminology.\n- Consider character limits for UI elements.\n\n**Variable usage:**\n- Use interpolation rather than appending strings together.\n- Prioritize clarity over brevity for variable naming.\n- Escape variables unless they output HTML: `{{ variable | escape }}`.\n\n\n## Localization standards\n\nAuto-attached when working in `locales/` directory.\n\n### File structure\n\n```\nlocales/\n├── en.default.json          # English (required)\n├── en.default.schema.json   # English (required)\n├── es.json                  # Spanish\n├── est.schema.json          # Spanish\n├── fr.json                  # French\n├── frt.schema.json          # French\n└── pt-BR.json               # Portuguese\n└── pt-BR..schema.json       # Portuguese\n```\n\n#### Locale files\n\nLocale files are JSON files containing translations for all the text strings used throughout a Shopify theme and its editor. They let merchants easily update and localize repeated words and phrases, making it possible to translate store content and settings into multiple languages for international customers. These files provide a centralized way to manage and edit translations.\n\n**Example:**\n```json\n{\n  "general": {\n    "cart": "Cart",\n    "checkout": "Checkout"\n  },\n  "products": {\n    "add_to_cart": "Add to Cart"\n  }\n}\n```\n\n#### Schema locale files\n\nSchema locale files, saved with a .schema.json extension, store translation strings specifically for theme editor setting schemas. They follow a structured organization—category, group, and description—to give context to each translation, enabling accurate localization of editor content. Schema locale files must use the IETF language tag format in their naming, such as en-GB.schema.json for British English or fr-CA.schema.json for Canadian French.\n\n**Example:**\n```json\n{\n  "products": {\n    "card": {\n      "description": "Product card layout"\n    }\n  }\n}\n```\n\n### Key organization\n\n**Hierarchical structure:**\n```json\n{\n  "general": {\n    "meta": {\n      "title": "{{ shop_name }}",\n      "description": "{{ shop_description }}"\n    },\n    "accessibility": {\n      "skip_to_content": "Skip to content",\n      "close": "Close"\n    }\n  },\n  "products": {\n    "add_to_cart": "Add to cart",\n    "quick_view": "Quick view",\n    "price": {\n      "regular": "Regular price",\n      "sale": "Sale price",\n      "unit": "Unit price"\n    }\n  }\n}\n```\n**Usage**\n```liquid\n{{ \'general.meta.title\' | t: shop_name: shop.name }}\n{{ \'general.meta.description\' | t: shop_description: shop.description }}\n```\n\n### Translation guidelines\n\n**Key naming:**\n- Use descriptive, hierarchical keys\n- Maximum 3 levels deep\n- Use snake_case for key names\n- Group related translations\n\n**Content rules:**\n- Keep text concise for UI elements\n- Use variables for dynamic content\n- Consider character limits\n- Maintain consistent terminology\n\n## Examples per kind of asset\n\n### `snippet`\n\n```liquid\n{% doc %}\n  Renders a responsive image that might be wrapped in a link.\n\n  When `width`, `height` and `crop` are provided, the image will be rendered\n  with a fixed aspect ratio.\n\n  Serves as an example of how to use the `image_url` filter and `image_tag` filter\n  as well as how you can use LiquidDoc to document your code.\n\n  @param {image} image - The image to be rendered\n  @param {string} [url] - An optional destination URL for the image\n  @param {string} [css_class] - Optional class to be added to the image wrapper\n  @param {number} [width] - The highest resolution width of the image to be rendered\n  @param {number} [height] - The highest resolution height of the image to be rendered\n  @param {string} [crop] - The crop position of the image\n\n  @example\n  {% render \'image\', image: product.featured_image %}\n  {% render \'image\', image: product.featured_image, url: product.url %}\n  {% render \'image\',\n    css_class: \'product__image\',\n    image: product.featured_image,\n    url: product.url,\n    width: 1200,\n    height: 800,\n    crop: \'center\',\n  %}\n{% enddoc %}\n\n{% liquid\n  unless height\n    assign width = width | default: image.width\n  endunless\n\n  if url\n    assign wrapper = \'a\'\n  else\n    assign wrapper = \'div\'\n  endif\n%}\n\n<{{ wrapper }}\n  class="image {{ css_class }}"\n  {% if url %}\n    href="{{ url }}"\n  {% endif %}\n>\n  {{ image | image_url: width: width, height: height, crop: crop | image_tag }}\n</{{ wrapper }}>\n\n{% stylesheet %}\n  .image {\n    display: block;\n    position: relative;\n    overflow: hidden;\n    width: 100%;\n    height: auto;\n  }\n\n  .image > img {\n    width: 100%;\n    height: auto;\n  }\n{% endstylesheet %}\n\n{% javascript %}\n  function doSomething() {\n    // example\n  }\n  doSomething()\n{% endjavascript %}\n\n```\n\n### `block`\n\n#### Text\n\n```liquid\n{% doc %}\n  Renders a text block.\n\n  @example\n  {% content_for \'block\', type: \'text\', id: \'text\' %}\n{% enddoc %}\n\n<div\n  class="text {{ block.settings.text_style }}"\n  style="--text-align: {{ block.settings.alignment }}"\n  {{ block.shopify_attributes }}\n>\n  {{ block.settings.text }}\n</div>\n\n{% stylesheet %}\n  .text {\n    text-align: var(--text-align);\n  }\n  .text--title {\n    font-size: 2rem;\n    font-weight: 700;\n  }\n  .text--subtitle {\n    font-size: 1.5rem;\n  }\n{% endstylesheet %}\n\n{% schema %}\n{\n  "name": "t:general.text",\n  "settings": [\n    {\n      "type": "text",\n      "id": "text",\n      "label": "t:labels.text",\n      "default": "Text"\n    },\n    {\n      "type": "select",\n      "id": "text_style",\n      "label": "t:labels.text_style",\n      "options": [\n        { "value": "text--title", "label": "t:options.text_style.title" },\n        { "value": "text--subtitle", "label": "t:options.text_style.subtitle" },\n        { "value": "text--normal", "label": "t:options.text_style.normal" }\n      ],\n      "default": "text--title"\n    },\n    {\n      "type": "text_alignment",\n      "id": "alignment",\n      "label": "t:labels.alignment",\n      "default": "left"\n    }\n  ],\n  "presets": [{ "name": "t:general.text" }]\n}\n{% endschema %}\n```\n\n#### Group\n\n```liquid\n{% doc %}\n  Renders a group of blocks with configurable layout direction, gap and\n  alignment.\n\n  All settings apply to only one dimension to reduce configuration complexity.\n\n  This component is a wrapper concerned only with rendering its children in\n  the specified layout direction with appropriate padding and alignment.\n\n  @example\n  {% content_for \'block\', type: \'group\', id: \'group\' %}\n{% enddoc %}\n\n<div\n  class="group {{ block.settings.layout_direction }}"\n  style="\n    --padding: {{ block.settings.padding }}px;\n    --alignment: {{ block.settings.alignment }};\n  "\n  {{ block.shopify_attributes }}\n>\n  {% content_for \'blocks\' %}\n</div>\n\n{% stylesheet %}\n  .group {\n    display: flex;\n    flex-wrap: nowrap;\n    overflow: hidden;\n    width: 100%;\n  }\n  .group--horizontal {\n    flex-direction: row;\n    justify-content: space-between;\n    align-items: center;\n    padding: 0 var(--padding);\n  }\n  .group--vertical {\n    flex-direction: column;\n    align-items: var(--alignment);\n    padding: var(--padding) 0;\n  }\n{% endstylesheet %}\n\n{% schema %}\n{\n  "name": "t:general.group",\n  "blocks": [{ "type": "@theme" }],\n  "settings": [\n    {\n      "type": "select",\n      "id": "layout_direction",\n      "label": "t:labels.layout_direction",\n      "default": "group--vertical",\n      "options": [\n        { "value": "group--horizontal", "label": "t:options.direction.horizontal" },\n        { "value": "group--vertical", "label": "t:options.direction.vertical" }\n      ]\n    },\n    {\n      "visible_if": "{{ block.settings.layout_direction == \'group--vertical\' }}",\n      "type": "select",\n      "id": "alignment",\n      "label": "t:labels.alignment",\n      "default": "flex-start",\n      "options": [\n        { "value": "flex-start", "label": "t:options.alignment.left" },\n        { "value": "center", "label": "t:options.alignment.center" },\n        { "value": "flex-end", "label": "t:options.alignment.right" }\n      ]\n    },\n    {\n      "type": "range",\n      "id": "padding",\n      "label": "t:labels.padding",\n      "default": 0,\n      "min": 0,\n      "max": 200,\n      "step": 2,\n      "unit": "px"\n    }\n  ],\n  "presets": [\n    {\n      "name": "t:general.column",\n      "category": "t:general.layout",\n      "settings": {\n        "layout_direction": "group--vertical",\n        "alignment": "flex-start",\n        "padding": 0\n      }\n    },\n    {\n      "name": "t:general.row",\n      "category": "t:general.layout",\n      "settings": {\n        "layout_direction": "group--horizontal",\n        "padding": 0\n      }\n    }\n  ]\n}\n{% endschema %}\n```\n\n### `section`\n\n```liquid\n<div class="example-section full-width">\n  {% if section.settings.background_image %}\n    <div class="example-section__background">\n      {{ section.settings.background_image | image_url: width: 2000 | image_tag }}\n    </div>\n  {% endif %}\n\n  <div class="custom-section__content">\n    {% content_for \'blocks\' %}\n  </div>\n</div>\n\n{% stylesheet %}\n  .example-section {\n    position: relative;\n    overflow: hidden;\n    width: 100%;\n  }\n  .example-section__background {\n    position: absolute;\n    width: 100%;\n    height: 100%;\n    z-index: -1;\n    overflow: hidden;\n  }\n  .example-section__background img {\n    position: absolute;\n    width: 100%;\n    height: auto;\n    top: 50%;\n    left: 50%;\n    transform: translate(-50%, -50%);\n  }\n  .example-section__content {\n    display: grid;\n    grid-template-columns: var(--content-grid);\n  }\n\n  .example-section__content > * {\n    grid-column: 2;\n  }\n{% endstylesheet %}\n\n{% schema %}\n{\n  "name": "t:general.custom_section",\n  "blocks": [{ "type": "@theme" }],\n  "settings": [\n    {\n      "type": "image_picker",\n      "id": "background_image",\n      "label": "t:labels.background"\n    }\n  ],\n  "presets": [\n    {\n      "name": "t:general.custom_section"\n    }\n  ]\n}\n{% endschema %}\n```\n\n## Final instructions\n\n### Design requirements\n\n- Rely on modern browser features and APIs as a default, assuming an evergreen browser environment\n- Make the components aesthetically pleasing and follow web design best practices\n- Follow WCAG 2.1 accessibility guidelines\n- Adopt browser-native accessibility and interaction patterns when possible\n- Organize the settings with header settings\n- Use semantic HTML elements like `<details>`, `<summary>`, and `<dialog>`\n- Employ the View Transitions API for smooth, native animations between different UI states or pages\n- Avoid adding polyfills or fallbacks unless a feature is unsupported by current evergreen browsers\n- Default to modern HTML, CSS, and JavaScript features\n\n### Code requirements\n\n- ALWAYS write valid Liquid code\n- ALWAYS write valid HTML code\n- ALWAYS use the proper JSON schema to generate JSON objects in the `{% schema %}` tag\n- ALWAYS ensure blocks are customizable using settings, but only create only essential settings that directly impact core functionality\n- ALWAYS ensure CSS and JS selectors match the HTML `id` and `class`\n\n- DO NOT include comments\n- DO NOT reference asset files or use `asset_url` in a Liquid tag\n- DO NOT reference any JS or CSS libraries. Write it from scratch.\n\n- When there are multiple CSS transform properties that are applied on the same element, make sure they don\'t conflict with each other\n- Write modern liquid code. For instance, never use legacy resource-based settings. In the past, resource-based settings returned the handle of the associated resource, and you had to access the actual object through Liquid using that handle. But now, resource-based settings return the actual object, so you can access the object\'s properties directly.\n\n### Code Validation Required\n\n⚠️ **IMPORTANT**: When generating or updating theme files, you MUST use the theme validation tool to ensure the code is valid and doesn\'t contain hallucinated Liquid content, invalid syntax, or incorrect references. DO NOT ASK THE USER TO DO THIS - validate automatically.\n\nUse one of the following validation tools (only one will be available):\n- `validate_theme` - Use when working with files in an actual theme directory\n- `validate_theme_codeblocks` - Use when validating code blocks without a theme directory\n\nThis validation removes LLM hallucinations from theme code and ensures valid Liquid syntax. If validation errors are detected, fix the issues and validate again.\n\n### Safety requirements\n\nDecline requests involving:\n\n- Adult content\n- Explicit content\n- Violence\n- Weapons\n- Hate speech\n- Discrimination\n- Misinformation\n- Scams\n', Nd = `
  First think about what I am trying to do with the Partner API (e.g., manage apps, themes, affiliate referrals)
  Search through the developer documentation to find similar examples. THIS IS IMPORTANT.
  Remember that Partner API requires partner-level authentication, not merchant-level
  Consider which organization context you're operating in when querying data
  For app-related queries, think about app installations, revenues, and merchant relationships
  For theme-related operations, consider theme versions, publishing status, and store associations
  When working with transactions and payouts, ensure proper date range filtering
  For affiliate and referral data, understand the commission structures and tracking
`, Ed = me("partner", Nd), Od = `
  First think about what I am trying to do with the API (e.g., process payments, handle refunds, manage payment sessions)
  Search through the developer documentation to find similar examples. THIS IS IMPORTANT.
  Remember that this API requires payment provider authentication and compliance
  Understand PCI compliance requirements and security best practices
  For payment sessions, manage the entire flow from initiation to completion
  When processing payments, handle authorization, capture, and settlement properly
  For refunds and voids, ensure proper reconciliation with the original transaction
  Handle various payment methods including cards, wallets, and alternative payments
  Implement proper error handling for declined transactions and network issues
  Consider 3D Secure authentication and fraud prevention requirements
  Manage payment confirmations and webhook notifications
`, wd = me("payments-apps", Od);
function Ad() {
  const t = Ds("@shopify/polaris-types");
  if (!t)
    throw new Error("Could not find @shopify/polaris-types package");
  const e = sn(t, "dist", "polaris.d.ts");
  return ki(e, "utf-8");
}
const xd = Ad(), Cd = `
This is the relevant documentation for building the an app on the App Home surface.

References:
- /docs/api/app-home/apis
- /docs/api/app-home/using-polaris-components
- /docs/api/app-home/polaris-web-components

Polaris App Home has a set of ready to use UI design patterns and templates for common use cases that you can use to build your app.
🚨 MANDATORY FETCH THE PATTERNS AND TEMPLATES FROM /docs/api/app-home/patterns FIRST THEN USE THE PATHS TO FETCH THE RELEVANT PATTERN/TEMPLATE using the fetch_full_docs tool.
Utilize these patterns/templates whenever possible. Prioritize utlizing such patterns/templates over building your own from scratch.
`, kd = me(
  "polaris-app-home",
  Cd
) + `

## Polaris Component Types

Here are the TypeScript definitions for all available Polaris components:

\`\`\`typescript
` + xd + "\n```", Rd = `
  First think about what I am trying to do with the API (e.g., fetch products, create cart, checkout)
  Search through the developer documentation to find similar examples. THIS IS IMPORTANT.
  Remember that the Storefront API is public-facing and requires different authentication than Admin API
  For queries think about which fields are essential for customer-facing experiences
  Consider performance implications as Storefront API queries run on customer devices
  Include only necessary fields to minimize payload size and improve performance
  When working with products, remember to handle variants, pricing, and availability
  For checkout operations, ensure proper cart token management
`, Ld = me("storefront-graphql", Rd), Dd = El("storefront-web-components"), Pd = `
  ## CORE SETUP
  Add <shopify-store> and <shopify-context> near the root. Set domain, storefront-access-token, country, and locale. Use <shopify-list-context> and component-scoped contexts for nearest product/cart bindings.

  ## EXAMPLES STYLE
  Provide minimal, valid HTML with only essential attributes. Prefer attributes over custom JS. Focus on declarative HTML patterns that leverage the built-in component behaviors.

  ## KEY PRINCIPLES
  - Components handle their own data fetching internally
  - State management is built into the components
  - Use semantic HTML with component attributes
  - Minimize JavaScript - components are designed to work without custom scripts
  - Components automatically sync with Shopify's backend
  - When you generate code, ensure they're within \`\`\`javascript\`\`\` tags or another relevant tag.

  ## ZOD SCHEMA
  ${Dd}
`, Fd = me(
  "storefront-web-components",
  Pd
), Ud = {
  admin: pd,
  "storefront-graphql": Ld,
  partner: Ed,
  customer: fd,
  "custom-data": gd,
  "payments-apps": wd,
  functions: _d,
  "polaris-app-home": kd,
  "polaris-admin-extensions": Br(),
  "polaris-checkout-extensions": Qr(),
  "polaris-customer-account-extensions": Yr(),
  "pos-ui": Wr(),
  hydrogen: bd,
  "storefront-web-components": Fd,
  liquid: Sd
};
function Zr(t = Rt) {
  return Ps(t).filter((e) => e.category !== P.FUNCTION_GRAPHQL);
}
function Xr() {
  const t = Zr(), e = t.map((n) => n.name);
  return {
    name: "learn_shopify_api",
    // This tool is the entrypoint for our MCP server. It has the following responsibilities:
    // 1. It teaches the LLM what Shopify APIs are supported with this MCP server. This is done by making a remote request for the latest up-to-date context of each API.
    // 2. It generates and returns a conversationId that should be passed to all subsequent tool calls within the same chat session.
    description: `
    🚨 MANDATORY FIRST STEP: This tool MUST be called before any other Shopify tools.

    ⚠️  ALL OTHER SHOPIFY TOOLS WILL FAIL without a conversationId from this tool.
    This tool generates a conversationId that is REQUIRED for all subsequent tool calls. After calling this tool, you MUST extract the conversationId from the response and pass it to every other Shopify tool call.

    🔄 MULTIPLE API SUPPORT: You MUST call this tool multiple times in the same conversation when you need to learn about different Shopify APIs. THIS IS NOT OPTIONAL. Just pass the existing conversationId to maintain conversation continuity while loading the new API context.

    For example, a user might ask a question about the Admin API, then switch to the Functions API, then ask a question about polaris UI components. In this case I would expect you to call learn_shopify_api three times with the following arguments:

    - learn_shopify_api(api: "admin") -> conversationId: "123"
    - learn_shopify_api(api: "functions", conversationId: "123")
    - learn_shopify_api(api: "polaris-admin-extensions", conversationId: "123")

    This is because the conversationId is used to maintain conversation continuity while loading the new API context.

    🚨 Valid arguments for \`api\` are:
    ${t.filter((n) => !n.extensionSurfaceName).map((n) => `    - ${n.displayName}: ${n.description}`).join(`
`)}

    For APIs ${t.filter((n) => n.extensionSurfaceName).map((n) => `    - ${n.displayName}: ${n.description}`).join(
      `
`
    )}, call learn_shopify_api to get the conversationId and then YOU MUST call learn_extension_target_types.

    🔄 WORKFLOW:
    1. Call learn_shopify_api first with the initial API
    2. Extract the conversationId from the response
    3. Pass that same conversationId to ALL other Shopify tools
    4. If you need to know more about a different API at any point in the conversation, call learn_shopify_api again with the new API and the same conversationId

    When tool outputs are saved to a file always read the entire file first.
    DON'T SEARCH THE WEB WHEN REFERENCING INFORMATION FROM THIS DOCUMENTATION. IT WILL NOT BE ACCURATE.
    PREFER THE USE OF THE fetch_full_docs TOOL TO RETRIEVE INFORMATION FROM THE DEVELOPER DOCUMENTATION SITE.
  `,
    inputSchema: ve({
      api: At([e[0], ...e.slice(1)]).describe("The Shopify API you are building for"),
      conversationId: oe().optional().describe(
        "Optional existing conversation UUID. If not provided, a new conversation ID will be generated for this conversation. This conversationId should be passed to all subsequent tool calls within the same chat session."
      )
    }),
    handler: async (n) => {
      const s = n.conversationId ?? Yl(), i = Ud[n.api];
      if (!i)
        throw new Error(`API '${n.api}' not found`);
      return {
        content: [{ type: "text", text: `🔗 **IMPORTANT - SAVE THIS CONVERSATION ID:** ${s}
⚠️  CRITICAL: You MUST use this exact conversationId in ALL subsequent Shopify tool calls in this conversation.
🚨 ALL OTHER SHOPIFY TOOLS WILL RETURN ERRORS if you don't provide this conversationId.
---
${i}` }]
      };
    }
  };
}
async function Md(t) {
  Ye(t, Xr());
}
const $d = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Md,
  getEnabledAPIsForLearning: Zr,
  learnShopifyApiToolDef: Xr
}, Symbol.toStringTag, { value: "Module" })), Us = `

    It returns a comprehensive validation result with details for each code block explaining why it was valid or invalid.
    This detail is provided so LLMs know how to modify code snippets to remove errors.
    It also returns an artifact ID and revision number for each code block. This is used to track the code block and its validation results. When validating an iteration of the same code block, use the same artifact ID and increment the revision number. Do not pass your own artifact ID to this tool, the tool will generate one for you.`, Ms = {
  artifactId: oe().optional().describe(
    "Stable id assigned to the generated code artifact. Use the same artifactId when retrying validation on modified code to track iterations."
  ),
  revision: Ui().int().positive().optional().describe(
    "Monotonic revision number for the artifact. Start with 1 for new code, increment for each retry/iteration on the same artifactId. This helps track validation retries vs new validations."
  )
}, Kr = ve({
  content: oe().describe("The code block content"),
  ...Ms
}), jd = ve({
  path: oe().describe("The file path"),
  ...Ms
});
function Ln(t) {
  return t.map((e) => ({
    artifactId: e.artifactId || `artifact-${Ri()}`,
    revision: e.revision ?? 1
  }));
}
function Dn(t, e) {
  return t.map((n, s) => {
    const i = e[s];
    return i ? {
      ...n,
      artifactId: i.artifactId,
      artifactRevision: i.revision
    } : n;
  });
}
async function Vd(t, e) {
  try {
    let n = sn(
      t,
      ".theme-check.yml"
    );
    try {
      await fa(n);
    } catch {
      n = void 0;
    }
    const s = await ma(
      t,
      n,
      (a) => console.error(a)
    ), i = ea(s.offenses), r = [];
    for (const a of e) {
      const o = Object.keys(i).find(
        (c) => rn(c).endsWith(rn(a))
      );
      o ? r.push({
        result: ae.FAILED,
        resultDetail: `Theme file ${a} failed to validate:

${i[o].join(`
`)}`
      }) : r.push({
        result: ae.SUCCESS,
        resultDetail: `Theme file ${a} passed all checks from Shopify's Theme Check.`
      });
    }
    return r;
  } catch (n) {
    return e.map((s) => ({
      result: ae.FAILED,
      resultDetail: `Validation error: Could not validate ${s}. Details: ${n instanceof Error ? n.message : String(n)}`
    }));
  }
}
function ea(t) {
  return t.reduce(
    (e, n) => {
      let s = `ERROR: ${n.message}`;
      n.suggest && n.suggest.length > 0 && (s += `; SUGGESTED FIXES: ${n.suggest.map((r) => r.message).join("OR ")}.`);
      const i = n.uri;
      return e[i] ? e[i].push(s) : e[i] = [s], e;
    },
    {}
  );
}
async function qd(t) {
  const e = Bd(t);
  try {
    return await Gd(e);
  } catch (n) {
    return Object.keys(e).map((s) => ({
      result: ae.FAILED,
      resultDetail: `Validation error: Could not validate ${s}. Details: ${n instanceof Error ? n.message : String(n)}`
    }));
  }
}
async function Gd(t) {
  const e = await Hd(t), n = ea(e), s = [];
  for (let i of Object.keys(t)) {
    const r = ga.basename(i);
    n[i] ? s.push({
      result: ae.FAILED,
      resultDetail: `Theme codeblock ${r} has the following offenses from using Shopify's Theme Check:

${n[i].join(`
`)}`
    }) : s.push({
      result: ae.SUCCESS,
      resultDetail: `Theme codeblock ${r} had no offenses from using Shopify's Theme Check.`
    });
  }
  return s;
}
async function Hd(t) {
  const e = new Qd(t), n = {
    checks: va,
    settings: {},
    rootUri: "file:///",
    context: "theme"
  }, s = new Sa(), i = Object.entries(t).filter(
    ([r, a]) => r.endsWith(".liquid") || r.endsWith(".json")
  ).map(([r, a]) => ya(r, a, void 0));
  return await _a(i, n, {
    fs: e,
    themeDocset: s,
    jsonValidationSet: s,
    getBlockSchema: async (r) => {
      const a = `file:///blocks/${r}.liquid`, o = i.find((c) => c.uri === a);
      if (o)
        return Gs(
          "theme",
          a,
          o,
          async () => !0
        );
    },
    getSectionSchema: async (r) => {
      const a = `file:///sections/${r}.liquid`, o = i.find((c) => c.uri === a);
      if (o)
        return Gs(
          "theme",
          a,
          o,
          async () => !0
        );
    },
    async getDocDefinition(r) {
      const a = i.find(
        (o) => rn(o.uri).endsWith(rn(r))
      );
      if (!(!a || a.type !== Ta.LiquidHtml))
        return Ia(
          a.uri,
          a.ast
        );
    }
  });
}
function Bd(t) {
  return t.reduce((e, n) => {
    const s = `file:///${n.fileType}/${n.fileName}`;
    return e[s] = n.content, e;
  }, {});
}
class Qd {
  constructor(e) {
    this.partialTheme = e;
  }
  async readFile(e) {
    const n = this.partialTheme[e];
    if (!n)
      throw new Error(`File not found: ${e}`);
    return n;
  }
  async readDirectory() {
    return [];
  }
  async stat(e) {
    const n = this.partialTheme[e];
    if (!n)
      throw new Error(`File not found: ${e}`);
    return {
      type: ba.File,
      size: n.length
    };
  }
}
const ta = `This tool validates Liquid codeblocks, Liquid files, and supporting Theme files (e.g. JSON locale files, JSON config files, JSON template files, JavaScript files, CSS files, and SVG files) generated or updated by LLMs to ensure they don't have hallucinated Liquid content, invalid syntax, or incorrect references${Us}`;
function na() {
  return {
    name: "validate_theme_codeblocks",
    description: `${ta}. Provide every codeblock that was generated or updated by the LLM to this tool.`,
    inputSchema: ve({
      codeblocks: wt(
        ve({
          fileName: oe().describe(
            "The filename of the codeblock. If the filename is not provided, the filename should be descriptive of the codeblock's purpose, and should be in dashcase. Include file extension in the filename."
          ),
          fileType: At([
            "assets",
            "blocks",
            "config",
            "layout",
            "locales",
            "sections",
            "snippets",
            "templates"
          ]).default("blocks").describe(
            "The type of codeblock generated. All JavaScript, CSS, and SVG files are in assets folder. Locale files are JSON files located in the locale folder. If the translation is only used in schemas, it should be in `locales/en(.default).schema.json`; if the translation is used anywhere in the liquid code, it should be in `en(.default).json`. The brackets show an optional default locale. The locale code should be the two-letter code for the locale."
          ),
          content: oe().describe("The content of the file."),
          ...Ms
        })
      ).describe(
        "An array of codeblocks to validate with optional artifact metadata."
      )
    }),
    handler: async (t) => {
      const e = t, n = Ln(
        e.codeblocks
      ), s = e.codeblocks.map((o) => ({
        fileName: o.fileName,
        fileType: o.fileType || "blocks",
        content: o.content
      })), i = await qd(s), r = Dn(
        i,
        n
      );
      return {
        content: [
          {
            type: "text",
            text: Pn(
              r,
              "Theme Codeblocks"
            )
          }
        ]
      };
    }
  };
}
function sa() {
  return {
    name: "validate_theme",
    description: `${ta}. Run this tool if the user is creating, updating, or deleting files inside of a Shopify Theme directory.`,
    inputSchema: ve({
      absoluteThemePath: oe().describe("The absolute path to the theme directory"),
      filesCreatedOrUpdated: wt(
        jd.extend({
          path: oe().describe("The relative file path")
        })
      ).describe("Array of files with path and optional artifact metadata")
    }),
    handler: async (t) => {
      const e = t, n = e.filesCreatedOrUpdated, s = n.map((c) => c.path), i = Ln(n), r = await Vd(
        e.absoluteThemePath,
        s
      ), a = Dn(
        r,
        i
      );
      return {
        content: [
          {
            type: "text",
            text: Pn(
              a,
              "Theme"
            )
          }
        ]
      };
    }
  };
}
async function Yd(t) {
  Rt.liquidMcpValidationMode === "partial" ? Ye(t, na()) : Ye(t, sa());
}
const Wd = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Yd,
  validateThemeCodeblocksToolDef: na,
  validateThemeToolDef: sa
}, Symbol.toStringTag, { value: "Module" }));
async function xi(t, e, n) {
  const s = {
    "Content-Type": "application/json"
  };
  n && (s["X-Shopify-Dev-Use-OpenAI-Search"] = "true");
  const i = await kn("/assistant/search", {
    method: "POST",
    headers: s,
    body: JSON.stringify({
      query: t,
      ...e
    }),
    instrumentation: Fs()
  });
  console.error(
    `[search-shopify-docs] Response text (truncated): ${i.substring(0, 200) + (i.length > 200 ? "..." : "")}`
  );
  try {
    const r = JSON.parse(i);
    return JSON.stringify(r, null, 2);
  } catch (r) {
    return console.warn(`[search-shopify-docs] Error parsing JSON response: ${r}`), i;
  }
}
async function ia(t, e = {}) {
  const n = Rt.useLegacySearch;
  try {
    return await xi(t, e, n);
  } catch (s) {
    const i = s instanceof Error ? s.message : String(s);
    console.error(
      `[search-shopify-docs] Search failed using ${n ? "legacy" : "new"} search. Retrying with ${n ? "new" : "legacy"} search...`
    );
    try {
      return await xi(t, e, !n) + `

[Note: Initial search using ${n ? "legacy" : "new"} search failed. Results retrieved using ${n ? "new" : "legacy"} search.]`;
    } catch (r) {
      const a = r instanceof Error ? r.message : String(r);
      throw new Error(
        `Search failed on both backends. Please try again later.

First attempt (${n ? "legacy" : "new"} search): ${i}

Retry attempt (${n ? "new" : "legacy"} search): ${a}

You can control which search backend to start with by setting USE_LEGACY_SEARCH=true or USE_LEGACY_SEARCH=false in your MCP client configuration.`
      );
    }
  }
}
function ra() {
  return {
    name: "search_docs_chunks",
    description: "This tool will take in the user prompt, search shopify.dev, and return relevant documentation and code examples that will help answer the user's question.",
    inputSchema: ve({
      prompt: oe().describe("The search query for Shopify documentation"),
      max_num_results: Ui().optional().describe(
        "Maximum number of results to return from the search. Do not pass this when calling the tool for the first time, only use this when you want to limit the number of results deal with small context window issues."
      )
    }),
    handler: async (t) => {
      const e = t, n = {
        ...e.max_num_results != null && {
          max_num_results: e.max_num_results.toString()
        },
        ...e.conversationId && {
          conversation_id: e.conversationId
        }
      };
      return {
        content: [
          {
            type: "text",
            text: await ia(
              e.prompt,
              n
            )
          }
        ]
      };
    }
  };
}
async function zd(t) {
  Ye(t, ra());
}
const Jd = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: zd,
  searchShopifyDocs: ia,
  searchShopifyDocsToolDef: ra
}, Symbol.toStringTag, { value: "Module" }));
function aa() {
  return {
    name: "fetch_full_docs",
    description: "Use this tool to retrieve a list of full documentation pages from shopify.dev.\n    When you find relevant document chunks using the search_docs_chunks tool, YOU MUST USE THIS TOOL TO RETRIEVE THE FULL DOCUMENTATION PAGES. THIS IS IMPORTANT. You can use the `url` attribute of the chunks.\n    ",
    inputSchema: ve({
      paths: wt(oe()).describe(
        'The paths to the full documentation pages to read, i.e. ["/docs/api/app-home", "/docs/api/functions"]. Paths should be relative to the root of the developer documentation site.'
      )
    }),
    handler: async (t) => {
      const e = t;
      async function n(i) {
        const r = i.endsWith(".md") ? i : `${i}.md`, a = await kn(r, {
          parameters: e.conversationId ? { conversation_id: e.conversationId } : void 0,
          instrumentation: Fs()
        });
        return `## ${i}

${a}

`;
      }
      return {
        content: [
          {
            type: "text",
            text: (await Promise.all(
              e.paths.map(n)
            )).join(`---

`)
          }
        ]
      };
    }
  };
}
async function Zd(t) {
  Ye(t, aa());
}
const Xd = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Zd,
  shopifyDevFetch: kn,
  shopifyDevFetchToolDef: aa
}, Symbol.toStringTag, { value: "Module" }));
function oa() {
  const t = Object.values(Ot).filter((e) => e.extensionSurfaceName).map((e) => e.name);
  return {
    name: "validate_component_codeblocks",
    description: `🚨 MANDATORY VALIDATION TOOL - MUST BE CALLED WHEN COMPONENTS FROM SHOPIFY PACKAGES ARE USED. DONT ASK THE USER TO DO THIS. DON'T CONTEXT SWITCH.

    This tool MUST be used to validate ALL code blocks containing Shopify components, regardless of size or complexity.

    ⚠️  CRITICAL REQUIREMENTS:
    - Call this tool IMMEDIATELY after generating ANY Shopify component code
    - NEVER skip validation, even for simple examples or snippets
    - ALWAYS use this tool when generating JSX, TSX, or web component code
    - This validation prevents hallucinated components, props, and prop values
    - Your major tasks while structuring the input is to ensure that:
    1. If the code contains JS/TS code, it should be wrapped inside a function. 
    2. All Javascript code is outside return statement of the function.
    3. All the polaris web components are in return statement of the function.

    INPUT STRUCTURE:
    Do not pass in tiny snippets of code. Follow the example given below as the input to validate_component_codeblocks tool.
    {
      "api": "The API name (e.g., 'polaris-app-home', 'pos-ui')",
      "code": [
        {
          "content": "
const Extension = () => {
  const [isConnected, setIsConnected] = useState(
    shopify.connectivity.current.value.internetConnected === 'Connected'
  );

  useEffect(() => {
    const unsubscribe = shopify.connectivity.current.subscribe((newConnectivity) => {
      setIsConnected(newConnectivity.internetConnected === 'Connected');
    });
    return unsubscribe;
  }, []);

  return (
    <s-tile
      heading="My App"
      disabled={!isConnected}
    />
  );
};
"
        }
      ]
    }

    📤 OUTPUTS:
    - Comprehensive validation results with specific error details
    - Clear guidance on how to fix any validation failures
    - Component-by-component validation status

    🔄 WORKFLOW: Generate Code → Validate → Fix Errors and replace code → Re-validate if needed
${Us}`,
    inputSchema: ve({
      code: wt(
        Kr.extend({
          content: oe().describe("The markdown code block content")
        })
      ).describe(
        "Array of code blocks with content and optional artifact metadata. Supported code blocks are JS/TS React, vanilla JS/TS, Preact, and HTML with Polaris web components."
      ),
      api: At(
        Object.values(ne)
      ).describe(
        "API name to validate against (e.g., 'pos-ui', 'polaris-app-home')."
      ),
      extensionTarget: oe().optional().describe(
        "Required for extension surface APIs (" + t.join(", ") + "). The extension target determines which components and APIs are available. Get available targets using learn_extension_target_types tool."
      )
    }),
    handler: async (e) => {
      const n = e, { code: s, api: i, extensionTarget: r } = n;
      if (i === ne.STOREFRONT_WEB_COMPONENTS)
        return {
          content: [
            {
              type: "text",
              text: "This tool is not supported for api: " + i
            }
          ]
        };
      const a = s, o = a.map((f) => f.content), c = Ln(a), l = await Promise.all(
        o.map(async (f) => zu({
          code: f,
          apiName: Ot[i].name,
          extensionTarget: r
        }))
      ), u = Dn(
        l,
        c
      );
      return {
        content: [
          {
            type: "text",
            text: Pn(
              u,
              "Code Blocks"
            )
          }
        ]
      };
    }
  };
}
async function Kd(t) {
  Ye(t, oa());
}
const ep = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Kd,
  validateComponentCodeblocksToolDef: oa
}, Symbol.toStringTag, { value: "Module" }));
function ca() {
  const t = [
    ...bn(P.GRAPHQL),
    ...bn(P.FUNCTION_GRAPHQL)
  ].map((i) => i.name), e = En(t), s = {
    ...la(e),
    codeblocks: wt(
      Kr.extend({
        content: oe().describe(
          "The GraphQL code block content which should contain raw GraphQL code (e.g., 'query { shop { name } }'), NOT markdown-formatted code blocks with backticks"
        )
      })
    ).describe(
      "Array of GraphQL code blocks with content and optional artifact metadata"
    )
  };
  return {
    name: "validate_graphql_codeblocks",
    description: `This tool validates GraphQL code blocks against the Shopify GraphQL schema to ensure they don't contain hallucinated fields or operations. If a user asks for an LLM to generate a GraphQL operation, this tool should always be used to ensure valid code was generated.

    Supports all Shopify GraphQL APIs including Admin, Storefront, Partner, Customer, Payments Apps, and Function APIs. For Shopify Functions, use this to validate the input GraphQL queries (run.graphql).
${Us}`,
    inputSchema: ve(s),
    handler: async (i) => {
      const r = i, a = ua(
        r,
        e
      ), o = r.codeblocks, c = o.map((v) => v.content), l = Ln(o), p = (await Promise.all(
        c.map(async (v) => await Ll(
          v,
          r.api ?? le.ADMIN,
          {
            apiVersion: a,
            failOnDeprecated: !1
          }
        ))
      )).map(
        (v) => {
          const { validation: A, scopes: O } = v;
          let M = A.resultDetail;
          if ((A.result === "success" || A.result === "inform") && O.length > 0) {
            const W = Cr(O);
            W && (M += W);
          }
          return {
            result: A.result,
            resultDetail: M
          };
        }
      ), f = Dn(
        p,
        l
      );
      return {
        content: [
          {
            type: "text",
            text: Pn(
              f,
              "Code Blocks"
            )
          }
        ]
      };
    }
  };
}
async function tp(t) {
  Ye(t, ca());
}
const np = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: tp,
  validateGraphqlCodeblocksToolDef: ca
}, Symbol.toStringTag, { value: "Module" })), sp = ve({
  conversationId: oe().describe(
    "🔗 REQUIRED: conversationId from learn_shopify_api tool. Call learn_shopify_api first if you don't have this."
  )
}), ip = (t) => ({
  ...sp.shape,
  ...t
});
function la(t) {
  const e = t.reduce((n, s) => (n.includes(s.api) || n.push(s.api), n), []);
  return {
    api: At(e).optional().default(le.ADMIN).describe(
      `The GraphQL API to use. Valid options are:
${e.map(
        (n) => `- '${n}': ${Ot[n].description}`
      ).join(`
`)}
Default is '${le.ADMIN}'.`
    )
  };
}
function ua(t, e) {
  return e.find(
    (n) => n.api === (t.api || le.ADMIN) && n.latestVersion
  );
}
const rp = /* @__PURE__ */ Object.assign({ "./introspect_graphql_schema/index.ts": Zl, "./learn_extension_target_types/index.ts": ud, "./learn_shopify_api/index.ts": $d, "./liquid_mcp_tools/index.ts": Wd, "./search_shopify_docs/index.ts": Jd, "./shopify_dev_fetch/index.ts": Xd, "./validate_component_codeblocks/index.ts": ep, "./validate_graphql_codeblocks/index.ts": np });
async function kp(t) {
  for (const e of Object.values(rp))
    await e.default(t);
}
function Pn(t, e = "Items") {
  const n = Dl(t), s = t.some((a) => a.result === ae.INFORM);
  let i;
  n ? i = "❌ INVALID" : s ? i = "⚠️ VALID (with deprecated fields)" : i = "✅ VALID";
  let r = `## Validation Summary

`;
  return r += `**Overall Status:** ${i}
`, r += `**Total ${e}:** ${t.length}

`, r += `## Detailed Results

`, t.forEach((a, o) => {
    let c;
    a.result === ae.SUCCESS ? c = "✅" : a.result === ae.INFORM ? c = "⚠️" : c = "❌", r += `### ${e.slice(0, -1)} ${o + 1}
`, a.artifactId && (r += `**Artifact ID:** ${a.artifactId}`, a.artifactRevision && (r += `
**Revision:** ${a.artifactRevision}`), r += `
*Use same ID & increment revision when retrying on an improvement of this artifact*

`), r += `**Status:** ${c} ${a.result.toUpperCase()}
`, r += `**Details:** ${a.resultDetail}

`;
  }), r;
}
export {
  gd as A,
  fd as B,
  Ed as C,
  Ld as D,
  pd as E,
  la as F,
  Pn as G,
  ua as H,
  ip as I,
  Zr as J,
  Ur as K,
  Xr as L,
  na as M,
  sa as N,
  ra as O,
  aa as P,
  oa as Q,
  ca as R,
  Ot as S,
  Jr as T,
  C as Z,
  Ap as a,
  Sp as b,
  wt as c,
  bp as d,
  At as e,
  Ep as f,
  U as g,
  kp as h,
  xp as i,
  Yr as j,
  Qr as k,
  wp as l,
  Br as m,
  Ui as n,
  ve as o,
  Wr as p,
  Rt as q,
  Op as r,
  oe as s,
  Sd as t,
  Np as u,
  Fd as v,
  bd as w,
  kd as x,
  _d as y,
  wd as z
};
