import r from "fs";
import { dirname as c, join as i } from "path";
import { fileURLToPath as s } from "url";
const m = s(import.meta.url), l = c(m);
function a() {
  const o = i(l, ".."), t = i(o, "promptfoo");
  if (!r.existsSync(t))
    throw new Error(`Promptfoo directory not found at ${t}`);
  return t;
}
function p(o) {
  const t = a(), e = i(t, o);
  if (!r.existsSync(e))
    throw new Error(`Eval file not found: ${e}`);
  return e;
}
function g(o) {
  const t = p(o);
  return r.readFileSync(t, "utf-8");
}
function u(o) {
  return o.replace(/[-_](.)/g, (t, e) => e.toUpperCase()).replace(/\.yaml$|\.yml$/, "");
}
function h() {
  try {
    const o = a(), t = r.readdirSync(o, { withFileTypes: !0 }), e = {};
    for (const n of t)
      if (n.isFile() && (n.name.endsWith(".yaml") || n.name.endsWith(".yml"))) {
        const f = u(n.name);
        e[f] = n.name;
      }
    return e;
  } catch {
    return {};
  }
}
const _ = h();
export {
  _ as evalConfigs,
  p as getEvalFilePath,
  h as getTopLevelConfigs,
  g as readEvalFile
};
