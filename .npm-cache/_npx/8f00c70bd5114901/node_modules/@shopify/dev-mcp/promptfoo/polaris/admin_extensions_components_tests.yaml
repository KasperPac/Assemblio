- description: admin_extensions components
  vars:
    prompt: "I\u2019m building an Admin action extension launched from the Orders\
      \ \u201CMore actions\u201D menu\u2014how can I set a clear title and wire up\
      \ a primary \u201CApprove refund\u201D action and a secondary \u201CCancel\u201D\
      \ action with proper loading/disabled states and success/error handling using\
      \ polaris web components?"
    matches: '```.*AdminAction.*```'
- description: admin_extensions components
  vars:
    prompt: "In an inline block extension on the Product details page, how can I make\
      \ my UI automatically adapt to the host container\u2019s width, spacing, and\
      \ background, support sticky section headers, and expose primary/secondary actions\
      \ that integrate with the surrounding Shopify Admin chrome using polaris web\
      \ components?"
    matches: '```.*AdminBlock.*```'
- description: admin_extensions components
  vars:
    prompt: How can I add a 'Print invoice' action to my Shopify Admin extension that
      appears in the More actions menu on the order details page and opens a printer-friendly
      URL for the current order, using polaris web components?
    matches: '```.*AdminPrintAction.*```'
- description: admin_extensions components
  vars:
    prompt: In an order details block extension, how can I display compact, color-coded
      status indicators (e.g., Fulfilled, Pending approval, Error) next to each line
      item and update them in real time after bulk actions complete, using polaris
      web components?
    matches: '```.*Badge.*```'
- description: admin_extensions components
  vars:
    prompt: "On the Order details page of my admin block extension, how can I display\
      \ a prominent, dismissible message with a title, description, icon, contextual\
      \ tone (info/success/warning/critical), and primary/secondary actions\u2014\
      for example to alert merchants about a high-risk order or required setup steps\u2014\
      using polaris web components?"
    matches: '```.*Banner.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I build a vertically aligned layout for an order return workflow\
      \ in my admin block extension\u2014stacking a header, status badge, customer\
      \ info, line items, a return-reason form, and primary/secondary actions\u2014\
      with consistent spacing, optional dividers, and responsive behavior using polaris\
      \ web components?"
    matches: '```.*BlockStack.*```'
- description: admin_extensions components
  vars:
    prompt: How can I build a reusable layout container with token-based padding,
      background, border radius, and shadow using polaris web components for a Shopify
      Admin block extension on the order details page, so I can wrap sections like
      customer notes and action buttons with consistent spacing and responsive behavior?
    matches: '```.*Box.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I implement primary Save and secondary Cancel actions in my product\
      \ details admin block extension\u2014with loading feedback, disabled states\
      \ until the form is valid, optional icons, and responsive full-width behavior\
      \ on mobile\u2014using polaris web components?"
    matches: '```.*Button.*```'
- description: admin_extensions components
  vars:
    prompt: "I\u2019m building a block extension on the product details page that\
      \ lets merchants select multiple fulfillment options (e.g., Ground, Express,\
      \ Local Pickup) with a \u201Cselect all\u201D control, and also requires an\
      \ explicit agreement to updated shipping terms before saving. How can I implement\
      \ this interaction using polaris web components, including indeterminate and\
      \ disabled states, helper text, validation on submit, and clean integration\
      \ with my form data and save action?"
    matches: '```.*Checkbox.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I present a grouped set of options\u2014single-select radios\
      \ for choosing a default fulfillment method and multi-select checkboxes for\
      \ add-on services like signature required or Saturday delivery\u2014with labels,\
      \ help text, error handling, and disabled states in a settings form for my Orders\
      \ page block extension, using polaris web components?"
    matches: '```.*ChoiceList.*```'
- description: admin_extensions components
  vars:
    prompt: How can I add an interactive color selector (with hue/saturation, optional
      alpha, hex input, and a live swatch preview) so merchants can set the color
      for custom badges on order detail pages and save it to a shop metafield, using
      polaris web components in my admin extension?
    matches: '```.*ColorPicker.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I build a reusable template UI for the Customers section that\
      \ merchants can apply inside the customer segment editor\u2014supporting preset\
      \ filters (like last purchase date, total spend, tags), editable criteria with\
      \ validation, and a live preview of the matching customer count\u2014using polaris\
      \ web components in my admin extension?"
    matches: '```.*CustomerSegmentTemplate.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I add a date selection field to my admin block extension on the\
      \ order details page to schedule a fulfillment follow-up\u2014supporting min/max\
      \ date constraints, disabling past dates, calendar popover, and validation before\
      \ saving\u2014using polaris web components?"
    matches: '```.*DateField.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I build an inline calendar-based date range selector in my Orders\
      \ index block extension\u2014supporting month navigation, store timezone handling,\
      \ disabled past dates, and quick presets like Today/Last 7 days\u2014that binds\
      \ to form state for filtering results, using polaris web components?"
    matches: '```.*DatePicker.*```'
- description: admin_extensions components
  vars:
    prompt: How can I add subtle separators between sections like Order Summary, Fulfillment
      Actions, and Notes in my order details block extension using polaris web components,
      ensuring consistent spacing, accessibility, and responsive behavior across different
      admin layouts?
    matches: '```.*Divider.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I add an email address field with label, help text, required\
      \ state, and inline validation/error messaging to a form in my Admin block extension\
      \ on the Customer details page, prefill it with the customer\u2019s primary\
      \ email, and save it on submit using polaris web components?"
    matches: '```.*EmailField.*```'
- description: admin_extensions components
  vars:
    prompt: How can I implement a settings editor in my product details block extension
      that collects text fields, numeric inputs, and checkboxes, validates the entries,
      and wires save/discard to the native Contextual Save Bar using polaris web components?
    matches: '```.*Form.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I build a metafield configuration form for my Shopify Function\
      \ inside a product details admin extension\u2014with text fields, checkboxes,\
      \ and select inputs\u2014and integrate it with the native Contextual Save Bar\
      \ to handle dirty state, save, and discard actions (including validation and\
      \ submit/reset handlers) using polaris web components?"
    matches: '```.*FunctionSettings.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I render a prominent page title and nested section headings (e.g.,\
      \ h1\u2013h3 levels) for a custom order insights block in Shopify Admin, with\
      \ control over size, alignment, and truncation, while maintaining an accessible\
      \ heading hierarchy using polaris web components?"
    matches: '```.*Heading.*```'
- description: admin_extensions components
  vars:
    prompt: How can I group a main section title with a subtitle and an optional status
      badge into a single accessible header area using polaris web components in my
      admin block extension for the order details page, ensuring correct heading levels
      and consistent spacing?
    matches: '```.*HeadingGroup.*```'
- description: admin_extensions components
  vars:
    prompt: "In a product details block extension, I want to add a compact toolbar\
      \ with common actions (edit price, adjust inventory, add tags), each displaying\
      \ a standard pictogram from the predefined Shopify set\u2014how can I select\
      \ the appropriate icons, control their size/color, and ensure proper accessibility\
      \ (labels, roles) using polaris web components?"
    matches: '```.*Icon.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I render a product\u2019s featured photo in my product-details\
      \ admin block extension with responsive sizing, proper alt text for accessibility,\
      \ lazy loading, and a fallback placeholder when the URL is missing or fails,\
      \ using polaris web components?"
    matches: '```.*Image.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I lay out a row of controls\u2014like a primary \u201CFulfill\u201D\
      \ button, a couple of secondary actions, a status badge, and a help icon\u2014\
      so they align horizontally with consistent spacing, optional wrapping on smaller\
      \ screens, vertical centering, and the last group right-aligned using polaris\
      \ web components in my Shopify Admin extension on the order details page?"
    matches: '```.*InlineStack.*```'
- description: admin_extensions components
  vars:
    prompt: How can I add a clickable text action in my order-details block extension
      that navigates to an external tracking URL or launches a custom myapp:// deep
      link (with control over opening in a new tab and proper rel attributes) using
      polaris web components?
    matches: '```.*Link.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I add a currency-aware amount input for merchants to enter a\
      \ refund or discount on the Order details page\u2014supporting shop currency\
      \ formatting, decimal precision, min/max validation, helper text, and inline\
      \ error messages\u2014using polaris web components in my admin extension?"
    matches: '```.*MoneyField.*```'
- description: admin_extensions components
  vars:
    prompt: How can I add a numeric input in my admin block extension on the product
      details page to let merchants set a restock threshold, with a clear label and
      help text, min/max limits, increment/decrement controls, a unit suffix, real-time
      validation/error messages, and form submission handling using polaris web components?
    matches: '```.*NumberField.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I display a short block of helper text under a heading on the\
      \ order details page\u2014with a subtle tone, smaller size, and support for\
      \ inline links/emphasis\u2014using polaris web components in my admin extension?"
    matches: '```.*Paragraph.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I add a masked input for merchants to enter a secret API token\
      \ in my admin extension\u2019s settings form (e.g., when configuring an integration\
      \ from an order details action), with a label, helper text, required/length\
      \ validation, a show/hide toggle, and proper error messaging using polaris web\
      \ components?"
    matches: '```.*PasswordField.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I make a custom row in my order details block extension clickable\
      \ (including keyboard activation) without changing its visual styling\u2014\
      only showing a pointer cursor on hover\u2014and capture click/press events for\
      \ its child elements using polaris web components?"
    matches: '```.*Pressable.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I add an accessible loading state to my admin extension when\
      \ a merchant triggers a bulk price update from the product details page, using\
      \ polaris web components? I want to disable the action buttons, show a loading\
      \ indicator with \u201CUpdating prices\u2026\u201D text while the job runs,\
      \ and then replace it with a success or error message when finished."
    matches: '```.*ProgressIndicator.*```'
- description: admin_extensions components
  vars:
    prompt: In an order details admin block extension, how can I group related content
      into thematic content groups that automatically increment heading levels for
      proper document structure (and support nesting to deepen the hierarchy) using
      polaris web components?
    matches: '```.*Section.*```'
- description: admin_extensions components
  vars:
    prompt: How can I let merchants choose a predefined fulfillment priority (Low,
      Normal, High) within my order details admin extension using polaris web components,
      including a clear label, helper text, and validation if no option is selected?
    matches: '```.*Select.*```'
- description: admin_extensions components
  vars:
    prompt: In a Shopify Admin block extension on the order details page, how can
      I display multi-line descriptions, subtle helper notes, and inline status labels
      with proper typography (size, weight, color, alignment, and truncation) while
      allowing custom styling using polaris web components?
    matches: '```.*Text.*```'
- description: admin_extensions components
  vars:
    prompt: How can I add a multi-line input field for internal order notes in my
      admin block extension, including a label, placeholder, helper text, max length
      with live character count, auto-resizing as the merchant types, and validation
      on submit, using polaris web components?
    matches: '```.*TextArea.*```'
- description: admin_extensions components
  vars:
    prompt: "On the order details page, I want to add an inline field where merchants\
      \ can enter an \u201CInternal fulfillment note\u201D with a label, placeholder,\
      \ required + max-length validation with a live character counter, helper text,\
      \ and inline error state\u2014how can I implement this using polaris web components\
      \ in my admin block extension?"
    matches: '```.*TextField.*```'
- description: admin_extensions components
  vars:
    prompt: "How can I add a URL input in my product details admin block extension\
      \ to capture an external size guide link\u2014complete with https-only validation,\
      \ inline error messaging, placeholder/help text, and a trailing \u201CTest link\u201D\
      \ action\u2014inside a form that saves to a metafield, using polaris web components?"
    matches: '```.*URLField.*```'
