- description: app_home polaris web components
  vars:
    prompt: "How can I display a user\u2019s profile photo in my App Home using polaris\
      \ web components, provide accessible alt text, automatically fall back to their\
      \ initials if the image doesn\u2019t load, and choose the appropriate size (small,\
      \ base, or large) for different sections of the interface?"
    matches: (<s\-avatar|(?:root\.)?createComponent\(s\-avatar)
- description: app_home polaris web components
  vars:
    prompt: How can I show a compact status indicator in my App Home using polaris
      web components to confirm when an action completes or highlight issues, and
      configure its tone (info/success/warning/critical), include an icon, and adjust
      the color intensity (base vs strong) and size (base, large, or large-100) based
      on context?
    matches: (<s\-badge|(?:root\.)?createComponent\(s\-badge)
- description: app_home polaris web components
  vars:
    prompt: "How can I show a prominent alert in my App Home using polaris web components\u2014\
      with a clear heading, the appropriate tone (info, success, warning, or critical),\
      \ make it dismissible, and persist the hidden state after the user closes it\
      \ so it doesn\u2019t reappear on subsequent renders?"
    matches: (<s\-banner|(?:root\.)?createComponent\(s\-banner)
- description: app_home polaris web components
  vars:
    prompt: "How can I wrap a group of elements in my App Home so the content keeps\
      \ its natural size while I add a subtle background, a 1px custom-colored border\
      \ with rounded corners, and control screen reader behavior (label it with a\
      \ role, or hide it if decorative) using polaris web components? Which properties\
      \ should I use\u2014background, border vs. borderColor/borderStyle/borderWidth,\
      \ borderRadius, accessibilityLabel, accessibilityRole, and accessibilityVisibility\u2014\
      and what are some example values?"
    matches: (<s\-box|(?:root\.)?createComponent\(s\-box)
- description: app_home polaris web components
  vars:
    prompt: "How can I wire up toolbar actions in my App Home using polaris web components\
      \ so that an icon-only control (with an accessibilityLabel) toggles a settings\
      \ panel via a --toggle command targeting the panel\u2019s id (commandFor), shows\
      \ a loading state that disables it during an async save, another action opens\
      \ external help via href in a new tab (target=_blank), and an export action\
      \ triggers a same-origin CSV download with a specific filename, while ensuring\
      \ navigation is prevented when a command is executed instead?"
    matches: (<s\-button|(?:root\.)?createComponent\(s\-button)
- description: app_home polaris web components
  vars:
    prompt: How can I display several related actions as a row of buttons in my App
      Home, control the gap between them (none or base), and add an accessibility
      label so screen readers understand the group, using polaris web components?
    matches: (<s\-button\-group|(?:root\.)?createComponent\(s\-button\-group)
- description: app_home polaris web components
  vars:
    prompt: "How can I build a settings section in my App Home where merchants agree\
      \ to terms (required) and select multiple notification channels using polaris\
      \ web components? I need to set clear labels and details text, provide an accessibilityLabel\
      \ for screen readers, handle defaultChecked vs. controlled checked states, show\
      \ an error message if the required agreement isn\u2019t selected, disable certain\
      \ options based on plan, and wire up unique id/name values for submission\u2014\
      what\u2019s the recommended approach?"
    matches: (<s\-checkbox|(?:root\.)?createComponent\(s\-checkbox)
- description: app_home polaris web components
  vars:
    prompt: "In my App Home interface, I want to show user-supplied keywords as tags\
      \ next to each order to help merchants categorize and discover related items\u2014\
      how can I render these, set an accessibilityLabel for screen readers, and adjust\
      \ the color intensity to highlight important tags using polaris web components?"
    matches: (<s\-chip|(?:root\.)?createComponent\(s\-chip)
- description: app_home polaris web components
  vars:
    prompt: In my App Home settings panel, how can I present a list of options using
      polaris web components where I can switch between single-select (radio) and
      multi-select (checkbox), provide a label (or hide it visually while keeping
      it accessible), include helper details text, preselect choices via an array
      of values, assign a unique name, display an error message when validation fails,
      and disable the entire field when needed?
    matches: (<s\-choice\-list|(?:root\.)?createComponent\(s\-choice\-list)
- description: app_home polaris web components
  vars:
    prompt: How can I build a pressable promo panel in my App Home using polaris web
      components that behaves like a button, with a soft background, a 2px dashed
      brand-colored border, rounded corners, and a fixed block size, while also setting
      an accessibility label/role and using exclusive accessibility visibility so
      duplicate content is only exposed to assistive tech once?
    matches: (<s\-clickable|(?:root\.)?createComponent\(s\-clickable)
- description: app_home polaris web components
  vars:
    prompt: "How can I render a set of clickable category tags in my App Home that\
      \ let merchants filter items, optionally navigate to a URL after click, support\
      \ removing a tag (and keep my state in sync), toggle hidden/disabled during\
      \ async operations, set an accessibilityLabel for screen readers, and adjust\
      \ the color intensity\u2014using polaris web components?"
    matches: (<s\-clickable\-chip|(?:root\.)?createComponent\(s\-clickable\-chip)
- description: app_home polaris web components
  vars:
    prompt: "In my App Home settings panel, how can I let merchants pick a brand color\
      \ from a palette using polaris web components, with an option to adjust transparency\
      \ (alpha), a default hex value, helper details text, an accessible label that\
      \ can be visually hidden in compact layouts, and inline error messaging for\
      \ invalid inputs? I also need to assign a stable id/name for form submission,\
      \ disable the field while saving, and turn off autocomplete\u2014what\u2019\
      s the best way to set this up?"
    matches: (<s\-color\-field|(?:root\.)?createComponent\(s\-color\-field)
- description: app_home polaris web components
  vars:
    prompt: How can I add a color palette selector to my App Home settings using polaris
      web components, with support for an adjustable alpha value, a default HSLA color,
      a unique form field name, and syncing the current value in HSL/HSLA formats?
    matches: (<s\-color\-picker|(?:root\.)?createComponent\(s\-color\-picker)
- description: app_home polaris web components
  vars:
    prompt: "How can I add a date picker to my App Home booking form using polaris\
      \ web components that pre-fills 2025-10-15, opens to October 2025 by default,\
      \ only allows dates from 2025-10-01 to 2025-12-31 on weekdays (Mon\u2013Fri),\
      \ blocks blackout dates like 2025-11-28 and 2025-12-25, shows a brief details\
      \ hint under the field, supports autocomplete, and displays a clear error message\
      \ if a blocked date is selected?"
    matches: (<s\-date\-field|(?:root\.)?createComponent\(s\-date\-field)
- description: app_home polaris web components
  vars:
    prompt: "How can I let merchants pick either a single date or a date range in\
      \ my App Home interface using polaris web components, with quick presets like\
      \ \u201CLast 7 days,\u201D respecting the shop\u2019s timezone, and disabling\
      \ future dates?"
    matches: (<s\-date\-picker|(?:root\.)?createComponent\(s\-date\-picker)
- description: app_home polaris web components
  vars:
    prompt: "In my App Home interface, I want to add separators to clearly distinguish\
      \ sections\u2014subtle horizontal lines between cards like \u201CRecent orders\u201D\
      \ and \u201CPerformance,\u201D and a stronger vertical rule between the sidebar\
      \ and main content. How can I implement this using polaris web components, choosing\
      \ the direction (inline vs block) and adjusting the color intensity (base vs\
      \ strong)?"
    matches: (<s\-divider|(?:root\.)?createComponent\(s\-divider)
- description: app_home polaris web components
  vars:
    prompt: How can I add a drag-and-drop upload area in my App Home settings page
      using polaris web components that accepts only .csv and .xlsx files, allows
      multiple files, has a visible label (but can switch to an accessibility-only
      label), includes a specific accessibility label for screen readers, marks the
      field as required, and shows an inline error if an invalid type is dropped?
      Also, how can I disable the input while processing and then programmatically
      clear the selected files after a successful import?
    matches: (<s\-drop\-zone|(?:root\.)?createComponent\(s\-drop\-zone)
- description: app_home polaris web components
  vars:
    prompt: "How can I collect a merchant\u2019s contact email in my App Home settings\
      \ panel using polaris web components, with the mobile email keyboard enabled,\
      \ email-specific autocomplete, a default value prefilled, helper details text,\
      \ min/max length constraints, a visually hidden but accessible label, and a\
      \ clear inline error when the format is invalid?"
    matches: (<s\-email\-field|(?:root\.)?createComponent\(s\-email\-field)
- description: app_home polaris web components
  vars:
    prompt: How can I build a responsive multi-row, multi-column layout for my App
      Home using polaris web components that adapts from three columns on desktop
      to a single column on mobile, lets me align sections vertically, set different
      background colors, add borders and rounded corners to each card, keep tiles
      at consistent heights, and set accessibility labels/roles while hiding decorative
      elements from assistive technologies?
    matches: (<s\-grid|(?:root\.)?createComponent\(s\-grid)
- description: app_home polaris web components
  vars:
    prompt: "How can I structure nested page titles in my App Home using polaris web\
      \ components so the levels auto-adjust based on their section nesting, set some\
      \ titles as real headings while others are purely presentational, optionally\
      \ hide decorative titles from the accessibility tree or make certain labels\
      \ screen-reader\u2013only, and clamp long titles to 2 lines?"
    matches: (<s\-heading|(?:root\.)?createComponent\(s\-heading)
- description: app_home polaris web components
  vars:
    prompt: "In my App Home dashboard, how can I display status and action symbols\
      \ using polaris web components\u2014choosing the right type, tone (info/success/warning/critical/neutral),\
      \ size (small or base), and color intensity (base or subdued)\u2014and have\
      \ them trigger visual interest on a related card or button via an interestFor\
      \ target when users hover or focus?"
    matches: (<s\-icon|(?:root\.)?createComponent\(s\-icon)
- description: app_home polaris web components
  vars:
    prompt: "How can I embed a product hero image in my App Home using polaris web\
      \ components so it loads lazily, fills the available width while preserving\
      \ aspect ratio, shows a 2px rounded border in my brand color, and includes meaningful\
      \ alt text with the proper semantic role for screen readers? Also, how can I\
      \ switch it to render at its natural size when space is constrained, and what\u2019\
      s the best way to choose between the border shorthand versus separate border\
      \ width/style/color properties?"
    matches: (<s\-image|(?:root\.)?createComponent\(s\-image)
- description: app_home polaris web components
  vars:
    prompt: "How can I add interactive text in my App Home that navigates to an in-app\
      \ route (via a URL), toggles a help sidebar when targeting a specific element,\
      \ downloads a CSV from a data URL with a custom filename, and opens external\
      \ docs in a new tab\u2014while providing an accessibility label for icon-only\
      \ content, setting the tone to neutral, and specifying lang for localized labels\u2014\
      using polaris web components?"
    matches: (<s\-link|(?:root\.)?createComponent\(s\-link)
- description: app_home polaris web components
  vars:
    prompt: In my App Home, how can I show a compact actions list for each product
      (e.g., Edit, Duplicate, Archive) using polaris web components, and set an accessibility
      label so screen readers understand the purpose of the actions?
    matches: (<s\-menu|(?:root\.)?createComponent\(s\-menu)
- description: app_home polaris web components
  vars:
    prompt: How can I add a confirmation overlay to my App Home that I can programmatically
      show/hide or toggle after a destructive action, set an accessibility label that
      overrides the title, choose an appropriate size (small/base/large), and control
      padding for edge-to-edge content, using polaris web components?
    matches: (<s\-modal|(?:root\.)?createComponent\(s\-modal)
- description: app_home polaris web components
  vars:
    prompt: How can I collect a price input in my App Home using polaris web components
      with built-in currency formatting and validation, including setting a default
      value (e.g., 29.99), enforcing min/max limits (e.g., 1 to 10000), displaying
      helper details text, showing an inline error when the value is out of range,
      keeping the label visually hidden but accessible, and allowing browser autofill?
    matches: (<s\-money\-field|(?:root\.)?createComponent\(s\-money\-field)
- description: app_home polaris web components
  vars:
    prompt: 'How can I build a mobile-friendly numeric input in my App Home using
      polaris web components to collect a discount percentage: show a numeric keyboard
      (inputMode=''numeric'' or ''decimal''), set a defaultValue, include contextual
      details, enforce a max (e.g., 100), surface inline error text when the value
      is invalid, optionally disable the field, control autocomplete, and keep the
      label accessible but visually hidden?'
    matches: (<s\-number\-field|(?:root\.)?createComponent\(s\-number\-field)
- description: app_home polaris web components
  vars:
    prompt: How can I show a step-by-step onboarding guide in my App Home interface
      using polaris web components, with a numbered list that supports nested sub-steps,
      a custom starting number, and inline links or actions inside each item?
    matches: (<s\-ordered\-list|(?:root\.)?createComponent\(s\-ordered\-list)
- description: app_home polaris web components
  vars:
    prompt: How can I set up the main container for my App Home dashboard using polaris
      web components, with a primary heading and automatic spacing between sections,
      and configure the inline size to be small, base, or large (including a full-width
      option with whitespace) to suit different screen sizes?
    matches: (<s\-page|(?:root\.)?createComponent\(s\-page)
- description: app_home polaris web components
  vars:
    prompt: How can I render a standalone text block in my App Home that includes
      inline links and a small button, switch its tone between info/success/warning
      based on status, use a subdued color, clamp the content to 3 lines on mobile,
      support RTL with automatic text direction, and control accessibility visibility
      (visible/hidden/exclusive) using polaris web components?
    matches: (<s\-paragraph|(?:root\.)?createComponent\(s\-paragraph)
- description: app_home polaris web components
  vars:
    prompt: "How can I securely collect an API secret in my App Home using polaris\
      \ web components\u2014disabling autocomplete, masking the input, showing guidance\
      \ via details, enforcing minLength/maxLength, displaying inline error messages,\
      \ and keeping the label accessible but visually hidden?"
    matches: (<s\-password\-field|(?:root\.)?createComponent\(s\-password\-field)
- description: app_home polaris web components
  vars:
    prompt: "In my App Home, I want a button that opens a filter overlay\u2014how\
      \ can I control its dimensions using polaris web components, like setting a\
      \ minInlineSize (e.g., 280px), capping maxInlineSize (e.g., 420px), keeping\
      \ blockSize auto, and constraining maxBlockSize so it doesn\u2019t overflow\
      \ the viewport, while still letting it expand with content on desktop and shrink\
      \ on mobile?"
    matches: (<s\-popover|(?:root\.)?createComponent\(s\-popover)
- description: app_home polaris web components
  vars:
    prompt: "In my App Home, I want a responsive analytics panel where child elements\
      \ adapt to the panel\u2019s width; how can I establish a container query context\
      \ and assign a custom container name (overriding the default) so I can target\
      \ it in my styles using polaris web components?"
    matches: (<s\-query\-container|(?:root\.)?createComponent\(s\-query\-container)
- description: app_home polaris web components
  vars:
    prompt: How can I add a single-line search input in my App Home that shows a search
      icon, supports placeholder text, includes a clear button, and debounces input
      changes using polaris web components so merchants can quickly find products
      or orders?
    matches: (<s\-search\-field|(?:root\.)?createComponent\(s\-search\-field)
- description: app_home polaris web components
  vars:
    prompt: In my App Home, how can I group related content into nested thematic areas
      that automatically adjust heading levels, set a visible heading when needed,
      provide an accessibility label when I intentionally omit the heading, and control
      padding for outer vs inner groups using polaris web components?
    matches: (<s\-section|(?:root\.)?createComponent\(s\-section)
- description: app_home polaris web components
  vars:
    prompt: How can I build a single-choice dropdown in my App Home using polaris
      web components that includes helper details text, supports a visible or visually
      hidden accessible label, shows a placeholder before selection, marks the field
      as required with an inline error if left empty, allows a leading icon, can be
      disabled during save, and uses unique id/name values for form submission?
    matches: (<s\-select|(?:root\.)?createComponent\(s\-select)
- description: app_home polaris web components
  vars:
    prompt: How can I show an inline loading indicator in my App Home dashboard while
      fetching data, set the accessibilityLabel so screen readers announce the loading
      purpose, and toggle it based on fetch state using polaris web components?
    matches: (<s\-spinner|(?:root\.)?createComponent\(s\-spinner)
- description: app_home polaris web components
  vars:
    prompt: "How can I arrange settings cards in my App Home to flow horizontally\
      \ with even spacing, center-align them on the cross axis, switch to a vertical\
      \ layout on small screens, and add a subtle background with rounded borders\
      \ and a 1px border\u2014while also setting an accessibilityLabel, assigning\
      \ an appropriate accessibilityRole, and marking a helper note as exclusive for\
      \ screen readers\u2014using polaris web components?"
    matches: (<s\-stack|(?:root\.)?createComponent\(s\-stack)
- description: app_home polaris web components
  vars:
    prompt: "How can I add a settings toggle in my App Home that lets merchants turn\
      \ \u201CBack in stock notifications\u201D on or off using polaris web components,\
      \ with a clear label and helper details, defaulted on for new installs, support\
      \ for visually hiding the label while keeping it accessible to screen readers,\
      \ a unique id/name, a temporary disabled state while saving, and an inline error\
      \ message if the update fails?"
    matches: (<s\-switch|(?:root\.)?createComponent\(s\-switch)
- description: app_home polaris web components
  vars:
    prompt: How can I build a paginated orders view in my App Home using polaris web
      components that displays rows and columns on desktop and a list on mobile, with
      next/previous pagination controls (respecting hasNextPage/hasPreviousPage),
      a loading state that disables interactions during page fetches, and an automatic
      layout that switches between list and table?
    matches: (<s\-table|(?:root\.)?createComponent\(s\-table)
- description: app_home polaris web components
  vars:
    prompt: "How can I style inline snippets inside a paragraph in my App Home overview\
      \ using polaris web components\u2014for example, show success/warning/critical\
      \ tones for statuses, use subdued color for secondary notes, apply strong emphasis\
      \ to key phrases, align numbers with tabular numerals, support RTL text, make\
      \ certain decorative badges visible but removed from the accessibility tree,\
      \ and have hovering those snippets highlight a related chart via interest linking?"
    matches: (<s\-text|(?:root\.)?createComponent\(s\-text)
- description: app_home polaris web components
  vars:
    prompt: How can I add an auto-expanding multi-line input in my App Home for merchants
      to write longer notes, with a label (optionally visually hidden for accessibility),
      helper details text, a default value, min/max length validation that surfaces
      an error message, and the ability to toggle autocomplete or disable the field,
      using polaris web components?
    matches: (<s\-text\-area|(?:root\.)?createComponent\(s\-text\-area)
- description: app_home polaris web components
  vars:
    prompt: How can I add a single-line input in my App Home settings where merchants
      can enter a short announcement, with a label, placeholder text, a 60-character
      limit with a live counter, and a clear button, and handle changes and save on
      Enter using polaris web components?
    matches: (<s\-text\-field|(?:root\.)?createComponent\(s\-text\-field)
- description: app_home polaris web components
  vars:
    prompt: How can I show a small preview image for each product in my App Home orders
      list using polaris web components, including setting an accessible alt description,
      choosing a size like small-100 or large-100, and falling back to a placeholder
      while the image is loading or when no src is available?
    matches: (<s\-thumbnail|(?:root\.)?createComponent\(s\-thumbnail)
- description: app_home polaris web components
  vars:
    prompt: "How can I add small hover/focus help overlays to explain actions in my\
      \ App Home interface, choose where they appear (e.g., above or below), fine-tune\
      \ the show/hide delay, and ensure they\u2019re accessible to keyboard users\
      \ using polaris web components?"
    matches: (<s\-tooltip|(?:root\.)?createComponent\(s\-tooltip)
- description: app_home polaris web components
  vars:
    prompt: "How can I collect a website URL from merchants in my App Home using polaris\
      \ web components, with automatic \u201Chttps://\u201D formatting, real-time\
      \ validation, inline error messages, and blocking the save action until the\
      \ URL is valid?"
    matches: (<s\-url\-field|(?:root\.)?createComponent\(s\-url\-field)
- description: app_home polaris web components
  vars:
    prompt: How can I present a bulleted list of setup steps in my App Home using
      polaris web components, with support for nested items, inline links, and responsive
      spacing/typography so it reads well on desktop and mobile?
    matches: (<s\-unordered\-list|(?:root\.)?createComponent\(s\-unordered\-list)
