- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "In an Admin action extension triggered from the More actions menu on\
      \ the order details page, how can I display an action modal with a custom heading,\
      \ a primary \u201CSend invoice\u201D action and a secondary \u201CCancel\u201D\
      \ action, and start in a loading state that prevents interaction until order\
      \ data is fetched, using polaris web components?"
    matches: (<s\-admin\-action|(?:root\.)?createComponent\(s\-admin\-action)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: In an inline admin block extension on the order details page, how can
      I set a custom header title and show a concise collapsed summary (truncated
      after 30 characters) that updates as order data changes, using polaris web components?
    matches: (<s\-admin\-block|(?:root\.)?createComponent\(s\-admin\-block)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I implement a print action in my admin extension that previews\
      \ and prints an order invoice from a dynamic URL using polaris web components?\
      \ I want to set the src to an HTML/PDF/image link, show an empty state and disable\
      \ the print button when the URL isn\u2019t available, and update the src when\
      \ the merchant switches orders."
    matches: (<s\-admin\-print\-action|(?:root\.)?createComponent\(s\-admin\-print\-action)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I show a compact profile indicator for a customer in a block\
      \ extension on the order details page using polaris web components? I want to\
      \ load the customer\u2019s photo from a URL (src), automatically fall back to\
      \ their initials if the image fails or is slow, provide accessible alternative\
      \ text (alt), and control the size (small, small-200, base, large, or large-200).\
      \ Could you share an example and any accessibility or performance tips?"
    matches: (<s\-avatar|(?:root\.)?createComponent\(s\-avatar)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "On the order details page of my admin extension, how can I show compact\
      \ status indicators like \u201CPayment captured,\u201D \u201CAwaiting fulfillment,\u201D\
      \ or \u201CAction required\u201D using polaris web components, with configurable\
      \ tone (info, success, warning, critical, neutral, caution, or auto), adjustable\
      \ color intensity (base vs strong), icon selection, and size options (base,\
      \ large, large-100), and have them update dynamically as the order progresses?"
    matches: (<s\-badge|(?:root\.)?createComponent\(s\-badge)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: In my Admin block extension on the Orders page, how can I add a prominent,
      dismissible notice with a heading that uses the appropriate status tone (info,
      success, warning, critical, or auto) and control its visibility (hide after
      dismiss, re-show based on app state) using polaris web components? I also need
      guidance on handling the dismiss event to update state reliably and ensuring
      the message is announced correctly as a live region.
    matches: (<s\-banner|(?:root\.)?createComponent\(s\-banner)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I group a set of order risk indicators and action buttons inside\
      \ a flexible container that preserves its natural size within a grid, apply\
      \ a subtle background with a 1px neutral border and rounded corners, set an\
      \ accessible label/role, and toggle whether it\u2019s included in the accessibility\
      \ tree when a detailed panel is opened, using polaris web components in my admin\
      \ extension?"
    matches: (<s\-box|(?:root\.)?createComponent\(s\-box)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I add an icon-only action in my Shopify Admin order details extension\
      \ that toggles a \u201CCreate shipping label\u201D panel via command/commandFor,\
      \ shows a loading state while the label is generated, disables itself when the\
      \ order isn\u2019t paid, and alternatively either downloads a same-origin PDF\
      \ packing slip (using the download attribute) or opens a carrier tracking link\
      \ in a new tab with target=_blank, all with a clear accessibilityLabel, using\
      \ polaris web components?"
    matches: (<s\-button|(?:root\.)?createComponent\(s\-button)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "In a product details block extension, how can I present a compact row\
      \ of related actions (Publish, Unpublish, Duplicate) as a single grouped layout\u2014\
      controlling the spacing between buttons (base or none) and adding an accessibility\
      \ label that describes the group for screen readers\u2014using polaris web components?"
    matches: (<s\-button\-group|(?:root\.)?createComponent\(s\-button\-group)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I add selection controls in my admin block extension for product
      details that let merchants agree to custom terms and choose multiple tags, with
      support for label text, details/helper copy, required validation and error messaging,
      programmatic checked/defaultChecked state, disabled state, unique id/name attributes,
      and accessibilityLabel, using polaris web components?
    matches: (<s\-checkbox|(?:root\.)?createComponent\(s\-checkbox)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: In my product details block extension, I want to show user-supplied keywords
      as small categorized tokens next to key attributes so merchants can quickly
      scan and group similar products. How can I render these with adjustable color
      intensity and set meaningful screen reader labels for accessibility, using polaris
      web components?
    matches: (<s\-chip|(?:root\.)?createComponent\(s\-chip)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "In a product bulk-edit block extension, how can I present a group of\
      \ choices where merchants can pick either a single fulfillment policy (radio)\
      \ or multiple product tags (checkboxes), with a custom label, helper details\
      \ text, a unique form name, preselected values, the ability to switch between\
      \ single and multiple selection, a visually hidden but accessible label, inline\
      \ error messaging, and a disabled state while saving\u2014using polaris web\
      \ components?"
    matches: (<s\-choice\-list|(?:root\.)?createComponent\(s\-choice\-list)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I build a custom, card-like interactive area in my product detail
      admin block extension using polaris web components that behaves like a button
      (with an accessibilityRole), includes an accessibilityLabel, supports background
      color, border/borderRadius/borderWidth, a specific blockSize, and lets me toggle
      accessibilityVisibility (visible vs hidden) based on state or permissions?
    matches: (<s\-clickable|(?:root\.)?createComponent\(s\-clickable)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I add clickable, removable attribute chips next to line items\
      \ on the order details page in my admin extension using polaris web components,\
      \ with accessible labels, adjustable color intensity, optional navigation after\
      \ click, and support for disabled/hidden states\u2014while keeping the hidden\
      \ state controlled and in sync with app state when a chip is removed?"
    matches: (<s\-clickable\-chip|(?:root\.)?createComponent\(s\-clickable\-chip)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "I\u2019m building a block extension on the product details page to let\
      \ merchants pick a brand color for a custom badge. How can I add a color palette\
      \ input using polaris web components that supports an alpha channel, sets a\
      \ defaultValue, shows helpful details text, uses a custom label (with the option\
      \ to keep the label visually hidden for accessibility), defines name/id attributes\
      \ for form submission, surfaces an error message for invalid input, lets me\
      \ toggle autocomplete on/off, and can be disabled while saving?"
    matches: (<s\-color\-field|(?:root\.)?createComponent\(s\-color\-field)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I let merchants choose a color from a palette for product swatches
      in a block extension on the product details page using polaris web components?
      I need optional alpha support, a default HSLA value, a unique form field name,
      and to read/write the current color value in HSL/HSLA to a product metafield.
    matches: (<s\-color\-picker|(?:root\.)?createComponent\(s\-color\-picker)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I add a date selector to an order details block extension using\
      \ polaris web components so merchants can schedule a pickup date, with only\
      \ the next 60 days selectable, weekdays only, and specific blackout dates excluded?\
      \ I\u2019d like to show a helpful description under the field, default the calendar\
      \ to next month, prefill an existing metafield value if present, disable the\
      \ field until the order is fulfilled, surface an inline error if a blocked date\
      \ is chosen, and control browser autofill behavior. How should I set up the\
      \ allowed/disallowed dates and days, default view/value, helper text, disabled\
      \ state, and error handling for this flow?"
    matches: (<s\-date\-field|(?:root\.)?createComponent\(s\-date\-field)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I let merchants pick a single date or a start\u2013end range\
      \ to filter orders by fulfillment date in my admin block extension, with a popover\
      \ calendar, quick presets (today, last 7 days), disabled future dates, and proper\
      \ timezone handling, using polaris web components?"
    matches: (<s\-date\-picker|(?:root\.)?createComponent\(s\-date\-picker)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I add separators to create clear visual separation between the
      order summary, payment status, and fulfillment sections in my admin block extension,
      with control over direction (inline vs block) and intensity (base vs strong),
      using polaris web components?
    matches: (<s\-divider|(?:root\.)?createComponent\(s\-divider)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I add an email address field to a customer notifications form
      in my admin extension, using polaris web components, so that mobile devices
      show the email keyboard, autocomplete is enabled, min/max length is enforced,
      inline details and error messaging are shown, a default value can be prefilled,
      the field can be disabled when needed, and I can control label visibility for
      accessibility?
    matches: (<s\-email\-field|(?:root\.)?createComponent\(s\-email\-field)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "In an order details action extension, I\u2019m building a small form\
      \ to add a tracking number and carrier. How can I wrap these inputs so pressing\
      \ Enter in any field submits the data, and wire up a JavaScript submit handler\
      \ (no HTTP auto-submit) using polaris web components?"
    matches: (<s\-form|(?:root\.)?createComponent\(s\-form)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I build a responsive multi-row/column layout for a product details
      admin block extension using polaris web components, where I control alignItems/alignContent
      for different breakpoints, set per-section background, border, and borderRadius,
      optionally adjust blockSize for taller rows, add meaningful accessibilityLabel
      and accessibilityRole, and mark decorative separators with accessibilityVisibility
      set to hidden or exclusive?
    matches: (<s\-grid|(?:root\.)?createComponent\(s\-grid)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: In a product details admin block extension, how can I render hierarchical
      titles that automatically adjust their level based on nesting, set appropriate
      accessibility roles for headings, control visibility for assistive technologies
      (visible/hidden/exclusive), and clamp long titles to a specific number of lines
      using polaris web components?
    matches: (<s\-heading|(?:root\.)?createComponent\(s\-heading)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: In a block extension on the Orders details page, I need to show actionable
      status symbols next to each line item. How can I render these graphics with
      configurable size (small/base), tone (info/success/warning/critical/neutral/auto),
      color intensity (base/subdued), choose the appropriate symbol type, and make
      them trigger hover/focus interactions on a related row via an interestFor target,
      using polaris web components?
    matches: (<s\-icon|(?:root\.)?createComponent\(s\-icon)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "In a product details admin block extension, how can I render a product\
      \ image using polaris web components with proper alt text, an appropriate accessibility\
      \ role (img vs presentation), a fixed aspect ratio, and control over inline\
      \ width (auto vs fill)? I\u2019d also like to lazy-load offscreen images and\
      \ customize the border (width, style, color) and border radius to match the\
      \ store\u2019s design."
    matches: (<s\-image|(?:root\.)?createComponent\(s\-image)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "In my admin block extension on the product details page, how can I add\
      \ interactive text that navigates to Shopify Admin pages or my app routes, opens\
      \ some destinations in a new tab, optionally triggers a toggle action on a side\
      \ panel instead of navigating, initiates a CSV download from a data URL, includes\
      \ an accessibility label for icon-only content, sets the correct language for\
      \ localized text, and applies a critical tone for risky actions\u2014using polaris\
      \ web components?"
    matches: (<s\-link|(?:root\.)?createComponent\(s\-link)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I present a compact list of actions for an order in my admin\
      \ extension, triggered from the \u201CMore actions\u201D entry, using polaris\
      \ web components, and ensure it\u2019s accessible by providing a clear accessibilityLabel\
      \ for screen readers?"
    matches: (<s\-menu|(?:root\.)?createComponent\(s\-menu)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "In an action extension opened from the order details page, how can I\
      \ collect a per\u2011order surcharge with built\u2011in currency formatting\
      \ and validation using polaris web components, including a labeled currency\
      \ input (optionally using exclusive label visibility), defaultValue, autocomplete\
      \ support, contextual details text, min/max constraints, clear error messaging,\
      \ a stable id, and the ability to disable the field when the order is already\
      \ paid?"
    matches: (<s\-money\-field|(?:root\.)?createComponent\(s\-money\-field)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: In an admin block extension on the order details page, I need to collect
      a refund amount with optimized numeric/decimal keyboards, support autofill,
      set a default value, enforce a max equal to the remaining balance, show inline
      error messaging, and include helper details while keeping the label accessible
      (optionally visually hidden). How can I implement this using polaris web components?
    matches: (<s\-number\-field|(?:root\.)?createComponent\(s\-number\-field)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I render a step-by-step fulfillment workflow as a numbered sequence\
      \ on the Order details page in my admin block extension\u2014supporting sub-steps,\
      \ headings, descriptive text, and inline action links\u2014using polaris web\
      \ components?"
    matches: (<s\-ordered\-list|(?:root\.)?createComponent\(s\-ordered\-list)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I show a standalone block of explanatory text in my Order Details\
      \ admin block extension\u2014with an inline \u201CView risk analysis\u201D link\
      \ and a \u201CCapture payment\u201D button\u2014while dynamically setting the\
      \ tone to warning or success based on the risk score, using subdued color for\
      \ neutral states, clamping to 3 lines on smaller screens, applying tabular-nums\
      \ for amounts and dates, respecting dir auto for RTL/LTR languages, and toggling\
      \ accessibilityVisibility to hidden or exclusive when a modal is active, using\
      \ polaris web components?"
    matches: (<s\-paragraph|(?:root\.)?createComponent\(s\-paragraph)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I build a secure input for collecting an API secret in my admin\
      \ block extension on the product details page using polaris web components,\
      \ with autocomplete set to off, a unique id, a clear label that\u2019s visually\
      \ hidden for accessibility, contextual details text explaining storage and usage,\
      \ minLength and maxLength validation, an error message when validation fails,\
      \ an optional defaultValue, and the ability to disable the field after the secret\
      \ is saved?"
    matches: (<s\-password\-field|(?:root\.)?createComponent\(s\-password\-field)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I set up a container-scoped responsive context so child sections\
      \ adapt to their parent\u2019s size (not just the viewport) in my product details\
      \ block extension, and when should I override the default container name with\
      \ a custom one to target specific container queries, using polaris web components?"
    matches: (<s\-query\-container|(?:root\.)?createComponent\(s\-query\-container)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I add a single-line search input to let merchants quickly find\
      \ related products by title or SKU within my admin block extension on the product\
      \ details page\u2014complete with a search icon, placeholder text, clear action,\
      \ debounced input for API queries, Enter-to-submit behavior, loading state,\
      \ and accessible labeling\u2014using polaris web components?"
    matches: (<s\-search\-field|(?:root\.)?createComponent\(s\-search\-field)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I group related content into nested areas in my order details\
      \ admin block extension using polaris web components, so that heading levels\
      \ adjust automatically, I can show a title when needed (or provide an accessibility\
      \ label when there\u2019s no visible title), and I can control padding (base\
      \ vs none) for full-bleed or compact sections?"
    matches: (<s\-section|(?:root\.)?createComponent\(s\-section)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: In an Admin Extension embedded on the order details page, I need a single-choice
      menu for selecting a shipping carrier from 8 options; how can I implement this
      using polaris web components with a configurable label (visible or visually
      hidden but accessible), placeholder text, helper details, an icon, required
      selection, custom error messaging when nothing is chosen, unique id/name for
      form submission, and the ability to disable the field when the order is archived?
    matches: (<s\-select|(?:root\.)?createComponent\(s\-select)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I show an animated loading indicator while my admin block extension
      fetches order details and recalculates totals, and provide a meaningful accessibilityLabel
      for screen readers, using polaris web components?
    matches: (<s\-spinner|(?:root\.)?createComponent\(s\-spinner)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: On the order details page, how can I arrange fulfillment actions (buttons,
      status badge, and an icon) in a horizontal layout with consistent spacing, align
      items along the cross axis, and automatically switch to a vertical layout on
      narrow screens using polaris web components in my admin block extension? I also
      want a subtle background, a thin border with rounded corners, a constrained
      block size, and to provide an accessibility label/role while hiding decorative
      elements from assistive technologies.
    matches: (<s\-stack|(?:root\.)?createComponent\(s\-stack)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I add an on/off setting in my admin block extension on the product
      details page using polaris web components, with a clear label (or a visually
      hidden but accessible label), an accessibilityLabel when the label is hidden,
      defaultChecked support, contextual details text, a disabled state based on product
      status, and an error message if saving the preference fails?
    matches: (<s\-switch|(?:root\.)?createComponent\(s\-switch)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I build a responsive rows-and-columns view of product variants
      in a product details admin block extension that switches to a list on small
      screens, supports pagination (hasPreviousPage/hasNextPage), shows a loading
      state while fetching, and lets me choose between an auto or always-list layout
      using polaris web components?
    matches: (<s\-table|(?:root\.)?createComponent\(s\-table)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I render inline phrases within a paragraph on an order details\
      \ block extension with semantic emphasis (e.g., strong for \u201CPaid\u201D\
      ), tone-based styling (success/warning/critical), subdued color for secondary\
      \ labels, tabular numerals for amounts, and proper LTR/RTL handling? I also\
      \ need some inline hints to remain visible but excluded from the accessibility\
      \ tree, and to have hover/focus on these phrases trigger interest on a related\
      \ action button\u2014using polaris web components."
    matches: (<s\-text|(?:root\.)?createComponent\(s\-text)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I add an auto-expanding multi-line input for staff notes in an
      embedded block on the order details page using polaris web components, with
      a visually hidden but accessible label, helper details text, a default value
      from existing order notes, min/max length validation, autocomplete turned off,
      a disabled state while saving, and clear inline error messaging when validation
      fails?
    matches: (<s\-text\-area|(?:root\.)?createComponent\(s\-text\-area)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I add a single-line input to my order details admin block extension
      to let merchants set a short fulfillment note (max 50 characters) with a label,
      placeholder, help text, live character count, validation and error messaging,
      and an optional clear button, using polaris web components?
    matches: (<s\-text\-field|(?:root\.)?createComponent\(s\-text\-field)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "How can I show compact preview images for each line item in an order\
      \ details admin block extension\u2014setting alt text for accessibility, switching\
      \ sizes between small-100 and large based on layout, and displaying a placeholder\
      \ while the image loads or when no URL is available\u2014using polaris web components?"
    matches: (<s\-thumbnail|(?:root\.)?createComponent\(s\-thumbnail)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: How can I add small contextual help overlays that appear on hover or keyboard
      focus for action buttons and data fields in my order details admin extension,
      with configurable placement (top/bottom), optional delay, and accessible behavior,
      using polaris web components?
    matches: (<s\-tooltip|(?:root\.)?createComponent\(s\-tooltip)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: In an Admin action extension from the Orders page, I need merchants to
      enter a tracking URL that auto-normalizes http/https, validates against a whitelist
      of domains, shows inline errors, and keeps the submit button disabled until
      the URL is valid. How can I build this URL entry with placeholder, helper text,
      and optional prefix/suffix using polaris web components?
    matches: (<s\-url\-field|(?:root\.)?createComponent\(s\-url\-field)
- description: polaris_admin_extensions polaris web components
  vars:
    prompt: "In my Shopify Admin block extension on the order details page, I want\
      \ to present a bulleted list of related follow-up actions (e.g., add an internal\
      \ note, contact the customer, issue a partial refund, flag for review) where\
      \ the order isn\u2019t important. How can I render and style this as a bulleted\
      \ list using polaris web components, with optional icons and clickable items\
      \ for each action?"
    matches: (<s\-unordered\-list|(?:root\.)?createComponent\(s\-unordered\-list)
