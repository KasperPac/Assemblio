- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I show small status indicators next to line items during checkout\
      \ to communicate states like \u201CPayment captured,\u201D \u201CAge verification\
      \ required,\u201D or \u201CDiscount applied,\u201D with different visual tones\
      \ based on each state and unique ids for each indicator, using Polaris web components\
      \ in my POS extension?"
    matches: (<s\-badge|(?:root\.)?createComponent\(s\-badge)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I prominently display an age-verification message during checkout
      with a clear title, a unique id for tracking, adjustable tone (info, success,
      warning, critical) based on the verification state, and the ability to hide
      it once the customer is verified, using Polaris web components in my POS extension?
    matches: (<s\-banner|(?:root\.)?createComponent\(s\-banner)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I build a responsive grouped section in the checkout screen using
      Polaris web components in my POS extension that wraps a customer note input
      and a tip selector with consistent spacing, where I can control different top
      vs bottom padding, set a minimum inline size, and cap the maximum block size
      so it stays tidy on various device sizes?
    matches: (<s\-box|(?:root\.)?createComponent\(s\-box)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I add an action in the cart view that opens, hides, or toggles\
      \ a custom \u201CCustomer notes\u201D dialog by targeting it via its ID, shows\
      \ a loading spinner and disables itself while saving to the backend, and switches\
      \ between primary/secondary styles with a caution or critical tone when needed,\
      \ using Polaris web components in my POS extension?"
    matches: (<s\-button|(?:root\.)?createComponent\(s\-button)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I add a selectable list during checkout in my POS extension using
      Polaris web components to let staff choose one or multiple fulfillment options
      (in-store pickup, local delivery, ship later) and extras (gift wrap, rush handling),
      with support for initializing default selected values and assigning a unique
      id for state tracking?
    matches: (<s\-choice\-list|(?:root\.)?createComponent\(s\-choice\-list)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I make a custom \u201CApply Discount\u201D card in my POS extension\
      \ so the entire area is tappable to open a discount selector, but temporarily\
      \ disable interaction while a discount is being applied (so no tap handlers\
      \ fire, even from nested elements), and also assign a unique id for analytics\
      \ tracking, using Polaris web components?"
    matches: (<s\-clickable|(?:root\.)?createComponent\(s\-clickable)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I capture a pickup date during checkout with a labeled date input
      that shows helper details, pre-fills a previously saved date, validates required
      range (e.g., no past dates), surfaces an inline error message when invalid,
      and can be disabled for certain order states, using Polaris web components in
      my POS extension?
    matches: (<s\-date\-field|(?:root\.)?createComponent\(s\-date\-field)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I add a calendar-style date selector for choosing a single pickup
      date during checkout in my POS extension using Polaris web components, with
      a unique id and an optional initial value that validates YYYY-MM-DD and defaults
      to no date if the value is invalid?
    matches: (<s\-date\-picker|(?:root\.)?createComponent\(s\-date\-picker)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I add a spinner-style date selector for choosing a pickup or delivery
      date during checkout using Polaris web components in my POS extension, set an
      element id, initialize it with a YYYY-MM-DD value, and handle changes to update
      the order details?
    matches: (<s\-date\-spinner|(?:root\.)?createComponent\(s\-date\-spinner)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I add a visual separator between the cart items list and the payment
      methods panel in my POS extension, with control over whether it renders in the
      inline or block direction and assign it a unique id for tracking, using Polaris
      web components?
    matches: (<s\-divider|(?:root\.)?createComponent\(s\-divider)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I add an email capture field for e-receipts during checkout using\
      \ Polaris web components in my POS extension, with built-in email validation\
      \ and the email keyboard, a label like \u201CCustomer email,\u201D a placeholder\
      \ like \u201Cname@example.com,\u201D helpful details text explaining it\u2019\
      s used for e-receipts and order updates, enforce a maxLength (e.g., 254), mark\
      \ it as required, display a custom error message when the format is invalid,\
      \ initialize it with a prefilled value from the customer profile, assign a unique\
      \ id, and disable it when the customer chooses a paper receipt?"
    matches: (<s\-email\-field|(?:root\.)?createComponent\(s\-email\-field)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I add hierarchical titles that automatically adjust their levels\
      \ based on nested content to organize a custom order pickup flow\u2014top-level\
      \ \u201CPickup Details\u201D with sub-sections like \u201CCustomer Info,\u201D\
      \ \u201CTime Window,\u201D and \u201CInstructions\u201D\u2014using Polaris web\
      \ components in my POS extension?"
    matches: (<s\-heading|(?:root\.)?createComponent\(s\-heading)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I show context-aware action symbols next to line items during
      checkout (e.g., add-on available, age verification required, return eligible)
      and configure their type, size, tone, and color intensity, while assigning unique
      IDs for event handling, using Polaris web components in my POS extension?
    matches: (<s\-icon|(?:root\.)?createComponent\(s\-icon)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I embed a brand promo image in the order summary panel using\
      \ Polaris web components in my POS extension, assign it a unique id, have it\
      \ fill the available width on mobile but use the image\u2019s natural size on\
      \ larger screens, switch between showing the whole image vs edge-to-edge cropping\
      \ based on aspect ratio, load the image from a remote URL, and show a placeholder\
      \ while it\u2019s loading or if the URL is missing?"
    matches: (<s\-image|(?:root\.)?createComponent\(s\-image)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I show an attention-required overlay with a custom heading and
      a stable id to confirm manager approval when applying a high-value discount
      during checkout in my POS extension using Polaris web components?
    matches: (<s\-modal|(?:root\.)?createComponent\(s\-modal)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I capture a numeric input for a custom tip amount during checkout,
      letting the cashier choose stepper controls to nudge the value by a set increment
      or use a decimal keyboard, enforce min/max limits, and show contextual details
      with inline error messaging when the value is out of range, using Polaris web
      components in my POS extension?
    matches: (<s\-number\-field|(?:root\.)?createComponent\(s\-number\-field)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I build a full-screen container for an age-restricted purchase\
      \ flow in my POS extension, with a primary heading in the action bar (e.g.,\
      \ \u201CID Check\u201D), a subheading that explains the verification steps,\
      \ and a unique identifier for state tracking, using Polaris web components?"
    matches: (<s\-page|(?:root\.)?createComponent\(s\-page)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I add a container in the payment section that displays a heading\
      \ like \u201CLoyalty rewards,\u201D shows the customer\u2019s points balance,\
      \ and includes an \u201CApply discount\u201D action button, while assigning\
      \ the container a unique id for analytics tracking, using Polaris web components\
      \ in my POS extension?"
    matches: (<s\-pos\-block|(?:root\.)?createComponent\(s\-pos\-block)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I add a scrollable section to the order summary in my POS extension
      to display long lists of line-item notes and promotions, and configure blockSize/inlineSize
      with minBlockSize/minInlineSize and maxBlockSize/maxInlineSize, plus padding,
      paddingBlock, and paddingBlockEnd so the content stays within the panel and
      remains readable, using Polaris web components?
    matches: (<s\-scroll\-box|(?:root\.)?createComponent\(s\-scroll\-box)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I add a single-line search bar to my POS extension to let staff
      quickly filter products by name or SKU, with a magnifying-glass look and a one-tap
      clear button, plus support for a placeholder hint, an initial value, a unique
      id, and the option to disable it during syncing, using Polaris web components?
    matches: (<s\-search\-field|(?:root\.)?createComponent\(s\-search\-field)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I organize my custom checkout screen into clearly defined areas\u2014\
      like Customer details, Pickup info, Discounts, and Payment summary\u2014with\
      \ accessible heading levels and unique IDs for deep linking and testing, and\
      \ optionally render some areas without a visible header, using Polaris web components\
      \ in my POS extension?"
    matches: (<s\-section|(?:root\.)?createComponent\(s\-section)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I arrange a customer info badge, loyalty points chip, and an\
      \ \u201CApply discount\u201D button in the POS checkout so they align horizontally\
      \ with space-between on wider screens but switch to a vertical layout on compact\
      \ devices, with configurable gaps (including separate inline/column gap), cross-axis\
      \ alignment for children, and constrained inline/max block sizes so the section\
      \ doesn\u2019t overflow the order summary, using Polaris web components in my\
      \ POS extension?"
    matches: (<s\-stack|(?:root\.)?createComponent\(s\-stack)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I display checkout messages in my POS extension using Polaris\
      \ web components\u2014for example, a strong success message when a discount\
      \ is applied, a cautionary warning for age-restricted items, and a small helper\
      \ note about return policies\u2014while controlling tone, adjusting color intensity,\
      \ choosing strong/small/generic styles for emphasis, and assigning unique ids\
      \ to each message so I can update them dynamically during the transaction?"
    matches: (<s\-text|(?:root\.)?createComponent\(s\-text)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I add a resizable multi-line input to capture detailed customer
      notes (like gift messages or special delivery instructions) during checkout
      using Polaris web components in my POS extension, with a clear label, contextual
      guidance text, a placeholder, a character limit, the option to make it required
      for specific items, adjustable visible rows, a pre-filled value from previous
      orders, inline error messaging when empty or over the limit, and the ability
      to disable it after payment is processed?
    matches: (<s\-text\-area|(?:root\.)?createComponent\(s\-text\-area)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: "How can I collect a short note from the cashier during checkout\u2014\
      like a gift message limited to 50 characters\u2014with a clear label, placeholder,\
      \ helpful guidance text, required validation that shows an error if empty or\
      \ too long, an initial value when available, and the option to disable it based\
      \ on order state, using Polaris web components in my POS extension?"
    matches: (<s\-text\-field|(?:root\.)?createComponent\(s\-text\-field)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I add a dynamic action button in the Smart Grid that launches
      an age-verification workflow, shows a title with supporting text, displays a
      small numeric badge for the number of age-restricted items in the cart, switches
      to an accent tone when the count is greater than zero, and becomes disabled
      when there are none, using Polaris web components in my POS extension?
    matches: (<s\-tile|(?:root\.)?createComponent\(s\-tile)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I collect a curbside pickup time during checkout using Polaris
      web components in my POS extension? I need a time input that enforces a strict
      24-hour HH:mm:ss format with leading zeros, allows setting an initial value
      or leaving it empty, shows helper details text to guide staff on acceptable
      hours, displays inline error messaging if the time is invalid or outside business
      hours, can be programmatically disabled based on order state, and includes a
      unique id and clear label for accessibility.
    matches: (<s\-time\-field|(?:root\.)?createComponent\(s\-time\-field)
- description: polaris_pos_ui_extensions polaris web components
  vars:
    prompt: How can I add a time selection field for scheduling a pickup or appointment
      during checkout, where the value is a 24-hour time string (HH:mm:ss) with leading
      zeros and defaults to '', and how do I read/update that value and give the field
      a stable unique id, using Polaris web components in my POS extension?
    matches: (<s\-time\-picker|(?:root\.)?createComponent\(s\-time\-picker)
