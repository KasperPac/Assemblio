- description: checkout_extensions components
  vars:
    prompt: "How can I display compact status labels next to line items and shipping\
      \ methods during checkout\u2014e.g., marking items as \u201CSubscription,\u201D\
      \ \u201CPreorder,\u201D or \u201CBackordered,\u201D and showing order-level\
      \ states like \u201CPaid\u201D or \u201CPending\u201D\u2014using components,\
      \ with different visual emphases and accessible text, so they appear consistently\
      \ in the order summary and on the Thank You page?"
    matches: '```.*Badge.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add a prominent, dismissible callout in the shipping step that
      alerts customers to regional delivery delays, supports info/warning/critical
      styles, includes an optional action link, and automatically updates based on
      the selected shipping address in my checkout extension?
    matches: '```.*Banner.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I stack several sections (delivery estimate, returns policy, and
      a promotional upsell) into multiple rows that evenly share the available vertical
      space in the checkout sidebar, and keep the layout consistent across desktop
      and mobile using components in my checkout extension?
    matches: '```.*BlockLayout.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add adjustable vertical gaps of different sizes between a promo
      banner, a discount notice, and an upsell product card in my payment step UI,
      with spacing that can change for mobile vs. desktop, using components in a checkout
      extension?
    matches: '```.*BlockSpacer.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I vertically arrange multiple elements\u2014like a heading, explanatory\
      \ text, a promo input, and a \u201CWhy choose express shipping\u201D benefits\
      \ list\u2014with consistent spacing and optional dividers in the shipping step\
      \ of my checkout extension, ensuring everything aligns to the start and looks\
      \ good on mobile using components?"
    matches: '```.*BlockStack.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add an action control in the checkout that advances the shopper\
      \ to the next step (for example, \u201CContinue to shipping\u201D) only after\
      \ my custom validation passes, with proper disabled and loading states, full-width\
      \ styling on mobile, and accessible labels using components in my checkout extension?"
    matches: '```.*Button.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add a real-time support conversation to my checkout extension
      that opens as an overlay during checkout and on the Thank You page, streams
      bot/agent replies with typing indicators, supports quick-reply suggestions and
      attachments, and persists the transcript between pages while scoping context
      to the current cart or order and keeping the UI accessible and theme-aligned?
    matches: '```.*Chat.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add an opt-in option during checkout for agreeing to store\
      \ terms (with a link to the policy), keep it unchecked by default, show an inline\
      \ validation message if the customer tries to continue without selecting it,\
      \ disable the payment button until it\u2019s selected, and persist the consent\
      \ to cart attributes and order metafields using components in my checkout extension?"
    matches: '```.*Checkbox.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I present a grouped list of selectable add-ons during checkout
      (e.g., gift wrap, rush processing, carbon-neutral shipping) with labels, helper
      text, and disabled options, allow either single- or multi-select based on my
      configuration, and persist the selections to cart attributes while updating
      the order summary in real time?
    matches: '```.*Choice.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add a selectable options list in my checkout extension that
      lets buyers choose one gift wrap style or multiple add-on services, shows helper
      text and prices for each option, supports required/disabled states, validates
      selections, and updates the order summary total in real time?
    matches: '```.*ChoiceList.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add a \u201CCopy order number\u201D link on the Thank You page\
      \ that, when clicked, copies the order number to the customer\u2019s clipboard\
      \ and shows a brief tooltip confirmation using checkout UI extension components?\
      \ I\u2019m looking for the pattern to wire a clickable element to a non-visual\
      \ target that holds the text to be copied."
    matches: '```.*ClipboardItem.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add localized consent checkboxes for things like Terms of Service\
      \ and SMS marketing in my checkout extension, link each label to the relevant\
      \ policy, block progression until required consent is given, show clear error\
      \ messaging, and persist the buyer\u2019s selections on the order or customer\
      \ for compliance auditing?"
    matches: '```.*ConsentCheckbox.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add an SMS marketing opt-in section with a phone input that
      automatically saves the number to the checkout, includes consent messaging,
      locale-aware formatting, and basic validation using components in my checkout
      extension?
    matches: '```.*ConsentPhoneField.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add a date picker in the shipping step so customers can choose
      a preferred delivery date, enforce min/max limits (no past dates and only within
      the next 30 days), block weekends, show inline validation errors, localize the
      date format, and then save the selection to a cart/order attribute so it appears
      in the order summary and on the Thank You page?
    matches: '```.*DateField.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add a calendar-style selector in the shipping step that lets\
      \ customers choose either a single delivery date or a delivery window, enforces\
      \ blackout dates and a 2-day lead time, localizes to the buyer\u2019s timezone/locale,\
      \ and saves the selection to checkout attributes with inline validation messages\
      \ using components in my checkout extension?"
    matches: '```.*DatePicker.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add an accessible expand/collapse section in my checkout extension\
      \ where a switch labeled \u201CAdd gift wrap\u201D toggles a hidden area that\
      \ reveals pricing, a short description, and a text field for gift notes laid\
      \ out in a grid/stack, with the section collapsed by default but programmatically\
      \ opened when the cart already includes gift wrap, and ensure it follows the\
      \ ARIA accordion pattern?"
    matches: '```.*Disclosure.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I insert a subtle visual separator between my custom shipping
      note and the default shipping methods in checkout, and control the spacing so
      it matches the theme using components in my checkout extension?
    matches: '```.*Divider.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add an accessible file upload area in my checkout extension
      that lets customers drag-and-drop or click a button to attach files (for things
      like ID verification or custom artwork), displays selected filenames and validation
      errors, supports Enter/Space keyboard activation, and uploads the files without
      relying on image previews or object URLs?
    matches: '```.*DropZone.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add a custom input section in the shipping step (e.g., delivery
      instructions and gift message) that lets customers press Enter in any field
      to submit, validates the values, and uses a submit handler to send the data
      to my API and display success or error messages using checkout extension components?
    matches: '```.*Form.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I create a responsive multi-column layout in my checkout extension
      that shows upsell products (image, title, price, add/remove button) with adjustable
      column count, consistent spacing between items, and support for some items to
      span two columns on desktop while stacking to a single column on mobile using
      components?
    matches: '```.*Grid.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I build a responsive grid-based section in my checkout extension\u2019\
      s order summary where a promo banner spans the full width, a featured upsell\
      \ card spans two columns, and smaller add-on tiles fill the remaining cells\u2014\
      letting specific items span multiple columns or rows across breakpoints?"
    matches: '```.*GridItem.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add clear, accessible section titles like \u201CContact information,\u201D\
      \ \u201CShipping address,\u201D and \u201CShipping method\u201D in my checkout\
      \ extension so they have the right visual hierarchy and produce a correct document\
      \ outline for screen readers? What\u2019s the best way to group these titles\
      \ and control their sizes without manually assigning HTML heading levels?"
    matches: '```.*Heading.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add an accessible \u201CDelivery preferences\u201D section\
      \ in the shipping step with a main heading and a few subheadings (like \u201C\
      Carrier notes\u201D and \u201CDelivery window\u201D), making sure screen readers\
      \ interpret the hierarchy correctly by grouping the headings with their related\
      \ fields using components in my checkout extension?"
    matches: '```.*HeadingGroup.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add small pictograms next to actions like Edit, Info, and Remove
      in the order summary and shipping method list, making sure they inherit the
      checkout theme colors, support different sizes, include accessible labels for
      screen readers, and trigger modals or navigation when clicked using components
      in my checkout extension?
    matches: '```.*Icon.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I display a full-width, responsive promotional banner image in\
      \ the shipping step that adapts to mobile and desktop, includes accessible alt\
      \ text, lazy-loads for performance, and swaps the source based on the customer\u2019\
      s country or cart contents using components in my checkout extension?"
    matches: '```.*Image.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I display a row of evenly sized upsell tiles in the order summary
      that stretch to fill the available width, and when I add more tiles they appear
      as additional columns on the same row (no wrapping), with control over spacing
      and alignment using checkout UI components?
    matches: '```.*InlineLayout.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I place a \u201CSave card for next time\u201D checkbox on the\
      \ left and a \u201CSecurity info\u201D link on the far right in the payment\
      \ step, with a flexible gap between them that grows or shrinks based on content\
      \ (not uniform spacing), using components in my checkout extension?"
    matches: '```.*InlineSpacer.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I display a responsive row of trust badges (like fast shipping,
      secure payment, and easy returns) beneath the payment section that automatically
      wraps to new lines on smaller screens using components in my checkout extension?
    matches: '```.*InlineStack.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add inline clickable text in my custom discount notice within\
      \ the order summary that takes shoppers to my store\u2019s promotion terms page\
      \ (optionally in a new tab), while ensuring proper accessibility labels and\
      \ capturing click analytics in my checkout extension?"
    matches: '```.*Link.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I display a bulleted checklist of key shipping and return policy
      highlights beneath the shipping methods during checkout, and dynamically update
      the points based on cart contents and locale using components in my checkout
      extension?
    matches: '```.*List.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I render a bulleted list of shipping and return policy highlights\
      \ beneath the order summary in my checkout extension, with each bullet as its\
      \ own child item that I can show or hide dynamically based on the customer\u2019\
      s cart and shipping address?"
    matches: '```.*ListItem.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add an interactive location view in the shipping step that\
      \ confirms the customer\u2019s address and shows nearby pickup points using\
      \ components in my checkout extension? I\u2019ll provide a Google Maps API key\u2014\
      what\u2019s the recommended way to configure the allowed domains (https://*.[MERCHANT-DOMAIN].com,\
      \ https://shop.app, https://shopify.com) and load the script so the view renders\
      \ reliably during checkout and on the Thank You page?"
    matches: '```.*Map.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add an interactive map to the shipping step that shows nearby\
      \ pickup locations as clickable pins, preselects the closest one based on the\
      \ customer\u2019s address, displays a small details card (name, hours, distance)\
      \ when a pin is tapped, and saves the chosen pickup location to the order using\
      \ components in my checkout extension?"
    matches: '```.*MapMarker.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I let customers explore nearby pickup locations on an embedded\
      \ map during the shipping step and, when they tap a marker, show an on-map info\
      \ panel with the location\u2019s name, hours, distance, pickup fee, and a button\
      \ to select that location\u2014with proper focus management, mobile-friendly\
      \ positioning, and dismissal behavior\u2014using components in my checkout extension?"
    matches: '```.*MapPopover.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I use components in my checkout extension to trigger a focused
      overlay from a button that asks customers to accept terms or verify age before
      payment, with primary/secondary actions, full keyboard and screen reader support,
      and blocking the checkout flow until they make a choice?
    matches: '```.*Modal.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I display a responsive row of payment method icons (Visa, Mastercard,\
      \ Amex, Shop Pay, etc.) in the payment step of my checkout extension, with accessible\
      \ labels, theme-aware styling for light/dark modes, and the ability to highlight\
      \ the customer\u2019s saved card type?"
    matches: '```.*PaymentIcon.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add a phone number input in the shipping step that initializes
      with a specific country dialing code, auto-formats as the customer types, and
      lets them change the country either via a country dropdown or by editing the
      dial code directly? I also want to validate the number, show inline errors,
      and persist the value to the order during checkout.
    matches: '```.*PhoneField.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add a \u201CWhat\u2019s this?\u201D help trigger next to a\
      \ handling fee in the order summary that, when clicked or tapped, opens a small\
      \ on-demand overlay with a title, a short description, and an optional link\
      \ to policy details? I want it to be accessible, manage focus correctly, dismiss\
      \ on outside tap or escape, and work well on both desktop and mobile within\
      \ a checkout extension."
    matches: '```.*Popover.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I build an unstyled interactive container in the payment step\
      \ that wraps text and icons, responds to click/tap/keyboard activation, toggles\
      \ a saved-card list, shows hover/focus/pressed feedback, supports disabled/loading\
      \ states, and triggers custom logic\u2014all without using the default button\
      \ or link styling?"
    matches: '```.*Pressable.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I display a small image preview for each line item in the order\
      \ summary of my checkout extension, using the variant\u2019s image when available,\
      \ adding accessible alt text, showing a placeholder if no image exists, and\
      \ ensuring the images are responsive and performance-friendly?"
    matches: '```.*ProductThumbnail.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I display a clear \u201Ctask completed\u201D state in my checkout\
      \ extension\u2014such as a success indicator that reaches 100%\u2014after a\
      \ shopper successfully applies a discount code or finishes address verification,\
      \ so they know that step is done before proceeding?"
    matches: '```.*Progress.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add a scannable code on the Thank You page that links to the\
      \ customer\u2019s order tracking URL, with configurable size and colors, a short\
      \ caption, and proper accessibility text using components in a checkout extension?"
    matches: '```.*QRCode.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add a vertically scrollable section inside the order summary\
      \ to handle many line items and discount details\u2014capped to a max height\
      \ with visible scroll indicators, keyboard accessibility, and a persistent header/footer\
      \ for actions like \u201CApply discount\u201D and \u201CEdit cart\u201D\u2014\
      using components in my checkout extension?"
    matches: '```.*ScrollView.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I add a single-choice menu in the shipping step that lets customers\
      \ choose from six delivery time windows (e.g., 9\u201312, 12\u20133, 3\u2013\
      6, 6\u20139, Next-day AM, Next-day PM), with a placeholder, helper text, validation/error\
      \ states, and dynamic price updates in the order summary when a premium window\
      \ is selected, using components in my checkout extension?"
    matches: '```.*Select.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add a bottom-of-screen overlay in my checkout extension that
      slides up to show essential privacy and consent details, opened by a link or
      button, using components and the Customer Privacy API for buyer consent while
      keeping it accessible and minimally disruptive to the checkout flow?
    matches: '```.*Sheet.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I display low-fidelity placeholders for product thumbnails in
      a custom upsell section during checkout until the actual images load, with adjustable
      dimensions, aspect ratio, and rounded corners to prevent layout shifting?
    matches: '```.*SkeletonImage.*```'
- description: checkout_extensions components
  vars:
    prompt: "In my checkout extension\u2019s shipping step, I fetch the estimated\
      \ delivery date from an external API. How can I show a lightweight placeholder\
      \ for that text while it loads\u2014ideally using sample copy (like \u201CDelivery\
      \ estimate arriving soon\u201D) to shape the placeholder so it matches the final\
      \ text\u2014and then seamlessly swap in the real value once it\u2019s available\
      \ using components?"
    matches: '```.*SkeletonText.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I show a low-fidelity placeholder for a multi-line message (like
      delivery ETA or loyalty benefits) while my checkout extension fetches data,
      and optionally seed it with example text so the placeholder matches the eventual
      content using components?
    matches: '```.*SkeletonTextBlock.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I show a lightweight loading indicator and temporarily disable\
      \ related inputs after the buyer clicks \u201CApply discount\u201D during checkout,\
      \ while my extension calls an external API to validate the code, then hide the\
      \ indicator and display success or error feedback once the request completes\
      \ using components in a checkout extension?"
    matches: '```.*Spinner.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add a compact +/- quantity control next to each line item in
      the order summary of my checkout extension so customers can adjust quantities
      (min 1, max based on inventory), see totals update instantly, and receive clear
      validation messages when limits are reached, with proper accessibility and keyboard
      support?
    matches: '```.*Stepper.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add an on/off toggle in the shipping step that lets customers
      opt into carbon-neutral shipping for a small fee, updates the order summary
      total immediately when tapped, persists the choice across steps, and handles
      loading/disabled states using components in my checkout extension?
    matches: '```.*Switch.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I display compact labels for each applied discount in the order
      summary and line items using components in my checkout extension, with different
      visual styles for automatic vs code-based discounts, support for icons, accessible
      text, and an optional action to reveal more details about each discount?
    matches: '```.*Tag.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I display concise helper text, status messages, and legal disclaimers
      beneath fields like shipping address or discount code in my checkout extension,
      with proper semantics for accessibility and visual variations (subdued, emphasized,
      critical) that update dynamically based on validation results using components?
    matches: '```.*Text.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I display a full-width informational paragraph below the shipping\
      \ methods that explains carrier delays and includes a link to the store\u2019\
      s shipping policy, with the content localized to the buyer\u2019s language,\
      \ using components in my checkout extension?"
    matches: '```.*TextBlock.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add a single-line text input in the payment step for customers
      to enter a PO number, with a label, placeholder, max-length and regex validation,
      inline error messaging, and a disabled state during submission, and then persist
      the value to a cart/order attribute using components in my checkout extension?
    matches: '```.*TextField.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I present delivery speed choices (Standard, Express, Overnight)\
      \ as tap-friendly pill options with an icon, brief help text, and a promo badge,\
      \ set a default selection, disable options that aren\u2019t available for the\
      \ customer\u2019s address, and update shipping costs in the order summary when\
      \ the selection changes using components in my checkout extension?"
    matches: '```.*ToggleButton.*```'
- description: checkout_extensions components
  vars:
    prompt: How can I add a set of button-like options where the customer can select
      exactly one shipping speed (Standard, Express, Overnight), lay them out horizontally
      with responsive wrapping, update the order total based on the selection, and
      persist the choice to a cart attribute in my checkout extension?
    matches: '```.*ToggleButtonGroup.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I show a brief, accessible hint that appears when a customer\
      \ focuses or hovers a \u201CWhat\u2019s this?\u201D link next to the CVV field\
      \ in the payment step of my checkout extension? I want it triggered by the link,\
      \ announced by screen readers, and dismissed when the trigger loses focus."
    matches: '```.*Tooltip.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I display a row of compact trust badges and delivery estimate\
      \ icons in the payment step so each item keeps its natural size (doesn\u2019\
      t stretch to fill the row) while still aligning neatly within a larger horizontal\
      \ layout using components in my checkout extension?"
    matches: '```.*View.*```'
- description: checkout_extensions components
  vars:
    prompt: "How can I apply conditional styles in my checkout extension for a custom\
      \ upsell panel in the order summary so that it switches to a single-column layout\
      \ on small viewports and two columns on larger screens, the call-to-action button\
      \ has distinct hover/focus/pressed/disabled styles, an error state adds a red\
      \ border with an icon when an upsell can\u2019t be added, and animations are\
      \ reduced when the user prefers reduced motion\u2014all using components?"
    matches: '```.*StyleHelper.*```'
